# 사전 등록 — **창 A2**(`slot3` · 계측기 수정판) · 2026-08-17 **21:2x**

> 🔴 **자료 전에 쓴다.** 하행 주입(3차① `inj4-*`)은 **아직 시작되지 않았다** — 이 시점 `[CNT] oow=0`.
> `§1`·`§2` 는 **사건 뒤에 고치지 마라.** 결과는 `§4` 에만 적는다.
> 작성 `monitor-engineer`. 창 A 판정: `JUDGMENT-2026-08-17-winA.md`

## 0. 창

| | |
|---|---|
| `t0` | **21:20:53** (탭 개방 DTR 리셋 · 부트 배너 같은 초 · uptime 3200→14) |
| `t_start` | 21:22:53 (`t0+120s`) |
| 펌웨어 | **`slot3`**(21:19 굽기 · hex 대조 IDENTICAL) — **거동 변경 없음. 계측기만 바뀌었다** |
| 서버 | `srv_parking` pid 46224 · `build=22f788d` · **큐 없음** — 창 A 와 **같은 인스턴스** |
| 탭 / 요약기 | pid 50028 → `monitor/serial-winA2.log` · `WINA2-STATUS.md` · `winA2-pulse.tsv` |

🔑 **축이 하나다**: 서버도 같은 인스턴스, 거동도 같고 **계측기만 바뀌었다.** 창 A 이후 처음이다.

---

## 1. 🔴🔴 먼저 박는다 — **이 창의 `oow` 는 기준선이다. 이전 값과 비교 불가**

```
slot2 (창 A) : oow = **처리** 시각이 송신 창 안 → 위험 구간 도착을 **놓친다**
slot3 (이 창): oow = **도착** 시각이 송신 창 안 → 처음으로 위험 구간을 **본다**
```
- ❌ **창 A 의 `oow 693` 과 이 창의 `oow` 를 같은 표에 넣지 마라.** 이름·단위·펌웨어 계열이 같은데
  **세는 것이 다르다**(CLAUDE.md 세 항목 중 1번만 어긋난 경우 — 가장 잡기 어려운 형태).
- ★ **`oow` 는 오를 것으로 예상한다. 그것은 새 고장이 아니라 보이기 시작하는 것이다**(arduino 예고).
  🔴 **늘어난 수를 악화로 읽지 마라.** 원장 7.108 의 **반대 방향 같은 함정**이다 —
  *계측기 개선이 지표를 악화시키는 것처럼 보이는 것.*
- ⚠ **`[SLOT-OOW]` 오프셋도 이 창부터 처음 "도착 시각"이다.** 창 A 의 오프셋 분포(116~598ms)는
  **폐기**다. arduino 보증: `pendLine` 우회로에도 도착 시각을 넘기고 회귀 `[22]` 가 그 경로를 확인한다
  (도착 +100ms · 처리 +700ms → **+100 으로 판정**).

## 2. 예측 (사건 뒤 수정 금지)

### ① 3차 조건(1.15s·300건)을 재현하면 **링크 사건이 다시 난다**

거동이 안 바뀌었으므로 창 A 3차와 **같은 결과**가 나와야 한다. 창 A 3차 실측:
```
오프라인 판정 5회 · 세션 종료 1회(errno=54) · TX-WAIT 44% · 상행 최대 공백 10초
하행 파괴 1 · ACK 계열 15 · 미도달 1 · esprst 0
```
- **판정선**: 오프라인 판정 **≥2회** 또는 세션 종료 ≥1회면 **재현됨**.
  **0회면 🔴 거동이 바뀐 것**이고(계측기만 고쳤다는 전제가 깨진다) 그것을 먼저 조사한다.
- ⚠ 표본이 같아야 한다 — **`--rid-prefix` 만 다르고 나머지 인자는 한 글자도 바뀌지 않는다**(socket 런북).

### ② `cksumng_lines`(내 줄 수) == `[CNT] cksumng`(펌웨어 계수) — **교차 검증**

둘이 어긋나면 **한쪽이 낡은 것**이다(원장 7.108 절차). ⚠ 이름을 갈라 뒀다(`_lines` 접미).
- **판정선**: 창 전체에서 두 수가 같아야 한다. 다르면 **차이를 원문으로 설명하고 나서** 판정한다.

### ③ 🔑 **위험 구간 폭이 이 창에서 처음 측정된다**

창 A 에서 `[116,171)ms` 를 냈다가 **철회**했다(처리 시각이었다). 이번엔 도착 시각이다.
- **예측**: 위험 구간은 **페이로드 송신 시간(38~39B ≈ 40ms)보다 넓다.**
  근거: 모델에 `AT+CIPSEND=NN\r\n` 16B(≈17ms)가 빠져 있었고(arduino 확인) **프롬프트 대기도 점유**다.
- **재는 법**: `[CKSUM NG]` 가 난 도착 오프셋과, **정상 도착 오프셋의 최소값**을 갈라 본다.
  🔴 **하한만 나온다**(파괴된 프레임은 오프셋을 못 남길 수 있다 — 누락 편향은 그대로다).
- ⚠ **표본이 작으면 폭을 숫자로 쓰지 마라.** 창 A 3차 전체에서 파괴는 1건이었다.

### ④ 무주입 구간(지금)은 **분모 0 이다**

이 글을 쓰는 시점 `[CNT] up=60 slot=50 oow=0 cksumng=0 ssovf=0 skip=2 esprst=0`.
🔴 **주입 전 `oow=0`·`cksumng=0` 을 "설계가 먹었다"로 쓰지 마라 — 자극이 없다.**
창 A 에서 내가 그 함정에 들어갔다가 스스로 알아본 자리다(§2.3 · 그때는 13분 뒤 판정을 낼 뻔했다).

## 3. 판정 절차

1. **분모를 먼저 찍는다**: 하행 신규 거래(socket `n_sent` − `미주입`) · 슬롯 수 · 노출 시간.
   `N < 315` 면 **판정하지 않는다**(창 A 규칙 그대로).
2. **중단 조건은 socket 의 자동 판정이 들어간 뒤에 돌린다**(루트 순서). 창 A 3차에서
   조건 충족 뒤 130건이 더 돌았다 — 같은 일이 반복되면 그 구간을 갈라 표시한다.
3. **회차 경계는 `rid` 접두로 자른다**(`inj4-*`). 시각으로만 자르면 내가 창 A 에서 밟은
   혼입(2차 132 → 120)이 재현된다.
4. **내 로그의 문자 손상 4.2%(에코 줄)** 를 `0` 옆에 같이 적는다(원장 7.106).
5. **창 A 와 이을 수 있는 것만 잇는다**: 노출·`전송 3회 연속 실패`·리셋 표지·`CKSUM NG`·`재전송`.
   ❌ `oow`·`[SLOT-OOW]` 오프셋은 **정의가 바뀌어 못 잇는다.**

## 4. 결과 — 3차①(`inj4`) 1차 기록 (21:2x · 창은 열려 있다)

```
주입    21:23:13 ~ 21:24:17 (64초) · 시도 51/300 · 🛑 자동 정지(오프라인 연속 5)
분모    유효 하행 = 51 − 미주입 5 = **46거래**   ← `계획 300` 으로 쓰지 마라(socket)
```
**① 링크 사건 재현됨 — 예측 적중**: `T1 초과 1` → `★★ T2 초과 1` → `CIPCLOSE`(21:24:16) ·
`[CNT] drop=1 stuck=1` · 서버 세션#8 종료(`errno=54`) · **최대공백 10.0초**
🔑 **창 A 3차의 10.2초와 같은 값** — 같은 고장이 다른 칩에서 재현됐다.

**🔴 그런데 `cksumng=0` 이다** — 46거래에 **프레임 파괴 0**인데 링크는 깨졌다.
→ **파괴 없이도 무너진다.** 창 A 3차는 파괴 1 + ACK 계열 15 였다. **이번은 ACK/타이밍 계열만으로 깨졌다.**
⚠ 표본 46거래다. **"파괴가 원인이 아니다"로 확대하지 마라** — 이번 회차에 안 났을 뿐일 수 있다.

**② `oow` 첫 기준선(도착 시각 정의)**: `21:23:51 oow=23` → `21:24:51 oow=35`.
46거래에 35 ≈ **76%**. 🔴 창 A 의 `oow 693`(처리 시각)과 **비교 불가**(§1). 이것이 새 기준선이다.

**③ 주입 전 구간은 깨끗했다** — `21:20:53~21:23:12`(2.3분) 표지 4건이 전부 **기동 사다리**
(`offline step=2·2·3` · up=3s/6s/9s)다. **링크 사건 0.** ⚠ socket 이 "주입 전부터 흔들렸다"고
걱정한 세션#7 종료는 **내 `t0` DTR 리셋이 만든 것**이다 — 내 창 안의 열화가 아니다.
→ **3차①의 빠른 붕괴(i=51 · 64초)를 "시작 상태가 나빴다"로 설명할 근거는 내 로그에 없다.**

**④ 자동 정지가 작동했다**: 창 A 에서는 같은 조건 충족 뒤 130건이 더 돌았고, 이번엔 그 자리에서 멈췄다.
🔑 **`시도 51` 자체가 창 B 대조값이다**(큐가 막으면 300 완주해야 한다 · socket).

_(창을 닫은 뒤 최종 판정)_


---

## 5. 창 A2 닫음 — `t_end 22:33:22` (사용자 지시 "관측을 내려라" · REQ-0177)

```
t0 21:20:49(탭 개방 · 부트 배너 21:20:53) ~ t_end 22:33:22   = 72분 33초 · 18,397줄
마지막 줄: «tap» 캡처 종료 — SIGTERM 수신. 이 줄이 이 로그의 마지막 경계다.
```
✅ **경계 줄이 있다 → 정상 종료다.** 🔑 **그 SIGTERM 은 내가 보냈다**(`kill 50028`, 22:33:22,
REQ-0177 의 사용자 지시 이행). 루트가 "세션 종료로 딸려 죽은 것일 수 있다"고 의심했는데
**로그가 아니라고 답한다** — 원장 7.96 의 판정 순서(`ps` → `lsof` → **로그 마지막 줄**)가 그대로 작동했다.
포트: `lsof` 0건 · `PORT-IN-USE.txt` 없음(탭이 지우고 나갔다).

## 6. 🔴🔴 유휴 구간에서 **모듈 리셋 2건** — 창 A 결론의 범위를 좁힌다

마지막 `[CNT]`(22:33:03):
```
up=4320 slot=3600 · **esprst=2** · resync=3 · drop=3 · stuck=2 · smiss=1 · ssovf=1
cksumng=0 · oow=35 · okto=3 · skip=49 · online=1
```
21:24:51 시점에는 `esprst=0 resync=0 drop=1 smiss=0 ssovf=0` 이었다. 즉 **주입이 끝난
21:24:17 이후의 무주입 유휴 69분에 리셋 2건·재동기 3건·drop 2건·`ssovf` 1건이 발생했다.**

> ### 🔴 **하행이 0인 구간에서도 모듈 리셋이 난다.**
> 창 A 판정 §0 의 *"하행 1,040거래에 모듈 리셋 0"* 은 **주입 구간에 대한 참**이고,
> **"리셋은 하행이 있어야 난다"로 확장하면 틀린다.** 이 창이 그 반례다.

⚠ **다만 이 69분은 판정 설계가 없는 구간**이다(주입 없음 · 사람 개입 없음으로 추정하나 확인 안 됨).
**시각별 귀속을 아직 안 했다** — `esprst` 2건이 언제·어떤 표지와 함께 났는지는 §7 로 남긴다.
🔑 **그래도 지금 적어야 한다**: 이 수를 모르면 창 B 에서 "리셋 0"을 큐의 성과로 읽게 된다.
**창 B 는 이 유휴 기준선(69분에 리셋 2)과 비교해야 한다.**

## 7. 남은 분석 (자료는 다 있다 · 관측 없이 가능)

1. `esprst` 2건의 **시각·표지 귀속**(배너/`0.0.0.0`/부트ROM · `chain_verify.py` 적용)
2. 예측 ② 교차 검증: `cksumng_lines`(내 줄 수) vs `[CNT] cksumng=0`
3. 예측 ③ **위험 구간 폭** — `[SLOT-OOW]` **도착** 오프셋 35건의 분포. 폐기된 `[116,171)ms` 대체값
4. 3차①(`inj4`, 46거래) 최종 판정 — 표본이 작아 `inj4b` 복제가 필요한지 판단 포함
