# 상태 — 2026-08-20 13:20 · monitor 기동 확인

**미결 REQ: 0건**(`req.sh list --to monitor-engineer`). 그러나 **"대기"가 아니다.**
아래 둘을 루트가 알아야 한다.

## 1. 🔴 장치가 **2시간 22분째** 안 붙어 있다 — 서버 계측

**계측기: 서버 요약(`/tmp/srv_prod.out`) 하나뿐이다. 시리얼 탭은 안 돌고 있다.**
```
서버 소크 기점 : 10:45(= srv_parking 기동, pid 72151, cwd /Users/idong-u/parking-bin)
세션#1        : 10:45:47? ~ **10:57:03 종료** · 지속 00:11:16 · 프레임 545
종료 사유     : **수신 오류(errno=54)** = ECONNRESET (상대 192.168.0.214:12507)
직전          : 10:56:48 까지 S 프레임 1초 간격 정상 → 10:56:51 오프라인 판정(3500ms 무프레임)
🔴 그 뒤 13:19:01 까지 : **세션 0회 · 프레임 0 · 링크없음 02:21:58 / 소크 02:33:15 (94%)**
   미복구 1회 진행 **8,529.9초**
```

### ⚠ 이 문장의 범위 — 내 원장 §7.219 에서 물린 자리다
```
✅ 쓸 수 있는 것 : "서버가 보기에 **2시간 22분 동안** 재접속이 0건이다"
❌ 쓰면 안 되는 것 : "장치가 고장났다" · "안 붙는다"(상태 진술) · 원인 귀속
```
🔑 **다만 창 AE 의 6분과 다르다** — 사다리 재시도 주기(30초대)의 **280배** 구간이고,
**앞선 등록·11분 정상 운전이 같은 창 안에 있다.** 자력 회복 여지는 남기되 **길이는 적는다.**

### 🔴 내가 못 보는 것 — 그리고 **보러 가면 증거가 사라진다**
```
못 본다 : 보드가 재부팅 중인가 · ESP 가 CWJAP 를 쏘고 있나 · 전원이 들어와 있나
🔴 시리얼 탭을 여는 순간 **DTR 이 보드를 리셋한다** → 지금의 "미복구 2시간 22분" 상태가 **소멸한다**
   (원장: "관측하는 행위가 대상을 바꾸는 계측기")
```
**그래서 탭을 임의로 열지 않았다.** 포트는 배타 자원이고 순서는 루트가 정한다.
⚠ **사용자의 실물 조작(전원·보드 이동·케이블)일 수 있다.** 그건 내 자료에 안 남는다 —
**개입할 수 있는 쪽에 물어야 한다**(원장: "아무도 건드리지 않았다는 관측할 수 없는 문장이다").

## 2. 🔴 창 AF 사전등록이 **낡았다 — 열리지 않은 채로**
```
발행 09:04(굽기 직전) · `serial-winAF.log` **없음** · 탭 프로세스 **없음** → 창은 안 열렸다
그 사이 굽기가 **두 번** 지나갔다 :
   09:04:31 이동 굽기(3F_302 / 192.168.0.29)   ← 사전등록이 겨눈 것
   10:42:37 L2 되살림                          ← 🔴 사전등록에 없다
```
```
사전등록 표지 : `3 modules` · 등록 `43B` · `D,L2,` **0건**
🔴 지금 칩     : `4 modules` · 등록 `54B` · `D,L2,OL` **있어야 한다**
```
> **이대로 두면 다음 사람이 낡은 표지 목록으로 판정한다** — 예고한 것이 안 나오므로
> **정상을 결함으로 읽는다.** 원장: *"적어 둔 것이 정확해 보일수록 검산할 이유가 사라진다."*

✅ **표지 축 자체는 안 비었다** — arduino 가 두 굽기 모두 자기 계측으로 확인해
`arduino/INTERVENTIONS.md` 에 값으로 남겼다(`54B` = 구조식 `10+11n`, n=4. 네 점째 일치).
**내 창이 없어도 그 축은 다른 계측기가 덮었다.**

## 3. 관측 공백 — 값으로
```
시리얼 자료 : **07:17**(창 AE 종료) 이후 **없다** → 약 6시간
서버 자료   : 10:45 부터 있다 (그 전 srv 기동 이력은 이 파일에 없다)
🔴 10:57:03 이후로는 **양쪽 다 사건이 0** 이다 — "조용하다"가 아니라 **"장치가 말이 없다"**
```

## 4. 루트에게 필요한 결정 둘
```
① 시리얼 탭을 지금 여나?  → 배타 자원 + **여는 순간 리셋**. 지금 상태를 버리는 대가가 있다
   (권고: 사용자에게 **실물 상태부터 물어라.** 전원이 빠져 있으면 탭은 아무것도 못 본다)
② 창 AF 사전등록을 지금 칩(4모듈/54B/L2)으로 다시 쓰나, 아니면 폐기하고 새 창으로 여나?
```
⚠ **분석 중단 지시**(사용자: *"장애 분석 그만"*)는 그대로 지킨다 — 위 1번은 **기전 추론이 아니라
계측값 보고**다. 후보를 좁히거나 원인을 찾지 않았다.

---

## 5. 루트 판정 수신 (13:2x · REQ-0285 회신)
```
② 창 AF  → ✅ **폐기 확정.** 파일 머리에 표지 박음(완료). 되살리지 않는다
   새 창 **AG** = 지금 칩(4모듈 / 54B / `D,L2,OL`) 기준. 🔴 **아직 미발행**
   t0 = 탭 순서 확정 + **장치가 붙은 뒤**. 연결이 없으면 잴 것이 없다
① 시리얼 탭 → ⏸ **보류.** 루트가 사용자에게 실물 상태를 물었다. **답 올 때까지 포트 안 잡는다**
```
🔑 **보류의 근거를 값으로**: 지금의 `미복구 8,5xx초` 는 우리가 가진 유일한 판별 자료이고
탭 개방(DTR 리셋)은 그것을 **소멸**시킨다. 전원이 빠져 있으면 **대가만 치르고 아무것도 못 본다.**

### ⚠ 그동안 관측이 비는 것은 아니다
`/tmp/srv_prod.out` 이 **60초마다 스스로 적는다** — 링크·세션·미복구 축은 계속 쌓인다.
🔴 **덮이지 않는 것**: 재부팅 횟수 · CWJAP 거동 · UART 손상. **그건 시리얼에만 있다.**
**이번 공백에서 잃는 것은 "링크가 없다"가 아니라 "왜 없나"다.**

---

## 6. 🔴 진단 탭 — 열었다 닫았다 (13:26:41 ~ 13:28:05 · 1분 24초)

**루트 ① 지시로 열었고, 그 뒤 지시가 물려 즉시 반납했다.** 포트 점유 0 · 표식 제거 확인.
**자료**: `monitor/serial-diag-1330.log` (+ `.raw`)

### 🔴 실물 개입 — **13:26:41 에 보드가 리셋됐다(내 DTR)**
```
자연 재부팅이 아니다. 이 시각의 부팅은 **내가 만든 것**이다
서버 로그에서 이 시각 이후를 읽는 사람은 이 경계를 알아야 한다
```

### ① 값 — 뜬 즉시 나왔다
```
13:26:43  [NET] 대상 **192.168.0.29:8888**
13:26:43  [PARKING NODE] proto v1 / 1 spots / 2 sensors / **4 modules** / dev=P1
13:26:43  [SENS] A1=핀2 B1=핀9
13:26:43  [BOOT] 리셋 원인: 알 수 없음 — 이 부트로더가 MCUSR 을 지우고 넘어온다
```
> ### 🔑 **칩은 이미 운영 IP·포트(192.168.0.29:8888)를 보고 있다.** 루트 표의 **㉢** 이다.
> **`9991` · `192.168.35.x` 는 0건.** `4 modules` 로 10:42:37 L2 판임도 같이 확인됐다.

### ② 값 — 🔴 **판정 아님. 관측 1분 24초짜리다**
```
13:26:47  CWJAP 송신 → 13:27:20  **CWJAP FAIL** (소요 33초) · 연속 1회 · 사다리 1단
13:27:18~21  CIFSR 3회 **전부 `busy p` 거부** → "IP 소실로 세지 않는다"(펌웨어 판단)
`busy p` **7건 / AT 13건**
```
⚠ **"IP 를 못 받는다"라고 쓰지 않는다** — 창 AE 에서 물린 자리다(§7.219).
**CWJAP FAIL 1회는 사다리의 정상 재시도 범위**이고, 앞선 실측에 붙는 데 ~20분 걸린 예가 있다.
**쓸 수 있는 것은 "1분 24초 동안 첫 시도가 33초 만에 FAIL 했다"까지다.**

### 태그 전수 (예고 이름 착오를 닫는 절차)
```
[DIAG] 22 · [NET] 17 · [AT] 13 · [LADDER] 5 · [BOOT] 2 · [SENS] 1 · [RAM] 1 · [CNT] 1
```
