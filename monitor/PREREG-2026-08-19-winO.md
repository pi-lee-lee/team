# 사전 등록 · 창 O — `b0d58d9` 효과 + **A[1] 이전 기준선**

**발행 07:06** — 창은 `07:05:57` 에 열렸고 **판정 자료를 보기 전에 적는다.**
**요청** `REQ-0238`(root)

## 0. 경계와 축

```
서버 epoch : b0d58d9 · 06:58:36 · srv_id b0d58d9-1787090316424 · pid 62347
             ✅ 루트가 값으로 확인 · 나도 서버 로그 INSTANCE 줄로 독립 확인했다
옛 창 끝   : 06:58:18 (INSTANCE-END pid=86090 · 9a2e3bb · frames=1084)
🔴 내 t0    : **07:05:57** (탭 개방 DTR 리셋) · monitor/serial-winO.log · pid 85993
```

### ⚠ 서버 기동과 내 t0 사이에 **7분 21초 구멍**이 있다
`06:58:36~07:05:57` 은 **내 시리얼 자료가 없다.** 그 구간을 창 O 분모에 넣지 않는다.
서버 쪽 계수(`소크` 요약)는 **epoch 부터 누적**이므로 내 창과 **시작점이 다르다** —
🔴 **두 계측기의 수를 크기 비교하지 마라.** 같은 구간으로 자른 뒤에만 비교한다.

### 축 목록 (창 O 안에서 움직일 수 있는 것)
```
A  socket b0d58d9 — flush_downq() 를 push_snapshot() **앞으로**            ← 이번 창의 대상
B  내 DTR 리셋 07:05:57 — 부팅 국면이 창 머리에 있다                        (원장 7.174)
C  web 150ms 티커 — 부하 축. 화면이 붙어 있으면 돈다
🔮 D  **화면 배포**(index.html 이 a3922e0 구판) — 루트가 창 밖으로 미루려 한다.
      창 안에서 일어나면 **그 시각을 표시하고 구간을 가른다. 한 분모에 안 묶는다**
```

## 1. 🔴 기준선 — **결과를 보기 전에 떠 둔다** (07:05:37 서버 요약 · epoch 누적)

```
화면 1(최대 1)          ← 🔑 `cksumng` 의 남은 후보축. **이 값이 갈리면 근거가 된다**
+WS 업그레이드 완료 2건  (06:58:36 · 06:58:41 · epoch 이후 전부)
순서변경 0 · 치유 0/346 · G 띄움 0·도달 0·응답 0·거절 0
버린줄 0 · 재전송 0 · ACK실패 0 · 배치 0 · 큐넘침 0 · 오프라인 0회
등록 완료 1(미해독 1) · 예약미해독 1 · S최대 25B/가정 60B · W_srv 550ms
세션 1회(#1 연결중) · 프레임 360
```
⚠ **이 수치는 내 t0 이전 것이다**(epoch~07:05:37). **창 O 값이 아니라 진입 조건이다.**

## 2. 이 창이 답해야 하는 것 — **둘. 한 문장에 묶지 않는다**

### ① `b0d58d9` 효과 — `S+Nms` 분포
```
기준 : 창 I 9.4% · 창 J 10.2% · 창 M **40.9%** (>=2ms 비율)
답할 것 : **"고쳐졌다 / 안 고쳐졌다" 뿐이다**
```
🔴 **심각도를 새로 주장하지 않는다.** 창 M 의 40.9% 는 실재하지만 장치 도착 예산(264~293ms)
안이라 아무것도 안 깨뜨린다는 것이 이미 판정돼 있다. **그 판정을 다시 열지 않는다.**
⚠ `S+Nms` 는 **socket 의 계측기**다(서버가 언제 보냈나). 내 것은 **도착 오프셋**(장치가 언제 받았나)이다.
**같은 사건의 양 끝이고 같은 수가 아니다.** 내 값으로 socket 의 결론을 대체하지 않는다.

### ② **A[1] 이전 기준선** — 다음 배포(`rid` 폭 고정)의 대조군
```
· `>=64B` 진입률 + **분모**(무엇 분의 무엇 · 어느 구간)
· 🔴 `rid` 자릿수 분포 — 창 J 에서 3→4자리 전이가 진입률에 섞였다. **안 적으면 A[1] 을 못 가른다**
· ACK 실측 폭 `B` = (tx - 23) / N     ⚠ 파생값. 머리 기준폭 23B 가 틀리면 통째로 틀린다
· T2 / `SEND OK` 실종 · `cksumng` · `penddrop`  — **분모와 함께**
```
🔴 **창 M 의 진입률 74.8% 를 대조군으로 쓰지 마라.** `b0d58d9` 가 flush 시점을 바꾸므로
**배치 구성이 달라질 수 있다. 진입률 기준선도 이 창에서 새로 뜬다.**

## 3. 분모 규율 — 지난 창에서 실제로 틀렸던 것들

```
❌ 구간 이름이 안 붙은 비율은 쓰지 않는다   (내가 SPEC-분모정의 §7 에 적어 놓고 내 표에서 어겼다)
❌ 자극 부재 구간과 주입 구간을 한 분모에 묶지 않는다
❌ `0/0`·`0/1` 을 건강으로 읽지 않는다      (창 L 의 penddrop 0 이 `0/1` 이었다 · 원장 7.174.2)
❌ 독립인 주장을 한 문장에 묶지 않는다      (cksumng 이 철회될 때 SEND OK 는 그대로 섰다)
❌ 경고를 결론 *옆* 이 아니라 *안* 에 쓴다  (hex/cksumng 이 그래서 넓게 옮겨졌다)
🔴 `[CNT]` 값은 **줄이 아니라 필드다** — `grep -c` 로 세지 않는다 (원장 7.174.1)
```

## 4. 정지·판정 조건 — **그리고 그것을 보는 감시**

```
정지 : 탭 프로세스 사망 · 포트 소실 · 서버 epoch 변경(srv_id 가 바뀌면 창을 닫는다)
판정 : `>=64B` 대역 거래 **300건 이상**이어야 진입률·T2 를 인용한다. 그 미만이면 **"흔적"이라 적는다**
```
🔴 **감시를 같은 자리에 만든다** — 조건만 적고 안 보는 것이 이 팀에서 다섯 번 났다.
```
· 탭 생존   : 로그의 «tap» 살아있음 줄이 끊기면 계측기가 죽은 것 (원장 §계측기 판본)
· 표본      : 판정 직전에 `[CNT]` 의 bigfill 로 분모를 확인한다 — **0/0 을 건강으로 안 읽는다**
· 화면 축   : 서버 요약의 `화면 N(최대 M)` 을 창 중간에 한 번 더 뜬다. 1→2 로 갈리면 구간을 가른다
```

## 5. 🔴 계측기 상태 — **정직하게 적는다**

```
monitor/rid_width.py  (신규 · `rid` 자릿수 / N / ACK 폭 B)
   🔴 **아직 교정 안 됐다.** 창 M 으로 대 봤을 때 어긋나는 값이 있다:
      · CIPSEND 줄 수  파서 1,876  대  grep 1,910   → **34줄을 파서가 못 잡는다**
      · 진입률         파서 43.0%(파일 전체)  대  판정문 74.8%(구간 다름)  → 분모 정의가 다르다
   → **교정 전에는 이 도구의 절대값을 판정에 쓰지 않는다.** 창이 도는 동안 교정한다.

✅ **07:1x 교정 완료** — 어긋난 둘을 다 설명했다(`FINDING-2026-08-19-0715-rid-candidate-dead.md` §6):
```
· 34줄 차 : 잡음이 아니라 **혼재 사건**(`T,207,S,A2AT+CIPSEND=52`). 파서가 제외한 것이 옳다
· 미짝 233 : 알려진 **에코 실종/지연** 갈래
· 진입률 차 : **분모 정의가 내 쪽이 더 엄격**하다
🔴 그러므로 이 도구의 절대값을 **옛 판정문과 크기 비교하지 않는다. 창끼리 같은 도구로만 쓴다.**
✅ 교차 확인 : `[CKSUM NG]` 줄 수 = 장치 `[CNT]` 의 `cksumng` (세 창 정확히 일치)
```
```
⚠ **어긋난 채로 쓰면 그것이 계측기의 거짓말이 된다**(원장 6). 창 M 재계산으로 맞춘 뒤에 쓴다.

## 6. 개입 통보
```
🔴 탭을 **열 때와 닫을 때** 루트에 시각과 함께 한 줄 — 내 DTR 리셋은 남의 로그에 "원인 없는 절단"으로 남는다
   개방 07:05:57 통보 완료
```

## 7. 실행 기록

```
창 닫은 시각 : (미기입 — 관측 중)
🔴 무자극 구간 : 07:05:57 ~ **07:18:58.315**   (약 13분. 주입 구간과 **한 분모에 안 묶는다**)
🔴 주입 구간   : **07:18:58.315** ~ (진행 중)   ← socket 이 로그에서 읽은 값(injO1-3-1)
화면 교체    : **07:20:43** — 🔴 **주입 시작 +105초. 주입 구간 *안* 이다**(아래 사건 2)
표본         : 07:21 현재 bigfill **72/300** → **흔적. 아직 판정 안 한다**
감시기       : monitor/winO_watch.py (20초) → monitor/winO-watch.log · 300 도달 시 `.team/STOP-O` 생성
```

### 🔴 사건 3 — 정지선 도달 (07:29:10) · **300 이 아니라 312 다**
```
07:29:10  bigfill=**312** >= 300  →  감시기가 `.team/STOP-O` 생성 (사람 손을 안 거쳤다)
```
⚠ **300 에서 정확히 끊긴 것이 아니다.** 장치 `[CNT]` 가 60초 주기라 그 사이에 넘었다 —
**실제 통과 시점은 07:28:10~07:29:10 사이 미상**이다. **분모는 312 로 적고 300 으로 적지 않는다.**
🔴 **그리고 이 창은 8라운드 자연 종료가 아니다** — 정지 신호가 발동했으므로 **창 M 과 규약이 갈렸다.**
(시간 상한 40분은 안 걸렸다: 주입 07:18:58 시작 · 정지선 07:29:10 = 약 10분)
⚠ 정지 지연 상한은 **한 라운드**다. 그 초과분이 분모에 들어간다 — **끊긴 것처럼 쓰지 않는다.**

### 주입 규약 — **socket 이 값으로 준 것을 그대로 옮긴다** (기억으로 "창 M 과 같게"가 아니다)
```
sh net/winG-rounds.sh 8 O
  라운드 8 · 인스턴스 8 (슬롯 A1 A2 A3 A4 A5 B1 B2 B3)
  downlink_inject.py --port 9900 --slot <S> --interval 0.3 --start-delay 0 --count 60
                     --rid-prefix injO<r>-<i> --stop-queue-full 20
  상한 3,840건 · 기록 net/inject-winO-2026-08-19.log
  서버 build=b0d58d9 · pid 62347 · srv_id b0d58d9-1787090316424
```
⚠ **창 M 에 없던 장치 둘이 이 창에 있다**(socket 통보): **정지 신호(`.team/STOP-O`)** 와 **시간 상한 40분**.
**발동하면 규약이 달라진 것이므로 판정문에 적는다.** 8라운드로 자연 종료면 창 M 과 같은 규약이다.
⚠ **정지 지연의 상한은 한 라운드**다(약 3분). 라운드 중간에는 못 멈춘다 → **그 지연분이 분모에 들어간다.**

### 🔴 사건 2 — `화면` 축이 **주입기에 오염됐다** (07:19:39)
```
07:13:39~07:18:39  화면 1(최대 1)     ← 내가 뜬 기준선
07:19:39           화면 **9(최대 9)**  ← 주입 시작 07:18:58.315 를 걸치는 구간
WS 업그레이드 완료  기준선 2건 → 284건
```
**주입기 인스턴스가 8개다. 8 + 기존 화면 1 = 9.** `화면` 은 이름이 브라우저를 뜻하는 것처럼 읽히지만
**실제로 세는 것은 `:9900` 의 WS 클라이언트**이고 **주입기도 거기 붙는다.** 이름과 정의가 어긋난 자리다.
```
🔴 결과 ① `cksumng` 후보 "화면 수"를 **이 창에서 검정할 수 없다** — 주입이 화면과 cksumng 을 같이 올린다.
        그건 인과가 아니라 **교락**이다. 후보 목록에 그대로 둔다(죽이지도 살리지도 못한다)
🔴 결과 ② 화면 교체(07:20:43)가 **실행 중인 화면에 반영되는 시각을 못 본다.**
        서빙 파일은 바뀌었고 실행 중인 페이지는 안 바뀌었다. 새로고침 시각은 **미상**이다
        (화면 수로도 WS 업그레이드 수로도 못 잡는다 — 둘 다 주입기가 덮는다)
⚠ 결과 ③ 창 M 과 비교 불가 — 그 창 서버 판(7c73c39)에는 이 필드가 없었다.
        **창 O 가 이 축의 첫 표본인데 첫 표본이 이미 오염돼 있다**
```
✅ **확정도**: 시각 일치 + 인스턴스 수 8 이 맞는 것까지가 근거다. **지금은 유력이지 단정 아니다.**

#### 🔴 내가 제안했던 검정은 **실행 불가능하다** — 폐기하고 바꾼다
```
제안했던 것 : "라운드 사이에 화면이 1 로 떨어지는지 본다"
실측        : 라운드 1 종료 07:22:14 → 라운드 2 시작 07:22:19  = **틈이 5초**
계측기      : 서버 요약은 **60초 주기**(매분 :39)  → **그 틈을 뜰 수 없다**
```
> **못 보는 것을 검정으로 적으면 "확인할 예정"인 채로 영영 안 닫힌다.** 내가 조건에 대해 적어 둔 것과 같은 함정이다.

✅ **실행 가능한 검정으로 바꾼다: 주입이 *끝날 때* 화면이 9 → 1 로 떨어지는가.**
주입 종료 뒤 휴지는 60초 주기보다 길다. **창을 닫을 때 반드시 이것을 확인하고 판정문에 적는다.**
(떨어지면 확정. 안 떨어지면 8은 주입기가 아니고 **다른 설명을 찾아야 한다**.)

#### ✅ 교체가 트래픽을 안 바꿨다는 **실측 대조** (루트 제공)
```
WS 업그레이드/분 :  07:19 **150**  ·  07:20 **172**  ·  07:21 **148**
                                        ↑ 화면 교체 07:20:43 이 낀 분
```
**교체 전후가 같은 대역이다.** 172 는 주입기 라운드이지 교체가 만든 것이 아니다.
서버 재기동 없음(`serve_file` 이 요청마다 연다) · WS 클라이언트 증감 0 · 장치 세션/주입 트래픽 영향 0.
> 🔑 **그러므로 "교체가 창을 오염시켰다"는 근거가 없다. 그리고 "구간 밖이었다"도 거짓이다.**
> **사실만 적는다: 07:20:43 에 서빙 파일이 바뀌었고, 그 시점에 관측 가능한 변화는 없었다.**

#### 📌 이 창에서 `화면 수` 축은 **포기한다** (루트 판단 · 동의)
창을 자르는 비용이 그 축의 값보다 크다. **`>=64B` 300건(A[1] 대조군)이 이 창의 목적이고 그건 화면 축과 무관하다.**
⚠ **열려 있던 브라우저가 새 화면을 언제 집어드는지는 미상이다. 대체 주장을 세우지 않는다.**

### 🔴 사건 1 — 링크 11초 단절 (07:10:54~07:11:05) · **귀속 안 함**
```
07:10:54  [DIAG] offline · ESP busy 거부 반복
07:10:58  [NET] "CIFSR 3회 소진 — 그러나 busy 거부였다. **IP 소실로 세지 않는다**"  ← 펌웨어 원문
07:11:05  [NET] online (CONNECT)      07:11:07  [REG] 등록 전송 145B
계수 : drop 0→1 · okto 0→1 · stuck 0→1 (up=300) · esprst 0→1 · smiss 0→1 (up=360)
```
✅ **내 탭이 아니다** — 부팅 60초부터 t0+4분까지(up=60·120·180·240) 위 계수가 **전부 0** 이었다.
   사건은 t0+5분이다. **내 DTR 리셋으로 설명되지 않는다.**
✅ **IP 소실이 아니다** — 1차 판별자(`CIFSR → 0.0.0.0`)가 아니라 **busy 거부**다(기전 A 계열).
🔴 **`b0d58d9` 에 귀속하지 않는다.** 창 O 가 첫 창인 것은 맞지만 **창 M 은 주입이 돌던 창이고
   창 O 는 자극이 없다. 조건이 달라 크기 비교가 성립하지 않는다.** **원인 미상.**

### ⚠ 막힘 — 자극이 없으면 ② 를 못 낸다
`>=64B` 대역은 **주입이 만든다**(원장 7.174.2 · 창 M 에서 `bigfill` 이 주입 전까지 1 고정이었다).
**지금 상태로는 창 L 과 같은 모양이 된다 — 분모가 안 차고, 그때의 `0` 은 건강이 아니라 못 센 것이다.**
①(`S+Nms`)은 **socket 의 계측기**라 내가 못 낸다.
