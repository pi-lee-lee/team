# 인수인계 — **관측 창 4** (2026-08-17 아침 · ~~🟢 지금도 돌고 있다~~ → 🔴 **관측 정지 상태**)

> **현재 유효한 인수인계다.** 확정 `2026-08-17 08:35`.
> 창3 판정: `monitor/JUDGMENT-2026-08-17-win3.md` · 창3 인수인계: `HANDOFF-win3.md`(판정 절차 정본)

---

## 🔴🔴 헤더 정정 — **`2026-08-17 19:28` 실측: 아무것도 기록되고 있지 않다**

이 파일의 제목·§0·§1 은 **"탭·요약기가 PPID=1 로 계속 돈다"** 를 전제로 쓰였다.
**그 전제는 지금 거짓이다.** 아래는 `19:28` 에 내가 직접 잰 값이다(원장 7.91 — 인용할 때
"언제 확인한 값인가"를 붙여라).

| 무엇 | 이 파일 본문(08:35 기준) | **19:28 실측** | 근거 |
|---|---|---|---|
| 시리얼 탭 | pid 53235 · PPID=1 · 기록 중 | 🔴 **없다.** `17:59:07` 종료 | `ps` 0건 · 로그 마지막 줄이 종료 경계 |
| 상시 요약기 | pid 90629 · PPID=1 · 5분 주기 | 🔴 **없다.** 산출 마지막 `14:55:39` | `ps` 0건 · `win4-pulse.tsv` mtime `14:55` |
| 주차 서버 | pid 18422 (`22f788d`) | ⚠ **다른 인스턴스** — `srv_parking` **pid 46224** (`17:57:49` 기동) | `ps` etime |
| 포트 | 탭이 점유 | **아무도 안 잡고 있다** | `lsof /dev/cu.usbmodem21201` → 0건 |
| `PORT-IN-USE.txt` | 양보 시 생기는 표식 | **없다** | `ls` |

**탭 종료는 죽은 것이 아니라 정상 종료다 — 실측:** 로그 마지막 줄이
`17:59:07 «tap» 캡처를 닫는다 — SIGTERM 받았다. 이 줄이 이 로그의 마지막 경계다.`
→ 경계 줄을 쓰고 닫았다. `PORT-IN-USE.txt` 가 없으므로 **양보 경로는 타지 않았다**(그 표식은
"남이 포트를 잡고 있어 못 열었다"를 뜻한다). **SIGTERM 을 누가 보냈는지는 미상이다** —
`17:59:3x` 의 `fdtest` 플래싱 직전 정지로 **추정**되지만 확정 근거는 없다. `?.미상` 으로 둔다.

🔴 **그래도 "관측이 손실되고 있다"로 읽지 마라.** 지금 칩은 `fdtest`(§"지금 칩은 fdtest 다")로
**주차 프레임을 안 보낸다.** 서버·web 이 "아두이노 오프라인"으로 보고해도 정상이고,
**지금 열린 관측 창이 없으므로 잃고 있는 자료도 없다.**

⚠ **그러므로 이 파일의 §0 "셋 다 PPID=1 이고 로그가 자라면 아무것도 하지 마라" 를
"탭을 다시 띄워라"로 읽지 마라.** 탭 재개는 **DTR 리셋을 만들어 t0 가 둘이 된다** —
§7 금지 항목이고 **루트가 순서를 정한다.**

> 정정 `monitor-engineer` 2026-08-17 19:28 (재기동 직후 상태 확인 · 실물 미접촉)

---

# 🔴🔴 창4 는 **`2026-08-17 14:48:10` 에 끝났다** — 그 뒤 자료는 다른 펌웨어다

```
14:48:10  [PARKING NODE nextgen] 배너 등장  ← **다른 펌웨어가 구워졌다**(0fba51f 아님)
15:00~16:08  재부팅 반복 · 배너 20회 이상
16:07:39  서버 인스턴스(pid 31062) 정상 종료  ← 그 뒤 서버 없음
17:4x     장치는 [TX-DROP] run 11800+ · online=0  (보낼 서버가 없다)
```

🔴 **창4 판정 구간은 `t_start 07:53:49 ~ 14:48:10` 이다.** 그 뒤는 **다른 펌웨어 · 서버 없음**이라
**같은 창이 아니다. 합산하지 마라.**

⚠ **탭(pid 53235)은 그 경계를 넘어 계속 기록 중이다** — `nextgen` 펌웨어의 거동이 그대로 남는다.
**버리지 마라.** arduino 의 새 빌드 자료다. 다만 **창4 통계에는 넣지 마라.**

---

## 🔴🔴 창4 를 판정할 때 **반드시 갈라야 하는 구간** — 브라우저 예약

```
13:18:43 ~ 16:07:09   브라우저 사용자가 예약을 누른 구간 (하행 86건)
                       user_id=uk43s3dv · rid r1~r52 · 한 세션이 52회
                       ⚠ socket 의 시험은 u17 · rc-* 10건뿐(13:05:32~13:07:29) — 별개다
                       **`user_id` 하나로 전량 갈린다**
```

🔴 **그 구간에 사건이 몰려 있다:**
```
창4 사건 11건 중 **7건**이 브라우저 구간 안
브라우저 구간 1.49h · 7건 → **4.70/h**
그 전       5.42h · 4건 → **0.74/h**       배수 **6.4배**
귀무(시간 비례) 기대 2.37건 · 관측 7건 → **P(X>=7) = 0.0031**
```

> 🔴🔴 **19:5x 정정 — 위 `p=0.0031` 을 그대로 인용하지 마라.**
> 브라우저 7건 중 셋이 **쌍**(34s·19s·86s)이다. **연쇄를 접으면 4건 · `p=0.072`** 로
> **유의하지 않다.** 접기 규칙은 이 파일 §4 에 내가 적어 뒀는데 비율에 적용하지 않았다.
> ✅ 대신 **순서**(신규 하행이 2~3s 선행 · 리셋형 4/4 vs 비리셋형 0/4 · Fisher `p=0.014`)가
> 접기에 안 흔들린다. → **판정 정본: `monitor/JUDGMENT-2026-08-17-win4.md` §4·§5**
✔ 교락 확인: 사진 촬영(08:14·08:23) · 그라운드 이탈(11:25~11:40) **둘 다 이 구간 밖**이다.

⚠ **그러나 CPU 경합 하나는 이 구간 전체를 덮는다** (socket 통보 17:55):
```
server_test(+600)  pid 39901   08:33 ~ 17:55:43   9시간 19분
fake_arduino       pid 42464   08:36 ~ 17:55:43   (--drop-rate 1.0)
```
**web 의 경우 C 측정용으로 띄운 것을 아무도 안 내렸다.** 운영 포트·로그에는 **안 섞였지만**
**CPU·메모리를 9시간 점유했다.**
🔴 **브라우저 구간(13:18~14:48)도 그 안에 있다.** → **이 대비의 교락 후보로 남겨라.**
⚠ 다만 **그 전 구간(08:33~13:18)도 똑같이 덮여 있다** — 두 구간에 **같이** 걸려 있으므로
**두 구간의 차이는 설명하지 못한다.** (덮는 방식이 균일했다면 그렇다. 확인은 못 했다.)

### 🔴 이 86건은 **자연 조작이 아니라 사용자가 의도적으로 한 부하 시험**이다 (사용자 정정 17:5x)

**그러면 결론이 둘로 갈린다. 둘 다 적어야 한다.**

**① 기전 쪽으로는 오히려 강해진다** — **조작된 변수**다. 사용자가 하행 부하를 의도적으로 올렸고
그 구간에 끊김이 6.4배 났다. **관측 연구가 아니라 개입 실험에 가깝다** —
🔑 **우리가 설계하려다 못 만든 그 실험(유도 시험)을 사용자가 만든 셈이다.**

**② 발생률로는 못 쓴다** — `4.70/h` 는 **"사람이 화면을 두들길 때"의 값**이지 실사용 값이 아니다.
```
1.49시간에 예약 86건 ≈ 58건/h   ← 시연 부하가 아니다
```
🔴 **이 값을 "실사용 끊김률"로 인용하지 마라.**

⚠ **그리고 용량-반응 곡선이 없다.** 부하가 극단이라 **"이 정도로 밀면 끊긴다"는 보였지만
"얼마부터 끊기나"는 여전히 모른다.** 시연 수준 부하에서 어떤지는 **측정된 적이 없다.**

🔑 **하행 가설의 첫 통계적 근거다** — 노출 86건 · p=0.003.
   반대편에 **창3 대조**(하행 1건 · 7.37h · 손상 0 · 끊김 0)가 붙는다. **양 끝이 같은 방향이다.**

⚠ **단정 범위 셋을 반드시 같이 적어라:**
1. **연관이지 인과가 아니다** — 그 3시간을 아무도 안 봤다
2. **하행 자체인지, 하행이 유발한 `resync_reservations` 추가 상행인지 못 가른다**
3. **반이중 근본 원인과 모순되지 않는다** — 들어오는 것이 늘면 충돌이 는다는 예측과 같은 방향

→ **창4 λ 를 낼 때 이 구간을 갈라라. 통짜로 내면 두 다른 조건이 섞인다.**

---

🔴 **요약기(win2_pulse)는 `14:55:39` 이후 죽어 있다.** 마지막 TSV 행이 그 시각이다.
**그 뒤 구간은 자동 요약이 없다** — 필요하면 로그에서 직접 세라.

## 🔴 `14:48:10` 이후의 구간 셋 — **전부 창4 와 별개다**

| 구간 | 무엇 | 쓸 수 있나 |
|---|---|---|
| `14:48:10 ~ 17:57:49` (3.16h) | **nextgen · 서버 없음** — 장치가 헛돌았다. `[TX-DROP]` **12,991건** | ❌ λ 아님. **"보낼 곳이 없을 때의 거동"** 자료 |
| `17:57:49 ~ 18:0x` | **nextgen + 서버**(pid 46224) — `[LINK] online (first)` 즉시 접속 | ⚠ 짧다. 별도 구간 |
| `17:59:3x ~` | **`fdtest` 최소 샘플이 구워졌다**(직전 = nextgen · 미상 커밋) | 별개 |

### 🔴 지금 칩은 `fdtest` 다 — **"장치 오프라인"은 고장이 아니다**

`fdtest` 는 **주차 클라이언트가 아니다.** `U,<seq>,…` 만 흘리는 계측용 스케치라
**서버에 상행 프레임이 안 간다.**
→ **서버·web 이 "아두이노 오프라인"으로 보고해도 그것은 정상이다.**
⚠ **08-15 에 `esplink/example` 이 올라가 있어 같은 오해가 났다**(원장에 있음). **두 번째다.**
🔑 **펌웨어를 바꿀 때는 "그 칩이 무엇을 안 보내는가"를 같이 알려야 한다** — 안 그러면
   다른 도메인이 없는 고장을 쫓는다.

### 개입 기록 — web 브라우저 시험 2차 (18:51~19:05:52 · **시험 인스턴스 +600 · 운영 미접촉**)

| # | 무엇 | 시각 | 근거 |
|---|---|---|---|
| 1 | 아이들 실제 WS | ≈18:51 | ⚠ 추정 |
| 2·3 | e2e 1·2차 | ≈18:52 · ≈18:53 | ⚠ 추정 |
| 4 | 문구 재측정 | **18:55:22~18:55:50** | 정확 |
| 5 | `queued` 확인 | ≈18:57~18:59 | ⚠ 추정 · `file://` 데모라 **서버 접촉 0** |
| 6 | 경우 C | **19:03:24~19:03:53** | 정확 |

**socket 의 `fake_arduino` 재시작 둘**: `18:54:21~18:54:30` · `19:00:29~19:00:31`.
🔴 **상대는 `fake_arduino` 다. 실물 보드가 아니다** — 그 시각 실물 칩은 `fdtest` 이고
`/dev/cu.usbmodem21201` 은 아무도 안 읽고 있었다(내가 확인). **운영과 완전히 분리됐다.**

### `fdtest` 결과 (arduino · 45초 · **내 창 자료와 나란히 놓지 마라**)

```
맥   : gap=0                       ← 하나도 안 잃었다
장치 : gap=36 bad=32 availhi=63    ← 링버퍼(64B)가 천장을 쳤다
```
- **전이중 자체는 된다** — 양방향이 동시에 회선 상한 근처로 흘렀다
- 장치가 약 **5.5%** 를 잃었고 원인은 *"못 들음"이 아니라 **"받아 놓고 못 꺼냄"***
- arduino 판단: **자기 샘플 결함일 가능성이 높다**(보고 줄 90B ≈ 94ms 블로킹 · 링버퍼는 66ms 분)

🔴 **창4 수치와 비교 금지** — 상대·부하·보율·펌웨어가 전부 다르다.
**물은 것은 손실률이 아니라 "구조적으로 되는가" 하나였고, 답은 "된다"** 이다.

🔴 **셋 다 창4(`07:53:49~14:48:10`)와 합산 금지.** 펌웨어도 서버 유무도 다르다.
⚠ **`monitor/serial-win4.log` 를 지우지 마라** — `nextgen` 거동이 그 안에 있다(경계 뒤 구간).
🔑 `17:57:49` 에 **즉시 붙었다** — 창2 를 죽인 IP 불일치 같은 문제는 없다.

---

# (이하는 창4 진행 중에 쓴 것 — 위 경계 이전에만 유효)

# ~~🟢 관측은 안 끊겼다 — 아무것도 재기동하지 마라~~ (🔴 **19:28 만료 — 위 헤더 정정을 보라**)

> 🔴 **이 절의 pid 셋은 전부 죽었다**(탭 `17:59:07` · 요약기 `14:55:39` · 서버는 다른 인스턴스).
> 아래 문장은 **08:35 시점의 기록**으로만 읽어라. 점검 절차 자체는 그대로 유효하다.

**우리(에이전트) 세션이 전부 죽어도 아래 셋은 `PPID=1` 이라 계속 돈다. 자료는 쌓이고 있다.**

```
탭        pid 53235   PPID=1   monitor/serial_tap.py  → monitor/serial-win4.log
요약기    pid 90629   PPID=1   monitor/win2_pulse.py  → WIN4-STATUS.md · win4-pulse.tsv
서버      pid 18422   PPID=1   srv_parking (build=22f788d)
```

🔴 **"관측이 끊겼다"고 오해하고 탭을 다시 열면 DTR 로 보드가 리셋되어 창이 깨진다.**
**확인부터 해라 — 30초면 된다:**
```
ps -Ao pid,ppid,command | grep -E 'serial_tap|win2_pulse|srv_parking' | grep -v grep
ls -la monitor/serial-win4.log     # 두 번 재서 크기가 느는가  ← 이것이 진짜 판정
cat  monitor/WIN4-STATUS.md        # 5분마다 자동 갱신
```
**셋 다 PPID=1 이고 로그가 자라면 아무것도 하지 마라.**
⚠ **PPID 는 부모 셸이 끝난 뒤에 재라**(원장 7.28). 띄우자마자 재면 부모 pid 가 찍힌다.
⚠ **프로세스 생존 ≠ 기록 지속.** 포트가 끊기면 살아도 로그가 안 자란다. **둘은 다른 고장이다.**

## 🔴 노출 경고 — **지금 닫으면 또 분모 부족이다**

```
창4 노출: t_start 07:53:49 ~ 08:35 = 약 41분
창3      : 7.37시간 (그것도 겨우 λ ≤ 0.41/h 를 냈다)
```
**창3 과 λ 를 견주려면 한참 모자라다.** 지금 "끊김 1건"으로 판정을 내면
**분모가 없어서 아무것도 못 말한다.** **판정을 요구받으면 먼저 노출부터 확인해라.**

## 창4 사건과 노출 (12:55 기준)

🔴 **사건 단위 = `전송 3회 연속 실패` OR `★ 정지 감지`**(창3·창4 만. A 창은 3진아웃만 — 원장 7.61)
**3진아웃만 세면 4건 중 2건을 놓친다.**

```
07:54:40  3진아웃      B.리셋계열   ← 🔴 우리 하행 주입. λ 에서 뺀다
09:02:15  ★ 정지 감지  10000ms (busy 8)
09:13:55  ★ 정지 감지  10000ms (busy 8)
11:53:08  3진아웃      ?.미상       ← 못 갈랐다(원장 7.60). 갈래를 만들지 않았다
```

```
총 노출 5.02h − 제외 0.25h(11:25~11:40) = **유효 4.77h**
```

### 🔴 λ 를 내기 전에 — **"자연 사건"이 몇 건인지 다시 세라 (13:0x 정정)**

내가 처음에 **"자연 사건 3건 · λ = 0.63/h"** 라고 냈다. **틀렸다.** arduino 가 `esprst` 오탐을
잡아 주면서 귀속이 바뀌었다(원장 7.73·7.74):

| 시각 | 무엇이 끊었나 |
|---|---|
| 07:54:40 | **우리 하행 주입** (진짜 리셋 동반) |
| 09:02:15 | 🔴 **우리 정지 감지기**(`★ 정지 감지 10000ms`) — 리셋 아님 |
| 09:13:55 | 🔴 **우리 정지 감지기** — 리셋 아님(`esprst` 는 **오탐**) |
| 11:53:08 | **못 가른다**(`?.미상` · `esprst` 오탐 의심) |

→ **4건 중 "장치가 스스로 죽어서" 끊긴 것은 확실히 0~1건이다.**
  **나머지는 우리 코드가 끊었거나 못 가른다.**

🔴 **그러므로 창4 에서 "자연 발생 λ" 를 낼 수 없다.**
`0.63/h` 는 **우리 코드가 만든 끊김을 자연 사건으로 센 값**이다. **인용하지 마라.**
⚠ **창3(0건)과 견주는 것도 그래서 성립하지 않는다** — 창3 에는 그 코드 경로가 사실상 안 돌았다
(정지 감지 1건 = 언더플로).
⚠ **`실패 1/3` 도 같이 봐라** — 3진아웃은 "3초 안에 3연속"을 요구해 **짧은 흔들림을 놓친다**(원장 7.58).
🔑 **`09:02`·`09:13` 의 값이 `10000ms` 다**(언더플로 `2^32−64` 아님) → **기전 C 수정은 작동한다.**
⚠ 다만 **`busy 8`** 이다 — **2단계가 자해를 없앤 게 아니라 `busy` → 정지 감지 경로로 옮겼다**(n=2).

---

> 작성 `monitor-engineer` 2026-08-17 07:5x · 최종 확정 08:35

---

## 0. 🔴 30초 점검

```
ps -Ao pid,ppid,command | grep -E 'serial_tap|win2_pulse|srv_parking' | grep -v grep
ls -la monitor/serial-win4.log       # 두 번 재서 크기가 느는가
cat  monitor/WIN4-STATUS.md          # 5분마다 자동 갱신
cat  monitor/ALERT-win4-stall.md     # 있으면 기전 C — §4
lsof /dev/cu.usbmodem21201           # 포트 판정의 1차 근거는 항상 이것
```

⚠ **`PPID` 는 부모 셸이 끝난 뒤에 재라**(원장 7.28). 띄우자마자 재면 부모 pid 가 찍힌다.
⚠ **프로세스 생존 ≠ 기록 지속.** 포트가 끊기면 살아도 로그가 안 자란다. **둘은 다른 고장이다.**

---

## 1. 프로세스와 경로  🔴 **아래 pid 는 08:35 기록이다 — 19:28 실측은 헤더 정정 표를 보라**

| 무엇 | 값 |
|---|---|
| 시리얼 탭 | **pid 53235** · PPID=1 · `/dev/cu.usbmodem21201` @115200 |
| 상시 요약기 | **pid 90629** · PPID=1 · 5분 주기 (`espbanner` 분리판) |
| 주차 서버 | `srv_parking` **pid 18422** · PPID=1 · `build=22f788d` · 기동 00:02:44 |
| **시리얼 로그** | **`monitor/serial-win4.log`** (+`.raw`) |
| 자동 요약 | `monitor/WIN4-STATUS.md` · `monitor/win4-pulse.tsv` |
| 기전 C 경보 | `monitor/ALERT-win4-stall.md` (요약기가 자동으로 씀 · §4) |

⚠ 같은 디렉터리에 창이 넷 있다. **파일 이름을 확인하고 열어라**:
`serial-newbase.log`(A) · `serial-win2.log`(폐기) · `serial-win3.log`(닫힘) · **`serial-win4.log`(지금)**

---

## 2. 창의 시각

| 기호 | 값 |
|---|---|
| **`t0`** (탭 개방 DTR 리셋) | **2026-08-17 07:51:49** |
| 첫 부트 배너 | 07:51:51 |
| 첫 `+? 연결 수락` | 07:52:01 — ⚠ **링크 성립으로 쓰지 마라** |
| 첫 `←ARD S` | **07:52:01** (`S,0` · uptime **9s**) |
| **`t_start`** | **2026-08-17 07:53:49** (`t0+120s` 와 첫 `←ARD S` 중 늦은 쪽) |
| `t_end` | 🔴 **비워 둔다.** 미래를 넣지 마라(원장 1.2) |

🔴 **t0 의 리셋과 첫 재접속은 우리 것이다.** `uptime 9s` 가 갓 부팅을 보여 준다.
도구가 **"재부팅 1건"** 으로 분류한다 — **사건으로 세지 마라.**

### 🔑 기동 창 **12초** — 원장 6.5 가 **네 번째** 계열에서 버텼다
EspLink 12s · `fa52a51` 12s · `64fc70b` 11s · **`0fba51f` 12s.**

---

## 3. 구성 — 창3 과 **무엇이 다른가**

| | 창3 | **창4** |
|---|---|---|
| 펌웨어 | `64fc70b` | **`0fba51f`** · DEBUG=1 · hex 대조 IDENTICAL · 굽기 **07:49:50** |
| 서버 | `22f788d` (pid 18422) | **같음 · 같은 인스턴스** |
| 하행 | 1건(01:25:04) | **1건 주입됨(07:54:35) → 끊김 1건 → socket 이 07:55:31 정지.** 이후 무주입 |

**펌웨어에서 바뀐 것은 둘뿐이다**(arduino):
1. **`lastTxOkAt` 비교를 폭 맞춘 부호 비교로** — **기전 C 수정**
2. `linkDrops` 선언 주석 정정 (**값·거동 불변**)

🔑 **그래서 이 창은 창3 과 달리 축이 하나만 바뀌었다** — 서버가 같은 인스턴스다.
**창3 이 "펌웨어·서버 동시 변경이라 원인을 못 가른다" 였던 것과 다르다.**
⚠ 다만 **λ 를 창3 과 견주려면** 펌웨어가 바뀌었다는 것을 여전히 적어야 한다.

### 🔴 이 창의 목적은 기전 C 검증이었다 — **그런데 검증에 실패했다(헛시험)**

전문: `monitor/FINDING-2026-08-17-0754-downlink-collision.md`

```
07:54:35  주입 1건  →  07:54:40  끊김 1건  →  07:55:31  socket 정지
```
- **끊김의 기전은 C 가 아니라 `B.리셋계열`** — `★ 정지 감지` **0건**, 배너 + `0.0.0.0`, `esprst=1`.
- 🔴 **그리고 C 는 시험되지 못했다.** `+IPD` **0건** · `[TX] A,` **0건** —
  **장치가 ACK 를 안 보냈으므로 언더플로를 만드는 코드 경로를 지나간 적이 없다.**
  → **`★ 정지 감지 0` 은 "고쳐졌다"가 아니라 "못 셈"이다**(원장 1.1).

🔴 **"C 를 고쳤다" 도 "이 자극에서 재현 안 됐다" 도 쓰지 마라. "시험되지 않았다" 가 전부다.**
→ **순서가 뒤집힌다**: C 를 시험하려면 `+IPD` 가 온전히 도착해야 하는데 지금 하행을 쏘면
  UART 에서 깨진다. **하행 흐름 제어(B 경로)를 먼저 풀어야 C 를 시험할 수 있다.**

---

## 4. 🔴 기전 C 감시 — **자동. 내가 죽어도 잡힌다**

`monitor/ALERT-win4-stall.md` 를 요약기가 5분마다 판정한다.

```
정지 감지 <= 하행(+IPD) + CONNECT 수   →  ✅ 알려진 경로
정지 감지 >  그 합                      →  🔴🔴 전제가 깨졌다
```

⚠ **경보 파일의 현재 식은 `<= +IPD` 까지만 본다.** arduino 가 `lastTxOkAt` 대입을
**`:2148 ALREADY CONNECTED` · `:2160 CONNECT`** 에서도 찾았으므로 **재접속만으로도 발동 가능**하다
(**C-2**). 창3 에서는 C-2 **0건**이었다. **판정할 때는 `+IPD + CONNECT 수` 로 계산하라.**

⚠ 🔴 **`★ 정지 감지 0` 을 수정 확인으로 읽지 마라** — §3 을 보라. **하행이 장치에 도달해
`[TX] A,` 가 나가는 조건**에서만 그 0 이 뜻을 갖는다. 창4 는 그 조건을 못 만들었다.
→ **경보의 용도는 "C 가 새로 뜨는가" 를 놓치지 않는 것**이지 수정 검증이 아니다.

**연쇄 접기**: `CONNECT` 직후의 C-2 는 **새 사건이 아니라 앞 사건의 꼬리**다.
**첫 끊김만 세고 접은 건수를 따로 찍어라.**

---

## 5. ⚠ 이름 함정 — **요약기 표는 "로그 줄 수" 다**

`WIN4-STATUS.md` 의 `txdrop`·`txresync`·`txwait_skip`·`txwait_okto` 는
**펌웨어 `[CNT]` 의 같은 이름 필드와 다른 것을 센다.**

🔴 **2026-08-17 07:4x 에 이것으로 사고가 났다.** 옛 칸 이름이 `drop` 이었고 루트가 표의
`drop 15`(=`[TX-DROP]` 줄 수)를 펌웨어 `[CNT] drop=`(=`linkDrops`)으로 읽고 **가설을 세웠다.**
실제 `[CNT] drop=1` 이었고 **가설은 폐기됐다.** → 칸 이름을 원문 표지대로 바꿨다.

**펌웨어 값이 필요하면 `[CNT]` 줄을 그대로 봐라** (STATUS 파일 하단에 마지막 줄이 찍힌다).

---

## 6. 창3 에서 이월되는 주의 (전부 유효)

| | |
|---|---|
| 장치 주기 | **약 1.11~1.12s** — **"1Hz" 를 상수로 쓰지 마라.** `3500ms` 판정이 3.14주기다 |
| `2s` 간격 | **맥놀이 + `skip` 이 섞인다.** `skip` 은 `3s` 도 만든다 → **문턱만으로 안 갈린다** |
| 🔴 거꾸로 읽기 | **링크가 나빠질수록 `skip` 이 늘어 `2s` 가 는다.** "맥놀이가 더 보인다"로 읽으면 **정반대** |
| 벽시계 | **창3 에서 두 번 뒤로 갔다**(−3s · −1s · NTP 추정). **±5초 근접 분석에 걸린다** — 장치 `uptime`(단조)을 대조로 |
| `resync_gap.py` | **이 칩에 그대로 쓰지 마라** — `skip` 결말이 N 칸을 오염시킨다. N/P/S 3칸으로 고친 뒤 |
| `frozen-A/busy_runs.py` | **구간 인자를 안 받는다** — `slice_window.py` 로 입력을 잘라 먹여라 |

**도구**
```
python3 monitor/frame_gaps.py ~/parking-logs/parking-server.log <시작> <끝> auto 5 monitor/serial-win4.log
python3 monitor/coincidence.py 5 <창시작> <창끝> <주입시각파일> <사건시각파일>
```
⚠ `frame_gaps.py` 의 6번째 인자가 **시리얼 로그**다(skip 대조용). **창4 것을 넘겨라** — 기본값이
`serial-win3.log` 라 안 바꾸면 **엉뚱한 창의 skip 을 뺀다.**
⚠ `coincidence.py` 는 **인자 없이 돌리면 A 창을 재현**한다(교정용). 기본값을 고치지 마라.

---

## 6.35 🔴🔴 창4 의 **실물 개입 · 제외 규칙** — 판정 전에 반드시 읽어라

| 구간 | 무엇 | 출처 | 처리 |
|---|---|---|---|
| **11:25~11:40** | ESP **그라운드선 이탈** | 🟢 **사용자 선언** | 🔴 **제외한다**(±5분 여유 포함) |
| **08:14** · **08:23** | **보드 사진 촬영 2회 — 사용자가 보드를 집어 들었다** | 🟢 **사용자 확정**(점 사건 · **지속 폭 미상**) | **표시만. 제외 안 함** |

**왜 하나는 빼고 하나는 안 빼나:**
- 11:30~11:35 은 **사용자가 선언**했고 **구간**이다 → 경계가 내 선택이 아니다 → **뺀다.**
- 08:14·08:23 은 **점 사건**이고 **지속 폭이 미상**이다 → 빼려면 **폭을 내가 정해야** 하고,
  그 순간 λ 가 내 선택의 함수가 된다.
  ⚠ **시각이 확정됐다고 판정을 바꾸지 않았다** — 판정의 근거는 "확정 여부"가 아니라
  **"폭이 미상이고, 빼도 숫자가 안 바뀐다"** 였다. 그 둘은 지금도 참이다.
- 🔑 **그리고 빼도 숫자가 안 바뀐다**: `08:15~08:40` 안에 **사건 0건 · 이상 표지 0건**
  (훑은 줄 64,826 · 구간 안 5,418). **이 확인을 먼저 했다** — 안 했으면 "안 뺀다"가
  숫자를 지키려는 선택으로 보인다.
- ⚠ **사용자가 촬영 시각을 알려 주면 확정 구간으로 올려라**(루트가 묻고 있다).

### 🔴 그러나 제외로 **못 고치는** 것이 있다 — 교락

**보드를 집어 든 것이 접촉을 느슨하게 했다면 그 영향은 나중에 나타난다. 구간을 빼도 안 없어진다.**

> 🔴 **창3↔창4 λ 차이로 펌웨어를 판정할 수 없다.**
> 두 창은 **펌웨어**(`64fc70b`→`0fba51f`)와 **실물 취급 여부**가 **같이** 달라졌다.
> 창3 = 사용자 취침 · 보드 미접촉 / 창4 = 촬영 + 그라운드 이탈.
> `0fba51f` 의 2줄 수정(언더플로)이 끊김을 **늘릴** 이유가 없으므로 **남는 설명은 실물 쪽**이지만
> **이 자료로는 못 가른다.**

**개입을 발견하면 먼저 갈라라: "구간을 뺄 문제(오염)인가, 비교를 접을 문제(교락)인가."**
**제외는 오염에만 듣는다.**

---

## 6.4 🔴 창4 안에서 **선언된 개입** — 판정 때 반드시 표시한다

| 시각 | 누가 | 무엇 | 운영 접촉 |
|---|---|---|---|
| 07:54:35 | socket | 하행 주입 1건 → **끊김 1건**(기전 B) → 07:55:31 정지 | ✅ 운영 |
| 08:15:46 | socket | 시험 인스턴스 **실패**(pid 871 · `reason=port_bind_fail` · 프레임 0) | ❌ `test+400` 로그 |
| 08:16:00 | socket | 시험 인스턴스 pid **1419** · 포트 **10591/10500/6100** | ❌ `test+600` 로그 |
| **13:05:32~13:07:32** | socket | 🔴 **실물 예약 `R`/`C` 10건**(상태 변경) — **10/10 성공 · 끊김 0** · 무주입 통계에서 **잘라낸다** |
| ~08:2x | web | **헤드리스 크롬 1개 + WS 1연결** — 10500 에 붙어 화면 거동 측정(~30분) | ❌ 시험 인스턴스 |

**내가 직접 확인한 것**(남의 선언을 그대로 믿지 않는다):
```
운영 로그 마지막 INSTANCE = pid 18422 (00:02:44)  ← 새 인스턴스 안 섞임
9991·9900 LISTEN = pid 18422                      ← 운영 포트 그대로
test+600.log 에 pid=1419 · test+400.log 에 port_bind_fail
```
→ **운영 로그·포트는 안 섞였다.** 서버 로그 기반 계수(λ·간격·세션)는 **오염되지 않는다.**

### ✅ CPU 경합 — **걱정으로 두지 않고 쟀다. 검출되지 않았다**

탭 타임스탬프는 **줄을 읽은 시점**이라 크롬이 도는 동안 간격이 부풀 수 있다(원장 1.7).
**그래서 크롬 측정A 구간(08:19:49~08:20:13, 24초)과 앞뒤 조용 구간·무개입 구간을 대조했다.**

| 구간 | `[TX] S` | 간격 분포 | 평균 | 최대 |
|---|---|---|---|---|
| 크롬 측정A 08:19:49~08:20:13 | 23 | `{1:20, 2:2}` | 1.091s | 2s |
| 직전 조용 08:19:00~08:19:24 | 23 | `{1:20, 2:2}` | 1.091s | 2s |
| 직후 조용 08:20:20~08:20:44 | 23 | `{1:20, 2:2}` | 1.091s | 2s |
| 무개입 08:05:00~08:05:24 | 23 | `{1:20, 2:2}` | 1.091s | 2s |

**네 구간이 완전히 동일하다.** 서버 로그 쪽도 같이 봤고 역시 동일했다.
→ **이 부하에서 크롬 헤드리스는 탭 타임스탬프를 왜곡하지 않았다.**

⚠ **말할 수 있는 범위**: 구간당 간격 22개 · **1초 해상도**라 **1초 미만 왜곡은 원리적으로 못 본다.**
   그리고 **24초짜리 측정 하나**다. 더 무거운 부하는 다를 수 있다.
🔑 그래도 **"경합이 섞일 수 있다"는 주의문이 "이 부하에서는 검출되지 않았다"는 값으로 바뀌었다.**
   A 창에서 개입 16% 를 못 갈랐던 문제와 다르다 — **이번엔 선언됐고, 좁고, 쟀다.**

⚠ **web 통보의 시작 시각이 `00:1x` 로 잘못 적혀 있었다 — 실제 `08:19:49`.** web 이 정정했다
(*"날짜가 바뀐 것만 보고 시각을 확인 안 했다"*). **개입 기록은 시각이 전부다** — 그대로 받았으면
**이미 판정문이 나간 창3 구간 안에 없는 개입**이 생길 뻔했다. 인용은 이 파일 값으로.
### ✅ 개입 구간 확정 — **통짜 40분이 아니라 조각 다섯, 합쳐 2분** (web 종료 통보 08:31:0x)

| # | 무엇 | 시각 | 길이 | 내 간격 측정 |
|---|---|---|---|---|
| 1 | 측정A(침묵 2초) | **08:19:49~08:20:13** | 24s | `{1:20, 2:2}` 최대 **2s** |
| 2 | B 시도·무효 ⚠추정 | ≈08:22:5x | ≈10s | `{1:8, 2:1}` 최대 **2s** |
| 3 | 측정A′(오프라인 6초) | **08:23:5x~08:24:2x** | 26s | `{1:24, 2:3}` 최대 **2s** |
| 4 | 측정B(n=3) | **08:25:1x~08:25:5x** | ≈40s | `{1:37, 2:4}` 최대 **2s** |
| 5 | 칸 문구 확인 ⚠추정 | ≈08:26:3x | ≈20s | `{1:16, 2:2}` 최대 **2s** |
| — | **무개입 대조** 08:05:00~08:05:30 | 30s | — | `{1:24, 2:3}` 최대 **2s** |

🔑 **다섯 조각 전부 무개입 대조와 같다.** `>=3s` **0건.** → **크롬 부하는 이 창을 왜곡하지 않았다.**
⚠ 범위: **1초 해상도**라 그 아래는 못 본다. #2·#5 는 web 이 **추정**이라고 표시했다(로그 없는 실행).

**시험 인스턴스 쪽 개입도 같이 남긴다**(내 창엔 안 섞임): socket 이 `fake_arduino` 재시작 —
**08:21:45 정지 / 08:21:50 기동 / 08:21:56 스트리밍 복귀.**

→ **판정할 때 통짜로 빼지 마라.** 개입은 **2분**이지 40분이 아니다.

## 6.45 🔑 `OFFLINE_MS=3500` 아래 침묵은 **서버 로그에 흔적이 없다** (socket 실측)

`fake_arduino --mute-*` 로 확인: `uptime 7 → 10` 으로 프레임이 비는데
**`online: true` 유지 · 오프라인 판정 없음 · 세션 종료 없음 · 재연결 판정 없음.**

→ 🔑 **3.5초 아래 장치 침묵은 내 간격 분석에만 보이고 서버 상태 지표에는 안 보인다.**
  **`frame_gaps.py` 로 간격을 따로 재는 것이 서버 요약이 못 보는 구간을 덮고 있다** — 그게 그 도구의 값이다.
⚠ 뒤집어 말하면 **서버 지표가 조용하다고 장치가 조용한 것이 아니다.** 둘을 같이 봐라.

---

## 6.5 🔴 도구가 **`0` 규약을 따르는지** — 목록 (전부 따른다고 가정하지 마라)

규약: `monitor/zeroguard.py` — **`0` 을 낼 때 훑은 줄 수와 찾은 문자열을 같이 찍는다.**
> **규칙이 아니라 출력 형식이라 안 지키면 눈에 띈다.**

| 도구 | 규약 | 비고 |
|---|---|---|
| `win2_pulse.py` (STATUS 표) | ✅ | "찾은 문자열" 칸 있음 · `espbanner` 는 **우리 DTR/사건** 갈라 찍음 |
| `garbage_lead.py` | ✅ | `calib()` 포함 |
| `reset_onset.py` | ✅ | `--calib` 포함 |
| `frame_gaps.py` | ❌ **옛 형식** | 다음에 손댈 때 |
| `resync_gap.py` | ❌ **옛 형식** | 새 칩에는 그대로 쓰면 안 됨(§4) |
| `coincidence.py` · `slice_window.py` | ❌ **옛 형식** | |
| `frozen-A/**` | 🔒 **손대지 마라** | 동결. 고치면 A 판정이 두 판본으로 갈린다 |

⚠ **창4 가 도는 동안 일괄로 안 고쳤다** — 계측기가 창 중간에 바뀌면 안 된다(오늘 지킨 규칙).
🔴 **그래서 지금은 도구마다 다르다. "다 따른다"고 가정하지 마라** — 그 가정이 `banner` 칸에서
방금 사고를 냈다.

## 6.6 🔴 사전 등록된 예측이 하나 걸려 있다 — **다음 리셋이 그것을 검증한다**

`monitor/PREREG-2026-08-17-reset-onset.md` (작성 08:2x · **그 시점 이후 리셋 0건**)

> **예측(arduino)**: 다음 ESP 리셋은 `AT+CIPSEND` 와 대응 `SEND OK` **사이**에서 시작한다.

**리셋이 나면 즉시:**
```
python3 monitor/reset_onset.py --calib                    # 먼저 교정
python3 monitor/reset_onset.py monitor/serial-win4.log <시작> <끝>
```
결과를 **사전등록 문서 §5 에만** 적어라. **§1·§2(예측과 판정 규칙)는 사건 뒤에 고치지 마라** —
고치면 사전 등록이 아니다.

⚠ **창4 `07:54:40` 건은 이 검증에 쓰지 않는다** — 사전등록보다 **먼저** 났고 **우리 주입이
방아쇠**였다(사전등록 §2.3).
⚠ **A 창 8건은 사후 분석이다**(교정용으로만). 참고로 교정 결과는 **적중 7 · 빗나감 1** 이었다 —
**사후 패턴이지 검증이 아니다.**
⚠ **`CIPSEND` 와 `SEND OK` 가 같은 초면 시각으로 순서를 못 가른다.** 도구가 `?` 와 경고를
붙인다. **그런 판정을 확정으로 쓰지 마라.**

---

## 7. 판정 절차 · 금지

절차는 `monitor/HANDOFF-win3.md` §6 과 `monitor/RECIPE-A-judgment.md`. 형식의 본보기는
`monitor/JUDGMENT-2026-08-17-win3.md`.

- 🔴 **"5배 변화는 배제했다"를 그대로 쓰지 마라.** 실측 n 으로 검정력을 다시 뽑아라.
  (창3 은 **0건/7.37h** 라 rule of three 로 `λ ≤ 0.41/h` 를 냈다 — 그건 그 창의 값이다)
- 🔴 **창을 닫으면 서버 `INSTANCE-END` 와 대조** — `frames=` 는 **S+A 합**이다(원장 7.15).
- ❌ A 창·창2·창3 과 **합산**. ❌ `t_end` 에 미래. ❌ 탭 재개(t0 가 둘이 된다 — 루트가 정한다).
- ❌ `net/ws_probe.py`·`net/fake_arduino.py` 를 **포트 명시 없이** 실행(기본값이 운영 포트).
- ❌ `git clean` · `git checkout -- .` · `git reset --hard` · `rm -rf`.

## 8. 배경

`monitor/LEDGER.md`(원장 · §7 이 최신) · `monitor/JUDGMENT-2026-08-17-win3.md` ·
`monitor/FINDING-2026-08-17-0125-underflow.md`(기전 C) ·
`monitor/JUDGMENT-2026-08-16-A320h.md`(A 창) · `monitor/RECIPE-newbaseline-t0.md`


---

## 9. 🔑 오늘(08-16~17) 만든 것 — **다음 사람이 바로 쓰는 형태로**

### 규약 · 도구

| 무엇 | 파일 | 쓰는 법 |
|---|---|---|
| **`0` 출력 규약** | `monitor/zeroguard.py` | `0` 을 낼 때 **훑은 줄 수 + 찾은 문자열**을 같이 찍는다. `calib()` 은 전수 전에 아는 사례로 교정 |
| **철회 도구** | `monitor/retract.py` | `python3 monitor/retract.py "<철회할 문구>"` — 그 결론이 남아 있는 곳을 전부 찾는다 |
| **판별자 v4** | `monitor/mech_split-v4.py` | 리셋 표지 3항 OR(`0.0.0.0` / 배너 / **부트ROM `@\xF9`**). ⚠ `frozen-A/` 는 A 정본이라 **안 건드림** |
| 리셋 시작 판정 | `monitor/reset_onset.py` | `--calib` 로 먼저 교정 → 사전 등록 검증용 |
| 가비지 분류 | `monitor/garbage_lead.py` | ①부트ROM(사건 아님) / ②UART 손상 을 가른다 |
| 프레임 간격 | `monitor/frame_gaps.py` | `>=3s` 후보 + 맥놀이 진단 + `skip` 제외. **주입 시각은 `auto`** |
| 우연 계산 | `monitor/coincidence.py` | 인자 없이 돌리면 **A 창 재현**(교정용). 기본값 고치지 마라 |
| 구간 자르기 | `monitor/slice_window.py` | 동결 도구에 구간을 먹일 때 (동결본은 구간 인자를 안 받는다) |
| 상시 요약 | `monitor/win2_pulse.py` | `PPID=1` 로 띄운다. 산출 경로는 **로그 이름에서 파생** |

### 미리 박아 둔 것 (사후에 고치면 값이 사라진다)

| 무엇 | 파일 |
|---|---|
| **사전 등록** — 리셋 시작 지점 예측 | `monitor/PREREG-2026-08-17-reset-onset.md` (§1·§2 **고치지 마라**) |
| **정지 조건** — 판별자를 언제 닫나 | 원장 7.55 · **`?.미상` 0 + 표지 2개** → **달성(7/8), 닫음.** 다시 여는 건 `미상` 1건 이상일 때만 |
| **중단 조건** — 주입을 언제 멈추나 | 원장 · HANDOFF-win3 §2.5 (끊김 3건 이상이 주입 ±5초 안) |

### 판정문 · 근거

`JUDGMENT-2026-08-16-A320h.md`(A 창) · `JUDGMENT-2026-08-17-win3.md`(창3) ·
`WINDOW2-VOID-2026-08-17.md`(창2 폐기) ·
`FINDING-2026-08-17-0125-underflow.md`(기전 C) · `FINDING-2026-08-17-0754-downlink-collision.md`(창4)

### 🔴 다음 사람이 제일 먼저 알아야 할 셋

1. **`0` 은 네 가지다** — 건강 / 미실행 / **못 셈** / **아직 안 왔다**(원장 1.1 · 7.44 · 7.57).
   **오늘 사고 대부분이 이 칸에서 났다.**
2. **이름이 같아도 다른 것을 센다** — `drop`·`frames`·`복구`·`전송 실패` 전부 그랬다.
   **비교 전에 "무엇을·어디서·누가·어느 계층" 넷을 답해라**(원장 7.56).
3. **규칙보다 형식이다** — "조심하라"는 여섯 번 실패했고, **출력에 분모와 검색어를 박은 것은
   한 번에 걸렸다**(원장 7.47). 새 주의사항이 생기면 **문장보다 출력 형식으로 만들 수 있는지** 먼저 봐라.
