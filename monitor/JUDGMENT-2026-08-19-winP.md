> # ✅ **관측 자료의 첫 창** — 2026-08-19 10:20 재설정 이후 (REQ-0256)
> **이 창부터가 관측 자료다.** 앞의 모든 창(G~O 및 그 이전)은 사용자 지시로 **자격이 낮춰졌다** —
> 값은 남아 있지만 **관측 비율과 그 위의 인과 귀속은 새 판정의 근거로 쓰지 않는다.**
> 🔴 **그러므로 이 창은 대조군이 없다.** 비율을 "좋아졌다"가 아니라 **"이만큼이다"** 로만 적었다.

> # 🔴 범위 — 결론만 읽는 사람이 반드시 볼 것
> **① `A[1]` 은 돈다.** `rid` 가 `[0,999]` 로 감기는 것을 **내 시리얼에서 직접** 봤다(§1)
> **② 🔴 그런데 격리는 한 번도 발동하지 않았다.** `건너뜀 0` 은 **"검증됨"이 아니라 "안 밟혔다"** 다(§2)
> **③ 대조군이 없다.** `A[1]` 이 이미 배포돼 이전 기준선을 다시 뜰 방법이 없다 —
> **비율은 "좋아졌다"가 아니라 "이만큼이다"로만 적었다**(§3)
> **④ 축이 셋이다**: `A[1]` · **망 이동(무선 환경 통째로 다름)** · 칩 교체(34커밋).
> **무선 의존 지표는 창 O 와 크기 비교하지 않았다**

# 판정 · 창 P — **`A[1]` 작동 확인. 격리 경로는 이 부하로 못 밟는다** (2026-08-19)

**규칙** `SPEC-분모정의-2026-08-18.md` · **사전 등록** `PREREG-2026-08-19-winP.md`(주입 전 발행 · 정정 3회)
**판본** 서버 `fceadf3` pid 64096 · `srv_id fceadf3-1787101222325`(내가 로그에서 값으로 확인)
장치 `2187cc3`(n=12 · 가상 O*) · 굽기 `10:09:55`(arduino)

```
🔴 창 t0     **10:20:00**  (사용자 지시 REQ-0256 — 그 앞은 사용자 환경설정 구간)
무자극 구간  10:20:00 ~ 10:30:00 (10분)
🔴 주입 구간 **10:30:01.103 ~ 10:41:09.198** (11분 08초 · socket 이 로그에서 읽은 값)
             첫 injP1-7-1 · 마지막 injP4-8-60 · R1~R4 (R5 미개시)
```
⚠ **내 탭 t0 는 10:15:11 이었다**(내 DTR 리셋). **부팅 국면은 창 밖**이라 이 창에는 부팅 기여가 없다.

## 0. 규약 이탈 — **먼저 적는다**
```
🔴 정지신호 발동 : 10:39:11 내 감시기가 `.team/STOP-P` 생성(bigfill=**334**) → R5 를 안 열었다
   실제 정지     : 10:41:09.198  → **지연 118초**(R4 잔여분). 진행 중 라운드는 안 끊었다
   40분 상한     : 안 걸렸다 (약 11분)
⚠ 판정선은 300 인데 **실측 분모는 334** — `[CNT]` 60초 주기라 그 사이에 넘었다.
   통과 시점 10:38:1x~10:39:11 **미상**. **300 에서 끊긴 것처럼 읽지 마라**
🔴 **8라운드 자연 종료가 아니다. 창 O 와 같은 형태다**
```

## 1. ✅ **`A[1]` 작동 — 문턱 A 통과. 내 계층에서 독립 관측**
```
10:33:55  A,**999**,B1,4,3F
10:33:56  A,**0**,A2,4,36        ← 🔴 999 다음이 **1000 이 아니라 0**
주입 구간 전체 : 4자리 rid **0건** · rid 범위 **0~999** · 고유 rid **1000**(공간 전체를 채웠다) · 누락 **0/1000**
자릿수 : 1자리 20(1.0%) · 2자리 180(9.4%) · 3자리 1724(89.6%)
```
> ### **`rid` 가 `[0,999]` 로 감긴다. 옛 코드였다면 1000(4자리)이 됐을 자리다.**
🔑 **socket 요약 없이 봤다** — rid 는 ACK 레코드에 그대로 찍힌다. **두 계층이 같은 사건을 따로 봤다.**
✅ **절대 판정이라 대조군 소실과 무관하다** — 사전 등록 §4' 에 미리 그렇게 정해 뒀다.

### ⚠ 45초 시점의 `4자리 0` 은 판정이 아니었다
`10:30:46` 에도 4자리는 0이었다. **그때 rid 는 462 이고 `A[1]` 이 *없는* 코드도 거기선 3자리다.**
**그때 "확인됐다"고 적었으면 근거가 빈 결론이 나중에 참이 되어 아무도 못 잡았을 것이다.**

## 2. 🔴 **격리는 한 번도 발동하지 않았다 — `0` 은 "안 밟혔다"** (socket 값 · 내가 못 보는 축)
```
rid 발행 **1920** · 다음 **244**/1000 · 격리 **1000** · 건너뜀 **0** · 강제 **0** · ACK 미상rid **4**
커서 검산 : 324 + 1920 = 2244 → mod 1000 = **244** ✅ (건너뜀 0 이라 모델 성립)
```
```
✅ 문턱 A (커서 1000) 통과 — 감김 관측 (§1)
✅ 문턱 B (커서 1324) 통과 — 커서 2244. **재사용이 실제로 일어났다**(각 값 약 2회)
🔴 그런데 `건너뜀 0` · `강제 0` — **격리가 한 번도 막지 않았다**
```
### 🔑 **그리고 이 부하로는 영영 안 밟힌다** — socket 이 값으로 보였다
```
한 바퀴(1000건) = 668초/1920건 = 2.87건/초 → **약 348초**
격리 기간       = **7.2초**
⇒ 값이 돌아올 때 격리는 **48배 전에 이미 풀려 있다. 막을 일이 없다**
🔑 격리가 실제로 막으려면 발행률이 **약 139건/초** 를 넘어야 한다. 주입기는 **2.87건/초** 다
```
> ### **그러므로 `건너뜀 0`·`강제 0` 을 "격리 검증됨"으로 읽으면 안 된다. 이 규약은 그 경로를 구조적으로 못 밟는다.**
⚠ **§"시험 경로가 실기 경로와 다르다 — 못 밟는다"의 실물**이다. socket 이 설계 §7 에 **미리 적어 뒀고**
**이 창이 그것을 값으로 확인했다.** ✅ **적어 둔 한계가 실측으로 닫힌 것**이라 형태가 좋다.

## 3. 이 창의 값 — **"이만큼이다". 창 O 와 비교하지 않는다**
**전부 주입 구간(10:30:01.103~10:41:09.198) 분모다.**
```
`>=64B` 진입률   **372 / 458 = 81.2%**   (분모 = 에코와 짝지은 상행 프레임)
CIPSEND 거래     **547** (정상순서 458 · 뒤집힘 **0** · 에코실종/지연 89)
ACK 개수 N       **N=4 75.5%(418)** · N=2 12.3% · N=3 5.4% · N=1 2.0% · N=0 4.3% · N=5 0.5%
```
🔴 **창 O 의 69.8% 와 크기 비교하지 않았다.** `A[1]` 배포로 대조군을 다시 뜰 방법이 없고,
**축 2(망 이동)까지 걸려 있다.** 이 값은 **A[2] 이후를 위한 새 기준선**이다.

## 4. ✅ `SEND OK` 축 — **대역 전체에서 T2 0**
```
대역        거래수    T2
<48           35      0
48~63         61      0
>=64        **451**   **0 = 0.00%**
갈래별 : 정상순서 0/458 · 에코실종 0/89 · 뒤집힘 0/0
```
> ### **`>=64B` 451거래에서 `T2` 0.**
⚠ **창 O(2/341)와 크기 비교하지 않는다** — 축 2 가 걸린다. 다만 **문맥은 적어 둔다**:
창 O 의 T2 2건은 **링크 재수립과 동시**였고, **이 창은 socket 기준 `주입 중 절단 0` 이다.**
**조건이 다르고 결과도 다르다. 인과로 읽지 마라 — 두 사실을 나란히 적을 뿐이다.**

## 5. 🔴 내 `중복` 지표의 뜻이 이 창에서 **바뀌었다** — 계측기를 고쳤다
```
고유 rid 1000 · ACK 레코드 1924 → 중복 **924**
🔴 그런데 관측 폭이 1000 이라 **최소 924 건은 비둘기집으로 강제된다**(공간을 다 돌았다)
✅ 실제 재전송은 socket 계수 **94** 다 — **자릿수부터 다르다**
```
> ### **창 O 에서 `중복`은 재전송이었고(3~19), 창 P 에서는 *공간을 감은 결과* 다. 같은 이름 다른 것이다.**
✅ **`rid_width.py` 에 경고를 넣었다** — `ACK > 관측폭` 이면 강제 중복 수를 같이 찍고
*"재전송은 서버 계수로 봐라"* 를 출력한다. **다음 사람이 이 자리에서 안 넘어진다.**

## 6. ✅ 확정 — **주입기가 `화면` 으로 세어진다** (창 O 결론이 이 창에서 재현)
```
주입 중 화면 **10** → 종료(10:41:09) 뒤 10:41:27 **2(최대 10)**   → 차 **8** = 주입기 수
그리고 라운드 사이에도 잡혔다 : 10:38:26 화면 **3**
```
### 🔴 내가 창 O 에서 "관측 불가"로 접은 근거가 **틀렸다**
```
내가 쓴 것 : "라운드 틈이 5초(감독기 로그)인데 계측 주기가 60초 → 못 뜬다"
실측       : 라운드 3 종료 **10:38:28** 인데 화면 3 은 **10:38:26** — 종료 표기보다 **먼저** 떨어졌다
🔑 원인    : 감독기의 "라운드 종료"는 **마지막 주입기가 끝난 시각**이고,
             주입기 8개는 **그 전에 하나씩 빠진다.** 실제 저값 구간은 5초보다 **길다**
```
> ### **경계 시각을 로그에서 읽을 때, 그 로그가 *무엇의* 경계를 적는지 확인해라.**
> ### **나는 감독기의 경계를 주입기의 경계로 읽었고, 그 위에 "관측 불가"를 세웠다.**
✅ **결과적으로는 무해했다** — 검정을 "주입 종료 시"로 옮겼고 그것도 성립했다.
⚠ **그러나 무해했던 것은 우연이다.** 옮기지 않았으면 **관측 가능한 것을 불가능하다고 적은 채** 끝났다.

## 7. 다른 도메인 값 (인용 · 내가 안 쟀다)
```
socket : 시도 **1920** · 미주입 **0** · 분모 **1920** · ACK(result=4) **1920 = 100.0%**
         세션 3회(#3 연결중 26:05 · **주입 중 절단 0**) · 재전송 94 · ACK실패 0 · 배치 533(줄 2014)
⚠ ACK 미상rid **4** (0.2%) — socket 계수가 *늦은 ACK* 과 *재전송 중복* 을 **한 칸에 담는다**(설계 §3.1).
  **못 가른다.** 다만 `건너뜀 0` 이라 **재사용 충돌은 일어날 수 없었다**
🔴 총 건수를 창 O(1679·미주입 122)와 크기 비교하지 마라 — **총량은 장치 상태의 함수다**
```

## 8. 📌 미결
```
🔴 격리 경로 — **이 규약으로는 못 밟는다.** 밟으려면 발행률 139건/초 이상이 필요하다.
   **별도 시험 설계가 필요하고, 그것은 이 창이 답할 문제가 아니다**
🔴 `cksumng` 왜 창 J 만 낮았나 — 이 창은 축 2(망 이동)가 걸려 자료를 보탤 수 없다
⚠ `ACK 미상rid 4` — 미분리(§7)
⚠ 창 I·J·H 의 대역별 T2 — 아직 같은 도구로 안 쟀다. **"네 창 연속 0"의 대역 기준 원본이 없다**
```

---

# 🔴 창 마감 (2026-08-19 10:46:31) — **이 창의 값이 `A[2]` 의 기준선이다**

## 마지막 상태 — 관측 전부 정지, 포트 반납
```
탭 종료   10:46:31 (종료 경계 줄 찍힘) · monitor/serial-winP.log
감시기    10:46:31 종료 · monitor/winP-watch.log
관측 프로세스 0 · 시리얼 포트 점유 0 — **보드·포트 비어 있다**
```
**닫은 이유(루트 지시)**: ① 판정 자료를 다 떴다 ② **web 이 브라우저를 붙인다** —
창이 열려 있으면 `화면` 축이 또 섞인다. **닫아 두면 그 조정이 아예 필요 없다.**

## 🔴 `A[2]`(`N 4→3`) 의 기준선 — **이 값들을 쓴다**
```
`>=64B` 진입률   **372 / 458 = 81.2%**        (분모 = 에코와 짝지은 상행 프레임 · 주입 구간)
`>=64B` T2       **0 / 451 = 0.00%**
ACK 개수 N       **N=4 75.5%(418)** · N=2 12.3% · N=3 5.4% · N=1 2.0% · N=0 4.3% · N=5 0.5%
구간             10:30:01.103 ~ 10:41:09.198 (11분 08초) · 주입기 회차 32
판본             서버 fceadf3 · 장치 2187cc3(n=12) · **망 192.168.0.x**
```
⚠ **`A[2]` 는 `N` 을 4→3 으로 줄인다. 그러니 위 `N=4 75.5%` 가 바로 그 축의 출발점이다.**
🔴 **비교할 때 반드시 확인할 것**: `A[2]` 창의 **망·칩·서버 판본이 이것과 같은가.**
**다르면 `>=64B` 계열은 크기 비교하지 마라** — 이 창이 창 O 와 그래서 비교 불가였다.

## ⚠ 이 기준선이 딛고 있는 것 — 넓히지 마라
```
· 주입 규약 `sh net/winG-rounds.sh 8 P`(라운드 8·인스턴스 8·count 60·interval 0.3) 에서만 참이다
· **정지신호로 R5 를 안 연 창**이다(8라운드 자연 종료 아님). 분모 334 는 300 이 아니다
· `A[1]` 이 들어간 뒤의 값이다 — `rid` 가 `[0,999]` 로 감긴다
· **격리는 이 창에서 한 번도 안 밟혔다**(발행률 2.87건/초 ≪ 필요 139건/초)
```

## 📌 다음 관측이 필요해지면 — 루트가 부른다
```
관측 지시 없음. 다음 작업은 설계·코드다(7영역 지형 · 모듈별 값 계약 · 화면 박스)
⚠ `A[2]` 는 뒤로 밀렸다 — 사용자가 낸 새 작업이 위다
```

---

# 🔴 보완 (10:5x) — **부재 주장 전수 검산에서 내 판정문의 빠진 곳이 드러났다**

**계기**: 루트가 준 규칙 — *"`없다`를 잘린 창에서 결론 내지 마라. `있다`는 잘린 창에서도 참이고
`없다`만 창 크기에 종속된다."* **내 부재 주장(`T2 0`·`4자리 0`)을 슬라이스가 아니라 파일 전체로 다시 셌다.**

## ① ✅ 부재 주장은 살아남았다 — 오히려 더 강해졌다
```
`4자리 rid` : **파일 전체(10:15:11~10:46:31) 0건** — 주입 구간만이 아니라 창 전체에서 0이다
```
**슬라이스보다 넓은 범위에서 참이므로 §1 의 `A[1]` 판정은 그대로 선다.**

## ② 🔴 그런데 **장치 계수를 판정문에 아예 안 넣었다** — 채운다
`[CNT]` 차분 (주입 시작 직전 `10:29:15 up=840` → 주입 종료 직후 `10:41:17 up=1560`):
```
ssovf   0 → **49**  (Δ49)        cksumng  0 → **52**  (Δ52)
bigfill 1 → **452** (Δ**451**)   bigdrop  0 → 0       oklost 0 → 0
pendfill 691 → 1287 (Δ**596**)   🔴 **penddrop 0 → 5 (Δ5)**
drop 0 → **0** · stuck 0 → **0** · okto 0 → 1
```
🔑 **교차 확인**: `bigfill` Δ**451** 이 §4 의 대역표 `>=64 451거래` 와 **정확히 일치한다** —
**장치 계수와 내 로그 파싱이라는 다른 각도 둘이 만났다.**

### 🔴 `penddrop = 5 / 596` — **이 창에서 버림이 났다**
```
✅ 분모가 크다(pendfill Δ596) → **"버림이 드물다"를 말할 수 있다**
✅ `bigdrop 0` · `oklost 0` → 버려진 5줄은 **`>=64B` 대역이 아니고 `SEND OK` 도 아니었다**
⚠ `oklost = 0` 은 여전히 **미분리**다 — *"구할 것이 없었다"* 와 *"잃을 경로가 안 밟혔다"* 를 못 가른다
```
**창 O 는 `penddrop 2 / bigfill 882` 였고 그중 `bigdrop 2`(전부 `>=64B`)였다.**
🔴 **창 O 와 크기 비교하지 않는다**(축 2 망 이동). **다만 형태가 다르다는 것은 적어 둔다** —
창 O 는 버림이 전부 큰 대역이었고, **이 창은 큰 대역 버림이 0 이다.**

### ✅ 주입 중 링크 사건 **0** — 두 계측기가 일치한다
```
내 장치 계수 : 주입 구간 `drop 0 · stuck 0 · esprst 0`
socket       : "세션 3회(#3 연결중) · **주입 중 절단 0**"
```
**서로 다른 계층이 같은 답을 냈다.** §4 에서 창 O 의 T2 2건이 링크 재수립과 동시였다고 적은 것과
**이 창의 T2 0 이 나란히 놓인다** — 다만 **인과로 읽지 마라**(축 2 가 걸린다).

## ③ ⚠ **창 꼬리에 `T2` 1건이 있다 — 주입 구간 밖이다**
```
10:44:26  [TX-WAIT] ★★ T2 초과 — SEND OK 가 끝내 안 온다 → 링크를 다시 세운다
같은 구간(10:41:17~10:46:19) 계수 : drop 0→1 · stuck 0→1 · okto 1→3
                                    ssovf·cksumng 은 **안 늘었다**(49/52 그대로)
```
> ### **§4 의 `T2 0/451` 은 주입 구간의 값이고 참이다. 창 전체로는 `T2` 가 1건 있다.**
**둘 다 적는다.** 주입이 끝난 뒤 무자극 상태에서 링크가 한 번 흔들렸다.
⚠ **이 1건을 `A[1]`·주입과 연결하지 마라** — 주입은 이미 끝났고 그 뒤 3분 17초 지난 시각이다. **미상**이다.

## 🔑 이 보완이 말해 주는 것
**부재 주장은 살아남았는데(①), 그 검산 과정에서 *적지 않은 값*(②)과 *구간 밖 사건*(③)이 나왔다.**
> ### **검산의 소득은 "틀렸다"만이 아니다. "안 적었다"가 더 자주 나온다.**
