# ❌ 폐기 — 창 AF 는 **열리지 않았다** (루트 확정 2026-08-20 13:2x · REQ-0285)

```
❌ 폐기(열리지 않음) — `serial-winAF.log` 없음 · 탭 프로세스 없음 → 자료가 0 이다
🔴 겨눈 칩은 **09:04 판**(3 modules / 등록 43B / `D,L2,` 0건)
   그 뒤 **10:42:37 L2 굽기**로 지금 칩은 **4 modules / 54B / `D,L2,OL`** 이 됐다
→ 아래 표지 목록은 **지금 칩에 대해 거짓이다.** 판정 근거로 쓰지 마라
```
**지우지 않는 이유**: 창 이름이 AF 를 건너뛰어 AG 로 가는 사유가 여기에만 남는다.
**되살리지 않는 이유**(루트): ①되살릴 자료가 없어 "갱신"이 아니라 신규다
②같은 이름이 두 구성을 가리키면 나중에 못 가른다 ③표지 축은 이미 `arduino/INTERVENTIONS.md`
가 값으로 덮었다(`54B` = 구조식 `10+11n`, n=4) — **이 창이 없어서 잃은 축은 없다.**

**후속**: 새 창 **AG** 를 지금 칩 기준으로 쓴다. 🔴 **아직 미발행** — 시리얼 탭 순서(루트 보류 중)가
정해지고 **장치가 붙은 뒤가 `t0`** 다. 연결이 없으면 잴 것이 없다.

---

# 사전 등록 · 창 AF — **샘플 축소(LED 하나). 판정은 둘뿐이다**

> ### 🔴 **분석 중단 지시 중이다**(사용자: *"장애 분석 그만"*).
> ### **이 창은 표지 확인과 자료 축적만 한다. 기전 추론·후보 좁히기를 안 한다.**

**발행 07:2x · 09:0x 갱신(자리이동 굽기) — 굽기 *전*.** 정본 = **arduino 굽기 보고**(직접 수신 · §7 규약).

## -1. 🔴 자리이동이 앞에 끼었다 — 이 창은 **이동 뒤** 창이다
```
이동 명시 **07:55:16**(하한 · 루트 통보 수신) → 안정 종료 하한 08:00:16
🔴 굽기 **09:04~** (`Config.h` 가 컴파일 상수라 굽는다)
   SSID **3F_302** · 서버 IP **192.168.0.29**(새 서브넷 · GW .0.1) · 포트 **8888 그대로**
🔴 t0 = **굽기·배포가 끝난 시각**. "이동 + 5분" 이 아니다
```
```
🔴 바뀌는 표지는 **한 줄** : `[NET] 대상 **192.168.0.29**:8888`
⚠ **나머지가 달라지면 뭔가 더 들어간 것이다 — 그 자리에서 알린다**
```
🔑 **`[NET] 대상` 덕에 "칩이 어느 서버를 보나"를 부팅 한 줄로 확인한다**(지난 이동 땐 `[AT]` 를 뒤졌다).

### ⏳ 붙는 데 오래 걸린다 — **결함으로 읽지 않는다**
```
어제 실측 **~20분** · **새 AP 라 `CWJAP FAIL` 이 몇 번 나는 것은 정상**
🔴 판정은 **붙고 나서.** 짧은 창의 "안 붙는다"를 상태 진술로 쓰지 않는다(창 AE 에서 물렸다)
```

### ⚠ 축이 여럿이다 — **귀속 안 한다**
```
망 : SSID · 서브넷 · **AP 자체가 다르다**   ·   물리 : **전원·접점이 다시 꽂혔다**
🔑 이 창의 값 : 창 AE 의 **재부팅 3회가 재현되나**
   재현 → **보드 쪽** · 안 됨 → **앞 위치의 환경**
⚠ 축이 여럿이라 **"재현됐다/안 됐다"까지만** 적는다
```

## 0. 구성
```
커밋 `809c279` (칩 ed5309b 의 자손 · 딸려 들어가는 커밋 **하나뿐**)
flash **27,030B (83%)** · RAM **1,223B** · 직전 27,874 → 🔴 **−844B (줄어든다)**
⚠ **줄어드는 굽기** → optiboot 은 쓰는 페이지만 지운다. **꼬리에 옛 펌웨어가 남는다.**
  **칩 읽기 대조에서 꼬리 불일치는 정상이다**
모듈 **6 → 3** (A1 B1 LD) · 명령가능 4 → **1**
🔴 분모가 또 바뀐다 — **앞 창과 상행 크기·대역 분포를 합치지 마라**
```

## 1. 표지 — **무조건**
```
✅ `[PARKING NODE] proto v1 / 1 spots / 2 sensors / **3 modules** / dev=P1`
✅ `[REG] 등록 전송 **43B**`  ·  **`D,*,7,3,`**
✅ `D,A1,IP` · `D,B1,IP` · **`D,LD,OG`**          ← 세 줄
✅ `[SENS] A1=**핀2** B1=**핀9**`                 ← 훅이 없으니 `훅` 이 아니라 `핀N`. **꼬리 경고도 없다**
✅ `[BOOT] 리셋 원인: **알 수 없음 — 이 부트로더가 MCUSR 을 지우고 넘어온다**`
🔴 ✅ `[NET] 대상 **192.168.0.29**:8888`   ← **이동으로 바뀐 유일한 줄**
```
```
❌ **0건** : `6 modules` · `D,LC,` · `D,DR,` · `D,L2,` · `시뮬` · `9991` · **`훅`** · 🔴 **`192.168.35.21`**
```

## 2. 🔴🔴 **사라지는 것 셋 — 미리 선언한다. 결함으로 읽지 않는다**

**arduino 가 굽기 전에 값으로 줬다. 안 적으면 셋 다 "고장"으로 보인다.**
```
🔴 ① `occ` 가 **안 움직인다** — 시뮬 훅을 뺐다. 자리는 미배선이라 **늘 0**
     → **직전 창의 24.0초 주기는 사라진다.** ❌ 아니다
🔴 ② **교차 불변식이 성립하지 않는다** — `0x10`(B1)이 이제 **LED 를 안 읽는다**(훅 제거)
     `0x08`(LD)만 명령을 따라간다 → **둘이 갈리는 것이 정상**
     ⚠ **`occ_period.py` 의 불변식 검사를 이 창에 돌리지 마라. 적용 조건이 사라졌다**
🔴 ③ 비트 배치가 바뀐다 (n=3 · 모듈 i → 비트 2−i · 폭 **1**, `ceil(3/4)=1`)
     **A1 `0x4` · B1 `0x2` · LD `0x1`**   ← 창 AE 의 `0x20/0x10/0x08` 은 **낡았다**
```
> ### **셋 다 "없어진 것"이라 예고 안 하면 결함으로 보인다.** §*"표지의 부재도 선언해야 한다"*.

## 3. 판정 — **둘뿐이다**
```
🔴 ① **8888 접속 유지** — `CIPSTART` → `CONNECT` → 등록 · **세션이 이어지나**
   ② **표지 6/6 + ❌목록 0건**
⚠ `occ` · 교차 불변식 · 24초 주기 → **이 창에서 미측정.** **분모에 넣지 않는다**
```
⚠ **①에 "N분 동안"을 붙여 적는다** — 사다리가 단계마다 30초씩 기다린다.
**짧은 창의 "안 붙는다"를 상태 진술로 쓰지 않는다**(창 AE 에서 물렸다).

## 4. 명령 왕복은 아직 밟을 수 있다
```
`LD` 는 남아 있다 → `srv.send("P1","LD",1)`
  → **보드 LED 가 켜진다(눈 · 로그와 독립)** + `[G] idx=2 arg=1` + ACK `0` + 에코 **`0x1`**
⚠ **자리 값은 안 바뀐다**(되읽기 훅 제거). **정상이다**
```

## 5. 계속 쌓되 판정 안 하는 것
```
UART 손상(`uart_damage.py` · 분모 같이) · esprst · CIFSR 0.0.0.0 · CWJAP 무응답 · busy p · 재부팅
```
⚠ **해석은 나중에 한다.** 값만 남긴다.

## 6. 실행 기록 (측정 후에만)
```
굽기 (미기입) · 탭 개방/t0 (미기입) · 표지 (미기입) · ❌목록 (미기입) · 접속 (미기입)
```
