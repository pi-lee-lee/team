# ❌ 폐기 — 선이 언제 빠졌는지 모른다. 이 창 전체가 오염 후보다 (사용자 2026-08-20 13:42)

---

# 사전 등록 · 창 AH — **t0 13:38:17. 계측기 둘을 갈라 적는다**

> ### 🔴 **장애 분석 중단 지시 중이다.** 자료 축적과 값 보고만 한다. 기전 추론·귀속을 안 한다.
> ### **판정문은 짧게. 보고는 정지 조건에 걸릴 때.**

**발행 2026-08-20 13:39** · 창은 이미 열려 있다(t0 가 과거다) · 앞 창 AG 는 **관측 제외**(정착 구간).

## 0. t0 와 그 앞의 개입 — **전부 창 밖이다**
```
13:26:41  DTR 리셋 — monitor(나) · REQ-0286
13:2x     🔴 **사용자의 TX 선 재배선 — 시각 미상.** 우리 자료에 없다
13:28:05  탭 반납
13:28:26~13:38:16  **창 AG = 배선 정착 구간 → 관측 제외**(자료는 남긴다)
✅ t0 = **13:38:17** — 사용자가 화면에서 정상 동작을 관측한 시점
```
**기준선 동결** : `monitor/winAH-t0-freeze.txt`
🔑 **요약 계수는 서버 기동(10:45) 누적이다.** 창 AH 값 = (닫을 때) − (동결값). **누적값을 그대로 인용하지 마라.**

### 🔴 t0 가 세션 한가운데 떨어졌다 — **분모 주의**
```
세션#8 시작 **13:36:35** · t0 는 그 **1분 42초 뒤**
→ 세션#8 의 지속은 **부분 표본**이다. 창 AH 의 세션 지속 표본으로 쓰면 **절단 편향**이 된다
✅ 창 AH 의 첫 **온전한** 세션은 **#9 부터**
```

## 1. 겨눈 구성 — 값으로 (원장 §7.221 처방 ①)
```
장치 : 칩 10:42:37 판(`d41ad13` 계열) · **굽기 없음** · 모듈 **4**(A1 B1 LD L2)
       등록 **54B**(= 10+11n, n=4) · 대상 **192.168.0.29:8888**
서버 : build `a8e04c5` · pid **72151** · cwd `/Users/idong-u/parking-bin`
       🔴 **배포되면 이 값이 바뀐다. 바뀌는 순간이 창의 끝이다**
```
> **창을 닫을 때 첫 일은 표지 측정이 아니라 "이 구성이 아직 그대로인가" 대조다.**

## 2. 🔴 계측기 **둘** — 분모가 다르다. 섞지 않는다
```
① 서버 요약 `/tmp/srv_prod.out` — 60초 요약 + 사건 줄. 스스로 쌓는다. **폴링 안 한다**
   보는 것 : **TCP 세션의 수립·종료**
② 사용자 화면(정성) — **내 자료가 아니다.** 사용자가 직접 보고 말해 주는 것만 있다
   보는 것 : **값이 갱신되고 조작이 먹히는가**
```
> ### 🔴 **둘은 다른 것을 본다. 한쪽을 다른 쪽의 반증으로 쓰지 마라.**
> **13:38 실측**: 서버는 재접속 6회를 보고 있었고 사용자는 화면에서 "정상"을 봤다. **둘 다 참이다** —
> **재접속이 짧으면 화면에는 안 나타난다.**
🔑 내 원장 §*"계측 주기 > 사건 지속"* 의 짝이다 — 거기서는 **내가** 못 봤고, 여기서는 **상위 계층이** 못 본다.

### ⚠ 이 창이 **못 보는 축** — 미리 적는다. `0` 으로 쓰지 않는다
```
❌ CWJAP 거동 · IP 취득 · UART 손상 · `busy p` · 사다리 단계 · 재부팅 기전 → **미계측**
   (시리얼 탭에만 있고, 탭 개방 = DTR 리셋 = 이 창을 내 손으로 끊는 것이다)
```

## 3. 재는 것 — **세션 표**(한 세션의 지속이 아니다)
### 🔴 종료 사유별로 갈라 센다 — 뭉치면 "끊겼다" 하나가 된다
```
창 AG 에서 실제로 나온 사유 **넷** :
  · 수신 오류(errno=54, ECONNRESET)     세션 #2 #7
  · **keepalive 시간초과(errno=60)**     세션 #3 #4   ← 창 AG 에서 처음 나왔다
  · 같은 device_id(P1) 재접속으로 대체   세션 #5     ⚠ 정상 운전에서도 나온다. 단독 근거 금지
  · **상대가 닫음**                      세션 #6     ← 넷째. 루트 목록에 없던 것
```
**축 : 사유별 건수 · 세션 지속 분포 · 재접속 빈도(회/시간) · 링크없음 비율 · 최대공백**

### 판정선 — **옛 분포를 같이 적는다**
```
세션#1(오늘)     11분 16초
창 AG            3:00 → 0:30 → 0:26 → **3:07** → 0:18   (평균이 아니라 분포로 봐라)
창 S             22분에서 무너짐        · 옛 위치 45분 · 50분
창 AB·AC·AD      **86분 포함 재부팅 0회**
🔴 → **11분·22분을 넘는 것은 아무것도 안 가른다.** 판정선 **60분**(등급) · **86분**(옛 분포 이탈)
   그 미만은 전부 **"흔적"** 이다
```

## 4. 정지 조건 — 걸리면 **즉시 정지하고 새 t0**
```
· socket 의 v2 API 배포 통보 (배포 직전 한 줄이 오기로 돼 있다)
· 실물 개입 통보 (굽기 · 전원 · 배선)
· **사용자 통보** (화면 관측·조작·자리이동)
· 세션 지속 **86분** 돌파 → 그때 비로소 "옛 분포를 벗어났다"를 쓸 수 있다
```
🔴 **배포 절단을 배선 회귀로 읽지 마라.** 미리 선언해 둔다.
