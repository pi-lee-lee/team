# 판정 · 창 H — **`T2` 가 사라졌다. 표본은 창 G 의 30배다** (2026-08-18)

**규칙** `monitor/SPEC-분모정의-2026-08-18.md` 하나만 사용(분모=CIPSEND 거래 · 결과=T2)
**창** 장치 t0 `17:01:20`(탭 개방) · 주입 `17:06:31~17:41:14`(34.7분) · 펌웨어 `6709ef7`
**대조** 창 G — 서버 동판(`16:37:15` 기동) · 부하 인자 동일 → **펌웨어 축 하나만 다르다**

## 1. 🔴 결과 — 대역별 `T2`
```
대역      창 H 거래수   T2        │  창 G 거래수   T2
<48        686        0  (0.0%)  │    748        1  (0.1%)
48~63       26        0  (0.0%)  │     43        0  (0.0%)
>=64      1025        0  (0.0%)  │     34        9  (26.5%)
합계      1737     ★★T2 **0** · ★T1 1 · stuck 0
```
> ### **`>=64B` 표본이 1,025건이다 — 창 G(34건)의 약 30배.**
> ### **`0` 이 "표본 부족"이 아니다.** 창 G 비율(26.5%)이 유지됐다면 **약 271건**이 나왔어야 한다.

⚠ **`48~63` 대역은 26건뿐**(창 G 43건)이라 **문턱 진술의 재현은 이 창에서 못 한다.**
`>=64` 가 0 이 된 이상 **대역 대비 자체가 성립하지 않는다** — 문턱은 **창 G 단독 근거로 남는다.**

## 2. ✅ arduino 사전 예측 채점 — **(나) 확정**
```
okstream = 1,926   penddrop = 9      →  okstream>0 AND penddrop>0
```
**arduino 사전 등록**: *"(나)라면 `okstream>0` **AND** `penddrop>0`"* → **성립.**
**버려진 줄이 실재했고(9건), 바이트 매칭이 그것을 구했다.**
⚠ **`okstream=1,926` 을 "1,926회 구했다"로 읽지 마라** — 이 계수가 **정상 도착까지 세는지**
확인되지 않았다(무부하 기저에서 `penddrop=0` 인데 이미 `okstream=234` 였다). **정의 확인 필요.**
**판정에 쓰는 것은 `penddrop=9 > 0` 쪽이다.**

## 3. ✅ 더미 pump 혼입 — `resync`
```
창 G  resync 3 / up=540   ·   창 H  resync 3 / up=2340
```
**절대 수가 같고 가동 시간은 4.3배**다 → **비율로는 낮다. 이 경로가 T2 감소를 설명할 수 있는 몫은 작다.**
⚠ **다만 `0` 이 아니므로 완전 배제도 아니다.**

## 4. 🔴 그런데 **다른 계수가 올랐다 — 이것을 성공에 묻지 마라**
```
17:40:28 [CNT] oow=1015 · ssovf=82 · cksumng=85 · esprst=2 · drop=1 · skip=2
```
🔴 **`cksumng=85` 와 `ssovf=82` 는 창 G 에서 보이지 않던 크기다.** T2 를 없앤 대신
**다른 자리에 손상이 나타났을 가능성**이 있다. **미검증이다** — 창 G 의 같은 칸과 **같은 규칙으로**
대조하지 않았다. **다음 작업 1순위.**
⚠ **`oow=1015` 은 하행이 송신 창에 도착한 것**이고 원장 7.x 대로 **충돌 계수가 아니다**(과대·누락 동시).

## 5. 세 갈래 합계 검산 (창 G 에서 57건이 사라졌던 그 검사)
**`T2` 원시 계수 0 · 갈래별 귀속 합계 0 → 일치.** ⚠ **분모가 0 인 검산은 힘이 약하다.**

## 6. 판정
```
✅ 이번 수정은 `>=64B` 대역의 T2 를 **1,025건 표본에서 0 으로 만들었다**
✅ (나) 기전 확정 — penddrop 9건이 실재
⚠ 문턱(64B) 진술은 **창 G 단독 근거**로 남는다 — 창 H 에는 재현 대상이 없어졌다
🔴 미검증 : cksumng 85 · ssovf 82 의 증감. **성공 보고에 이것을 빼고 쓰지 마라**
```

---

# 7. 마감 (18:00 이동 · 창 H 는 여기서 닫는다)

## 7.1 ✅ 확정된 것
```
`>=64B` 1,025거래 → T2 **0건**   (창 G: 34거래 → 9건 · 26.5%)   ← 표본 30배, 0 은 부족이 아니다
전체 T2 0 · T1 1 · stuck 0 · okto 1
(나) 기전 확정 : **penddrop = 9** (버려진 줄이 실재했다) · okstream 1,926
더미 pump 혼입은 작다 : resync 창G 3/540 · 창H 3/2340 (절대수 같고 가동시간 4.3배)
`ssovf` 는 안 올랐다 : 창G 35/900슬롯(3.9%) · 창H 82/2200슬롯(3.7%) — **슬롯당 비율은 같다**
```

## 7.2 🔴 못 정한 것 ①: **`cksumng` 0 → 85 의 해석** — *자극 부족도 표본 부족도 아니다. 분모가 다르다*
**원문을 열었다. 바이트 손상이 아니라 줄이 잘려 붙은 것이다:**
```
17:06:41  [CKSUM NG] T,283,S,A3,1AT+CIPSEND=66      ← 하행이 우리 CIPSEND 에코와 붙었다
17:07:17  [CKSUM NG] T,375,S,A4,176,S,B2,1,58       ← 하행 두 줄이 서로 붙었다
```
**하행 도착 분모로 나누면:**
```
창 G  0 / 약 220     = 0.00%
창 H  85 / 약 3,923  = 2.17%
```
> ### 🔴 **분모가 18배 다르다.** 창 G 는 링크가 계속 끊겨 **하행이 거의 도달하지 못했다.**
> ### **"수정이 하행 손상을 만들었다"와 "이제야 하행이 도달해서 손상이 보인다"를 못 가른다.**
**🔑 판정 불가.** 억지로 한쪽을 고르지 않는다.

## 7.3 🔴 못 정한 것 ②: **`64B` 문턱** — *재현 대상이 사라졌다*
창 H 는 `>=64` 가 0 이라 **대역 대비가 성립하지 않고**, `48~63` 은 **26건**뿐이다.
**문턱 진술은 창 G 단독 근거로 남는다**(48~63 43건 전부가 socket 의 단일 주입기 시험 한 창에서 나왔다).
⚠ **수정 전 펌웨어를 덮어썼으므로 같은 조건 재현은 이제 불가능하다.**

## 7.4 🔴 못 정한 것 ③: **`okstream` 의 정의**
무부하 기저에서 **`penddrop=0` 인데 `okstream=234`** 였다. **정상 도착까지 세는지 확인 안 됐다.**
그래서 **판정에는 `penddrop` 만 썼다.** `okstream` 수치를 성과로 인용하지 마라.

## 7.5 다음 세션이 **먼저 잴 것 한 줄**
> ### 🔴 **`하행 도착 수를 분모로 한 `cksumng` 비율** — 새 창에서 하행이 창 H 만큼 도달하게 부하를 주고,
> ### **2.17% 가 유지되는지 본다. 유지되면 수정이 만든 것이고, 떨어지면 창 H 특유의 것이다.**

⚠ **창 H 는 이동으로 닫힌다** — 망·서버 IP 가 바뀌므로 다음은 **새 창**이고, 창 H 와 **직접 비교하지 마라.**
