# `cksumng` 후보 하나가 죽었다 — **`rid` 자릿수 분포는 창 J 를 설명하지 못한다**

**2026-08-19 07:1x · monitor** · 계기: `REQ-0238`(창 O A[1] 기준선)을 준비하며 만든
`monitor/rid_width.py` 로 **기존 자료를 다시 쟀다. 새 관측은 없다.**

> # 🔴 먼저 — **이 문서는 후보를 하나 줄일 뿐이다**
> `FINDING-2026-08-19-0630` 에 이미 `❌` 다섯이 붙어 있고, **여기서 여섯째가 붙는다.**
> **`❌` 가 늘수록 남은 것이 자동으로 참처럼 보인다.** 오늘 `hex → cksumng` 이 정확히 그렇게 무너졌다.
> **남은 후보는 여전히 미검증이고, 이 문서는 그것들에 아무 무게도 싣지 않는다.**

## 1. 무엇을 시험했나

`FINDING-2026-08-19-0630` §3 이 남긴 미검증 후보 셋 중 하나:
```
🔮 그 창의 주입 회차 구성 · **rid 자릿수 분포** · 그날 무선 환경
```
`rid` 자릿수는 하행/상행 프레임 크기를 직접 밀어 올리므로 **크기 의존 기전이면 여기서 갈려야 한다.**

## 2. 실측 — 세 창

| 창 | `cksumng` | rid 4자리 비율 | rid 범위 | 누락 | 중복 |
|---|---|---|---|---|---|
| I | **3.73%** | 68.9% (2209/3208) | 1~3368 | **5.31%** (179/3368) | 19 |
| J | **0.87%** | 73.3% (2746/3745) | 1~3745 | 0.21% (8/3745) | 8 |
| M | **4.10%** | 74.2% (2864/3860) | 4~3866 | 0.16% (6/3863) | 3 |

**분모·구간**: 각 창의 **로그 전체**(`rid_width.py` 인자 없이). 세 창 모두 같은 도구·같은 처리다.
`cksumng` 은 장치 `[CNT]` 최종 누적값 기준의 기존 판정값을 그대로 인용했다.

## 3. 판정 — **두 축 다 창 J 를 설명하지 못한다** (독립인 주장이라 갈라 적는다)

### ① `rid` 자릿수 분포 → ❌ **후보 사망**
```
4자리 비율이 68.9% · 73.3% · 74.2% 로 **셋이 같은 대역**이다.
그런데 cksumng 은 3.73% · 0.87% · 4.10% 로 **J 만 4~5배 낮다.**
🔑 J 의 자릿수 구성은 M 과 거의 같다(73.3% 대 74.2%). **같은 입력에 다른 출력이다.**
```
> ### **`rid` 자릿수로는 창 J 의 낮은 `cksumng` 을 만들 수 없다.**

### ② `rid` 누락률 → ❌ **이것도 설명 못 한다**
```
J 0.21% 와 M 0.16% 는 사실상 같은데 cksumng 은 0.87% 대 4.10% 로 갈린다.
⚠ 창 I 만 누락 5.31% 로 튄다 — 이건 **별개 축**이고 이 문서에서 닫지 않는다.
```

## 4. 🔑 곁들여 확정된 구조 (원장으로 올린다)

```
`rid` 는 창(에폭) 안에서 **1 부터 1씩 증가하며 각 값이 정확히 한 번** 나타난다
  근거: 1자리 9건 · 2자리 90건 · 3자리 900건 = 그 자릿수의 **전수**. 세 창 모두 동일
→ 그래서 **누락**(범위 안에서 안 온 rid)과 **중복**(재전송)을 뺄셈으로 잴 수 있다
```

## 5. ⚠ 이 결론이 딛고 있는 것 — **넓히지 마라**

```
· **창 전체** 기준이다. 세 창은 자극 구성이 다르다
  → 🔴 같은 표의 `진입률` 은 창끼리 비교하지 마라(자극이 분모를 만든다). **자릿수만 비교했다**
· 자릿수 분포가 같다는 것은 **"크기 축이 창 J 를 못 만든다"**까지다.
  **"크기 축이 cksumng 과 무관하다"가 아니다** — 그건 다른 진술이고 안 쟀다
· `누락` 은 이 창 로그 기준이라 **창 경계에 걸친 rid 가 누락으로 보인다**(양끝 몇 건)
· 계측기 `rid_width.py` 는 **오늘 만든 새 도구**다. 교정 내역은 §6
```

## 6. 계측기 교정 — **어긋난 둘을 다 설명했다**

```
① CIPSEND 줄 : 파서 1,876 대 grep 1,910 (34줄 차)
   → 🔑 그 34줄은 **잡음이 아니라 혼재 사건**이다:
     `[AT] "T,207,S,A2AT+CIPSEND=52"` — 하행 조각에 상행 명령이 붙어 한 줄이 됐고
     같은 시각 `[CKSUM NG]` 가 찍힌다. **파서가 제외한 것이 옳다**
② 미짝 233건 → 알려진 **"에코 실종/지연"** 갈래다(`sendok_order.py` 세 번째 갈래)
③ 진입률 76.1%(주입구간) 대 판정문 74.8% → **분모 정의가 다르다**(내 쪽이 더 엄격)
   → 🔴 **이 도구의 절대값을 옛 판정문과 크기 비교하지 마라. 창끼리 같은 도구로만 쓴다**
```
**교차 확인 하나 얻었다**: `[CKSUM NG]` **줄 수 = 장치 `[CNT]` 의 `cksumng` 값**이 세 창 모두 정확히 일치했다
(150=150 · 33=33 · 165=165). **로그 파싱과 장치 계수라는 다른 각도 둘이 만났다.**

## 7. 재현
```
python3 monitor/rid_width.py monitor/serial-winJ.log        # 창 전체
python3 monitor/rid_width.py monitor/serial-winM.log 06:12:23 06:35:53   # 구간 지정
```
