# 창 AW t0 동결 — 2026-08-20 22:20:07 (굽기 리셋 · 칩 73a9b7f · hex IDENTICAL)
# 🔴🔴 **이 창은 "자극 대기" 창이다. 판정하지 않는다**
#   근거 : S 151건 중 occ=0 ×150 · occ=4 ×1 → 전이 1회
#          `V` 의 B1 값 119건 → **1건**(99.3% 못 쟀다) = **B1 앞 물체가 없어졌다**
#   → 내 판정선(R · 분모 전이 20건)의 **분모를 못 채운다**
#   ⚠ 이 구간을 "조용했다"로 적지 마라 — **"자극 없음"** 이다
#
# 🔑 t1 을 따로 잡는다 : `V` 에 B1 값이 **꾸준히** 오기 시작하는 시각.
#    **t0 와 t1 을 갈라 적는다.** 진짜 관측 구간은 t1 부터다
#
# 🔴 계측기 구분 (내 사전 등록 개정 · PREREG-hysteresis.md 22:22)
#   [USD] = 측정된 **모든 값**의 min/max   ·   V = **슬롯 순간**의 마지막 값
#   → 폭이 넓은데 V 가 빈 칸이면 **원거리 잡음**이다. 실패로 읽지 마라
#
# ✅ A1 8건이 설명됐다 : [USD] A1 = **56~64cm** → 문턱 60 **바로 경계**에 물체가 있다
#    내 "22:08 이후 누가 손을 댔다" 가 값으로 확증됐다
