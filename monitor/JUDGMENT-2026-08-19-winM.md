> # 🔴 **범위 — 결론만 읽는 사람이 반드시 볼 것**
> **① 축이 셋이다**: arduino 가상 O*(`n` 10→12) · socket `G` 명령 · **web 150ms 티커(부하 축)**
> **② 진입률 74.8% 는 주입이 만든 모집단이다.** 실사용을 재는 자료는 **없다**(원장 7.154).
> **③ `>=64B` 는 창 J 이전과 *다른 것* 을 센다** — 이제 **ACK 동봉 경로 하나뿐**이다. **절대값 비교 금지.**
> **④ 창 앞부분(탭 개방~첫 `G`)은 *자율 토글 구간* 이다** — 그 구간의 인과는 못 쓴다(원장 7.168).

# 판정 · 창 M — **`SEND OK` 축은 네 창 연속 0. `cksumng` 축은 무너졌다** (2026-08-19)

**규칙** `SPEC-분모정의-2026-08-18.md` · **사전 등록** `PREREG-2026-08-19-winL.md`(굽기 전 발행)
**창** t0 `05:58:16`(탭 개방) · **주입 `06:12:23~06:35:53`**(23분 30초 · socket 로그 기준)
**판본** 장치 `9edbcca`(n=12 · 가상 O*) · 서버 `7c73c39`

---

## 1. 대역별 결과
```
대역        거래수     T2        │ 창 J        │ 창 I        │ 창 G
<48           41       0        │    88  0    │    58  0    │   748  1
48~63        258       0        │    50  0    │    33  0    │    43  0
>=64        **886**  **0**      │   927  1    │ 1,067  0    │  **34  9 (26.5%)**
합계       1,185  ★★T2 **0** · ★T1 0 · stuck 0 · resync 0 · esprst 0 · drop 0 · 재접속 0회
진입률 **74.8%**  (등록 프레임 없음 — 이 창에는 재등록이 안 일어났다)
```
> ### **`>=64B` 886거래에서 `T2` 0. 창 G 비율(26.5%)이면 **약 235건**이 나왔어야 한다.**
> ### **`SEND OK` 실종 축은 창 H·I·J·M **네 창 연속** 사실상 0 이다.**

## 2. 🔑 **처음으로 `penddrop` 이 0 이 아니다 — 기전이 발동했고 대책이 구했다**
```
pendfill **1,834** · penddrop **2**   ·   bigfill **882** · bigdrop **2**   ·   oklost **0** · bigoklost **0**
```
**창 H·I·J·L 에서는 `penddrop` 이 계속 0 이라 *"발동을 안 한 것"* 으로 남겨 뒀다. 이번에 발동했다.**
```
✅ 분모가 크다(1,834) → `penddrop=2` 는 **"버림이 드물다"** 를 말할 수 있다
✅ `oklost = 0` → **버려진 2줄 중 `SEND OK` 는 없었거나 바이트 매칭이 구했다**
✅ 불변식 성립 : `pendfill(1,834) ≥ bigfill(882)` · `penddrop(2) ≥ bigdrop(2)`
🔑 `bigdrop = penddrop = 2` → **버림이 전부 `>=64B` 대역에서 났다**
```
⚠ **`oklost=0` 은 여전히 "미분리"다**(원장 7.167): *"구할 것이 없었다"* 와 *"잃을 경로가 안 밟혔다"* 를
못 가른다. **다만 `penddrop=2` 가 붙어 앞의 것 쪽으로 기운다** — **버릴 줄은 실제로 있었다.**

## 3. 🔴 `cksumng` 축은 무너졌다 — **어제 결론을 철회했다**
```
창 I  3.73% (밀도 171.7줄/분)  ·  창 J **0.87%** (175.1)  ·  창 M **4.10%** (171.1)
```
**밀도가 셋 다 같은데 내려갔다 올라갔다.** **`hex` 는 창 J·M 에 둘 다 있다 → hex 로 설명 안 된다.**
**상세·후보 목록**: `FINDING-2026-08-19-0630-cksumng-refuted.md`
```
❌ 죽은 후보 : 서버 하행 생성 경로 · 서버 송신 지연 · 하행 도착 오프셋 · 하행 밀도 · hex(S 크기)
🔮 남은 것   : WS 연결 수 / web 티커 · rid 자릿수 · 무선 환경 · **아직 안 적은 것**
```
⚠ **후보가 하나뿐으로 보이면 결론이 아니라 목록이 짧은 것이다**(원장 7.171).

## 4. ✅ 조작 사슬 — **명령 ↔ 에코 짝이 4쌍. 자율이 멈춘 뒤라 인과가 선다**
```
06:03:35 G,7,10,0(닫기) → 06:03:36 occ 60B→**609**    06:05:19 G,8,10,1(열기) → 06:05:20 609→**60B**
06:05:38 G,11,10,0(닫기) → 06:05:40 60B→**609**       06:07:51 G,12,10,0(닫기) → **변화 없음(멱등)**
```
🔑 **자율이 도는 동안에는 이 짝짓기가 *원리적으로 불가능*하다** — 값이 저절로 바뀌므로.
**socket 의 닫는 전이를 내 계측기가 독립 확인했다.** ❌ **창 L 의 `608→60A` 는 자율 구간이라 안 쓴다.**

## 5. 📌 미결·판정 불가
```
🔴 `cksumng` 왜 창 J 만 낮았나 — 후보 다섯이 죽었고 남은 것은 미검증. **닫지 않는다**
🔴 `장치거절` — **실물 경로에서 구조적으로 0**(서버가 범위 밖 idx 를 안 만든다). **도달 불가**이지 건강 아님
⚠ `oklost = 0` — 미분리(§2)
⚠ `tmask`(f[6]) — 장치가 안 보낸다. **관측 밖**
⚠ WS 연결 수 — **이 창에는 그 축이 아무 데도 안 남았다.** 다음 배포부터 요약에 생긴다
```

## 6. ⚠ **서버 송신 지연 회귀 — 실재하지만 이 축으로는 아무것도 안 깨뜨린다** (socket 요청 기록)
```
socket `S+Nms` ≥2ms :  창 I 9.4%  ·  창 J 10.2%  ·  창 M **40.9%**   ← **4배. 실재한다**
내 도착 오프셋 중앙  :  293ms     ·  273ms      ·  **264ms**       ← **오히려 빨라졌다**
```
🔑 **규모가 다르다**: socket 의 절대 변화는 **~1~3ms**, 내 예산은 **264~293ms** 다.
> ### **"회귀가 없다"가 아니라 "이 축으로는 §8.15(UART 혼재)를 만들지 않는다".**
✅ **고칠 값이지만 지금 아는 한 아무것도 안 깨뜨린다.** ⚠ **나중에 이 4배를 발견한 사람이 놀라지 않게 여기 적는다.**
🔑 **한쪽만 봤으면 "4배 악화"로 보고했을 것이고, 그건 참이면서 오해를 만든다** —
**socket 은 "서버가 언제 보냈나", 나는 "장치가 언제 받았나"를 잰다. 같은 사건의 양 끝이다.**
