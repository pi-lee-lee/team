# 사전 등록 · 창 X — **접점 수리 뒤. 목표는 큰 분모의 `0` 이다**

> # 🔴 머리 배너 — 결론만 읽는 사람이 볼 것
> ### **① 반례가 있다: 창 S 는 `깨진 AT 0 / 997` 인데 `busy p` 868 · 23분 미복구였다.**
> ### **UART 손상은 *고장을 만들 수 있는 조건* 이지 *모든 고장의 원인* 이 아니다**(FINDING-1650)
> ### **② "정상" = 끊김 0 이 아니라 **끊겨도 자력 회복**(사용자 확정 · 원장 §7.188)
> ### **③ `hwrst 0/0` 은 판정 자료가 아니다** — `ESP_RST_WIRED=0` 이라 **4단이 *없다***
> ### **④ 자력 복구의 천장은 3단 `AT+RST`.** `busy p` > 0 이면 **사람이 개입해야 한다**

**발행 18:18** — 탭 `18:17:41` 개방 · **판정 자료를 보기 전에 적는다.**

## 0. 경계
```
사용자 수리 : **TX/RX 핀을 꽉 끼게 조절**(접점) — 16:4x~
16:16:28  옛 서버 끝 `198e15b` pid 13000 · frames 4482 · sessions 34
18:16:30  🔴 새 서버 **`572a4cb`** pid 44577 · cwd=/Users/idong-u/parking-bin · PPID=1
🔴 내 t0   **18:17:41** (탭 개방 = DTR 리셋 1회 · 18:17:43 부팅)
```

### ⚠ **"붙는 과정을 창 밖에 두라"는 지시를 물리적으로 못 지킨다**
**내 탭 개방이 곧 DTR 리셋이다.** 장치가 이미 붙어 있었어도 **내가 여는 순간 끊고 다시 붙는다.**
> ### **그러므로 재결합 구간은 이 창에 *반드시* 들어온다. 뺄 방법이 없다.**
✅ **할 수 있는 것**: **첫 `[CNT]` 줄이 부팅 60초의 기여분**이므로 그것으로 갈라 읽는다(원장 §7.174).
⚠ `up=60` 을 리터럴로 잡지 않는다 — **"첫 `[CNT]` 줄"이 방법이다**(§7.174.5).

## 1. 🔴 관측 공백 — **16:43:47 ~ 18:16:28 (1시간 33분) 내 자료 없음**
**socket 이 서버 쪽 값을 줬다:**
```
세션 이벤트 **43건**(접속+종료 합)
종료 사유 : 같은 device_id 재접속 대체 **7** · 상대가 닫음 **6** · errno=54 **5** ·
            keepalive errno=60 **3** · 서버 종료 1(18:16:28 배포)
```
> ### **"조용했는가"에는 답이 나왔다 — 조용하지 않았다.**
🔴 **그러나 귀속하지 않는다.** 그 구간에 **사용자 배선 작업**이 있었고 **절단들이 그것과 섞여 있다.**
⚠ **서버 계측기는 서버가 본 것만 본다** — 장치 쪽 원인(전원·배선·리셋)은 여전히 못 본다.

## 2. 🔴 1번 축 — **깨진 나가는 AT.** 두 기준을 합산하고 분모를 같이 적는다
```
도구 : `python3 monitor/uart_damage.py monitor/serial-winX.log`
       ① 비출력 손상(`\x`) + ② **출력가능 손상**(`AT+CIPSEND!24` — `=`→`!`)
🔴 목표 분모 : **CIPSEND 3,470건 급**
   **그 전에는 `0` 을 근거로 쓰지 않는다** — 창 T 가 3,470 이라 값이 있었다
```
**대조(같은 도구로 다시 센 값 · FINDING-1650):**
```
✅ 0 / 3,470  창 T   41분 무사고        ← 가장 강한 0
   0 / 2,260  창 H   ·  0 / 3,714 창 J  ·  0 / 1,910 창 M
🔴 14 / 963 = 1.45%  창 R   +15분 붕괴
🔴  6 / 417 = 1.44%  창 V
🔴  6 / 228 = 2.63%  창 W   Unlink 4 · 반복 절단
🔴🔴 0 / 997         창 S   **손상 0 인데 `busy p` 868 · 23분 미복구** ← **반례**
```

## 3. 판정 축 — **`busy p` 하나**
```
`busy p` **0**  → 파서 살아 있음 → 3단이 먹는다 → 자력 회복 = **정상**
`busy p` **>0** → 파서 먹통 → 🔴 **천장. 4·5단이 없으므로 사람이 개입해야 한다**
```

## 4. ⚠ 비교 금지 · 규율
```
🔴 **자리 기준 비율을 창 너머로 비교하지 마라** — 자리가 **10 → 7** 로 바뀌었다
🔴 창 G~W 와 크기 비교하지 않는다 — 기판·펌웨어·서버가 다 다르다. **"이만큼이다"로만**
⚠ **60분 미만은 "흔적"** · **판정 쓰기 전 배경 집계 상태부터 확인**한다
🔴 손상 계수는 **`uart_damage.py` 로만 센다** — heredoc·subprocess 안에서 세면 `0` 이 나온다
   (오늘 네 번 밟았고 **창 V 판정문에 틀린 `0` 이 실렸다**)
```

## 5. 이번 배포의 축 (socket 제공)
```
✅ 거동 0 : RidPool 추출 · Lot 추출 — **전선 형식·타이밍 미접촉**
🆕 로그 둘 : `정적 자원`(기동 1회) · **`화면 판본`(판본 바뀔 때만)**
🔑 **`화면 판본` 줄이 다시 뜨면 그것이 "화면 교체" 표지**다 — 그 사건 말고는 안 만들어진다
🔴 없어진 것 없음 · 요약 형식 불변 · state/map 계약 불변
확인값 : rid 커서 **이어받음 4164**(전선값 164) · 지형 판 2 · 등록 n=12 drain=7 · GET / 200
         화면 판본 src=3572266c2cf8 git=25b1be3 · 211428B
```

## 6. 실행 기록 (측정 후에만)
```
창 닫은 시각 : (미기입) · 깨진/분모 : (미기입) · busy p : (미기입) · 배경집계 : (미확인)
```
