# 사전 등록 · 창 P — **A[1] 판정 창** (REQ-0253)

**발행 10:15** — 탭은 `10:15:11` 에 열렸고 **판정 자료를 보기 전에 적는다.**

## 0. 🔴 `t0` — **굽기 리셋이 아니라 내 DTR 리셋이다. 흐리지 않는다**
```
arduino 굽기 리셋 : 시각은 arduino 가 준다 (서버가 본 첫 접속 10:14:04 은 **부팅보다 뒤**다)
🔴 내 t0          : **10:15:11** (탭 개방 = DTR 리셋 = **새 부팅**)
```
> ### **굽기 직후 부팅 국면은 내 로그에 없다. 탭을 여는 행위가 그 자체로 보드를 다시 리셋하기 때문이다.**
**내 창 머리는 "굽기 부팅"이 아니라 "내 리셋 부팅"이다.** 굽기 부팅(10:14:04~10:15:11)의
장치측 자료는 **아무도 안 떴다** — 서버 로그에만 남는다. **그 구간을 내 분모에 넣지 않는다.**
🔑 부팅 국면 기여를 재는 방법은 창 L/M 때와 같다: **첫 `[CNT]` 줄이 곧 부팅 60초의 기여분**(원장 7.174).
⚠ `up=60` 을 리터럴로 잡지 마라 — **"첫 `[CNT]` 줄"이 방법이다**(7.174.5).

## 1. 축 — **셋이다. 판정문 머리에 그대로 박는다**
```
축 1  **A[1]**  rid 폭 고정 1000([0,999] · 최대 3자리) + 격리 7200ms   ← **재려는 것**
축 2  🔴 **망 이동** 192.168.35.x → 192.168.0.x · AP 교체 — **무선 환경이 통째로 다르다**
축 3  칩 교체 `.basefw`(8b63846) → 정본(2187cc3) · 34커밋 +1555/-61
      (그중 954줄이 주석 → 실제 코드 증가 약 600줄. **미지가 아니라 열거 가능**)
```
### 🔴 축 2 를 빼지 마라 — arduino 의 "축이 줄었다"는 **기준점이 다르다**(원장 7.180)
```
arduino 기준 : 지금 칩 → 정본   (칩에서 칩으로)  → 망 값이 이미 칩에 있으니 축 아님. **맞다**
내 기준      : 창 O  → 창 P     (창에서 창으로)  → 🔴 **망은 그 사이에 바뀌었다. 축이다**
```
**망 값은 사용자 플래싱(시각 미상 · 07:36:48<T<10:04)에서 들어왔다. 축이 사라진 게 아니라 다른 경계에서 들어왔다.**
⚠ 그리고 축 2 는 펌웨어 상수가 아니라 **실제 전파 환경**이라 **칩 바이트 대조로 안 지워진다.**

## 2. 🔴 비교 규율 — **무엇을 창 O 와 대 볼 수 있는가**
```
✅ 대조 가능 : `rid` 자릿수 · ACK 폭 `B` · `N` 분포 — **무선과 무관한 축**
🔴 대조 금지 : `cksumng` · `ssovf` · 링크 사건(`esprst`·`drop`·`stuck`·T2) — **축 2 에 걸린다**
```
**금지 항목은 값을 내되 창 O 와 크기 비교하지 않는다.** 이 창 안의 절대값으로만 적는다.

## 3. ⚠ 서버측 계수의 `0` — **"안 일어났다"가 아니라 "표본이 없다"**
`fceadf3`(pid 64096 · 10:00:22)는 **장치를 이번에 처음 본다.** 모든 서버측 누적이 0 에서 시작한다.
> **원장 1.1 그대로다. `0` 을 건강으로 읽지 않는다.**

## 4. 이 창이 답할 것
```
① 🔴 A[1] 1차 확인 : **`rid` 4자리 = 0 인가.** 하나라도 나오면 **A[1] 이 안 도는 것**이다
   ⚠ `G` 명령이 나가야 표본이 생긴다. **지금은 상행만 돈다** → 주입 전에는 판정 불가
② `>=64B` 진입률 + 분모 — 창 O 대조군 **259/371 = 69.8%**
   🔴 창 O 는 **3자리 57.3% / 4자리 36.3% 가 섞인 창**이다. A[1] 이 4자리를 없애면
      **진입률 차이의 일부는 그 전이가 사라진 몫**이다. **분리해서 읽는다**
③ ACK 폭 `B` · `N` 분포 — 창 O 대조군 N=4 **60.0%**
```

## 5. 표본·판정선 — **미리 정한다**
```
무자극 기준선 : 먼저 뜬다(주입 전 구간). **주입 구간과 한 분모에 안 묶는다**
판정선        : `>=64B` 대역 거래 **300건 이상**이어야 진입률·T2 를 인용한다
                미달이면 **"흔적"이라 적고 판정하지 않는다**
rid 판정      : 4자리 **1건이라도** 나오면 A[1] 미작동으로 보고한다(비율 아님)
```
🔴 **감시를 같은 자리에 만든다** — `monitor/winO_watch.py` 를 창 P 로 돌려 쓴다
(탭 생존 · 서버 epoch 대조 · bigfill 분모 · 링크 사건 · 화면 수).
⚠ **`화면` 은 브라우저가 아니라 `:9900` WS 클라이언트 수다. 주입기가 들어간다**(창 O §5 확정).

## 6. 개입 통보
```
🔴 탭 개방 10:15:11 — 내 DTR 리셋. 루트·socket 에 알린다(남의 로그에 "원인 없는 절단"으로 남는다)
```

## 7. 실행 기록 (측정 후에만)
```
굽기 리셋 시각 : (미기입 — arduino 가 준다)
무자극 구간    : 10:15:11 ~ (미기입)
주입 구간      : (미기입 — 루트가 지시한다)
표본           : (미기입 — bigfill 분모)
창 닫은 시각   : (미기입)
```

---

# 🔴 정정 (10:22) — **사용자 지시로 `t0` 를 옮긴다. 결과를 보고 바꾼 것이 아니다**

**사용자 원문(REQ-0256)**: *"오늘도 이전 데이터는 관측에서 제거한다. **현 시점(10:20)부터 관측 데이터로
이용한다.** 절단 관련은 **내가 이동 후 환경셋팅 시 발생한 것이다.** 이 부분들은 관측 데이터에서 제거한다."*

## ⚠ 사전 등록을 고치는 것이므로 **사유를 먼저 적는다**
```
🔴 이 정정은 **외부 사실**(사용자가 자기 개입을 공개했다)에서 왔다.
   **자료를 보고 기준을 옮긴 것이 아니다.** 그건 사전 등록을 무의미하게 만드는 행위다.
✅ 판정선·분모 규율·비교 금지 항목은 **하나도 안 바꿨다.** 바뀐 것은 창의 시작점뿐이다.
```

## 0'. 새 `t0` — **10:20:00**
```
❌ 제외 : 10:15:11 ~ 10:20:00  — 내 DTR 부팅 + **사용자 환경설정 구간**(절단 09:42:30·09:57:48·10:14:17·10:15:21)
✅ 창 P : **10:20:00 ~ (진행 중)**   · 로그는 그대로 `monitor/serial-winP.log`(탭 유지)
```
🔑 **탭을 다시 열지 않았다.** 다시 열면 **DTR 리셋이 하나 더 생긴다** — 지금 막 정리한 그 문제를 내가 또 만들게 된다.
**구간만 갈랐다.** 🔑 그 결과 **새 창에는 부팅 국면이 아예 없다**(부팅은 10:15:14, 창 시작은 10:20:00 = 부팅 +4분 46초).
**창 L/M 에서 하던 부팅 기여분 가르기가 이 창에는 필요 없다.**

### ✅ 한 부팅 안이다 — 차분이 성립한다
```
`System Ready` 횟수 = **1**  (탭 개방 이후 추가 리셋 없음 · 실측)
→ `[CNT]` 는 부팅 이후 누적이므로 **차분으로 읽는다.** 절대값을 창 값으로 쓰지 않는다
```
**기준 표본(t0 직후 첫 `[CNT]`)** — 10:20 컷오프에 가장 가까운 줄:
```
10:20:14  up=300  ssovf 0 · cksumng 0 · bigfill 1 · pendfill 244 · penddrop 0
                  esprst 0 · drop 0 · stuck 0 · okto 0 · smiss 1
```
⚠ `smiss=1` 은 **`up=60` 에 이미 있었다 → 부팅 국면 산물**이고 **새 창 밖**이다. 창 값으로 세지 않는다.
⚠ 기준 표본이 컷오프보다 **14초 뒤**다(`[CNT]` 60초 주기). **그 어긋남을 숨기지 않는다.**

## 4'. 🔴 판정 방식이 바뀐다 — **대조군이 없어졌다**
`A[1]` 은 이미 배포됐고 **이전 기준선을 다시 뜰 방법이 없다.** 창 O 의 `69.8%` 는 **더 이상 대조군이 아니다.**
```
✅ 절대 기준으로 판정한다 — **1차 판별자가 원래 절대값이라 그대로 성립한다**
   🔴 `rid` 4자리 = **0** · 범위 `[0,999]` · 격리 발동 여부 · **재사용이 일어나기는 하는가**
⚠ `>=64B` 진입률은 **"좋아졌다"가 아니라 "이만큼이다"로 적는다.** 비교 대상이 없다
```

## 8'. ⚠ 이전 자료는 **지우지 않는다 — 자격을 낮추는 것이지 삭제가 아니다**
```
✅ 남는다 : 창 O 판정문 · 대역 소급 계산 · `sendok_order.py` 교정 · 소스 판독 · 자기 프로세스 안에서 닫히는 실측
❌ 낮춘다 : 그 값들을 **창 P 의 대조군으로 쓰는 것**
```
**§"자료를 무효화할 때 셋을 갈라라" 그대로다.** *"결함이었는가"* 와 *"그 결함이 이 증상을 만들었는가"* 는 다르다.

## ✅ 곁들여 — 미상 넷이 닫혔다
`09:42:30` · `09:57:48` · `10:14:17` · `10:15:21` 절단이 **전부 사용자의 환경설정**이었다.
🔑 **우리 계측기 셋(서버 로그·시리얼·칩 판독) 어느 것도 이걸 볼 수 없었다. 개입할 수 있는 쪽만 답할 수 있었다.**
✅ **`[DROP-OVF]` 를 후보로 두되 원인으로 승격시키지 않은 것**이 값을 했다 — 원인이 아니었다.

---

## 9. 무자극 기준선 — **확정 (10:20:00 ~ 10:30:00 · 10분)**

**10:23 현재 실측**(기준 표본 10:20:14 대비 차분): `esprst 0 · drop 0 · stuck 0 · okto 0 ·
ssovf 0 · cksumng 0 · penddrop 0 · bigfill 1`(변화 없음) · `System Ready` 1회(추가 리셋 없음).
> **무자극 상태에서 링크·손상 계열이 전부 0 이다. 이것이 창 P 의 진입 조건이다.**
⚠ `bigfill = 1` 은 **`>=64B` 대역에 자극이 없다는 뜻**이지 건강이 아니다(원장 7.174.2).

## 10. 🔴 socket 이 사전에 알린 것 — **판정문에 그대로 옮긴다**

### 주입 규약 (창 O 와 **한 글자도 다르지 않다**)
```
sh net/winG-rounds.sh 8 P   · 라운드 8 · 인스턴스 8(A1~A5·B1~B3)
--interval 0.3 --start-delay 0 --count 60 --rid-prefix injP<r>-<i> --stop-queue-full 20
기록 net/inject-winP-2026-08-19.log · 서버 fceadf3 pid 64096
정지선은 **내가 본다** → `.team/STOP-P` · socket 상한은 라운드 수와 40분
```

### 🔴 이 창에서 **처음 밟히는** 축 — `0` 을 건강으로 읽지 않기 위해 미리 적는다
```
새 계수 : 발행 N(다음 x/1000 영속) · 격리 q · 건너뜀 s · 강제 f · ACK 미상rid u · 자리불일치 m
지금 전부 0 이고 그 `0` 은 **"표본 없음"**이다 — 하행이 한 건도 없었다
```
🔑 **재사용 문턱은 발행 1000건째**이고 커서가 **324** 에서 시작했으므로 **1324 에서 첫 재사용**이다.
창 O 규모(시도 1679)면 **넘는다 → 격리 로직이 이 창에서 처음 실제로 밟힌다.**
⚠ **안 넘으면 `격리/건너뜀 0` 도 "표본 없음"이다.** **밟혔는지 아닌지를 갈라 적는다** —
socket 이 끝나고 `발행` 수를 값으로 준다. **그 값 없이 0 을 인용하지 않는다.**

### ⚠ 이 기동만의 단서
```
10:00:22  🔴 rid 커서 파일을 못 읽어 **임의 지점 324** 에서 시작
          → **이 기동만** 장치 멱등 캐시(최근 16)와 겹칠 수 있다(약 16/1000)
```
겹치면 **명령이 조용히 삼켜지는데 ACK 은 온다.** 내 쪽 서명은
**`G` 를 보냈는데 에코 비트가 안 바뀌고 ACK 은 오는 사건**이다.
🔑 **다만 이번 주입은 `T` 라 자리 조작이 없어 그 축은 안 밟힌다.** **`자리불일치 0` 도 표본 없음이다.**

---

## 🔴 정정 2 (10:25) — **절단 넷을 전부 사용자 것으로 적었다. 둘은 우리 것이다**

**§정정(10:22)에서 내가 이렇게 적었다:**
> ~~*"사용자 환경설정 구간(절단 09:42:30·09:57:48·10:14:17·10:15:21)"*~~ ← 🔴 **틀렸다**

**시각으로 가르면 둘로 갈린다:**
```
09:42:30 · 09:57:48  →  ✅ **사용자의 이동 후 환경 셋팅** (팀은 07:41 에 정지해 있었다)
10:14:17 · 10:15:21  →  🔴 **arduino 캡처 개방(10:14:07) · 내 탭 개방(10:15:11)** — 각각 포트 개방 +10초
```
> ### **사용자의 설명이 포괄적이라 *전부를 덮는 것처럼* 읽혔다. 나는 그렇게 읽었고 socket 에게도 그렇게 전했다.**
> ### **§"한 설명이 맞으면 다른 설명을 찾지 않는다" 의 가장 강한 형태가 *권위 있는 설명* 이다.**

🔑 **내 탭 개방도 그 목록에 있다. 내 것이 아니라고 적지 않는다.**
⚠ **그리고 이 오류는 자료를 오염시키지는 않았다** — 어느 쪽이든 `10:20:00` 이전이라 창 밖이다.
**틀린 것은 값이 아니라 귀속이고, 귀속이 틀리면 다음 사람이 원인을 엉뚱한 데서 찾는다.**
✅ **socket 에 즉시 정정을 보냈다**(내가 잘못 전한 것이므로).

### ✅ 곁들여 — arduino 의 판별자가 좋았다
*"`[DROP-OVF]` 는 **매 등록마다** 뜨는데 세션#3 은 **안 끊긴다** → 공변만으로 인과가 안 된다."*
**그리고 세션#3 은 그 뒤 아무도 포트를 안 잡아서 지금도 살아 있다.**

---

## 11. 🔴 상시 규칙 — **자리이동에는 환경 안정 5분이 붙는다** (사용자 확정 · 다음 이동부터)
```
사용자가 "자리이동" 명시  →  ⏳ **5분 안정 구간(❌ 관측 제외)**  →  ✅ 여기부터 관측 자료
```
**운영 정의(루트 확정 — 안 정하면 각자 다르게 잰다):**
```
기산점 : 사용자가 **이동을 명시한 시각** — 양쪽이 볼 수 있는 유일한 값
🔴 5분은 **하한이지 상한이 아니다** — 지나도 장치·서버가 안 올라와 있으면 창은 그때부터 연다
표기   : 사전 등록에 `이동 명시 <시각> · 안정 종료 <시각> · t0 <시각>` 을 **값으로**
```
⚠ **관측에서 빼는 것과 기록에서 빼는 것은 다르다.** 로그·원장에는 그대로 남긴다.
**실물 개입 통보 의무도 그대로다.**
📌 **오늘 이동은 이미 끝났으므로 이 규칙은 다음 이동부터 적용된다.** 창 P 는 `t0 = 10:20:00` 그대로.

---

## 12. 🔴 주입 시작 · **그리고 "지금 4자리 0" 은 판정이 아니다**

```
주입 첫 건 : **2026-08-19 10:30:01.103** (`injP1-7-1` slot=B2 · socket 이 로그에서 읽은 값)
✅ 무자극 구간 10:20:00~10:30:00 은 **안 잘렸다** — 감독기가 스스로 기다렸다
```

### ⚠ 10:30:46 실측 (주입 45초) — **인용하지 마라. 판정선 미달이다**
```
rid 3자리 139/139 = 100% · 4자리 **0** · 범위 **324~462** · 중복 0 · 누락 0
```
> ### 🔴 **`4자리 0` 을 "A[1] 작동 확인"으로 읽으면 틀린다.**
> ### **rid 가 462 다. A[1] 이 *없는* 펌웨어도 여기선 3자리다. 자를 대상이 아직 없다.**

**A[1] 은 rid 를 `[0,999]` 로 자른다. 그러니 판정은 발행이 999 를 넘을 때 처음 성립한다:**
```
✅ A[1] 작동  : rid 가 999 다음에 **되돌아간다**(0 쪽으로) — 자릿수가 안 늘어난다
🔴 A[1] 미작동 : rid 가 **1000 으로 넘어간다** = 4자리 출현
⏳ 지금       : **표본 없음.** 커서 324 에서 시작했으므로 999 까지 **675 발행** 남았다
```
**그래서 이 창의 A[1] 판정은 `발행 > 999` 가 된 뒤의 자료로만 한다.**
🔑 **socket 이 끝나고 주는 `발행` 수가 그 조건 충족 여부를 가른다. 그 값 없이 `4자리 0` 을 인용하지 않는다.**
⚠ 원장 1.1 그대로다 — **`0` 이 "안 일어났다"인지 "일어날 수 없었다"인지 갈라야 한다. 지금은 후자다.**

### ⚠ `srv_id` 를 판본 판별자로 쓰지 마라 (socket 지적)
```
srv_id = `b0d58d9-1787090316424`  인데 실제 판본은 **fceadf3** 다
→ 그 id 는 **기동 때 한 번 만들어진 문자열**이고 접두가 낡을 수 있다
✅ 판본은 `build=` 로 본다 — 내 감시기는 INSTANCE 줄의 `pid=`·`build=` 를 보므로 안 걸린다(확인함)
```
🔑 **§7.181("배너는 선언, 값은 결과")의 서버 판본**이다. **이름에 박힌 문자열은 낡는다.**

---

## 🔴 정정 3 (10:33) — **`srv_id` 절을 통째로 철회한다. 값이 틀렸고 나는 확인 없이 옮겼다**

**§12 에서 내가 이렇게 적었다:**
> ~~*"`srv_id = b0d58d9-1787090316424` 인데 실제 판본은 `fceadf3` 다 → 그 id 는 기동 때 한 번
> 만들어진 문자열이고 접두가 낡을 수 있다. §7.181(배너는 선언, 값은 결과)의 서버 판본이다."*~~

### ✅ 내가 서버 로그에서 직접 확인한 값
```
06:36:58  9a2e3bb-1787089018518
06:58:36  b0d58d9-1787090316424     ← 내린 인스턴스
10:00:22  **fceadf3-1787101222325** ← 🔴 **지금 도는 것. 접두가 판본과 일치한다**
```
> ### **`srv_id` 접두는 낡지 않는다. 판본을 그대로 따른다. 내가 적은 "낡을 수 있다"는 거짓이다.**

### 🔴 무엇이 잘못됐나 — 오늘 두 번째다
```
socket 이 확인 안 한 값을 보내며 **그럴듯한 설명을 붙였다**("id 는 기동 때 한 번 만들어지니 정상")
→ 나는 그 값과 설명을 **확인 없이 내 사전 등록에 적었다**
→ 게다가 **내 원장 §7.181("배너는 선언, 값은 결과")을 근거로 덧붙여 더 그럴듯하게 만들었다**
🔴 **그 값은 `grep` 한 줄이면 확인됐다. 서버 로그는 내 손에 있었다.**
```
> ### **설명이 붙으면 검산 압력이 줄어든다. 받는 쪽에는 "확인된 값 + 근거"로 보인다.**
> ### **그리고 내가 참인 일반 규칙을 덧붙이는 순간, 틀린 값이 내 원장의 권위까지 빌려 입었다.**

⚠ **§7.182 와 같은 계열이고 방향만 반대다**: 거기서는 *가진 것을 안 대 봤고*, 여기서는
**안 대 보고 대신 설명을 만들었다(그리고 남의 설명에 내 근거를 보탰다).**
🔑 **일반 규칙을 인용해 남의 값을 떠받치기 전에, 그 값을 먼저 재라.**
**규칙이 참이어도 값이 틀리면 결론은 틀리고, 규칙이 참이라서 더 안 잡힌다.**

### ✅ 실무 영향 — 없다 (확인했다)
```
내 감시기는 `srv_id` 를 **안 쓴다.** INSTANCE 줄의 `pid=`·`build=` 만 본다
   monitor/winO_watch.py:78  re.search(r"pid=(\d+).*build=(\S+)")
   ("srv_id" 는 15행 **주석**에만 있다 — 코드 경로 아님)
✅ 창 P 의 epoch 감시는 처음부터 `build=fceadf3` 로 대조하고 있었다. **자료 영향 0**
```
📌 **판본·id 는 문장으로 쓰기 전에 `grep` 한 줄을 먼저 친다** — socket 과 같은 규율을 세운다.

---

## 13. 🔴 A[1] 판정 문턱은 **둘이다** — 다른 것을 증명한다 (socket 정정 · 접수)

```
🔴 A : 커서 **1000** 도달 → rid 가 999 다음에 **0 으로 감긴다**
       = **"공간이 실제로 1000 으로 잘린다"** 의 증거. 옛 코드였다면 1000(4자리)이 됐을 자리
🔴 B : 커서 **1324** 도달 → 한 값이 **두 번째로 발행된다**
       = **"격리·건너뜀 로직이 처음 밟힌다"** 의 증거
```
> ### **A 없이는 "3자리다"가 무의미하고**(462 면 A[1] 없는 코드도 3자리),
> ### **B 없이는 `격리/건너뜀/강제` 의 `0` 이 전부 표본 없음이다.**
⚠ **A 를 넘어도 B 는 별개다. 판정문에 갈라 적는다.**

### ⚠ 재는 것은 `발행` 이 아니라 **커서**다
```
`건너뜀 = 0` 인 동안에만 `커서 = 324 + 발행` 이라 둘이 같이 간다
🔴 `건너뜀` 이 오르면 갈린다(건너뛴 칸도 커서를 먹는다) → 그때부터 **`다음 x/1000` 을 본다**
```
**socket 실측 10:32:47** — 모델이 값과 맞는다:
```
발행 449 · 다음 **773**/1000 · 격리 441 · 건너뜀 **0** · 강제 0 · ACK 미상rid 0 · 자리불일치 0
324 + 449 = 773 ✅
남은 거리(건너뜀 0 가정) : A 까지 **227 발행** · B 까지 **551 발행**
```
⚠ **이건 예측이다. 판정은 사후에 값으로 한다.**

### 🔑 `격리 441` 이 이미 말해 주는 것 (socket)
격리표에 441칸이 들어 있다 = **해제 경로가 실제로 돌고 있다.**
**B 를 넘기면 그 441칸이 곧바로 시험대에 오른다:**
```
격리가 살아 있으면 → `건너뜀` 이 오른다
격리가 안 걸리면   → `건너뜀 0` 인 채로 **재사용이 일어난다**
```
**둘이 다른 결과를 낸다. 그래서 `건너뜀` 의 값이 그때 처음 의미를 갖는다.**

### ✅ **A 는 내가 독립으로 볼 수 있다**
`rid` 값은 내 시리얼의 ACK 레코드에 그대로 찍힌다. **socket 의 요약 없이도 감김을 직접 관측한다** —
`rid_width.py` 의 자릿수 분포와 범위가 그것을 낸다. **두 계층이 같은 사건을 따로 본다.**
🔴 **B 는 내 자료로 못 본다** — 재사용 여부는 커서 상태이지 프레임에 안 찍힌다. **socket 값이 유일한 근거다.**

---

## 14. 🔴 **문턱 A 통과 — `A[1]` 이 돈다. 내 시리얼로 직접 관측했다** (10:33:56)

### 원문 (감김 지점)
```
7446  10:33:55  A,**999**,B1,4,3F
7454  10:33:55  " S,921,608,000,1118,P1,0F A,996,B3,4,32 A,997,B2,4,32 A,998,A5,4,39 A,**999**,B1,4,3F"
7463  10:33:56  A,**0**,A2,4,36      ← 🔴 **999 다음이 1000 이 아니라 0 이다**
7464  10:33:56  A,1,A3,4,36
```
```
전체 로그 4자리 rid(`A,[0-9]{4},`) : **0건**
```
> ### ✅ **`rid` 가 `[0,999]` 로 감긴다. 옛 코드였다면 1000(4자리)이 됐을 자리다.**
> ### **문턱 A 통과 = "공간이 실제로 1000 으로 잘린다"가 증명됐다.**

🔑 **socket 의 요약 없이 내 계층에서 독립으로 관측했다.** `rid` 값은 ACK 레코드에 그대로 찍히므로
**두 계측기가 같은 사건을 따로 본다.**
✅ **이건 절대 판정이라 대조군이 없어진 것과 무관하다** — 사전 등록 §4' 에서 미리 그렇게 정해 뒀다.

### ⚠ 그러나 **문턱 B 는 아직이다. 섞지 마라**
```
✅ A(1000 감김)  = **통과 · 관측됨**       → "공간이 잘린다"
⏳ B(1324 재사용) = **아직 미도달**         → "격리·건너뜀 로직이 밟힌다"
```
**B 이전의 `격리/건너뜀/강제 = 0` 은 전부 "표본 없음"이다.** A 를 통과했다고 B 가 따라오지 않는다.
🔴 **B 는 내 자료로 못 본다** — 재사용은 커서 상태이지 프레임에 안 찍힌다. **socket 의 `발행`·`다음 x/1000` 이 유일한 근거다.**

### 📌 이 시점 실측 (참고 · 판정선 미달이므로 비율은 인용하지 않는다)
```
10:33:5x 기준 rid 범위 324~999 → 감김 → 0~ · 중복 0 · 누락 0 (감김 전 구간)
```
