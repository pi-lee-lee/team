# ❌ 폐기 — 선이 언제 빠졌는지 모른다. 이 창 전체가 오염 후보다 (사용자 2026-08-20 13:42)

---

# ❌ 관측에서 뺀다 — 창 AG 는 **배선 정착 구간**이었다 (사용자 지시 2026-08-20 13:38)

```
사유 : 사용자의 실물 TX 선 재배선 **직후 구간**이다.
       사용자가 화면에서 정상 동작을 관측한 **13:38:17** 을 새 t0 로 받았다
🔑 §"자리이동 후 5분은 관측 제외" 와 같은 형태 —
   **개입 직후 구간은 고장처럼 보이는 것이 가장 많고 그 대부분이 셋업이다**
```
> ### 🔴 **관측에서 빼는 것과 기록에서 빼는 것은 다르다.**
> ### **아래 세션 표(#2~#6)는 지우지 않는다.** 그 구간에 무슨 일이 있었는지는 남아야 한다.
> **다만 판정의 분모로 쓰지 마라.**

**후속** : 창 **AH** (t0 **13:38:17**) — `monitor/PREREG-2026-08-20-winAH.md`
**t0 기준선 동결** : `monitor/winAH-t0-freeze.txt`

---

# 사전 등록 · 창 AG — **배선 수리 뒤. 계측기는 서버 로그 하나뿐이다**

> ### 🔴 **장애 분석 중단 지시 중이다.** 이 창은 **자료 축적과 값 보고**만 한다.
> ### 기전 추론·후보 좁히기·귀속을 하지 않는다. **판정문은 짧게, 보고는 물을 때만.**

**발행 2026-08-20 13:34** (루트 지시 · 재개 후) — 창은 **이미 열려 있다**(t0 가 과거다).

## 0. 🔴 이 창의 경계는 **굽기가 아니라 배선이다**
```
앞 창들 : 경계 = 굽기(펌웨어가 바뀐다)
🔴 창 AG : 경계 = **사용자의 아두이노 TX 선 재배선.** 펌웨어는 **안 바뀌었다**
→ 그래서 표지(등록 바이트·모듈 수·대상 IP)는 **바뀌면 안 된다.** 바뀌면 뭔가 더 들어간 것이다
```

### t0 와 그 앞의 개입 — **셋 다 창 밖이다**
```
13:26:41  탭 개방 **DTR 리셋** — monitor(나)가 만들었다 · REQ-0286
13:2x     🔴 **사용자의 TX 선 재배선** — **시각 미상.** 우리 자료에 없다
13:28:05  탭 반납(점유 0)
✅ t0 = **13:28:26**(세션#2 접속. 서버 로그에 값으로 있다) → 위 셋이 자동으로 창 밖
```
⚠ **재배선 시각을 모른다는 것을 지운 채로 쓰지 마라.** t0 앞 어딘가이고, 그 이상은 말할 수 없다.

## 1. 겨눈 구성 — **값으로 박는다** (원장 §7.221 처방 ①)
```
장치 : 칩 = 10:42:37 굽기 판(`d41ad13` 계열) · **굽기 없음 — 이 창에서 안 바뀐다**
       모듈 **4** (A1 B1 LD L2) · 등록 **54B** (= 10 + 11n, n=4) · 대상 **192.168.0.29:8888**
       (13:26:43 시리얼 부팅 로그에서 직접 확인 — `monitor/serial-diag-1330.log`)
서버 : build `a8e04c5` · pid **72151** · cwd `/Users/idong-u/parking-bin` · LISTEN 8888/9990/8911
```
> **창을 닫을 때 첫 일은 표지 측정이 아니라 "이 구성이 아직 그대로인가" 대조다.** (§7.221 처방 ②)

## 2. 🔴 계측기 — **서버 요약 하나뿐이다. 시리얼은 없다**
```
✅ `/tmp/srv_prod.out` — 60초 요약 + 사건 줄. **스스로 쌓는다. 폴링하지 않는다**
🔴 시리얼 탭 **안 연다** — 개방 = DTR 리셋 = **이 창의 연속성을 내 손으로 끊는 것**이다
```
### ⚠ 이 계측기가 **못 보는 것** — 미리 적는다
```
❌ CWJAP 거동 · IP 취득 과정 · UART 손상 · `busy p` · 사다리 단계 · 재부팅의 기전
→ 이 축들은 **이 창에 없다.** 없는 것을 `0` 으로 적지 마라 — **"미계측"이다**
✅ 덮는 것 : 세션 수·지속·종료 사유 · 프레임 · 링크없음 · 재부팅감지 · 오프라인 · 등록
```

## 3. 재는 것 — **"유지되는가" 하나**
🔴 **단위는 "한 세션의 지속"이 아니라 *세션 표* 다** — 아래 §5 대로 이미 갈아탔다.

### 판정선 — **옛 분포를 같이 적는다** (그래야 "넘었다"가 뜻을 갖는다)
```
세션#1(오늘)      **11분 16초** → 🔴 이 선은 아무것도 안 가른다. 넘어도 "그 범위 안"이다
창 S              22분에서 무너짐
옛 위치           45분 · 50분 이력
창 AB·AC·AD       **86분 포함 재부팅 0회**  ← 🔑 "달라졌다"를 말하려면 여기까지 와야 한다
→ 판정선 : **60분**(등급) · **86분**(옛 분포를 벗어남). 그 미만은 전부 **"흔적"**
```

## 4. 🔴 창을 끊는 것 — 미리 선언한다. **회귀로 읽지 않는다**
```
socket 의 v2 API 배포 — 배포 직전 한 줄이 오기로 돼 있다
→ 받으면 규약대로 **관측을 멈추고**, 배포 완료 뒤 **새 t0** 로 다시 연다
🔴 그 절단을 **배선 회귀로 읽지 마라**
```

## 5. 🔴🔴 t0+5분 실측 — **연결이 유지되지 않았다** (13:33 시점 · 값만)
```
13:28:25  세션#2 → 13:31:26 종료 **errno=54(ECONNRESET)**       지속 03:00 · 프레임 144
13:31:28  세션#3 → 13:31:59 종료 **keepalive 시간초과(errno=60)** 지속 00:30 · 프레임 9
13:32:14  세션#4 → 13:32:40 종료 **keepalive 시간초과(errno=60)** 지속 00:26 · 프레임 6
13:32:45  세션#5 → 13:33:06 종료 **같은 device_id(P1) 재접속으로 대체** 지속 00:21 · 프레임 17
13:33:06  세션#6 접속 — 관측 시점 현재
```
```
누계(13:33:03 요약) : 세션 **5회** · 프레임 721 · 재부팅감지 **5**(새연결 5 / uptime추론 0)
                      오프라인 **7회** · 등록 완료 **5**(전부 `미해독 1`) · 버린줄 0 · 재전송 0
```
> ### **"붙었다"는 참이고 "유지된다"는 아직 아니다. 지속이 3:00 → 0:30 → 0:26 → 0:21 로 짧아졌다.**
⚠ **귀속하지 않는다**(분석 중단). ⚠ **`같은 device_id 재접속으로 대체` 는 정상 운전에서도 나온다** —
단독으로 결함의 근거로 쓰지 마라(CLAUDE.md 실측).
⚠ **`미결속모듈 0/20`** 은 5회 등록 × 4모듈이 **누적된 값**이다. 계기가 아니라 계수다 — 20모듈이 아니다.

## 6. 정지 조건
```
· socket 배포 통보 수신 → 즉시 정지, 새 t0
· 실물 개입 통보(굽기·전원·배선) 수신 → 즉시 정지, 새 t0
· 세션 지속이 **86분**을 넘김 → 그때 비로소 "옛 분포를 벗어났다"를 쓸 수 있다
```
