# 창 AP t0 동결 — 2026-08-20 17:45:07 (장치 재등록)
# 서버 교체 17:45:05 pid 85518 build=9a18c55 · 로그 >> 유지
# ✅ 기준선 = 0 (새 프로세스). 창 값 = 최신 '⏱ 소크' 줄 그대로. 옛 동결과 빼지 마라
#
# 🔴 내 예측이 틀렸다 — socket 이 값으로 정정했다
#   내 예측 : DR 선언 제거 → 명령가능 2→1 → 명령결과·G 축 분모 변경
#   실측    : **명령가능 2 그대로**(LD·L2). DR 은 처음부터 결속된 적이 없다
#   → 그 축들은 앞 창과 **합쳐도 된다**
#   🔑 기전 : 나는 "선언 수"의 변화를 "결속 수"의 변화로 읽었다. 둘은 다른 수다
#      바뀐 것은 buildLot() 의 **모듈 선언 5 → 4** 하나뿐. 자리 수 6 은 그대로
