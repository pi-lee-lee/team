# 창 AT t0 동결 — 2026-08-20 21:07:28 (socket 배포 = 마지막 개입)
# 서버 build 994010e pid 48093 · 새 프로세스 → ✅ 기준선 0 · 최신 요약 그대로 읽는다
# 장치 : 새 망 상수 판(20:44:46 굽기) · 모듈 4(A1 B1 LD L2) · 등록 54B · D,*,7,4,6D
# 망   : SK_WiFiGIGA50DC_2.4G · 서버 192.168.35.21:8888 · 장치 192.168.35.79 (새 자리·새 AP)
#
# 🔴 결합 시간 옛 분포 : ~20분 / 6분17초 / 16분12초
#    → **20분 미만의 부재는 그 범위 안이다. "안 붙는다"로 쓰지 마라**
# 🔴 새 계수 ` · 점유변화 N` — 이름으로 찾는다(위치 아님). LED 토글의 계수다
# ✅ 무자극 기준선(21:00:58~21:01:12 · occ 15/15 = 0) 은 **앞 판에서 떴다.**
#    지금부터는 **자극 구간**이다 — 무자극 분모와 섞지 마라
# 미측정 : ramLow · smiss · ssovf (시리얼 전용 · 탭 안 연다) → "0" 아님
# 🔴 occ 는 "점유"가 아니라 **모듈 비트열**이다 (루트가 Modules.h:294-296 으로 확정)
#   n=4 : A1 0x8 · B1 0x4 · **LD 0x2(에코)** · L2 0x1(에코)
#   → 점유를 셀 때 **센서 마스크 0xC** 만 쓴다. 0x3 은 액추에이터 에코다
#   ⚠ occ != 0 을 점유로 읽으면 **LED 켜진 것을 차로 읽는다**
#   판별 규칙 : kind 첫 글자 I*=센서(점유) · O*=액추에이터(에코)
# 🔴 자리는 **A1 하나**다. B1 은 자리가 아니라 그 자리에 붙은 모듈이다
#   → `= 자리 B1 점유` 는 원리적으로 안 나온다. 내 앞 보고 ③은 **없는 결함이었다**
#   ✅ B1 이 읽히는가 = **닫혔다** (비트2 가 선 프레임이 있다)
