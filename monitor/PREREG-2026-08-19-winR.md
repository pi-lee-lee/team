# 사전 등록 · 창 R — **링크가 얼마나 오래 유지되는가. 그것 하나다**

**발행 12:41** — 탭은 `12:41:0x` 에 열렸고 **판정 자료를 보기 전에 적는다.**

## 0. 🔴 이 창의 전제 — **앞 구간이 왜 못 쓰였나**
```
12:14 ~ 12:40  사용자가 **시리얼을 여러 번 여닫았다**
               Uno 는 시리얼 개방 = **DTR = 리셋**이고, 스케치가 부팅하며 ESP 에 `AT+RST` 를 쏜다
🔴 그 구간의 재접속·CWJAP 실패 상당수가 **그것이 만든 것**이다 → 자료로 안 쓴다
✅ **12:40 부터 사용자가 안 건드린다** — **이 창이 처음 얻는 조건이다**
```
🔑 **우리 탭도 같은 성질이지만 한 번 열고 유지하므로 리셋이 `t0` 하나뿐이다.**
**그 차이가 이 창을 쓸 수 있게 만든다.** (사용자 원문: *"내가 자꾸 시리얼을 끊었다 붙였다 해서 그게 더 문제였다"*)

## 1. 창 정의
```
t0     **탭 개방** (= DTR 리셋 **1회**. 그 뒤로는 아무도 안 건드린다)
목적   🔴 **링크가 얼마나 오래 유지되는가 — 그것 하나다**
길이   🔴 **최소 60분.** 옛 위치 **45분** · 창 Q 앞 **50분** 뒤 무너진 이력이 둘 다 있다
       → **30분으로는 "고쳐졌다"를 말할 수 없다**
주입   ❌ **안 한다.** 부하 축이 아니라 **결합 축**이다
로그   monitor/serial-winR.log · 감시기 120초 · .team/STOP-R
```

## 2. 개시 직전 대조군 (루트가 얼린 값 · **개입이 섞여 있다**)
```
12:40:15  세션 13회(현재 #13 연결중 **1분 28초**) · 프레임 759 · 오프라인 23회 · 링크없음 22:12
분당 S프레임 : 12:30→44 · 12:32→38 · 12:34→44 · 12:36→43 · 12:38→34
```
⚠ **최근 10분이 분당 34~44 로 정상 대역이지만 "회복했다"로 못 읽는다** — 그 사이 사용자 개입이 섞여 있다.

## 3. 볼 것
```
🔴 결합유지  — 개입 전 **8~27초(하한)**. **분 단위로 늘면 그것이 답이다**
   ⚠ 단별로 가른다 — 2단 이상은 우리 `CWQAP` 이 잘라 낸 값일 수 있다(창 Q §2.1)
   ✅ 패턴은 `결합유지 [^·]*` 로 잡는다 — **원문이 `8s` 이지 `8초` 가 아니다**(두 번 밟았다)
esprst · CWJAP FAIL/무응답 · CIFSR→0.0.0.0 · 세션 지속 · 오프라인 횟수
🔑 okto · skip · T2 — 사용자 캡처에서 **okto 4/100슬롯 · skip 12** 가 나왔다.
   `bigfill=1` 인데도 `SEND OK` 가 늦는다 → **64B 기전이 아닌 지연**이다. **별도로 센다**
새 계수 셋(이름충돌·센서갈림·비자리예약) — 없으면 **"표지없음"** 으로 찍힌다(0 아님)
```

## 4. 🔴 판정 규율 — **결과 보기 전에 정한다**
```
🔴 창 Q·개입 구간과 **크기 비교 금지** — 조건이 다르다. **"이만큼이다"로만 적는다**
🔴 **60분을 못 채우면 "흔적"** 이라 적고 판정하지 않는다
🔴 **셋째 갈래를 계속 열어 둔다** — *한동안 멀쩡하다 무너지는* 것이 **이 고장의 알려진 모양**이다
   (옛 위치 45분 · 창 Q 앞 50분) → **"60분 무사고"도 "고쳐졌다"가 아니라 "60분은 버텼다"이다**
⚠ `ssovf 0 · cksumng 0` 을 건강으로 읽지 않는다 — 주입이 없어 **그 경로가 안 밟힌다**
```

## 5. 곁들여 확정된 사실
```
✅ `A2 ↔ ESP RST` 선은 **물려 있다**(사용자 확인)
🔴 그러나 펌웨어가 `ESP_RST_WIRED = 0` 이라 **안 쓴다** → **사다리 4단은 여전히 미실행**
→ 이 창에서도 실질 조치는 (0·1)`CIFSR` · (2)`CWQAP` · (3+)`AT+RST` 셋뿐이다
⚠ 굽기는 이 창 뒤에 정한다
```

## 6. 개입 통보
```
🔴 탭 개방 = 내 DTR 리셋 **1회**. 루트·socket 에 알린다
🔴 그 뒤로는 **아무도 포트를 안 건드린다** — 그것이 이 창의 성립 조건이다
```

## 7. 실행 기록 (측정 후에만)
```
t0 : (개방 줄에서 확인) · 창 닫은 시각 : (미기입) · 최장 결합유지 : (미기입)
```


---

## 8. 🔴 `t0` 확정 — **두 번 실패한 뒤에 열렸다. 그 사실을 남긴다**

```
❌ 12:41:03  1차 시도 → **Errno 16 Resource busy** → 탭이 양보하고 즉시 종료. **캡처 0줄**
❌ 12:42:24  2차 시도 → 같은 오류. **캡처 0줄**
   점유자   : **VS Code `Code Helper (Plugin)` pid 79141** 이 **`/dev/tty.usbmodem21201`** 을 잡고 있었다
   🔴 내 확인이 `cu` 노드만 봐서 "점유 0"으로 보였다 — **`cu` 와 `tty` 를 둘 다 봐야 한다**
✅ 12:43:56  cu 0 · **tty 0** 둘 다 확인
✅ **12:44:00  t0 — 실제로 열렸다** (12:44:02 부팅 배너 확인 · 캡처 정상)
```
**사용자 선언 시각 `12:40:58` 은 `t0` 가 아니다** — 그때 사용자가 포트를 잡고 있었다.
**12:40:58~12:44:00 은 사용자가 시리얼을 잡고 있던 구간이고 자료가 아니다.**

### ⚠ 1차 실패가 **없는 고장을 만들 뻔했다**
로그가 헤더 3줄뿐이고 장치 출력이 **0** 이었다 — **"리셋 뒤 장치가 아무 말도 안 한다"로 읽힐 수 있었다**
(창 Q 는 리셋 3초 만에 배너가 떴으므로 더 그럴듯했다).
✅ **탭이 자기 stderr 에 실패 사유를 남겨서 갈렸다.**
> ### **계측기가 멈춘 것과 대상이 조용한 것 — 그 한 줄이 갈랐다.**

### 열기 직전 값 (루트 제공 · 이 창의 "전")
```
12:43:26  세션#14 **연결중 2분 37초** · 프레임 886
12:43:24  ←ARD S,196,608,000,328,P1,3B  ← **정상 송신 중이었다**
```
🔑 **내 탭 개방이 이 세션을 끊는다. 그 뒤 회복까지 걸리는 시간이 첫 값이다** (창 Q 는 **57초** 걸렸다).
