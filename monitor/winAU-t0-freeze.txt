# 창 AU t0 동결 — 2026-08-20 21:29:28 (socket 배포 · pid 25905 · build 426a8e4)
# ✅ 새 프로세스 → 기준선 0. 최신 '⏱ 소크' 줄 그대로 읽는다
# 장치 : 20:44:46 굽기 판 · 모듈 4(A1 B1 LD L2) · 등록 54B · 192.168.35.79 · 서버 192.168.35.21:8888
#
# 🔴 `점유변화` 뜻이 이 창부터 **모듈 단위**다(창 AT 는 자리 단위). **앞 창과 합치지 마라**
# 🔑 새 줄 : `= 자리 A1 · 모듈 B1 점유 → …`  ← 모듈 이름이 붙는다. 첫 줄 21:29:56 확인
# 🔑 마스크는 상수로 안 박는다 : D 줄 순서 i → 비트 (n-1-i) · kind 'I' 만 OR (지금 0xC)
#
# ✅ 닫힌 축 : 화면은 `occ` 를 안 받는다(봉투에 그 키가 없다 · socket wsjson.h 전수 0건)
#    → 4.25배 과대는 **로그의 occ 를 그렇게 읽을 때만** 생긴다. 재료 쪽은 닫혔다
#    ⚠ 소비 쪽(화면이 모듈 value 를 스스로 OR 하는가)은 **web 만 안다. 열려 있다**
#
# ═══ 🔴 사전 등록한 검정 — **결과 보기 전에 적는다** ═══
# 창 AT 에서 "앞 10분에 나쁜 것이 몰려 있었다"(세션#1 9분36초 · 최대공백 30.2초)
# socket 가설 : "그 앞 10분은 내 배포 직후 구간이다"
#   ✅ 재현되면(이 창도 앞 ~10분에 이탈·큰 공백 후 조용) → **배포 직후 구간** 쪽
#   ❌ 안 되면(처음부터 조용) → 창 AT 의 앞 10분은 **그 창 특유**의 것
#   ⚠ 표본 2다. 어느 쪽이든 **"확정"이 아니라 "한 번 더 같았다/달랐다"** 로만 적는다
#   ⚠ 그리고 앞 창은 배포 + 새 AP 이동 직후가 겹쳐 있었다. 이 창은 이동 축이 없다 —
#      **그것만으로도 조건이 다르다.** 완전한 반복이 아니다
# ✅ occ 축 **완전히 닫혔다** — 소비 쪽까지(web 소스 전수)
#   화면은 서버가 계산한 zone.occupied 만 쓴다 · modules[].value 집계 0곳 · 비트연산 0곳
#   → LD 가 켜져도 자리는 occupied 대로 그려진다. **점유로 승격되는 경로가 없다**
#   🔑 내 4.25배는 **참이고 범위가 좁다**: 로그 raw occ 를 != 0 으로 읽을 때만
