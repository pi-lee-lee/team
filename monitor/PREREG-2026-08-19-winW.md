# 사전 등록 · 창 W — **배포 뒤 기준선. 판정 축은 `busy p` 하나다**

> # 🔴 머리 배너
> ### **① "정상" = 끊김 0 이 아니라 **끊겨도 자력 회복**(사용자 확정 · 원장 §7.188)
> ### **② `hwrst = 0/0` 은 판정 자료가 아니다** — `ESP_RST_WIRED=0` 이라 **4단이 *없다***
> ### **③ 자력 복구의 천장은 3단 `AT+RST` 다.** `busy p` 가 0 이 아니면 **사람이 개입해야 한다**
> ### **④ 기전 B 는 옛·새 기판 양쪽에 났다 → 기판 축이 아닐 수 있다.** 이 창이 깨끗할수록 묻힌다

**발행 16:34** — 탭 `16:33:46` 개방 · **판정 자료를 보기 전에 적는다.**

## 0. 경계 — 🔴 **둘이 겹쳤다. 갈라 적는다**
```
16:19:06  arduino 굽기  `699147b`   (commitAck · 시뮬 지형 · OBV→OB · ESP_RST_WIRED=0)
16:31:52  옛 서버 끝    `7f17b99` pid 79990 · frames 11299 · sessions 64
16:31:59  🔴 새 서버    **`198e15b`** pid 13000 · cwd=/Users/idong-u/parking-bin
🔴 내 t0  **16:33:46** (탭 개방 = DTR 리셋 1회)
```
⚠ **굽기가 배포보다 12분 먼저다.** 그 사이(16:19~16:31)가 **창 V** 이고 프리즈해 뒀다.
✅ **겹침 걱정 하나는 뺄 수 있다** — `OBV`→`OB` 는 **옛 서버에서도 정상 동작**(socket 실기 확인).

## 1. ✅ **내 파서 표지 둘 — 값으로 통과했다** (16:33:46)
```
`WS접속 : 3(최대 4)`        ← 🔑 **`[옛이름]` 꼬리표가 사라졌다** = 새 이름을 실제로 잡았다
`이름충돌 0 · 센서갈림 0 · 비자리예약 0`  ← **"표지없음"이 아니라 숫자다** = 파서가 찾았다
```
> ### **못 찾았으면 `0` 이 아니라 `표지없음` 이 떴을 것이다. 그 구분을 만들어 둔 것이 여기서 값을 했다.**
🔑 `mod_order_changed` 때 침묵이 `0` 으로 보여 아무도 못 본 자리를 막으려고 넣은 장치다.

⚠ **`센서갈림 0`(전 3)은 시뮬 지형 정렬이 들어갔다는 *정합성 확인* 으로만 읽는다.**
**표지로 안 쓴다** — 내 감시기가 16:10:53 에 **굽기 없이 `3→0` 거짓 표지**를 낸 적이 있다.
**표지는 `143B` 와 `D,E1,OB` 다**(창 V 에서 확인 완료).

## 2. socket 이 준 값 (사전 등록에 옮긴다)
```
자리 **7개** : A1 A2 A3 A4 A5 E1 X1     ← 🔴 **10 → 7**
지형 판 1 = "조립 표에서 지형 구성"
rid 커서 **이어받음 3908**(전선값 908) · 예약 4164   ← **영속이 살아 있다. "임의 지점"이 아니다**
장치 재등록 16:32:02 : n=12 · drain=7 · 명령가능 2개 — **`OB` 로도 정상**
```

## 3. 🔴 판정 축 — **`busy p` 하나**
```
`busy p` **0**  → 파서 살아 있음 → 3단이 먹는다 → 자력 회복 = **새 기준으로 정상**
`busy p` **>0** → 파서 먹통 → 3단이 안 먹는다 → 🔴 **천장. 4·5단이 없으므로 사람이 개입해야 한다**
```
**이 창에서 `busy p` 가 0 이 아니면, 그것이 "지금 하드웨어로는 못 고친다"의 실측이 된다.**

## 4. ⚠ 비교 금지
```
🔴 **자리 기준 비율을 창 너머로 비교하지 마라** — **분모가 10에서 7로 바뀌었다**
🔴 창 Q~V 와 크기 비교하지 않는다 — 서버·펌웨어가 둘 다 다르다. **"이만큼이다"로만**
⚠ `B*` 예약은 이제 `not_reservable` 거절 → `비자리예약` 이 오른다(지금 0).
   **그 `0` 은 "막혔다"가 아니라 "아무도 안 눌렀다"** 다(socket 이 준 사유)
⚠ **60분 미만은 "흔적"** · **판정 쓰기 전 배경 집계 상태부터 확인**한다
```

## 5. 실행 기록 (측정 후에만)
```
창 닫은 시각 : (미기입) · busy p : (미기입) · 최장 결합유지 : (미기입) · 배경집계 : (미확인)
```
