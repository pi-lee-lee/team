# 새 기준선 첫 집계 — 2026-08-16 16:59:54 ~ 18:33:14

> **원본 (LEDGER 1.5 — 출력은 스스로 출처를 밝힌다)**
> 서버: `/Users/idong-u/parking-logs/parking-server.log`
> 시리얼: `/Users/idong-u/learn/monitor/serial-newbase.log` (tap pid 79289, 16:59:54~ 생존)
> 산출물: `monitor/out-newbase.md` · `monitor/out-newbase.json` · `monitor/out-newbase-link.txt` · `monitor/out-newbase-power.txt`
> 작성 `monitor-engineer` · REQ-0112 처리 중

## 0. ⚠ 루트의 "관측 창 A" 와 이 문서의 창은 **다른 구간**이다

18:33 루트 지시로 **창 A = 17:53:07 ~ ~21:53 (4h)** 이 확정됐다. 이 문서가 다루는
`newbase_all = 16:59:54~` 은 **A 보다 53분 넓고, 그 53분 안에 서버 인스턴스 2개와 교체 2회가 들어 있다.**

| 이름 | 구간 | 서버 인스턴스 | 쓰임 |
|---|---|---|---|
| `newbase_all` (이 문서) | 16:59:54 ~ | I1·I2·I3 **셋** | 재개 직후 기술 통계 · λ 실측 |
| **`A` (루트 판정 대상)** | **17:53:07 ~ ~21:53** | **I3 하나** | **21:53 판정** |
| `srv_I3` (이 문서 2절) | 17:54:37 ~ | I3 | A 와 사실상 같음(90초 유예만 잘라냄) |

🔴 **A 판정에 이 문서의 `newbase_all` 수치를 쓰지 마라.** 창이 다르고 서버 빌드가 섞여 있다.
A 는 `monitor/frozen-A/` 의 동결 파서(`parserA-2026-08-16T1838`)로만 재고,
그 명세는 `monitor/frozen-A/MANIFEST.md` 에 있다.

## 🔴 이 문서로 하면 안 되는 것 — 먼저 읽어라

**표본 1.56시간이다. 기술 통계일 뿐 비교 판정이 아니다.**

**옛 13.92h·3.98h 구간과 같은 표에 넣지 마라.** CLAUDE.md "숫자 둘을 비교하기 전에"의
3번(누가 만들었는가)이 어긋난다 — 이 창은 **넷이 동시에 다르다**:

| 변수 | 옛 판정창 | 이 창 |
|---|---|---|
| 펌웨어 | EspLink(nextgen) | `조별과제샘플/client.ino` 계열 (REQ-0109) |
| 실물 구성 | 이전 배선·전원 | **사용자가 15:26~17:0x 에 만졌다 — 내용 미상** |
| 서버 빌드 | 단일 | **창 안에서 2회 교체**(아래) |
| 표본 | 3.98h / 13.92h | **1.56h** |

→ 크기 비교를 하지 마라. 이 창은 **자기 자신이 새 기준선**이다.

## 1. 창이 균질하지 않다 — 서버 인스턴스가 셋이다

서버 로그가 계약 v0.1 로 **스스로 경계를 선언**한다(`=== INSTANCE`). 창 안에 3개 인스턴스:

| 인스턴스 | pid | 구간 | logfmt | 종료 사유 |
|---|---|---|---|---|
| I1 | 72134 | 16:55:58 ~ 17:06:42 | (v0.1) | signal |
| I2 | 92344 | 17:06:49 ~ 17:52:50 | 2 | normal |
| I3 | 75781 | 17:53:07 ~ 생존 | 3 | — |

**서버 부재 공백 2회**: 17:06:42→17:06:49 (7초) · 17:52:50→17:53:07 (17초).

LEDGER 3.5 — 서버가 없으면 장치는 **0.90초 주기로 백오프 없이** 재시도한다. 그래서 교체
직후의 접속은 "끊겨서 다시 붙은 것"이 아니라 "계속 두드리던 것이 받아들여진 것"이다.
**제외했고, 제외한 것을 여기 적는다**(LEDGER 3.5 단서):

```
08-16 17:06:50  uptime  412 → 장치 생존 (I2 기동 직후)
08-16 17:53:08  uptime 3180 → 장치 생존 (I3 기동 직후)
```

⚠ 이 제외 창(각 90초) 안의 **진짜 끊김도 같이 지워진다.** 제외 없이 세면 재연결 6건이 된다.

## 2. 기술 통계 (인스턴스별)

| 창 | 길이 | 프레임 | 분당 | 드롭 | 세션종료 | errno54 |
|---|---|---|---|---|---|---|
| newbase_all | 1.55h | 4,912 | 52.84 | 0 | 7 | 1 |
| srv_I1 | 0.11h | 351 | 51.62 | 0 | 2 | 1 |
| srv_I2 | 0.74h | 2,350 | 52.79 | 0 | 2 | 0 |
| srv_I3 | 0.64h | 2,049 | 53.57 | 0 | 1 | 0 |

## 3. `0` 을 셋으로 갈라 적는다 (LEDGER 1.1)

| 지표 | 값 | 어느 `0` 인가 |
|---|---|---|
| 드롭 전체 | 0 | **건강** — 프레임 4,912건이 실제로 흘렀고 그중 버려진 것이 없다 |
| 바이트손상(real_S) | 0 | **건강** — 같은 근거 |
| 서버 회수(유휴 마감·keepalive) | 0 | **건강** — 서버가 끊은 것이 아니다(끊은 주체 판별에 쓰임) |
| 자발 MCU 재부팅 | 0 | **건강이되 판별력 없음** — 옛 창에서도 0 이었다. 이 지표로는 아무것도 못 가른다 |
| ~~하행 `→ARD`~~ | ~~0 = 미실행~~ | **뒤집혔다 → 아래 5절.** 이 창에서 실제로 돌았다 |

## 4. 🔑 1급 지표 — 재부팅 없는 링크 끊김

```
ACCEPT 5건 · 서버 기동 직후 90초 제외 2건
  재부팅(uptime 작음)        1건   ← 아래 4.1
  🔑 재부팅 아님(uptime 큼)  4건
  판별불가                   0건
같은 연결 복구 2건 · 재연결 복구 5건
끊은 주체: 서버 유휴회수 0 · keepalive 0 · TCP 리셋(errno54) 1 · 무프레임 판정 7
→ 4건 / 1.3h = 3.02건/h
```

| ACCEPT | uptime | 직전 상황 |
|---|---|---|
| 17:05:25 | 327 | 세션#3 **상대가 닫음**(지속 5:18) |
| 17:21:36 | 1295 | 세션#1 **같은 device_id(P1) 재접속으로 대체**(지속 14:46) |
| 17:41:42 | 2496 | 세션#2 **상대가 닫음**(지속 20:04) |
| 18:24:58 | 5083 | 무프레임 판정 후 재접속 |

**끊은 쪽은 서버가 아니다** — 유휴회수 0, keepalive 0. 장치가 스스로 닫았거나 TCP 가 끊겼다.
LEDGER 3.6·3.7 의 `send_fail_x3` → 자해 경로와 양상이 일치하지만, **이 창만으로 단정하지 않는다.**

### 4.1 재부팅 1건은 **우리가 만든 것**이다 — 자발 아님

`17:00:06 +ARD 세션#3 device=P1` = tap 개방 DTR 리셋(**t0 = 16:59:54**)의 +12초.
LEDGER 3.3 의 "부팅 → 서버 등록 ≈ 12초"와 정확히 일치한다.
`arduino/INTERVENTIONS.md:248` 에 선언돼 있다.
→ **이 창의 자발 재부팅은 0 이다.** 선언이 없었으면 집계기가 "자발"로 분류했을 것이다(LEDGER 4.4).

직전 `17:00:04 -ARD 세션#2 종료(errno=54)` 도 같은 리셋의 결과다. **errno54 1건은 우리 몫**이다.

## 5. 하행이 실제로 돌았다 — 옛 "0 = 미실행"이 뒤집혔다

`→ARD` **7건 · `←ARD A,` 응답 7건 (7/7, 6건 같은 초 · 1건 +1초)**

```
17:23:08 R,1,A1 → A,1,A1,0,30      17:23:41 C,2,A1 → A,2,A1,0,33
17:23:49 R,3,B2 → A,3,B2,0,32      17:23:52 R,4,A4 → A,4,A4,0,30
17:24:00 C,5,B2 → A,5,B2,0,34      17:53:08 C,1,A4 → A,1,A4,0,35
17:41:42 R,6,A4 → A,6,A4,**2**,30
```

- **도달률 7/7.** 하행 0 은 더 이상 "미실행"이 아니다(REQ-0115 로 socket 이 실제로 내려보냄).
- ⚠ **1건만 상태 필드가 `2`**(나머지 6건은 `0`). 그 1건의 시각 `17:41:42` 는
  **세션#3 접속과 같은 초**다(17:41:41 종료 → 17:41:42 접속 → 같은 초에 하행).
- 🔴 **표본 1건이다. 기전을 붙이지 않는다**(LEDGER — 표본 1건에 기전 금지).
  `2` 의 의미는 프로토콜 소유자(socket-engineer)의 것이고 내가 단정하지 않는다.
  다음 창에서 재접속 순간의 하행이 또 비-0 을 받는지만 센다.

## 6. λ 를 실측했다 — 다음 A/B 에 필요한 길이

`monitor/power_plan.py` 에 실측 λ=3.02/h 를 넣었다(출력 `monitor/out-newbase-power.txt`).

| λA=3.02/h | 2h | 4h | 6h | **8h** | 12h |
|---|---|---|---|---|---|
| 3배 개선 | 0.25 | 0.57 | 0.75 | **0.88** | 0.97 |
| 5배 개선 | 0.41 | 0.78 | 0.92 | 0.97 | 1.00 |

→ **3배 개선을 주장하려면 양팔 각 8시간.** 지금 A 팔은 1.56h 다.
⚠ λ 자체가 4건에서 나온 값이라 오차가 크다. 눈금이지 확정 계획이 아니다.

## 7. 이 창의 성격 — REQ-0116 "수정 전" 팔이다

REQ-0116(`busy` 를 실패로 세지 않는다 · 더미 주입 중단)은 **구현만 되고 플래싱은 금지**됐다.
즉 **지금 칩 위에는 자해 경로가 그대로 있다.** 이 창은 그 상태의 관측이다.

→ 굽고 나서 재는 것이 B 팔이 된다. **그때 A 팔로 쓸 수 있는 유일한 자료가 이 창이다.**
   그러므로 이 창을 더 길게(최소 8h) 이어 두는 것이 다음 판정의 전제다.

## 8. 계측기 교정 — `tick.py` 가 거짓 경고를 내고 있다

18:31 실행 결과, `tick.py` 는 아직 **옛 상수**를 가리킨다:

- `SERIAL = monitor/serial-esplink.log` → 이미 unlink 된 옛 경로. 그래서
  `🔴 시리얼 경로 소실` 을 찍는데 **지금은 사실이 아니다**(`serial-newbase.log` 는 살아서 자란다).
- `LOG = /tmp/parking-soak.log` → 옛 서버 로그. 그래서 `🔴 로그 마지막 줄이 8180초 전`,
  `🔴🔴 마지막 S 프레임이 8240초 전` 을 찍는데, **새 서버 로그는 1초 전까지 살아 있다.**

LEDGER 1.9 — 늘 뜨는 거짓 경고는 곧 무시당하고, 그러면 진짜가 왔을 때 아무도 안 본다.
**아직 고치지 않았다**(옛 판정창 재현이 `/tmp/parking-soak.log` 에 걸려 있어 교정 6/6 을
깨지 않는 방식으로 분리해야 한다). 다음 작업으로 남긴다 — 그때까지 `tick.py` 의 시리얼·신선도
경고는 **읽지 마라.** 새 창의 숫자는 이 문서와 `monitor/out-newbase*` 가 정본이다.
