> # 🔴 **관측 자료의 시작 = 2026-08-19 10:20** (사용자 지시 · REQ-0256)
> **그 이전 창(창 G ~ 창 O 및 08-16·08-17·08-18 전부)은 *자격이 낮춰졌다*.**
> ```
> ✅ 남는다    : 소스 판독 · 자기 프로세스 안에서 닫히는 실측 · 도구 교정 · 계측기의 성질
> ❌ 자격 상실 : **관측 비율**과 **그 위에 세운 인과 귀속**
> ```
> **왜**: 사용자가 자리이동 후 환경 셋팅을 했고, 그 구간의 절단·자료를 관측에서 뺀다고 확정했다.
> 🔴 **지우지 않는다. 값은 남기고 자격만 낮춘다** — 왜 갈렸는지가 다음 사람의 근거다.
> ⚠ **10:20 이전 자료를 인용해 새 판정을 세우지 않는다.** 소급 계산으로 그 창들의 빈칸을 채우는 것도
> 마찬가지다 — **관측 자료가 아닌 것에서 새 관측 값을 만드는 것**이고 **채워도 인용할 데가 없다.**
> ✅ **관측 자료의 첫 창은 `창 P`** (`monitor/JUDGMENT-2026-08-19-winP.md`).
>
> ### ⚠ 그리고 **자리이동에는 환경 안정 5분이 붙는다** (사용자 확정 · 다음 이동부터)
> ```
> 사용자가 "자리이동" 명시 → ⏳ 5분 안정(❌ 관측 제외) → ✅ 여기부터 관측 자료
> 기산점 : 사용자가 **이동을 명시한 시각** · 🔴 5분은 **하한이지 상한이 아니다**
> 표기   : 사전 등록에 `이동 명시 <시각> · 안정 종료 <시각> · t0 <시각>` 을 **값으로**
> ```

---

# 분모 정의 — **창 G / 창 H 를 같은 규칙으로 세기 위한 확정본** (2026-08-18 · monitor)

🔴 **판정 분모는 이 문서의 정의 하나만 쓴다.** 루트 확인 요청 · arduino 합의
(*"판정 분모는 monitor 가 창 H 에서 창 G 와 같은 규칙으로 다시 낸 값 하나만 쓴다"*).

## 1. 🔴 세 배 차이가 났던 이유 — **둘 다 내 숫자이고 둘 다 맞다. 세는 것이 다르다**
```
[SLOT] tx>=48B   75건   ← **줄** 을 센다. T2 대기 중 `r=0` 슬롯도 같은 tx= 를 계속 찍는다
CIPSEND >=48B    19건   ← **실제 나간 전송 시도** 를 센다
```
> ### **약 4배 차이이고, 고장이 심할수록 격차가 커진다**(건너뛴 슬롯이 늘기 때문).
> ### 🔴 **고장이 분모를 키우는 방향의 편향이라 특히 위험하다.**

## 2. ✅ 확정 — **분모는 `CIPSEND 거래` 다**
```
분모 1건 = 로그에 `[AT] "AT+CIPSEND=<n>"` 이 한 번 찍힌 것
크기     = 그 줄의 <n>  (CIPSEND 가 선언한 바이트 수. `[SLOT] tx=` 가 아니다)
```
⚠ **`[SLOT] tx=` 는 분모로 쓰지 않는다.** 참고 지표로만 낸다.
⚠ **`<n>` 과 `[SLOT] tx=` 는 보통 1 차이다**(`tx=37` ↔ `CIPSEND=38`). **대역 경계에서 어긋난다** —
**대역은 항상 `CIPSEND` 의 `<n>` 으로 자른다.**

## 3. ✅ 크기 대역 — **문턱은 64B 다. `48B` 는 쓰지 않는다**
```
<48   ·   48~63   ·   >=64        ← 세 대역으로 낸다
```
🔴 **`>=48B` 를 판정 기준으로 쓰지 마라.** 그것은 **표본이 모였나**를 보는 추출 기준이었고,
**기전 경계는 `>=64`** 다(`_SS_MAX_RX_BUFF`, arduino). 창 G 실측:
```
<48  748거래 → T2 1 (0.1%)   ·   48~63  43거래 → T2 0 (0.0%)   ·   >=64  34거래 → T2 9 (26.5%)
```

## 4. ✅ 결과 지표 — **`T2` 다. `SEND OK 유무` 가 아니다**
```
성공/실패 = 그 거래 뒤 12초 안에 `[TX-WAIT] ★★ T2 초과` 가 났는가
```
🔴 **`SEND OK` 유무로 세면 틀린다** — 뒤집힌 거래에서는 `SEND OK` 가 `AT+CIPSEND` 줄보다
**먼저** 찍혀 "그 줄 이후" 검사에 안 잡힌다(실제로 `48~63` 이 32.6% 로 잘못 나왔다).
⚠ **관측 대상이 순서를 어기는 것이 우리가 쫓는 현상이므로, 순서를 전제한 파서는 반드시 틀린다.**

## 5. ✅ 구간 경계
```
창 t0        탭 개방 DTR 리셋 시각 (로그 첫 줄) — **서버 t0 와 다른 시계다**
주입 구간    socket 이 통보한 시작·종료 시각 (초 단위) 으로 자른다
자극 전/후   주입 구간 밖. **자극 부재 구간과 주입 구간을 같은 분모로 묶지 않는다**
```
⚠ **판정은 주입 구간에서만 한다.** 자극 없는 구간의 `T2 0` 은 **자극 부재**이지 건강이 아니다.

## 6. 창 G 확정값 (창 H 는 이 표와 같은 칸으로 낸다)
```
구간        CIPSEND거래   <48        48~63      >=64        T2
자극전       321         318         3          0          1
1차 주입      16           2         0         14          1
2차 주입      54          35         0         19          8
자극후       118         118         0          0          0
판별시험      81          41        40          0          0
```
🔑 **창 H 비교는 `>=64` 칸의 `거래수`와 `T2 비율`로 한다.** 창 G 의 `>=64` 는 **34거래 · T2 9 (26.5%)** 다.
⚠ **총 주입 건수는 비교하지 마라** — 감독기가 정지 뒤 라운드를 다시 열어
**장치가 나쁠수록 총 주입이 는다. 인과가 거꾸로 읽힌다**(socket).

---

# 7. 🔴 **hex 전환 이후 — `≥64B` 를 분모로만 쓰면 개선이 "판정 불가"로 보인다** (2026-08-18 추가)

**socket 통보**: hex 인코딩으로 `S` 프레임이 **60B 대 → 21B** 가 된다(arduino 실측).
→ **CIPSEND 배치가 통째로 작아져 `≥64B` 대역 거래 수가 줄고, 최악은 `0/0` 이다.**
⚠ **`0/0` 을 "T2 가 사라졌다"로 읽으면 안 된다** — `queue_full 0`·`미주입 0` 과 같은 형태(원장 1.1).

## 7.1 ✅ 그래서 지표를 **하나 추가한다** — `위험대역 진입률`
```
위험대역 진입률 = (CIPSEND >=64B 거래 수) ÷ (CIPSEND 전체 거래 수)
```
🔑 **이건 `T2` 표본이 0 이어도 항상 계산된다.** 그리고 **개선을 직접 잰다:**
```
창 G(창 전체)  34/823 = 4.1%   🔴 **주입 구간만: 1차 87.5% · 2차 35.2%**
                              **구간 이름 없는 비율은 인용 금지**(2026-08-18 정정)
창 H  1,025 / 1,737 = 59.0%
창 I  1,067 / 1,158 = **92.1%**   ← 주입 부하가 이 대역을 지배했다
hex 이후 : 이 값이 떨어지면 **위험 조건 자체가 줄어든 것**이다
```
> ### **`T2` 가 "그 대역에서 얼마나 깨지나"라면, 진입률은 "그 대역에 얼마나 들어가나"다.**
> ### **hex 의 이득은 앞이 아니라 **뒤**에서 나온다. 뒤를 안 재면 이득이 안 보인다.**

## 7.2 판정문에 반드시 같이 적을 것
```
① `≥64B` 결과에는 **분모를 항상** 병기한다. 분모가 창 I(1,067)보다 크게 작으면 **"판정 불가"**
② **`위험대역 진입률`을 같이 낸다** — 분모가 0 이어도 이 값은 산다
③ 🔴 hex 이후 창의 `≥64B` 를 창 G/H/I 와 **같은 축에 놓지 마라** — **프레임 구성이 바뀌었다**
④ 주입 규약은 **(가) 그대로 유지**한다(socket 권고 채택) — **강도를 올리면 조건 비교가 깨진다.**
   **대역을 억지로 만든 창은 어느 것과도 비교가 안 된다.**
```

## 7.3 ⚠ 그 굽기에는 축이 셋 실린다 (`hex 인코딩` · `등록(D/Q)` · `자리 유동화`)
**창 I 에서 귀속을 못 한 것과 같은 자리다.** arduino 가 **굽기 전에** 축별 기대를 적어 뒀다
(`docs/arduino/PLAN-axes-2026-08-18.md`). **판정할 때 그 문서를 먼저 읽고, 내 관측이 어느 축을
가를 수 있는지부터 적는다** — 가를 수 없으면 **"귀속 불가"** 라고 쓴다.
