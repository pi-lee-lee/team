> # 🔴 이 문서의 **관측 비율은 자격이 낮춰졌다** — 2026-08-19 10:20 재설정
> **사용자 지시(REQ-0256)**: *"오늘도 이전 데이터는 관측에서 제거한다. **현 시점(10:20)부터 관측
> 데이터로 이용한다.** 절단 관련은 **내가 이동 후 환경셋팅 시 발생한 것이다.**"*
>
> ```
> ✅ 남는다    : 소스 판독 · 자기 프로세스 안에서 닫히는 실측 · 도구 교정 · 계측기의 성질
> ❌ 자격 상실 : **관측 비율**과 **그 위에 세운 인과 귀속**
> ```
> 🔴 **지우지 않았다.** 값은 그대로 두고 자격만 낮춘다 — **왜 그렇게 갈렸는지가 다음 사람의 근거다.**
> ⚠ **여기 숫자를 새 판정의 근거로 인용하지 마라.** **관측 자료의 첫 창은 `창 P`(10:20 이후)** 다.
> 🔑 소급 계산으로 이 창들의 빈칸을 채우는 것도 **하지 마라** — 관측 자료가 아닌 것에서
> 새 관측 값을 만드는 것이고, **채워도 인용할 데가 없다.**

---

> # 🔴 범위 — 결론만 읽는 사람이 반드시 볼 것
> **① 축이 넷이다**: socket `b0d58d9`(하행 flush 순서) · arduino `9edbcca`(가상 O* + `espInit`) ·
> web 화면 파일 교체(07:20:43 · **주입 구간 안**) · 주입기 8인스턴스
> **② `>=64B` 는 주입이 만든 모집단이다.** 실사용을 재는 자료가 아니다
> **③ 🔴 이 창은 창 M 과 규약이 다르다** — 8라운드 자연 종료가 아니라 **정지신호로 R6 을 안 연 창**이다
> **④ 창 앞 13분은 무자극 구간이다.** 주입 구간과 **한 분모에 안 묶었다**

# 판정 · 창 O — **A[1] 이전 기준선. `SEND OK` 축은 네 창 만에 0 이 아니다** (2026-08-19)

**규칙** `SPEC-분모정의-2026-08-18.md` · **사전 등록** `PREREG-2026-08-19-winO.md`(주입 전 발행)
**판본** 서버 `b0d58d9` pid 62347 · srv_id `b0d58d9-1787090316424` — **주입 내내 안 바뀌었다**(감시기 매 20초 대조)
장치 `9edbcca`

```
창 t0        07:05:57 (탭 개방 = 내 DTR 리셋)
무자극 구간  07:05:57 ~ 07:18:58.315   (13분 01초)
🔴 주입 구간 07:18:58.315 ~ 07:30:13.536 (11분 15초)  ← socket 이 로그에서 읽은 값
             첫 injO1-3-1 · 마지막 injO5-4-51 · R1~R5 (R6 미개시)
```

## 0. 규약 이탈 — **먼저 적는다**
```
🔴 정지신호 발동 : 07:29:10 내 감시기가 `.team/STOP-O` 생성(bigfill=312) → R6 을 안 열었다
   실제 정지     : 07:30:13.536  → **지연 63초**(R5 잔여분). 창 M 에 없던 장치다
   시간 상한 40분 : 안 걸렸다
⚠ 판정선은 300 인데 **실측 분모는 312 다** — `[CNT]` 가 60초 주기라 그 사이에 넘었다.
   통과 시점은 07:28:10~07:29:10 사이 **미상**. **300 에서 끊긴 것처럼 읽지 마라.**
```

## 1. 🔴 `SEND OK` 축 — **네 창 연속 0 이 깨졌다. 다만 셋 다 링크 사건과 붙어 있다**

**분모: 주입 구간의 CIPSEND 거래 476건** (정상순서 370 · 뒤집힘 0 · 에코실종/지연 106)
```
T2  **3 / 476**   ← 창 H·I·J·M 은 사실상 0 이었다
  귀속 : 정상순서 거래 **3/370** · 에코실종 **0/106** · 뒤집힘 0/0
  시각 : 07:22:06 · 07:23:57 · 07:29:57
```
🔑 **세 건 모두 링크 재수립 사건과 시각이 맞는다.** 원문이 인과를 직접 말한다:
`[TX-WAIT] ★★ T2 초과 — SEND OK 가 끝내 안 온다 → **링크를 다시 세운다**`
장치 계수도 같은 분에 움직였다(`drop`·`stuck`·`okto`·`esprst` 가 셋 다 짝을 이룬다).

> ### **그러므로 "`SEND OK` 실종이 돌아왔다"와 "링크가 끊겨 `SEND OK` 가 못 왔다"를 이 자료로는 못 가른다.**
❌ **"네 창 연속 0 이 깨졌다"를 회귀로 읽지 마라.** 창 H·I·J·M 은 **링크 사건이 없던 창**이고
이 창은 **`esprst` 가 4번 난 창**이다. **분모가 같아도 조건이 다르다.**
### ✅ **대역 귀속 — 메웠다** (2026-08-19 10:2x · `sendok_order.py` 에 대역 축을 덧붙였다)
```
             <48        48~63        >=64
창 O 주입   0 / 20     **1 / 115**   **2 / 341 = 0.59%**
창 M 주입   0 / 85       0 / 180       **0 / 886 = 0.00%**
```
✅ **교정 확인**: 창 M 의 `>=64` 분모 **886** 이 기존 판정문 값과 **정확히 일치**한다.
같은 도구·같은 분모라 **창끼리 대조가 성립한다.** (기존 갈래별 계수는 한 글자도 안 바뀌었다 — 덧붙이기만 했다)

> ### 🔴 **그러므로 `>=64B` 대역 기준으로도 "네 창 연속 0" 은 깨졌다: `0/886` → `2/341`.**

⚠ **그런데도 회귀로 읽으면 안 되는 이유는 §1 본문 그대로다** — **2건 다 링크 재수립과 동시**이고
**창 M 은 `esprst` 가 0 이었다.** 조건이 다른 두 창의 비율을 크기 비교한 것이다.
🔑 **그리고 옛 체제로 돌아간 것도 아니다**: 창 G 는 `9/34 = 26.5%` 였다. **0.59% 는 자릿수가 다르다.**
**`SEND OK` 실종 축이 부활했다고 적지 마라 — 링크가 끊긴 창에서 2건 났다고 적어라.**

## 2. ✅ A[1] 이전 기준선 — **다음 배포(rid 폭 고정)의 대조군**

**전부 주입 구간(07:18:58.315~07:30:13.536) 분모다.**
```
`>=64B` 진입률   **259 / 371 = 69.8%**   (분모 = 에코와 짝지은 상행 프레임)
대역별 CIPSEND   <48 20 · 48~63 135 · >=64 341 · 합계 496
🔴 rid 자릿수    1자리 9(0.6%) · 2자리 92(5.9%) · **3자리 895(57.3%)** · **4자리 567(36.3%)**
                 rid 범위 1~1570 · **이 창 안에서 3→4자리 전이가 일어난다**
rid 유일성       고유 1551 / ACK 1563 → 중복 12 · 누락 19/1570 = 1.21%
ACK 개수 N       N=4 **60.0%**(289) · N=2 25.7%(124) · N=3 9.5%(46) · N=1 1.2% · N=0 2.9%
```
🔴 **A[1] 판정 때 반드시 이 줄을 같이 봐라**: 이 창은 **3자리와 4자리가 섞인 창**이다(57:36).
**A[1] 뒤 창이 자릿수가 고정되면 진입률 차이의 일부는 그 전이가 사라진 몫**이다 — **분리해서 읽어라.**
⚠ **이 도구(`rid_width.py`)의 절대값을 옛 판정문과 크기 비교하지 마라.** 분모 정의가 더 엄격하다(§5).

## 3. 장치 계수 — 주입 구간 끝(`[CNT]` 07:30:03 표본)
```
ssovf 99 · cksumng 99 · bigfill 343 · bigdrop **0** · pendfill 1100 · penddrop **0** ·
oklost 0 · okstream 1096 · slot 1200 · oow 462
```
⚠ `[CNT]` 는 60초 주기라 **주입 구간 경계와 정확히 안 맞는다**(끝 10초가 빠졌다).
✅ `penddrop 0` · `bigdrop 0` — **분모가 크다**(bigfill 343). 창 M(2/882)과 달리 **발동 안 했다**.
🔴 다만 **"버림이 없다"와 "버릴 것이 없었다"는 다른 진술**이고 `oklost=0` 은 여전히 미분리다.

## 4. 🔴 `esprst` 4건 — **창 M 은 0이었다. 귀속하지 않는다**
```
07:12:00 (무자극 구간) · 07:23:02 · 07:25:03 · 07:30:03 (주입 구간)
창 M : esprst 0 (창 전체 38분) · 창 O : 4 (25분)
```
```
           서버        주입     esprst
창 M       7c73c39     있음     0
창 O 무자극  b0d58d9     없음     1  (13분)
창 O 주입   b0d58d9     있음     3  (11분)
🔴 빈 칸    7c73c39     없음     — **이 칸이 없어 서버와 창이 교락돼 있다**
```
> ### **`b0d58d9` 에 귀속하지 않는다.** 표본이 1·3 이고 무선 환경은 통제 안 됐다.
🔑 **다만 무자극 구간에서도 1건 났다** — **주입이 필요조건은 아니다**까지는 말할 수 있다.
⚠ **`b0d58d9` 첫 창이라는 사실이 귀속 압력을 만든다.** 오늘 아침 `hex → cksumng` 이 그 압력에 무너졌다.
**이건 판정이 아니라 다음 창이 볼 신호다.**

## 5. ✅ 확정 — **주입기가 `화면` 으로 세어진다** (검정 통과)
```
07:18:39 화면 1(최대 1) → 07:19:39 **9(최대 9)** → [주입 종료 07:30:13] → 07:30:40 **1(최대 9)**
```
**주입 시작에 1→9, 종료에 9→1.** 인스턴스 8 + 기존 1 = 9. **확정이다.**
🔑 이 검정은 **처음에 실행 불가능한 형태로 적었다가 바꾼 것**이다 —
*"라운드 사이(5초)"* 는 서버 요약 60초 주기로 못 뜬다. *"주입이 끝날 때"* 로 옮겨서 잡혔다(원장 7.177).
```
🔴 결과 : `cksumng` 후보 "화면 수"는 **이 창에서 검정 불가**다(주입이 둘을 같이 올린다 = 교락).
        **후보 목록에 그대로 둔다. 죽이지도 살리지도 않았다.**
🔴 그리고 화면 파일 교체(07:20:43)가 **실행 중인 화면에 언제 반영되는지 못 본다** — **미상**이다
```

## 6. 다른 도메인 값 (인용 · 내가 안 쟀다)
```
socket : 시도 1679 · 미주입 122 · **분모 1557** · ACK(result=4) 1556 = 99.9%
         07:29:51 아두이노 오프라인 판정 · 07:30:12 세션#5 종료(errno=60) · 하행 큐 8줄 폐기(전선 미도달)
⚠ 그 8줄은 **세어질 곳이 없다** — 시도에는 들어갔고 전선에는 안 나갔다(socket 지적)
```

## 7. 📌 미결
```
~~🔴 `>=64B` 대역의 T2 수 — 도구가 대역별로 안 가른다. **미상**~~
   ✅ **메웠다**(§1): `2 / 341 = 0.59%`. 창 M 은 `0 / 886`. 도구에 대역 축을 덧붙여 소급 계산했다
🔴 `esprst` 4건의 원인 — 미상. 다음 창이 볼 신호
🔴 `cksumng` 왜 창 J 만 낮았나 — 여전히 미해결. 화면 수 후보는 이 창에서 검정 불가였다
⚠ `ssovf`(99) 와 `cksumng`(99) 이 이 창에서 정확히 같다 — **항등식은 아니다**(arduino 소스 전수).
   다만 `ssovf` 는 **사건 수가 아니라 하한**이고 민감도가 `espRead` 호출 빈도에 딸린다(원장 7.178).
   **창을 가로지른 비교는 그 빈도가 비슷할 때만 성립한다 — 아직 안 맞췄다.**
```

## 8. 다음 배포 — **판정문에 남긴다**
socket 이 넷을 **한 배포로** 낸다: `ad37119`(A[1] 본체) · `9e6e6fb`(A[1]-B 커서) ·
`ad437bd`(문서) · **`fceadf3`(INSTANCE 줄에 `cwd=` 추가)**.
⚠ **`cwd=` 로그 추가가 같이 실린다.** 축이 아니라는 판단은 루트 것이고, **다음 사람이 그 판단을 다시 볼 수 있어야 한다.**
(내 파서 셋은 전부 키 기반이라 영향 없음을 실물 줄로 확인했다 — `logfmt` 은 3 유지.)

---

# 🔴 세션 마감 (2026-08-19 07:36:48) — 다음 세션이 여기서 이어받는다

**루트가 사용용량으로 마감했다. `SendMessage` 로 보낸 것은 남지 않는다 — 이 파일이 정본이다.**

## 마지막 상태 — 관측 전부 정지
```
창 O 탭 종료 : 07:36:48 (종료 경계 줄 찍힘) · monitor/serial-winO.log
감시기 종료  : 07:36:48 · monitor/winO-watch.log
관측 프로세스 0 · 시리얼 포트 점유 0 · 보드/포트는 비어 있다
```

## 🔴 배포는 안 나갔다
socket 의 넷(`ad37119` A[1] 본체 · `9e6e6fb` A[1]-B 커서 · `ad437bd` 문서 · `fceadf3` `cwd=`)은
**루트가 보류를 확정**했다. **창 O 는 A[1] *이전* 기준선이고, 그 상태 그대로 멈춰 있다.**

## socket 이 물은 것 — **답을 여기 남긴다**(그 세션도 사라질 수 있다)
```
물음 : INSTANCE 줄에 `cwd=` 가 는다. logfmt 3 유지(가) 대 4로 올림(나)?
🔑 답 : **(가) 3 그대로.** 내 INSTANCE 파서 셋이 전부 **키 기반**이라 필드 수·순서를 안 본다
        winO_watch.py:78    re.search(r"pid=(\d+).*build=(\S+)")
        link_drop_probe.py  re.search(r"\bstart=(\S+)")
        soak_stats.py:50-51 RE_INSTANCE + KV_RE=(\w+)=([^\s]+) → findall 로 dict
        ✅ 네가 준 실물 줄(한글 cwd 포함)로 실제로 돌려서 확인했다. `log=` 도 정상 파싱된다
⚠ 단서 : 확인한 것은 **내 파서 셋**이다. `frozen-A/` 동결본과 다른 도메인 파서는 안 봤다
```

## 다음 창이 먼저 할 것 — 우선순위대로
```
① 🔴 `>=64B` 대역의 T2 수를 못 냈다 — **이 창의 가장 큰 구멍**
   `sendok_order.py` 는 갈래별(정상/뒤집힘/에코실종)로만 T2 를 세고 **크기 대역으로 안 가른다**.
   그래서 "네 창 연속 0 이 깨졌나"에 **대역 기준으로는 답 못 했다**.
   → 도구에 대역 축을 붙이면 **이 창 로그로 소급 계산된다**(새 관측 불필요)
② `esprst` 4건 — 창 M 은 0. **미상**. 교락 표의 빈 칸(`7c73c39`+무자극)이 인과를 막는다
③ A[1] 배포 뒤 창 : **이 창의 `rid` 자릿수(3자리 57.3% / 4자리 36.3%)와 갈라서 읽어라.**
   진입률 차이의 일부는 **전이가 사라진 몫**이다. 안 가르면 창 J 의 "왜 달라졌지"를 반복한다
④ `cksumng` 왜 창 J 만 낮았나 — 여전히 미해결. **화면 수 후보는 이 창에서 검정 불가였다**(교락)
```

## ⚠ 다음 사람이 밟기 쉬운 자리
```
· 이 창의 T2 3 을 **회귀로 읽지 마라** — 셋 다 링크 재수립과 시각이 맞고 원문이 인과를 말한다
· `esprst` 4 를 **`b0d58d9` 탓으로 읽지 마라** — 무자극 구간에서도 1건 났고 표본이 1·3 이다
· 분모는 **312 다**. 300 에서 끊긴 것이 아니다
· 이 창은 **정지신호로 끊긴 창**이다. 8라운드로 끝난 창 M 과 규약이 다르다
· `화면` 은 브라우저가 아니라 **:9900 WS 클라이언트 수**다. 주입기가 들어간다(§5 확정)
```

---

## 🔴 창 O 이후 — **굽기 경계가 하나가 아니라 둘이다** (2026-08-19 10:1x 추가)

**이 창을 뒤 창과 잇는 사람은 반드시 읽어라.**
```
07:36:48        창 O 탭 반납 (이 판정문의 자료는 여기서 끝난다)
🔴 시각 미상    **사용자가 `arduino/.basefw/client.ino` 로 플래싱했다** (시험 목적 · 본인 통보)
                → 칩이 `.basefw`(= `8b63846`, 08-16 · SLOT_N=10 · 가상 모듈 없음)로 **되돌아갔다**
10:0x           arduino 가 정본(`2187cc3`)으로 다시 구웠다 = 창 P 의 t0
```
> ### **그러므로 창 O → 창 P 사이에는 굽기가 **둘** 있다. 하나로 세면 귀속이 틀어진다.**

**창 O 시점의 칩 판본은 확정이다**(되돌아가기 *전* 이다):
```
① 내 시리얼   S,2,608 = **1544**  ·  .basefw 는 n=10 이라 최대 1023  → **1544 > 1023 이므로 .basefw 아님**
② 서버 로그   07:06:10 `등록 시작 — 선언 drain=7 · **n=12**`
🔑 장치 시리얼과 서버 로그 — **다른 계층의 계측기 둘이 같은 답을 냈다**
```
✅ **이 판정문의 `장치 9edbcca(n=12 · 가상 O*)` 표기는 그대로 맞다.** 되돌아간 것은 창이 닫힌 뒤다.
