# 🔴 `★ 정지 감지` 2건 — 2026-08-17 14:55:39 자동 기록

| | |
|---|---|
| `★ 정지 감지` | **2** |
| `+IPD`(하행) | 96 |
| `CONNECT`(재접속) | 13 |

## 🔴 값을 먼저 봐라 — 그것이 기전을 가른다

- `10000ms` 같은 **정상 범위** → **진짜 정지**(TX_STALL_MS 도달). **실제 사건이다**
- `4294967232ms`(=2^32-64) → **언더플로 재발**(기전 C). arduino 에 즉시 알려라

⚠ **재접속 수를 면죄부로 쓰지 마라** — 정지가 재접속을 일으킨다(인과 반대).

원문:
```
grep -a -n '정지 감지' monitor/serial-win4.log
```
