# 사전 등록 · 창 G — **순서 뒤집힘 ↔ T2 를 외부 검증한다** (2026-08-18)

🔴 **이 문서는 자료를 보기 전에 쓴다.** 창 G 의 시리얼 캡처는 아직 시작하지 않았다.
근거: 창 F 판정 `monitor/JUDGMENT-2026-08-18-sendok-order.md` (분모 3,783건)

## 0. 창 정의 — **t0 가 둘이다. 섞지 마라**

```
서버 t0   2026-08-18 16:37:15   커밋 ad01845 · pid 53187 (socket 통보)
장치 t0   탭 개방 DTR 리셋 시각 — **아래 실행 기록에 적는다**
판정 구간 t_start = 장치 t0 + 120초  (부팅 과도구간 제외 · 종전 규약)
```
⚠ **탭을 열면 보드가 리셋된다.** 그래서 socket 이 본 `세션#1` 은 끊기고 새 세션이 선다.
**서버 세션 번호를 창 F 와 이어 세지 마라.**

## 1. 전제 — 이것이 깨지면 나머지 예측은 판정하지 않는다

**P0 `devid거절 = 0`** (socket 신판이 처음 내보내는 칸)
> 🔴 **0 이 아니면 같은 망에 동일 `devid` 보드가 붙은 것이고, 그 창의 장치 지표는 우리 것이 아니다.**
> 그때는 **예측 판정을 중단하고 오염 구간을 기록만 한다.** (오늘 오전에 이것을 못 봐서 4시간을 잃었다)
⚠ **이 칸은 이번 판에 처음 생긴다 — 이전 창과 크기 비교 불가.**

## 2. 예측 — 전부 분모와 함께 판정한다

| # | 예측 | 반증 조건 |
|---|---|---|
| **P1** | **정상 순서 거래의 T2 = 0** (창 F: 0/3,772) | 정상 순서 거래에서 T2 가 **1건이라도** 나면 분리 가설이 깨진다 |
| **P2** | **뒤집힌 거래의 T2 비율 ≥ 50%** (창 F: 10/11 = 91%) | 뒤집혔는데 대부분 T2 가 안 나면 뒤집힘은 지표일 뿐 기전이 아니다 |
| **P3** | **T2 가 계속 난다 — 시간당 5~20건** (창 F: 14건 / 1.38h ≈ 10.2/h) | **T2 = 0 이면 내 인과가 틀렸다.** 🔴 서버만 바뀌었는데 장치 증상이 사라졌다는 뜻이 되고, 그러면 원인은 장치가 아니다 |
| **P4** | **뒤집힌 거래의 `CIPSEND` 크기 중앙값 > 정상의 중앙값** (창 F: 뒤집힘 54~136B / 정상 40B 지배) | 크기 차이가 없으면 "큰 프레임" 축은 버린다 |
| **P5** | **서버 `세션종료` 수 = 내 `[NET] online (CONNECT)` 수** (창 F: 22 = 22) | 어긋나면 **둘 중 하나가 우리 보드를 안 보고 있다** — 오염 재점검 |

## 3. 🔴 `0` 을 읽는 규칙 (원장 1.1)
```
T2 = 0  →  ① 정말 안 났다        ② 창이 짧아 표본이 없다      ③ 표지가 안 찍혔다
```
**분모를 먼저 적는다**: 짝지어진 거래 수 · 창 길이 · `[CNT]` 표본 수.
🔴 **거래 1,000건 미만이면 P1·P2 를 판정하지 않는다** — 창 F 분모가 3,783 이었다.

## 4. 정지 조건 — **손으로 보지 않는다. 도구가 본다**
```
① devid거절 > 0            → 즉시 오염 표시 · 예측 판정 중단
② 짝지어진 거래 ≥ 1,000    → P1·P2·P4 판정 가능
③ 창 길이 ≥ 60분           → P3 판정 가능
```
`monitor/sendok_order.py` 가 ②를 분모로 찍는다. ①은 서버 요약줄에서 읽는다.
⚠ **①을 보는 코드가 없으면 조건으로 적지 않는다**(팀 규약) — 서버 요약줄에 있으므로 볼 수 있다.

## 5. 이 창이 **고치지 못하는 것**
- socket 명시: **이번 배포는 `8.4초 침묵`을 안 건드린다.** 그러니 T2 가 남는 것이 정상 기대다.
- **arduino 의 `pendLine` 수정은 아직 안 구워졌다.** 구워지면 **이 창은 거기서 끊고 새 창을 연다.**
- 🔴 **내 계측기는 여전히 "버려진 줄"을 못 본다.** (가)전송 순서 대 (나)출력 순서는 **이 창으로도 못 가른다.**

## 6. 실행 기록 (자료 수집 중 채운다)
```
장치 t0        : **16:39:44**  (탭 개방 · DTR 리셋 · `serial-winG.log` 첫 줄로 확인)
                 ~~16:41:34~~  🔴 **내가 재지 않고 미리 적은 값이다. 실측과 110초 다르다.**
                 **사유**: 문서를 먼저 커밋하려다 실행 기록칸까지 채웠다. 지우지 않고 남긴다 —
                 **사전 등록의 값이 사후에 바뀌면 그 자체가 신뢰를 깎으므로 흔적이 있어야 한다.**
                 ⚠ `t_start = t0+120s` = **16:41:44** 로 계산한다(잘못된 값 기준이 아니다)
로그           : monitor/serial-winG.log
중단 사유      : (미기입)
```

## 7. 주입 구간 기록 (socket 하니스 · 루트 지시)
```
주입 시작   2026-08-18 16:46:45   (= 장치 t0 + 421초)
구성        8개 동시 · 슬롯 A1~A5·B1~B3 · rid 접두 injG1~injG8 · 각 120건(최대 960)
하니스 기록 net/inject-winG-2026-08-18.log
주입 종료   (미기입 — socket 통보 대기)
```
🔴 **판정 구간은 주입 구간으로 자른다.** 주입 전(16:39:44~16:46:45)은 **자극 부재 구간**이라
그 구간의 `뒤집힘 0`·`T2 1` 을 주입 구간과 **같은 분모로 묶지 마라.**

## 8. 🔴 P1 은 이미 반증됐다 — 자료가 아니라 **내 도구가 틀렸다**
창 G `16:40:23` 에 **뒤집힘 없이 T2** 가 났다. 원문을 열어 보니 **에코가 8초 지연**된 갈래였고,
**내 도구가 그 갈래를 분모에서 빼고 있었다**(창 F 에서 57건 · 그 안에 T2 4건).
→ **도구를 고쳐 세 갈래로 다시 셌다**(`정상 3,772 / 뒤집힘 11 / 에코실종 57`, T2 합계 14 = 원시 계수).
**P1 의 문장("정상 순서 거래의 T2 = 0")은 고친 도구로도 참이다.** 다만 **P1 만으로는 T2 를 다 못 덮는다** —
**`P1' 세 갈래의 T2 합계 = 원시 T2 계수`** 를 새 판정선으로 쓴다(분류 누락을 스스로 잡는 형태).
