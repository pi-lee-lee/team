# 🔴 `★ 정지 감지` 1건 — 2026-08-17 07:46:36 자동 기록

**기전 C(계측기 자해 · 부호 없는 뺄셈 언더플로)의 표지다.**
근거: `monitor/FINDING-2026-08-17-0125-underflow.md`

| | |
|---|---|
| `★ 정지 감지` 누계 | **1** |
| `+IPD`(하행 수신) 누계 | **1** |

✅ `정지 감지 1` <= `하행 1` — **알려진 경로로 설명된다**(하행 1건당 거의 1건).
⚠ 이 수를 **넘는 순간** 전제가 깨진 것이다. 이 파일이 그때 바뀐다.

원문을 열어라:
```
grep -a -n '정지 감지' monitor/serial-win3.log
```
