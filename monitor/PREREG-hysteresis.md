# 사전 등록 · 히스테리시스 판정 — **결과 보기 전에 적는다** (2026-08-20 22:14)

## 🔴 루트가 준 지표에 구멍이 있다 — 고쳐서 쓴다
```
루트 : "1초 간격 = 연속 슬롯 = 진동"
🔴 슬롯은 **1.2초**다. 로그 시각은 **정수 초**다
   → 인접 슬롯 전이가 **1초로도 2초로도 찍힌다.** `1초`만 세면 **약 70% 를 놓친다**
✅ 지표 : **인접 슬롯 전이 = 간격 ≤ 2초**
```

## 기준선 — 이번 창 실측 (T0 22:00:35 · 굽기 뒤 첫 접속)
```
[B1] 전이 32건(찼다 16/비었다 16) · 간격 31개
     1s×3 · 2s×7 · 3s×4 · 5s · 6s · 7s×2 · 8s · 9s×2 · 10s×2 · 15s · 16s · 22s
     🔴 **인접 슬롯(≤2s) = 10/31 = 32.3%**   (1초만 세면 3/31 = 9.7% — 3배 차이)
     최장 조용 89초
[A1] 전이 6건 · 간격 5개 · ≤2s = 2/5 · 최장 조용 113초
     ⚠ **표본 6이다. A1 은 판정에 안 쓴다**
```

## 🔴 판정선 — 지금 정한다
```
지표 : R = (간격 ≤2초인 전이) / (전체 전이 간격)   — **B1 만**
분모 조건 : 🔴 전이 **20건 이상**. 미만이면 **판정 안 한다**("표본 부족")
   근거: 이번 창 B1 이 32건이고 A1 은 6건이라 갈렸다. 6건으로는 아무 말도 못 한다

✅ 들었다   : R < **10%**        (기준선 32.3% 의 1/3 이하)
⚠ 미상     : 10% ≤ R < 25%
❌ 안 들었다 : R ≥ **25%**
```
🔴 **절대 수를 비교하지 않는다** — 사용자가 손대는 양이 창마다 다르다.
§*"시험의 총량이 결과의 함수이면 총량을 비교하지 마라"*. **`점유변화` 총수도 같은 이유로 판정에 안 쓴다**
(줄어도 그것이 히스테리시스인지 사용자가 덜 만진 것인지 안 갈린다).

## ⚠ 이 지표가 답하지 **않는** 것
```
R 이 낮아도 : **자극이 경계에 안 걸렸을 뿐**일 수 있다
→ 그래서 `[USD]` 폭을 같이 본다. **폭이 문턱 여유(40cm)보다 크면 경계에 걸린 것**이다
🔴 이번 창 B1 폭 **70cm** → 여유 40 으로 **못 덮는다.** arduino 도 "덮었다"고 안 적었다
   무엇이 이 미상을 닫나 : **다음 창 `[USD]` 폭이 32 이하로 유지되는가**
   폭 70 급이 또 나오면 **히스테리시스로는 안 닫힌다** — 다른 처방이 필요하다
```

## 같이 얼리는 기준선
```
ssovf 0 · ramLow 392B · smiss 1/150 · 묶음미룸 0 · 창놓침 0 · 최장 조용 B1 89초 / A1 113초
⚠ `n` → `호출` 로 이름이 바뀐다(캐시 반환을 세고 있었다 · 65535 포화). **min/max 는 유효**
⚠ 링크 축은 열려 있다 — 창 AU 는 깨끗했다(오프라인 0 · 공백 1.7초). **표본 1**
```

---

## 🔴 개정 (22:22) — **내 `폭 32 이하` 조건에 전제가 빠져 있었다**

~~"다음 창 `[USD]` 폭이 32 이하로 유지되는가"~~ **← 이대로는 못 쓴다**
```
🔴 `[USD]` 와 `V`·`occ` 는 **다른 계측기다**
   [USD] = **측정된 모든 값**의 min/max   ·   V = **슬롯 순간의 마지막 값**
   → A1 이 57~60cm 를 봐도 슬롯 시점에 못 쟀으면 `V` 는 **빈 칸**이다
실물(22:20 창) : [USD] A1 폭 3·8 인데 `V` 의 A1 칸은 **151건 전부 빈 칸**
```
> ### **그러므로 `[USD]` 폭은 `occ` 전이의 분모가 아니다. 같은 표에 놓으면 안 된다.**

**개정된 조건 — 전제를 붙인다:**
```
✅ 폭 조건은 **물체가 있는 구간에서만** 유효하다
   판별 : 같은 구간의 `V` 칸에 **값이 꾸준히 있는가**
🔴 값이 없는데 폭이 넓으면 → **원거리 잡음을 재고 있는 것**이다. "안 들었다" 가 아니다
   실물 : 물체가 없어진 뒤 B1 폭이 43~54 로 나왔다 — 이것을 실패로 읽으면 틀린다
```
🔑 §*"숫자 둘을 비교하기 전에 — **누가 만들었는가**"* 를 내가 어겼다.
**두 값이 같은 로그에서 나오고 이름도 비슷해서**(둘 다 거리 cm) 같은 계측기로 읽었다.

## 🔴 판정표 (arduino 완성 · 접수)
```
A1 칸 값 **있음** + 전이 있음  → 사람 손 (정상)
A1 칸 값 **없음** + 전이 있음  → 🔴 **못쟀다 진동** (N=3 이 안 들었다)
A1 칸 값 **없음** + 전이 0     → ⚠ 조용함 — **자극 없음일 수도.** 판정 아님
🔴 물체가 **있는데** occ=0    → 진동이 아니라 **문턱·배치 문제**
```
⚠ 해제 지연 **약 6초**(장치 N=3 × 1.2 = 3.6 + 서버 하강 디바운스 2.4)
→ **"찼다→비었다" 간격이 3~6초대에 몰리면 N=3 이 도는 증거다**
🔑 그러므로 `R`(≤2초 비율)과 **별개로 "찼다→비었다 간격 분포"를 같이 본다.**
   R 은 *진동이 줄었나*, 이쪽은 *N=3 이 실제로 도나* — **다른 물음이다**

---

## 🔴 판정자 확정 (22:5x) — **N=3 수정의 판정자는 "해제 직전 연속 빈칸 수 분포"다**
```
지금(결함 있는 칩) : {0:2, **1:16**, 3:1, 10:1, 16:1}   → **76% 가 빈칸 1 에서 해제**
✅ 고쳐졌다 : 분포가 **{3:다수}** 로 옮겨간다 (N=3 이 진짜 3슬롯이 된다)
⚠ 미상     : 1 과 3 이 섞여 있다
❌ 안 고쳐졌다 : 여전히 1 이 다수
분모 조건 : 해제 **15건 이상**(이번 21건). 미만이면 판정 안 한다
```
**기전(arduino 소스)**: `readSensors()` 가 **매 `loop()`** 돈다 → `noRead[i]++` 가 **루프마다** 올라
`N=3` 을 즉시 넘는다. **`N=3` 이 "3 슬롯"이 아니라 "3 루프"였다.**
✅ 고침: 슬롯 번호가 바뀔 때만 증가(RAM +8B).

⚠ **`V` 는 조건부가 아니다 — `S` 가 나가는 슬롯마다 나간다**(arduino 소스).
내가 본 88% 의 12% 는 **`S` 자체가 안 나간 슬롯**(`smiss`)이지 `V버림`(실측 0)이 아니다.
🔑 그래서 내 "빈칸 1 ≠ 1 슬롯" 우려는 **방향은 맞고 크기가 작다.** 결론은 안 바뀐다.

---

## ⚠ 이름 정정 (23:0x) — **이 굽기가 고치는 것은 "히스테리시스"가 아니다**
```
✅ 고쳐지는 것 : **못쟀다 경로**(noRead 를 슬롯 단위로) — 내 빈칸 분포가 판정한다
🔴 여전히 **미측정** : **히스테리시스(값 경로)** 자체
   근거 : 해제 직전 마지막 값 전수가 **2~64cm · 100cm 초과 0건**
        → 해제선(>100cm)을 **한 번도 안 밟았다.** 도는지 안 도는지 모른다
   무엇이 이것을 닫나 : **물체가 100cm 밖으로 나가는 자극**. 손으로는 안 된다
```
🔑 **이 파일 이름이 `PREREG-hysteresis` 라서 오독을 만든다** — 여기 실린 판정자는
**히스테리시스가 아니라 못쟀다 경로의 것**이다. 그 사실을 여기 박아 둔다.
⚠ 굽기 뒤 빈칸 분포가 `{3:다수}` 로 가도 **"히스테리시스 확인됨"으로 적지 마라.**

## ⚠ 그리고 판정에는 **자극이 필요하다**
```
해제 15건을 채우려면 사용자가 센서를 가려야 한다
자극이 없으면 → **"판정 불가(자극 없음)"** 이지 "고쳐졌다"가 아니다
🔑 앞 창(AW)에서 29분을 자극 없이 보냈다. 같은 일이 또 날 수 있다
```

---

## 🔴 `R` 지표의 선행 조건 — **문턱 `X` 를 정한다** (루트가 나에게 맡겼다 · 23:1x)

```
★ **`V` 빈칸률 > 20% 이면 `R` 을 진동 지표로 쓰지 않는다.**
   그 구간의 전이는 **측정 탈락**이 만든 것이지 물체 진동이 아니다
```
**근거(자릿수 논증)**: 해제는 연속 빈칸 `N=3` 으로 난다. 빈칸률 `p` 면 3연속 확률 ≈ `p³`
```
p=0.50 → 12.5%   p=0.30 → 2.7%   **p=0.20 → 0.8%**   p=0.10 → 0.1%
→ 슬롯당 0.8% 미만이면 탈락 유래 해제가 실제 해제에 묻힌다고 본다
```
⚠ **독립 가정이다. 실제 빈칸은 상관이 있어(연속으로 몰린다) 3연속 확률이 더 높다.**
**그러므로 20% 는 넉넉한 선이 아니라 *하한* 이다.** 애매하면 R 을 쓰지 마라.
🔑 실물이 양 끝을 준다: **A1 0.0%**(R 유효) · **B1 94.9%**(🔴 R 무효).
**지금 B1 에 R 을 쓰면 "안 들었다"가 나오는데 그건 틀린 결론이다.**

---

## ✅ **닫혔다 — 히스테리시스가 장치에서 돈다** (2026-08-21 00:23 · 서버 로그 독립 확인)

### 🔴 먼저 내 **닫는 조건이 틀렸었다**
```
~~"물체가 **100cm 밖**으로 나가는 자극이 필요하다. 손으로는 안 된다"~~
🔴 100 은 **해제선**이고, 밟아야 했던 것은 **60~100 유지 구간**이었다
   문턱 60 초과 · 해제선 100 미만 → **히스테리시스가 없으면 해제되고 있으면 유지된다**
→ **손으로 충분했다.** 조건을 틀리게 적어 두고 하루 종일 "미측정"으로 들고 있었다
```
🔑 §*"감시할 수 없는 것은 조건으로 적지 마라"* 의 반대 실패다 —
**감시할 수 있는 것을 감시 불가능한 조건으로 적어 스스로 막았다.**

### 실측
```
00:22:58  occ **3 → 7**  ·  = 모듈 B1 점유 → 찼다  ·  ▸ [값] B1 60
00:23:00  occ **7**  ·  ▸ [값] B1 **62**   ← 🔴 문턱 초과
00:23:01  occ **7**  ·  ▸ [값] B1 **63**   ← 🔴 초과
00:23:03~ occ **7** 유지 · 값 58·49·48·37·35…  ·  🔑 `비었다` **0건**(36초)
00:23:26  ▸ [값] B1 **62** 다시 초과 → 유지  ← **재현 2회**
n=4 비트 : A1=8 · B1=4 · LD=2 · L2=1  →  occ 7 = B1+LD+L2 (**B1 비트 서 있다**)
```

### 🔴 대안 설명을 죽인 것이 이 판정의 핵심이다
```
⚠ 대안 : **서버 하강 디바운스 2.4초**가 해제를 가린 것 아닌가?
   62·63 이 **약 2초**뿐이었다(00:23:00~01) → 디바운스 창 안이다. **그럴듯했다**
✅ 죽인 방법 : **`occ` 는 장치가 전선에 실어 보내는 비트다.** 서버 처리가 안 낀다
   그 비트가 62·63 을 읽는 슬롯에 **계속 서 있었다** → **장치가 유지한 것이다**
```
> ### **로그 줄(`비었다` 부재)로는 못 가른다. `occ` 비트로만 갈린다.**
🔑 §*"판정자는 시각이 아니라 구조로 잡아라"* 의 재현 — **디바운스된 뷰가 아니라 원본 비트를 봐라.**

### ⚠ 이 판정이 **재기동 11회에 안 걸리는 이유**
```
분모가 **시간이 아니라 사건**이다 — "62 를 본 슬롯에 비트가 유지됐나"는 36초 안에서 닫힌다
🔑 §"기준선 이동은 시간을 분모로 쓴 진술만 친다"
→ **링크·소크 판정은 이 구간에서 불가능하지만 이 판정은 유효하다.** 갈라 적는다
```
### 남은 미계측
```
🔴 A1 값 — socket 의 모듈 필터가 print 앞이라 로그에 안 남는다. 다음 배포까지
🔴 해제선 100cm 자체 — 여전히 안 밟았다. **유지는 확인됐고 해제 임계는 미확인**
```

---

## 🔴 미결 종료 (2026-08-21 00:3x · 사용자 아두이노 전원 제거)

```
🔴 **해제선 100cm — 미측정으로 종료한다**
   확인된 것 : **유지**(60~100 구간에서 점유 비트가 안 풀린다) — occ 비트로 확정
   미확인    : **해제 임계**. 값이 100 을 넘은 적이 한 번도 없다
   ★ 무엇이 이것을 닫나 : **값이 100cm 를 넘는 자극**(물체를 1m 밖으로). 그때 `occ` 비트가 풀리는가
🔴 **A1 값 — 미계측으로 종료한다**
   기전 : socket 의 모듈 필터가 print **앞**에 있어 A1 값이 로그에 안 남는다
   ★ 무엇이 닫나 : socket 이 필터를 print 뒤로 옮기는 배포
```
⚠ **둘 다 "확인됨"이 아니다.** §*"미상 옆에 무엇이 이것을 닫는가를 적어라"* —
**안 적으면 조용한 나날에 목록에서 사라진다.**
