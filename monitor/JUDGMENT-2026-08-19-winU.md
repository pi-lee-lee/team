> # 🔴 범위 — 결론만 읽는 사람이 반드시 볼 것
> ### **① 이 창은 원래 질문에 *답하지 못했다*.** 장치가 안 무너져서 **4단이 오지 않았다**(`hwrst 0/0`)
> ### **② `hwrst = 0/0` 은 "4단이 안 듣는다"가 아니라 "4단이 올 일이 없었다"이다**
> ### **③ 🔴 "잘 돌았다"를 "고쳐졌다"로 쓰지 마라** — 사전 등록에 그렇게 미리 박아 뒀다
> ⚠ 그리고 **기전 B 는 옛 기판(창 Q)에도 새 기판(창 S)에도 났다 → 기판 축이 아닐 수 있다.**
> **이 창이 깨끗할수록 그 함의가 묻힌다.**

# 판정 · 창 U — **주입을 견뎠다. 그래서 판별 시험이 성립하지 않았다** (2026-08-19)

**사전 등록** `PREREG-2026-08-19-winU.md`(**주입 시작 전** 발행 · 함정을 미리 적음)
**판본** 칩 `0b2a056` · 서버 `7f17b99` pid 79990 — 🔑 **창 T 와 같은 판본**(socket 이 값으로 확인)
```
🔴 t0 **15:38:27.661** (주입 첫 건 `injU1-2-1`) · 마감 **15:46:39**(주입 전체 종료) · **약 8분 11초**
   ⚠ **리셋 없다.** 장치 세션이 창 T 에서 이어진다 — **경계는 자극이지 부팅이 아니다**
   창 T(무자극) 14:57:40~15:38:27 · **약 40분 47초** — 분모를 안 묶었다
```
> ### **창 T ↔ 창 U 의 유일한 축은 *자극* 이다.** 판본·기판이 같다.

## 0. 규약 이탈 — 먼저 적는다
```
🔴 정지신호 발동 : **15:46:32** 내 감시기가 `.team/STOP-U` 생성(`bigfill=301`)
   실제 정지     : **15:46:39** → **지연 7초**  (창 O 63초 · 창 P 118초)
   40분 상한     : 안 걸렸다 (약 8분)
⚠ 판정선 300 인데 실측 **301** — 이번엔 거의 안 넘겼다. **그래도 301 로 적는다**
🔴 **8라운드 자연 종료가 아니다** — 정지신호로 끊긴 창이다
```

## 1. 🔴 검정 ① — **답하지 못했다**
```
`hwrst = 0/0`  →  **4단이 한 번도 안 왔다**
LADDER 실패 **1** · esprst **1** · drop 1 · stuck 1   ← 사다리가 거의 안 돌았다
```
> ### **4단은 고장이 나야 온다. 고장이 안 났으므로 그 계수는 아무것도 안 말한다.**
> ### **이 창은 *"무너지면 스스로 일어나는가"* 에 답하지 못했다. 못 밟은 경로의 결과는 못 센다.**
✅ **사전 등록에 이 함정을 미리 적어 뒀다** — *"끝까지 안 무너지면 `hwrst` 는 0/0 이고 그때 이 창은
①에 답 못 한 것이다. '잘 돌았다'를 '고쳐졌다'로 쓰지 않는다."* **그 규율을 지킨다.**

## 2. ✅ 그래도 이 창이 말할 수 있는 것 — **주입 부하를 견뎠다**
**주입 구간(15:38:28~15:46:39) 분모:**
```
CIPSEND 거래 **373** (정상순서 351 · 뒤집힘 1 · 에코실종/지연 21)
대역별 T2 : `<48` **1/31** · `48~63` **0/11** · 🔴 `>=64` **0 / 331 = 0.00%**
갈래별    : 정상순서 **0/351** · 에코실종 **0/21** · 뒤집힘 0/1
`>=64B` 진입률 **311 / 351 = 88.6%** · `N=4` **85.5%**(319)
```
```
🔴 깨진 **나가는** AT **0** · 상행 AT 혼재 **0**   ← **부하를 걸어도 기전 A 가 안 나타났다**
   (옛 기판 창 R 은 무자극에서도 각각 **14**)
CNT 차분(창 U) : ssovf **9** · cksumng **9** · penddrop **3** · bigdrop **3** · okto **5** · oow 304
```
🔑 **`ssovf 9` 와 `cksumng 9` 가 정확히 같다** — 원장 §7.178 의 관찰과 일치한다(항등식은 아니다).

### ✅ `A[1]` 은 이 창에서도 돈다
```
rid 4자리 **0** · 범위 **0~999** · 3자리 92.5%
```

## 3. ⚠ 비교 규율 — 지켰다
```
🔴 창 O·P(옛 펌웨어·옛 기판)와 **크기 비교하지 않았다.** 진입률 88.6% 는 **"이만큼이다"** 다
🔴 총 건수로 창끼리 비교하지 않는다 — §"시험의 총량이 결과의 함수다"
   (장치가 나쁘면 서버가 즉시 거절해 주입이 *늘어난다*)
⚠ `bigfill` 301 은 **정지선을 거의 안 넘긴 값**이라 분모가 앞 창들(334·882)보다 작다.
   **대역 비율은 그 분모 위에서 읽어라**
```

## 4. 📌 미결 — **가장 중요한 것이 그대로 남았다**
```
🔴 **"무너지면 스스로 일어나는가"** — 이 창도 못 답했다. **4단이 안 왔다**
   → 답하려면 **장치가 실제로 무너지는 조건**이 필요하다. 주입 8분으로는 안 무너졌다
🔴 기전 B(결합/IP 상실)의 원인 — 옛 기판·새 기판 양쪽에서 났다. **기판 축이 아닐 수 있다**
⚠ `센서갈림` **3자리 = 기준선**(0270 미도입). **검정 불가**는 그대로다
⚠ socket 의 축(시도·미주입·분모·비율)은 **아직 안 받았다 — 받으면 §5 에 추가한다**
```

## 5. 다른 도메인 값 (socket 제공)
```
구간   : 15:38:27.661 ~ **15:46:32.358**(마지막 `injU3-7-60`) · 주입기 회차 **24**
시도 **1398** · 미주입(서버거절) **48** · **분모 = 1350** · ACK(result=4) **1350 = 100.0%**
라운드 : R1~R3 · **R4 미개방**(정지신호) · 40분 상한 안 걸림 · 서버 판본 7f17b99 불변
격리   : 건너뜀 **0** · 강제 **0**  → 🔴 **"정상"이 아니라 "표본 없음"**
         (한 바퀴가 격리 7.2초보다 훨씬 길어 **이 부하로는 구조적으로 못 밟는다** · 창 P 에서 재 뒀다)
ACK 미상rid 4 → **16**  ⚠ *늦은 ACK* 과 *재전송 중복*을 한 칸에 담는다(설계 §3.1). **못 가른다**
```

### 🔴 socket 이 물은 것 — **"링크없음 51분·세션 52회가 주입 탓인가"** → **아니다. 내가 갈랐다**
socket 이 *"주입이 원인인지 나는 못 가른다"* 고 넘겼다. **그 값들은 서버 기동 이후 누적이라 구간을 잘라야 한다:**
```
14:56:55 (창 T t0 직전) : 링크없음 **3064s** · 세션 49
15:38:00 (창 U t0 직전) : 링크없음 **3064s** · 세션 50
15:48:01 (현재)         : 링크없음 **3065s** · 세션 52
```
```
🔴 창 T 구간(약 41분) : 링크없음 **0초** · 세션 **+1**
🔴 창 U 구간(약 10분) : 링크없음 **1초** · 세션 **+2**
```
> ### **누적 `51분` 은 거의 전부 창 T *이전*(12:00~14:57)의 것이다. 두 창에서는 합쳐 1초 늘었다.**
✅ **그러므로 "이 창의 링크가 매우 잦게 끊겼다"는 성립하지 않는다.** 주입 탓도 아니다.
🔑 §*"구간 이름이 안 붙은 비율은 인용하지 않는다"* — **이번엔 그 규칙이 없는 걱정을 지웠다.**
⚠ 세션 +2 는 있었다(창 U). **`esprst 1 · drop 1 · stuck 1` 과 자릿수가 맞는다** — **작은 사건 한둘이다.**


---

## 6. 🔴 **판별자를 찾았다 — `busy p...` 가 창 S 와 창 T/U 를 가른다** (루트 발견 · 내가 검산)
```
창 S (23분 미복구) : ESP 의 `busy p...` 응답  **868회**
창 T/U (자력 회복)  : `busy p...`             **0회**
```
> ### **결합 상실은 양쪽 다 났다. 갈린 것은 *ESP 의 AT 파서가 먹통이 됐는가* 다.**
```
파서 살아 있다 → 3단 `AT+RST` 가 **먹는다** → `esprst=1` 로 한 번에 회복    ← 창 U
파서 먹통      → `AT+RST` 를 **처리조차 못 한다** → 사다리 289회 헛돌고 미복구  ← 창 S
```
### 🔑 그래서 이번 굽기의 값이 **어디 있는지** 드러났다
> ### **4단(HW 리셋)은 *파서 먹통* 인 경우에만 값이 있다. 그 경우가 이 창에 안 왔다.**
**`hwrst 0/0` 이 "안 듣는다"가 아닌 이유가 이걸로 더 강해진다 — 부를 일이 없었다.**
⚠ **그러나 이것은 사용자의 물음을 닫지 않는다** — *"왜 창 S 는 먹통이 됐고 지금은 안 되나"* 는 **미상**이다.
**대체 주장을 세우지 않는다.**

### ⚠ 그리고 **끊김은 났다** — "안 끊긴다"가 아니다
```
15:43:20  [AT] "0.0.0.0"  ← 기전 B 판별자. **창 S 와 같은 사건**
          esprst 0→**1** · okto 2→5 · stuck 0→1 · ackstale→4 · cksumng 0→9
15:46:32  bigfill 301/300 → **내 정지선. 계획된 종료지 고장이 아니다**
```
> ### **"끊김이 안 난다"가 아니라 "끊겨도 스스로 일어난다" 이다. 이 둘은 완전히 다른 진술이다.**

## 7. ✅ 루트가 넘긴 이상한 줄 — **손상이 아니다. 프레임 구조다**
```
15:38:50  A,474,B3,4,33 / A,475,A3,4,31 / A,476,A5,4,34 / A,477,A1,4,31
15:38:50  **A,478,B1,4,3D1,OBV,5A**      ← 마지막 ACK 뒤에 `D,X1,OBV,5A` 의 꼬리가 붙었다
15:38:50  [SLOT] n=2051 **tx=138** ack=8   → AT+CIPSEND=138 → **[DROP-OVF] 줄이 96바이트를 넘어 버렸다**
```
**상행 프레임 하나에 ACK 8개 + 모듈 등록(`OBV`)이 이어 붙는다.** 디버그 로그가 ACK 레코드를 줄 단위로
찍으므로 **마지막 줄이 등록 꼬리를 물고 나온다.** 바이트가 깨진 것이 아니다.
```
이 형태 **206회** vs `[CKSUM NG]` **10회**  →  🔴 **수가 다르다. 같은 사건이 아니다**
```
✅ **`[DROP-OVF]` 는 arduino 가 미리 알려 준 그것**이다 — 등록 배치 에코가 `RX_CAP=96` 을 넘는
**구조적 결과이지 결함이 아니다.** 여기선 138B 다.
🔑 **로그의 줄 나눔이 프레임의 경계와 다르다** — §*"이름이 아니라 정의를 봐라"* 의 **로그 판본**이다.

---

## 8. 🔴 **결합유지 `45분 26초` — 이 프로젝트에서 잰 것 중 가장 길다** (배경 집계 · 늦게 나옴)

```
15:43:25  [LADDER] 실패 · CWJAP소요 2731s · **결합유지 2726s** · **현재 0단**  ← ✅ 깨끗한 표본
15:51:37  [LADDER] 실패 · CWJAP소요 489s  · **결합유지 482s**  · **현재 0단**  ← ✅ 깨끗한 표본
```
⚠ **귀속을 정확히 한다** — 이 `2726초` 는 **창 T 와 창 U 를 가로지른다**:
```
결합 시작 ≈ **14:57:59**(창 T t0 직후) → 유지 → **15:43:25 종료**(창 U 주입 +5분)
= 무자극 **40분 47초** + 주입 **4분 58초**
🔴 **창 T 만의 값도, 창 U 만의 값도 아니다.** "창 T→U 를 잇는 한 번의 결합"이다
```

### 지금까지 잰 **깨끗한(단0) 결합유지** 전부
```
창 Q  **8초**            (옛 기판 · 옛 펌웨어)
창 R  46~447초 (n=10)    (옛 기판)
창 S  1084초 = 18분 4초  (새 기판 · 옛 펌웨어)
🔴 창 T→U  **2726초 = 45분 26초**   ·  그 뒤 **482초 = 8분 2초**
```
> ### **자릿수가 하나 더 올라갔다. 그리고 두 번의 종료 모두 *자력 회복* 으로 이어졌다**(`busy p` **0**).
⚠ **크기 비교가 아니라 대조다** — 기판·펌웨어가 다르다. **비교 가능한 것은 "얼마나 버텼나"의 자릿수뿐이다.**

### 🔴 그래도 **"고쳐졌다"가 아니다**
```
옛 위치 : **45분** 정상 뒤 붕괴 (REQ-0071 실측)
창 T→U  : **45분 26초** 유지 뒤 끊김 — **그 분포와 사실상 같은 자리다**
```
> ### **옛 이력의 붕괴 시점과 자릿수가 같다. 이 값으로 셋째 갈래를 배제하지 못한다.**
✅ **다른 것은 그 뒤다** — 옛 위치는 **18분 미복구**였고, 이번엔 **자력 회복**했다.
🔑 **버틴 시간이 아니라 *끊긴 뒤* 가 달라졌다.** 그것이 이 창들의 실제 소득이다.

### ⚠ 그리고 이 값은 **판정문을 쓴 뒤에 나왔다**
```
배경 집계(60분 대기)가 16:01 에 끝나면서 나왔다. §2·§6 을 쓸 때는 없었다
```
🔴 **창 R·창 S 에 이어 세 번째로 "배경 집계에서만 나오는 축"이 늦게 왔다.**
✅ **다만 이번엔 §5 에 "미집계"를 명시해 뒀고, 나와서 바로 채웠다** — 조용히 빠지지 않았다.
🔑 **그 규율이 실제로 값을 했다.** 안 적었으면 이 창은 결합유지 없이 마감됐을 것이다.
