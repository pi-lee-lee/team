# 창 H — 실행 기록 (2026-08-18)

## t0 가 **둘**이다. 섞지 마라
```
굽기 리셋   16:59:36   (arduino 업로드 DTR · 커밋 6709ef7 · hex 대조 IDENTICAL)
🔑 내 t0    **17:01:20**  (탭 개방 DTR 리셋 · `serial-winH.log` 첫 줄)  ← 관측 기준은 이것
```
⚠ **굽기 리셋과 탭 개방 리셋 사이 104초는 내 로그에 없다.** 그 구간을 판정에 쓰지 마라.

## 판정 규칙 — `monitor/SPEC-분모정의-2026-08-18.md` 하나만 쓴다
```
분모 = CIPSEND 거래 · 크기 = CIPSEND 의 <n> · 대역 = <48 / 48~63 / >=64 · 결과 = T2
```

## 창 G 대조값 (이 칸과 같은 규칙으로 낸다)
```
>=64  34거래 → T2 9 (26.5%)      resync 3 / up=540
```

## arduino 가 요청한 산출 넷
```
① 크기 대역별 T2      ② penddrop·okstream 을 같은 대역으로
③ resync (더미 pump 혼입 검증)  ④ >=64B 표본 수 — 창 G 34건과 같은 자릿수여야 분모 성립
```
⚠ **`T2 = 0` 이면 오히려 의심한다**(arduino). 다른 것이 같이 바뀌었다는 뜻이다. **4 근처가 성공.**
🔴 **`>=64B` 표본이 안 차면 판정하지 않는다** — socket 주입 재개가 필요하다.
