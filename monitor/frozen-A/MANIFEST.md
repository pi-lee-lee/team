# 동결 파서 — 관측 창 A 판정 전용 (판본명 `parserA-2026-08-16T1838`)

> **이 디렉터리의 파일을 고치지 마라.** 고치는 순간 A 판정이 두 판본으로 갈린다.
> 개발은 `monitor/` 의 원본에서 계속한다. 여기는 A 를 처음부터 끝까지 같은 자로 재기 위한 사본이다.

## 왜 있나

루트 지시(2026-08-16 18:33, 팀 재기동 직후):

> A 의 첫 37분과 남은 3시간 20분이 **다른 코드로 측정되면**, 우리가 오늘 세 번 밟은
> 그 실수(같은 이름·다른 계측기)를 판정 안에 심는 것이다. A 판정은 **한 판본의 파서로
> 처음부터 끝까지** 돌리고, **어느 판본인지 판정문에 이름을 적어라.**

CLAUDE.md "숫자 둘을 비교하기 전에" 3번 — *누가 만들었는가(같은 계측기인가)* 를
사후에 확인하는 대신 **사전에 고정**해 버리는 조치다.

## 🔴 창 A 는 **REQ-0116 의 A 팔이 아니다** — 목적이 바뀌었다 (2026-08-16 19:10, 루트 확정)

처음엔 "`busy` 자해 수정의 A/B 중 A 팔"로 잡았다. **그 목적으로는 성립하지 않는다.**

- REQ-0116 이 없앨 수 있는 것은 **순수 자해** 부류뿐인데, 그 부류의 A 실측은 **0건**이다.
- **기저가 0 인 부류에서 3배 감소는 잡을 수 없다.** 검정력 표의 어느 칸도 여기 적용되지 않는다.
- λ=3.02/h 는 **전체 링크 끊김**이다. λ_전체로 계획을 세우고 λ_자해로 판정하려던 것이 오류였다
  (monitor 가 근거를 제공했고 monitor 가 깨서 올렸다. 루트가 사용자에게 정정함).

**바뀐 목적**: **리셋 기전의 특성 파악** — 리셋계열 λ 측정 · 갈래 비율(순수자해/리셋계열/혼합) 가르기.
8시간 연장은 **이 목적에서 여전히 옳다.**

⚠ **판정문에 "이 창은 REQ-0116 의 A 팔이 아니다"를 반드시 적어라.** 안 적으면 다음 사람이 A/B 표로 읽는다.
⚠ **판정은 3칸으로 낸다**(순수 자해 / 리셋계열 / 혼합). 2칸으로 보고하면 결정하는 쪽이
   내가 오늘 두 번 밟은 실수를 그대로 이어받는다.

## 창 A

| 항목 | 값 | 근거 |
|---|---|---|
| 시작 | **2026-08-16 17:53:07** | 서버 인스턴스 pid 75781 기동 (`=== INSTANCE logfmt=3`) |
| 종료 예정 | **~2026-08-16 21:53** | 루트 지시 (4시간) |
| 서버 빌드 | `337c926-dirty(REQ0118-A+F,logfmt3)` | 서버 로그 INSTANCE 줄 |
| 칩 펌웨어 | `조별과제샘플/client.ino` 계열 | REQ-0109. **REQ-0116 자해 수정 미반영** |
| 서버 로그 | `/Users/idong-u/parking-logs/parking-server.log` | 계약 v0.1 |
| 시리얼 | `/Users/idong-u/learn/monitor/serial-newbase.log` | tap pid 79289 |

⚠ **A 시작 직후 90초(17:53:07~17:54:37)의 접속은 제외 대상**이다 — 서버가 없던 17초 동안
장치가 0.90초 주기로 두드린 것이 받아들여진 것이다(LEDGER 3.5). 도구가 자동 제외하고 찍는다.
실제 제외 1건: `17:53:08 uptime 3180`.

## 동결 시각·해시

동결 2026-08-16 18:38 (A 시작 +45분). **A 의 첫 45분도 이 판본으로 측정됐다** —
동결 전에 `monitor/` 원본으로 돌린 집계와 이 사본은 이 시점에 바이트 동일하다.

```
399061cd4e462206bb8661b6ebdc121d05837f2a39fb13462f160766f7086113  busy_runs.py
44ec5783f5d5f1260fa4d5af3f4d26d61a9c953786544500d9bff7a604cb1bb3  collapse_probe.py
74bb9155ea7c7785bdb79ff4ceb34e6debfefa71847ba2a9d01a87adb6a03fd9  link_drop_probe.py
d0cc4be2091774caee0ee86b5ce128ab231d90c3f2ea580bce8e342c54cb559d  soak_stats.py
18d6d1256d2251bc351dafc5ce31d9e21818b35f34e41c9b5c2fed6242d6e8d9  mech_split.py
```

### mech_split.py — 나중에 들어왔고, 한 번 교체됐다

**들어온 시각** 2026-08-16 18:52 (A 시작 +59분). 서버 로그가 아니라 **시리얼 로그**를 읽어
링크 끊김을 기전별로 가른다. 위 넷과 원본이 다르므로 별도 도구다.

⚠ **18:57 에 한 번 교체했다. 그 사유를 남긴다** — 판본 관리의 요점이 이것이다.

- 최초판은 사건을 `자해`/`리셋` **둘 중 하나로 강제**했다.
- 그 판으로 재 보니 `17:41:05` 이 `자해`로 떨어졌는데, **원문에는 IP 소실 표지도 같이 있었다.**
  라벨을 강제하는 바람에 증거 하나가 지워졌다.
- 그리고 `18:48:12` 리셋은 `AT+CIFSR` **응답 자체가 가비지**라 `0.0.0.0` 문자열이 안 찍혔다.
  → **`0.0.0.0` 은 정밀하지만 완전하지 않다.** 펌웨어 자신의 결론 줄
  `CIFSR 3회에도 IP 가 없다` 를 OR 로 더했다(UART 가 손상돼도 이 줄은 온전하다).
- 교체판은 라벨을 강제하지 않고 **표지를 그대로 낸다.** `AB.혼합` 칸이 새로 생겼다.

**교체 시점에 A 판정은 아직 하나도 나오지 않았다.** 그래서 "한 판본으로 처음부터 끝까지"는
깨지지 않는다 — 깨졌다면 교체 대신 최초판을 그대로 썼어야 했다. 판정이 나온 뒤였다면
교체하지 않고 **판정문에 결함을 적었을 것**이다.

**교정 통과 기록**(교체판 기준, 구간 16:59:54~18:58):
사건 6건 · 순수 자해 1 · 리셋계열 4 · 혼합 1 · 미상 0.
손으로 grep 해서 낸 결과와 대조했고, 배너 6건 중 `16:59:57`(우리 DTR 리셋)만
"짝 없음 = 정상 부팅"으로 분리되는 것까지 확인했다.

**자정 넘김 시험 통과**: `monitor/fixture-midnight.log`(합성 픽스처, 머리말에 실물 아님을 명시)로
`00:14`·`01:40` 이 **08-17** 로 붙는 것을 확인했다. A 는 01:53 에 끝나므로 이 경로가 반드시 필요하다.
이것이 없으면 자정 이후 사건이 창 앞으로 밀려 **조용히 0** 이 된다(LEDGER 2.2).

전부 표준 라이브러리만 쓴다(`argparse·json·re·sys·os·datetime`) — 프로젝트 내부 import 없음.
따라서 이 4개 파일만으로 재현이 닫힌다. **의존 누락으로 나중에 안 돌 위험이 없다.**

## 쓰는 법 (21:53 판정 때)

```
python3 monitor/frozen-A/link_drop_probe.py ~/parking-logs/parking-server.log \
        2026-08-16T17:53:07 <A 종료 ISO>
python3 monitor/frozen-A/soak_stats.py --log ~/parking-logs/parking-server.log \
        --base-date 2026-08-16 --now <현재 ISO> \
        --win "A=2026-08-16T17:54:37..<A 종료 ISO>"
```

⚠ 끝 시각에 **미래를 넣지 마라**(LEDGER 1.2 — 6.99h→0.09h, 78배 희석 사고).
도구가 분모를 자료 끝으로 보정하고 그 사실을 찍지만, 애초에 넣지 않는 것이 맞다.

## 동작 확인 (동결 직후 시험)

`monitor/out-A-link-frozen.txt` — 18:38 시점 A 부분집계로 정상 실행 확인(rc=0).
그 시점 A 누적: S프레임 2,346 · 재부팅 0 · 재부팅없는 재연결 1(18:24:58) · 서버기동직후 제외 1.
**표본 0.7시간이다. 판정이 아니라 도구가 돈다는 증명일 뿐이다.**

## 해제 조건

A 창이 닫히고 판정문이 나온 뒤. 그때까지 새 파서는 **A 이후 구간에만** 쓴다.
