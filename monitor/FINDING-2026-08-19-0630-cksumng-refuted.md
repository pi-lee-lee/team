> # 🔴 **이 문서를 읽기 전에 — 후보 목록을 볼 때**
> ### **남은 후보가 하나뿐으로 보이면 그건 결론이 아니라 *목록이 짧은 것* 이다.**
> **`❌` 가 늘수록 남은 것이 참처럼 보이는 압력이 커진다.**
> ⚠ **오늘 나도 socket 도 정확히 그것으로 틀렸다** — 배제를 쌓고 남은 하나를 참으로 만들었다.
> **여기 `❌` 다섯이 붙어 있다. 지금이 그 함정이 가장 센 때다.**

# 🔴🔴 반증 · **"hex 가 `cksumng` 을 4.3배 낮췄다"는 틀렸다 — 창 J 가 이상치였다** (2026-08-19)

## 1. 같은 밀도의 세 번째 점이 나왔다
```
창       cksumng ÷ 하행도착        하행 밀도        판본
창 I     **3.73%** (150/4,017)     171.7줄/분      hex 이전
창 J     **0.87%** ( 33/3,783)     175.1줄/분      hex 이후          ← 내가 "4.3배 개선"이라 쓴 창
창 M     **3.99%** (117/2,934)     174.1줄/분      hex + n=12 + O*   🔴 **다시 올랐다**
```
> ### **밀도가 셋 다 171~175줄/분으로 사실상 같다. 그런데 3.73 → 0.87 → 3.99 다.**
> ### **`hex` 는 창 J 와 창 M 에 *둘 다* 들어 있다. 그러므로 hex 로는 설명되지 않는다.**

## 2. 🔴 철회한다
`JUDGMENT-2026-08-18-winJ.md` §3 의 다음 진술을 **철회한다:**
> ~~*"밀도가 거의 같은데 손상률이 4.3배 낮다. hex 축 귀속 근거가 지금까지 중 가장 강하다."*~~

**틀린 이유**: **두 점(창 I·J)으로 방향을 정했다.** **세 번째 점이 반대로 갔다.**
⚠ **그때 내가 스스로 적어 둔 경고가 이것이었다:**
> *"같은 밀도에서 두 판본을 비교해야 하는데 밀도 통제는 **우연** 이었다. 재현 방법이 없다."*
🔑 **한계를 적어 놓고도 결론 문장은 강하게 썼다.** **경고를 적는 것과 결론을 그만큼 약하게 쓰는 것은 다른 일이다.**

## 3. 그러면 창 J 는 왜 낮았나 — **모른다**
```
창 J 와 창 M 의 차이 : n 10→12 · 가상 O* 모듈 · 서버(D/Q → G 명령) · web 150ms 티커
```
**넷 다 `cksumng` 을 *올릴* 이유는 되어도 창 J 를 *낮출* 이유는 안 된다**(창 J 가 더 단순한 판이다).
🔴 **`창 J 가 왜 낮았나` 는 미해결로 남긴다.** ⚠ **"우연히 낮았다"도 답이 아니다 — 33/3,783 은 작은 표본이 아니다.**
🔮 **후보(전부 미검증)**: 그 창의 주입 회차 구성 · rid 자릿수 분포 · 그날 무선 환경.

## 4. ✅ 손상 유형은 창 I 와 같은 모양이다
```
창 M : C 잘림 43 · D 조각 38 · A 하행 2줄 병합 21 · B 우리 에코와 병합 15
창 I : C 잘림 57 · D 조각 52 · A 병합 29 · B 에코 병합 12   (분모가 더 큼)
```
**비율이 비슷하다 — 줄 조립 실패와 바이트 탈락이 둘 다, 대략 2:1.** **새 기전이 아니다.**

## 5. 🔑 이 창에서 **안 흔들린 것**
```
✅ `>=64B` 647거래 → **★★T2 0** · ★T1 0 · stuck 0 · resync 0
✅ pendfill 1,487 · penddrop **0** · bigfill 626 · bigdrop **0**   (분모가 크다)
✅ 진입률 76.9% — 창 J 87.0% · 창 I 92.1% 와 같은 대역
```
**`SEND OK` 실종 축은 세 창 연속으로 0 이다. 그 결론은 그대로 선다.**
🔴 **`cksumng` 축만 무너졌다** — **두 축을 한 문장으로 묶지 않은 것이 이번에 값을 했다.**
