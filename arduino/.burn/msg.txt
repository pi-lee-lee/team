초음파 블로킹 예산 — 두 번째 상한을 찾았다 (시험 [45])

루트: "센서가 여럿이면 25ms × N. 상한을 계산해라" → 계산이 아니라 실측했다.

## 🔴 RAM 말고 두 번째 상한이 있었다

게이트는 **함수마다** 따로 만료되고, 부팅 직후에는 전부 만료 상태라
첫 `readSensors()` 에서 N개가 **한꺼번에** 막힌다. 우연이 아니라 늘 일어난다.

```
슬롯당(기본 구성) : pulseIn 6회 · 41ms = 3.4%   ← 계산 예측 3.5% 와 일치
실기 상한 8개 전부 초음파 → 55.7ms · 유입 53B / 링버퍼 64B (83%)
🔴 넘치는 경계 = 10개
→ MODULE_CAP=8 이라 구성으로는 도달 못 한다. 올리면 RAM 이 남아도 넘친다
🔑 타임아웃을 4m 로 두면 경계가 3개로 떨어진다 — 문턱 유도의 두 번째 이유
```

`Module.h` 의 MODULE_CAP 주석에 적었다. 지금까지 그 값의 근거가 RAM 하나였다.

## ⚠ 내 분모가 틀렸다 — 처음엔 FAIL 이 나왔다

```
🔴 첫 판 : MODULE_CAP (=13, 시험이 지형을 넓히려고 올려 둔 이론 상한) → 86B → FAIL
✅ 고침  : SAMPLE_MODULE_CAP (=8, 실기가 컴파일하는 값) → 53B → PASS
```

**실기에서 도달할 수 없는 구성으로 없는 결함을 만들 뻔했다.**
🔑 시험이 지형을 넓히려고 올려 둔 값을 **예산 계산의 분모로 쓰면 안 된다.**
그래서 두 값을 이름으로 갈랐다 — 섞이면 다음 사람이 또 같은 FAIL 을 본다.

## ㉠(슬롯 기반 읽기) 보류 — 조건을 붙였다

3.4% → 0.58% 로 줄지만 둘 다 안전 범위이고, `occMask` 갱신 시점이 바뀌어 축이 는다.
조건: "ssovf 가 실물에서 오르면 즉시. 안 오르면 다른 축이 없는 창에 단독으로."

## ⚠ 곁들여 — 커밋 메시지도 셸 치환을 당한다

이 커밋의 첫 판이 그렇게 깨졌다. `git commit -m "…"` 안의 `(83%)` · `**10개**` 가
zsh 글로브로 해석되어 `no matches found` 가 나고 **그 줄들이 통째로 빠진 채 커밋됐다.**
🔴 오류는 났는데 커밋은 성공했다 — 그래서 안 보였다.
CLAUDE.md 의 "REQ 본문은 `--body-file` 로" 가 **커밋 메시지에도 그대로 적용된다**:
값이 든 메시지는 파일로 쓰고 `git commit -F <파일>` 로 넘긴다.

시험 318 → 321 PASS / 0 FAIL. flash 27,586 불변(주석만 바뀌었다).

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
