// config.h — 손잡이 모음. 포트·타이밍·상한과 **그 값의 근거**.
// ═══════════════════════════════════════════════════════════════════
// 🔴 **단독 컴파일용이 아니다.** `server.cpp` 안 그 자리에 include 된다(파일 스코프).
//
// 🔑 **여기 있는 것은 숫자가 아니라 *종속이 붙은* 숫자다.** 값만 옮겨 적으면 종속이 떨어진다.
//   예: `TAKEOVER_GRACE_MS` 는 **IP 가 다를 때만** 쓰인다. 그 종속을 빼고 "정상 재접속 문턱"으로
//   읽으면 **0초 재접속을 막는 값**이 된다.
//
// 🔴 값을 고치려면 **그 위 주석을 먼저 읽어라.** 그리고 **유도값(`= 2 * DOWN_SLOT_MS` 꼴)을
//   리터럴로 바꾸지 마라** — 바꾸는 순간 상위 값이 움직여도 안 따라오고, 그 어긋남은 조용하다.
// ═══════════════════════════════════════════════════════════════════

// ---------------------------------------------------------------- 포트
// ⚠ `dev_server` 는 **다른 값**을 쓴다(9900/9991/5500) — 그래야 손잡이 없이 둘이 동시에 돈다.
// ⚠ 장치(`조별과제샘플/ardu/Config.h` 의 `SERVER_PORT`)도 같아야 한다. **바꾸면 굽기가 필요하다.**
static const int  PORT_ARDUINO   = 8888;
static const int  PORT_HTTP      = 9990;
static const int  PORT_PHONE     = 8911;    // digitcam 폰(카메라) 수신
// 폰 프레임은 JSON 이라 아두이노 라인(64B)보다 길다.
static const size_t MAX_PHONE_LINE = 1024;
static const size_t MAX_PLATE_BYTES = 32;   // 번호판 저장 상한(신형 10B, 여유 포함)

// ---------------------------------------------------------------- 전선 기본
// 명세값은 64 다. `--max-line N` 은 **시험 인스턴스 전용 계측기 확장**이지 명세 변경이 아니다.
// 🔴 왜 필요한가: `AT+CIPSEND` 의 실제 최대 길이를 재려면 장치가 64B 를 넘겨 보내야 하는데,
//   서버가 **체크섬 검사 전에** `과길이` 로 버리면 **AT 잘림이 아니라 내 상수를 재게 된다.**
static int        MAX_LINE       = 64;      // §2.1 (기본값. --max-line 으로만 바뀐다)
static const int  ACK_MAX_TRIES  = 3;       // 최초 + 재전송 2회
static const int  OFFLINE_MS     = 3500;    // §3.4
static const int  SELECT_TICK_MS = 200;     // 타이머를 돌리기 위한 select 최대 대기

// ---------------------------------------------------------------- 하행 슬롯(반송파)
//   정본: `docs/net/DESIGN-server-slot-queue.md`
// 🔑 **이벤트가 전송을 만들지 않는다.** 명령은 큐에 담기고 **장치 프레임이 도착한 순간**
//   (= 내 창의 시작) 큐 전부가 **한 거래로 묶여** 나간다. 창 시작이 시계가 아니라 사건이므로
//   장치가 느려지면 내 창도 같이 밀린다 — **자기교정된다.**
static const int  DOWN_SLOT_MS       = 1200;  // 슬롯 주기(사용자 확정: 1.2초)
static const int  DOWN_WIN_MS        = 600;   // 내 창의 이론 폭(0.6초). RTT 를 여기서 깎는다
static const int  DOWN_WIN_MARGIN_MS = 50;    // 여유 — 내 처리시간·타이머 해상도
static const int  DOWN_WIN_MIN_MS    = 150;   // W_srv 하한. RTT 가 튀어도 창이 0 이 되면 안 된다
// `S` 가 안 오면 창이 영영 안 열린다 → 포기하고 쏜다.
// 🔴 **`OFFLINE_MS`(3500)보다 작아야 한다** — 그 뒤에 쏘면 서버가 이미 "장치 없음"으로 판단한
//   상태에 쏘는 것이다. 2슬롯 = 2400 이 그 조건을 만족한다.
static const int  DOWN_DMAX_MS       = 2 * DOWN_SLOT_MS;
// 한 줄 상한. 장치 `RX_CAP=96` 안에 `"+IPD,<n>:"` 접두(8~9B)가 **포함된다.**
// **배치 전체가 아니라 줄 하나의 제약이다.**
static const int  DOWN_LINE_MAX_B    = 87;

// 🔴 **한 창에 밀어 넣는 바이트 상한.** 제약은 바이트가 아니라 **장치 UART 를 몇 ms 붙잡는가** 다.
// ```
//   장치 송신 종료(이론 최악) = slotStart + due 400 + txTime 468 = slotStart + 868
//   다음 장치 송신            = slotStart + 1200      → 내가 쓸 시간 332ms − d1(망 지연)
//   192B = 192 ÷ 960 B/s + 접두 9B = 209ms  ✅        320B = 342ms  🔴 이미 넘는다
// ```
// ⚠ **종속 셋. 하나라도 바뀌면 다시 계산한다:** ① 960 B/s(9600 baud) ② 장치 `txTime_max`
//   ③ **망 왕복 `d1+d2` — 미측정**(내 요약 `왕복 최대 Nms` 가 그 값을 낸다).
// 🔑 **이 값은 "회선의 한계"가 아니라 "이론 최악을 덮는 값"이다.** 실측 여유는 1152ms 라
//   192B 는 그 18% — 약 20배 보수적이다. **올릴 근거가 생기는 조건 셋을 미리 적어 둔다:**
//   ① `[SLOT] due=` 분포에 400 근처가 없다 ② `resync` 계수가 0 에 가깝다 ③ `왕복 최대` 실기값.
//   **셋이 다 나오면 재계산한다. 그 전에는 이 값으로 간다.**
// → 재빌드 없이 갈아끼우려면 `--down-cap=<B>`.
static int        DOWN_BATCH_CAP_B   = 192;
// 큐 깊이는 **독립 상수가 아니라 유도값이다** — "마감 안에 나갈 수 있는 양".
//   `cap` 이 바뀌면 깊이가 저절로 따라온다. 두 값이 따로 놀면 마감을 넘긴 항목이 큐에 남는데
//   아무도 그것을 설명하지 못한다.
static const int  DOWNQ_WAIT_CAP_MS  = 4 * DOWN_SLOT_MS;   // 4.8초 — 큐 대기 마감(임시)
// 🔴 착지 위상을 **의도적으로 겨냥하는 시험**을 위한 옛 거동(즉시 송신). 하행을 원하는 순간에
//   쏠 수단이 없으면 "착지 타이밍" 물음을 영영 못 가른다. **기본값은 off.**
static bool       DOWN_IMMEDIATE     = false;

// 🔴 **건수 상한 — 바이트와 다른 축이다.** 바이트만으로는 못 막는다: 명령이 작을수록 헐거워진다
//   (9B 명령이면 192B 가 21건을 통과시킨다). **회선은 바이트를 나르지만 장치는 건당 ACK 하나를 만든다.**
// **유도**: 유입 N · 배출 D. 한 슬롯을 놓치면 적체 2N → 2N−D → 3N−2D.
//   2슬롯에 회복하려면 `3N − 2D ≤ 0` → **N ≤ 2D/3** → D=6 이면 **4**.
// 🔴 **유입 = 배출이면 한 번만 밀려도 영구히 못 따라잡는다**(ρ=1). 여유가 필수다.
// ⚠ **부하에서 배출률이 6 밑으로 떨어지는 것이 관측됐다. 4 를 올리지 마라.**
static const int  DEV_ACK_DRAIN_PER_SLOT = 6;   // arduino 확정 · REQ-0206

// 🔴 **장치의 수신 링 크기** — 한 창에 밀어 넣을 수 있는 바이트의 진짜 천장.
//   장치 `loop()` 가 자주 비우면 더 흘릴 수 있지만 **그건 타이밍 종속이지 보장이 아니다.**
// ⚠ 늘리려면 **장치 쪽 링이 먼저 커져야 한다.** 서버에서만 올리면 거짓 보장이 된다.
static const int  DEV_RX_RING_B = 64;
// 🔴 **한 창에 묶을 수 있는 건수.** 위 배출률에서 유도한다.
// ⚠ arduino 쪽 상한 `9` 와 **크기 비교가 성립하지 않는다** — 이쪽은 **비율**(슬롯당)이고
//   저쪽은 **용량**(프레임당)이다. **올리려면 두 정의를 먼저 맞춰라.**
static const int  DOWN_BATCH_MAX_N = (2 * DEV_ACK_DRAIN_PER_SLOT) / 3; // = 4 · 리터럴 아님

// 🔴 **배출 `6` 은 상수가 아니라 함수다**: (BATCH_CAP − S_worst − 1) ÷ (ACK_worst + 1).
//   `S_worst` 는 자리 수 `n` 에 따라 커진다 — n=10 → 60B → 배출 6 · n=15 → 75B → 배출 **5**.
//   그러면 유입 4 가 조용히 1건 초과가 된다.
// 🔑 **서버가 이 값을 다시 계산하지 않는다.** `BATCH_CAP`·`ACK_worst` 는 장치의 값이고
//   **파생값은 원본을 가진 쪽이 계산한다.** 사본을 두면 규칙이 두 곳에 생긴다.
//   → 최종형은 **장치가 등록(`D`)에서 자기 배출률을 선언하는 것**이다.
//   → 그때까지 서버는 **전제가 깨진 것만 감지한다** — 볼 수 있는 것은 `S` 의 길이뿐이고,
//     그것이 이 가정을 넘으면 배출 6 의 전제가 깨진 것이다.
static const int  DEV_S_WORST_ASSUMED_B = 60;   // n=10 기준 · arduino REQ-0206

// ---------------------------------------------------------------- 등록(§5)
// **상수가 아니라 유도값이다.** 접속 → 자기 송신 창 1슬롯 · `S`(승격) 1슬롯 · `D` 1슬롯 = 3슬롯.
static const int  REG_TIMEOUT_MS = 3 * DOWN_SLOT_MS;   // = 3600ms
static const int  REG_Q_MAX      = 3;                  // `Q` 상한 — **비용이 서버에 있어서 서버가 둔다**
static const int  GETMAP_MAX_PER_SEC = 5;   // 화면 하나가 1초에 이보다 자주 물으면 뭔가 잘못된 것이다
static const int  REG_MODS_MAX   = 32;                 // 방어적 상한. 전선 예산은 장치가 더 낮게 건다

// 🔴 **증거 없는 창에 보내는 양.** `S` 가 도착해서 여는 창에는 증거가 있다(장치가 방금 말했다).
//   **창 포기에는 증거가 없다.** 그때 보낼 것은 **판정에 필요한 최소 표본**이고, 링크가 사는지는
//   한 건이면 답이 나온다. 더 보내도 정보는 안 늘고 **노출만 배로 는다.**
// ⚠ **이 값은 유도값이 아니라 정의다** — *최소 표본 = 1* 은 계산 결과가 아니라 탐침의 뜻 자체다.
static const int  DOWN_PROBE_N = 1;

// 🔴🔴 **자리 인수 유예 — `IP 가 다를 때만` 쓰인다. 그 종속을 빼고 읽으면 반드시 틀린다.**
//   *"정상 재접속 문턱"* 으로 읽으면 **`0초` 재접속을 막는 값**이 된다.
//   정상 재접속의 공백은 **최소 0초**다(실측) — 장치가 TCP 를 다시 세울 때 옛 소켓의 마지막
//   프레임 직후에 새 연결이 올 수 있다. **공백만으로 갈랐다면 우리 재접속을 여러 건 막았다.**
// 🔑 **1차 판별자는 시간이 아니라 IP 다.** `devid` 가 고유하지 않은 것이 뿌리이고, 그때 남는
//   유일한 구분자가 커널이 주는 주소다.
//     같은 IP → 같은 장치의 TCP 재접속 → **공백과 무관하게 교체 허용**
//     다른 IP → 두 대다 → 이 유예로 판정
//   하한: 정상 주기(1슬롯)보다 충분히 커야 한다
//   상한: 장치의 자체 재접속(약 8초)보다 작아야 한다 — 안 그러면 자리를 못 비켜 준다
// ⚠ 계약 줄에도 `(IP 다를 때만)` 을 붙여 찍는다. **값만 옮겨 적히는 것을 막으려는 것이다.**
static const int  TAKEOVER_GRACE_MS = 5 * DOWN_SLOT_MS;   // = 6000ms · 다른 IP 가 조용한 자리를 가져갈 때만

// ── ACK 마감 — 🔴 **값이 아니라 유도식이다**
// **유도**: ACK 은 장치의 **다음 송신 창**을 기다린다 → 한 슬롯. 그 창을 **한 번 놓친 경우까지
//   봐준다** → 한 슬롯 더. → `2 × DOWN_SLOT_MS`. 정상 왕복 ≈1230ms 대비 여유 ≈1170ms.
// 🔴 **`rtt_max` 를 쓰지 않는다.** 최대는 이상치를 포함하므로 거기 맞추면 **실패 감지가 그만큼
//   느려진다** — 이 마감이 하려는 일의 반대다.
// 🔑 **리터럴로 두지 않는 이유**: 슬롯 주기가 바뀌면 이 값이 따라 움직여야 한다. 박아 두면
//   `DOWN_SLOT_MS` 를 고친 사람이 이 상수를 잊는다.
static const int  ACK_TIMEOUT_MS = 2 * DOWN_SLOT_MS;   // §7.3 · 유도값(2400ms)

// ---------------------------------------------------------------- rid 폭·격리
//   정본: `docs/net/DESIGN-rid-width-and-quarantine.md`
// **왜 폭을 자르나**: 묶음 이득이 `rid` 자릿수에 걸려 있다.
//   3자리 → ACK 13B → N=3 이면 62B ✅ (대역 밖) · 4자리 → 14B → 65B 🔴 (이득 소멸)
// 단조 증가는 uint16 끝까지 자라므로 **설정을 안 바꿔도 지표가 저절로 움직인다.**
// 그래서 고정하는 것은 "지금 값"이 아니라 **자릿수의 상한**이다.
static const uint16_t RID_SPACE = 1000;    // rid ∈ [0,999] — 최대 3자리
static const uint16_t RID_NONE  = 0xFFFF;  // "발행 못 했다" — RID_SPACE 밖이라 유효값과 안 겹친다
// 🔴 **폭보다 먼저 답해야 하는 것은 재사용 주기다.** 장치는 최근 서로 다른 `rid` 16개를 멱등
//   캐시에 들고, 그 안에서 값이 재등장하면 **명령을 적용하지 않고 옛 ACK 을 재전송한다.**
//   서버는 ACK 을 받으므로 타임아웃도 안 뜬다 — **실패가 성공처럼 보인다.**
// ⚠ `RID_SPACE` 를 내릴 때는 반드시 이 상수를 같이 봐라. **16 이하면 구조적으로 항상 충돌한다.**
static const int  DEV_RID_CACHE_N = 16;    // arduino CACHE_N (그쪽 소스 판독 · 실기 실측 아님)
// 격리 — 해제된 rid 를 얼마나 묵혔다 재사용하나. **리터럴 금지: 상수에서 유도한다.**
//   근거: 서버가 명령을 포기하는 것은 `ACK_MAX_TRIES` 번의 타임아웃을 다 쓴 뒤다. 그보다 늦게
//   오는 ACK 은 재전송 예산 전체보다 늦은 것이므로 없는 것으로 다룬다.
// ⚠ **이건 가정이지 관측이 아니다.** 그래서 `ack_unknown_rid` 를 요약에 낸다 —
//   **가정이 깨지면 그 칸이 오른다.**
static const long long RID_QUARANTINE_MS = (long long)ACK_TIMEOUT_MS * ACK_MAX_TRIES;  // 7200ms
// 🔴 **재시작을 건너 단조성을 유지한다.** 서버가 1부터 다시 시작하면 장치 캐시에 남은 옛 rid 와
//   겹쳐 **명령이 삼켜지고 서버는 ACK 을 받아 성공으로 기록한다.**
// **블록 예약**: 한 번에 이만큼 앞을 예약해 디스크에 적고 그 안에서는 디스크를 안 만진다.
// 🔑 **적는 값이 실제 사용보다 항상 *앞서* 있다** — 갑자기 죽어도 재시작이 방금 쓴 값을 다시
//   내주지 않는다(건너뛸 뿐이고 건너뛰는 것은 무해하다).
// ⚠ 반대로 "쓴 뒤에 적는" 방식이면 크래시 때 **가장 최근에 쓴 값들**을 재발행해
//   장치 캐시와 겹칠 확률이 가장 높은 구간을 정확히 다시 밟는다.
static const long long RID_PERSIST_BLOCK = 256;

// ---------------------------------------------------------------- 관측·로그
static const int  SOAK_REPORT_MS = 60000;   // 소크 관측 주기 보고(REQ-0065)
static const int  LOG_KEEP       = 2;       // §9.1 최신 2건
static const uint64_t WS_MAX_FRAME = 64 * 1024;  // 클라이언트가 선언한 길이의 상한

// ---------------------------------------------------------------- 전송 타임아웃
// 순서를 못 박는다:  한 프레임 ≈80ms  ≪  1000ms  <  `ACK_TIMEOUT_MS`  ≪  `OFFLINE_MS`
//   · `ACK_TIMEOUT_MS` 보다 짧아야 한다 — 길면 send() 에 갇힌 동안 재전송 타이머가 제 시각을
//     놓친다. **타임아웃이 타임아웃을 망친다.**
//   · `OFFLINE_MS` 보다 훨씬 짧아야 한다 — 길면 "장치가 죽었다"를 알아채야 할 시간에
//     서버가 send() 안에 멈춰 있는 셈이다.
// ⚠ 이 값은 `send_raw()` **한 번 전체**의 마감시각으로도 쓴다. `SO_SNDTIMEO` 는 `send()`
//   **한 호출**에만 걸려서, 상대가 조금씩만 빼가면 총 정지 시간이 얼마든지 길어진다.
//   그래서 둘 다 건다 — 소켓 옵션은 한 호출을, 마감시각은 한 프레임 전체를 막는다.
static const int SEND_TIMEOUT_MS = 1000;

// ---------------------------------------------------------------- 좀비 소켓 회수
// 장치가 FIN 없이 죽으면(전원 붕괴·ESP 행) 서버는 그 fd 를 영원히 들고 있는다. 재접속 자체는
// 안 막히므로(새 연결이 옛 소켓을 대체한다) 이건 결함 수리가 아니라 **위생**이다.
//
// ⚠ **두 장치는 중복이 아니다. 잡는 고장이 서로 다르다.**
//   · keepalive(OS) : 상대가 통째로 사라진 경우 — 전원 붕괴·망 분리. ACK 가 아예 안 온다.
//   · 유휴 마감(앱) : 상대 TCP 스택은 살아서 ACK 를 꼬박 보내는데 **앱이 조용한** 경우.
//                     🔴 keepalive 는 이걸 **원리적으로 못 잡는다** — 커널이 대신 응답한다.
//
// 하한 근거 — **"이 소켓 위에서 장치가 다시 말할 수 있는 최장 정체"보다 커야 한다.**
//   장치 사다리: CIPCLOSE 3회 ≈2.5초 · AT+RST 한 사이클 ≈14초 · CWJAP 대기 상한 30초
//   → 최악 약 44초. 60초는 그 위다.
// 상한 — 목적이 fd 회수이므로 길수록 뜻이 준다. 60초면 fd 하나가 최대 1분 낭비다.
// ⚠ **`OFFLINE_MS`(3.5초)에 바로 끊지 않는다.** 그건 "화면에 오프라인이라 쓴다"는 표시 판정이지
//   소켓 수명이 아니다. 거기서 끊으면 잠깐 늦은 프레임까지 죽인다.
//   순서:  3.5초(표시)  ≪  25초(keepalive)  <  60초(유휴 마감)
//
// ⚠⚠ **어느 시계로 재는가가 이 값보다 중요하다.** 유휴는 `ard_last_ms`(체크섬을 통과한 `S`
//   프레임 시각)로 잰다. `sess_last_line_ms`(수신한 모든 줄)로 재면 **AT 잡음이나 깨진 줄을
//   흘리는 장치가 영원히 살아남는다** — 줄은 계속 오니 유휴가 리셋되는데 `device_online()` 은
//   계속 false 다. **그게 정확히 회수해야 할 좀비다.**
static const int ARD_IDLE_CLOSE_MS = 60000;
// 탐지까지 10 + 5×3 = **25초**. 유휴 마감(60초)보다 먼저 걸리게 둔다 — OS 가 잡을 수 있는 고장은
// OS 가 먼저 잡는 편이 낫다(앱이 안 깨어나도 fd 가 돌아온다).
// `KEEPALIVE_IDLE_S` 는 `OFFLINE_MS` 보다 넉넉히 위라 **건강한 1Hz 링크에서는 한 번도 안 쏜다.**
static const int KEEPALIVE_IDLE_S  = 10;
static const int KEEPALIVE_INTVL_S = 5;
static const int KEEPALIVE_CNT     = 3;
// 복구시간 표본 상한 — 중앙값을 내려면 표본을 들고 있어야 하는데 상시가동에서 무한히 쌓을 수
// 없다. 넘으면 **더 담지 않고 그 사실을 요약에 적는다**(조용히 버리지 않는다).
static const size_t RECOV_SAMPLE_MAX = 10000;

// ---------------------------------------------------------------- 다중 노드
// 조원들이 각자 노드를 올린다. 옛 구조는 소켓을 하나만 들어 **두 번째 노드가 첫 번째를 끊었다.**
// **8 의 근거**: 조별과제 규모 4~6대 + 개발 예비 2대. 노드마다 1Hz 하트비트가 오므로 8대면
//   초당 8프레임이고, 그 이상은 단일 스레드 select 루프가 아니라 다른 구조를 논할 규모다.
// 🔑 **상한에 걸리면 새 연결을 거절한다(가장 오래된 것을 쫓아내지 않는다).**
//   살아서 도는 노드를 끊고 정체 모를 새 소켓을 들이는 것은 **확실한 것을 버리고 불확실한 것을
//   얻는 거래**다. 같은 `devid` 의 재접속은 자리를 물려받으므로 거절이 재부팅을 막지도 않는다.
static const size_t MAX_ARD_NODES = 8;

// **id 를 아직 모르는 소켓**의 상한과 마감. 첫 프레임을 받아야 `devid` 를 아는데 소켓은 그 전에
// 이미 존재한다. 그 사이의 소켓을 무한히 받아 주면 그것이 곧 자원 고갈이다.
// **근거**: `OFFLINE_MS`(3.5초) 무프레임이면 "말하지 않는 장치"다. 붙자마자 말을 안 하는 소켓은
//   **명세 자신의 기준으로 이미 죽은 것**이다. 접속 직후 AT 잡음으로 첫 줄이 깨질 수 있어 2배를
//   준다 = 7초. 1Hz 이므로 그 안에 **유효 프레임 기회가 7번** 있다.
// ⚠ **이 시계는 부팅이 아니라 `accept` 에서 시작한다.** 장치는 WiFi 결합·IP 확보를 **끝낸 뒤에야**
//   TCP 를 연다 — 부팅 12초는 **소켓이 존재하지도 않는 구간**이라 이 마감이 볼 수 없다.
//   (실측: accept→devid 확정 최대 1.0초 · 7초 초과 0건. 프레임 없이 마감된 소켓이 다시 말한 것은
//    중앙 404초 뒤 — **7초와 12초 사이에는 아무것도 없다.**)
// 🔴 **줄이지 마라.** 1~2초까지 낮추면 정상 세션을 끊기 시작하고, 끊긴 장치는 **0.90초 주기
//   무백오프**로 즉시 재접속한다 → `수락 → 마감 → 재접속` 사이클을 **서버가 스스로 만들어 낸다.**
static const int UNKNOWN_TIMEOUT_MS = OFFLINE_MS * 2;
static const size_t MAX_UNKNOWN_SOCKS = MAX_ARD_NODES;   // id 없는 소켓이 노드 예산을 넘지 못한다

// 🔴 **기여자가 안 바꾼 예시 `devid`.** 기본값을 안 바꾸면 **모두가 같은 id** 로 붙는다.
//   🔴 혼자 시험할 때는 아무 증상이 없고 **합치는 순간 서로 쫓아낸다** — 결함이 생기는 단계와
//   드러나는 단계가 멀다. 그래서 **로컬 서버가 그 자리에서 말한다.**
// ⚠ **여기는 목록이 맞다**(§"불변식은 범위로" 의 예외) — `P1` 은 형식이 아니라 **우리가 정한
//   특정 값**이고 유효한 기여자 id 와 형식으로 구분되지 않는다. 유한 집합이라 셀 수 있다.
// 🔴 **`client.ino` 의 `#define DEVICE_ID` 기본값이 바뀌면 이 목록도 같이 고쳐라.**
//   그쪽만 바뀌면 **새 기본값을 아무도 안 잡는다** — 조용히 낡는다.
static const char* EXAMPLE_DEVIDS[] = { "P1" };
static const size_t EXAMPLE_DEVIDS_N = sizeof(EXAMPLE_DEVIDS) / sizeof(EXAMPLE_DEVIDS[0]);

static const char* SLOT_ID[10] = {"A1","A2","A3","A4","A5","B1","B2","B3","B4","B5"};
