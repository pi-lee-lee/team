# 개발용 주차 서버 — 이 폴더 **하나만** 있으면 돈다

```
macOS/Linux   c++ -std=c++11 -O2 -o srv server.cpp lot.cpp   →  ./srv
Windows(VS)   dev_server.sln 을 열고 F5              →  콘솔 창이 뜬다
화면          http://127.0.0.1:9900/
```

## 🔴 Windows / Visual Studio — **우리는 빌드해 보지 못했다**

> 사용자: *"만들지 마라. **빌드 못 한다며.**"*
> ### **빌드 못 하면 "된다"고 말할 수 없고, 말할 수 없는 것을 만들면 값이 0 이다.**
> **더 나쁜 것은 그게 *낡을 수 있는 것* 하나를 더 남긴다는 것이다** —
> 아무도 안 도는 프로젝트 파일이 저장소에 있으면 다음 사람이 그걸 근거로
> *"VS 지원이 있다"* 고 읽는다. (`.vcxproj`/`.sln` 을 만들었다가 **지웠다.**)

**그래서 산출물이 아니라 *진단* 만 남긴다. 아래는 전부 잰 값이다.**

### ✅ 잰 것 — 이식성 자체는 이미 있다
```
`_WIN32` 분기 **17곳** (runtime 9 · server.cpp 3 · metrics 2 · nodes/persist/wire 1씩)
POSIX 전용 헤더가 `#ifdef` 밖으로 새는 것 → **0건**
MSVC 에 없는 이름(`ssize_t`·`strcasecmp`·`__attribute__`·`typeof`) → **0건**
시각 : `GetTickCount64` · `GetSystemTimeAsFileTime` 로 이미 갈려 있다
🔑 분기가 **소켓·시각 계층에 갇혀 있다** — 흩어져 있지 않다
```

### 🔴 실제로 잡은 결함 하나 — `Gate::IN` (고쳤다)
```
`<windows.h>` 는 `IN`·`OUT` 을 **빈 매크로**로 정의한다(SAL 주석용)
→ `enum Kind { IN, OUT };` 이 `enum Kind { , };` 로 펼쳐진다
```
**Windows 없이 재현했다** — 매크로를 흉내 내면 어디서든 난다:
```
c++ -fsyntax-only -DIN= -DOUT= -x c++ parking.h
→ parking.h:44: error: expected identifier      ← 고치기 전
→ (통과)                                        ← 고친 뒤
```
🔑 **`parking.h` 가 스스로 `#undef` 한다.** `server.cpp` 쪽에서 걷으면
**다른 포함 순서에서 다시 깨진다** — 기여자의 파일이 그렇다.
> **이름을 가진 헤더가 그 이름을 지킨다.**
⚠ **이 결함은 옛 단일 파일(`vs_win_server.cpp`)에는 없었다** — 조립 API(`parking.h`)가
새로 생기면서 들어왔다. **옛 포팅이 통과했다는 사실이 지금을 보증하지 않는다.**

### 🔴 남은 함정 (우리가 못 재는 것) — 직접 만들 때 쓰라
```
① ✅ **고쳤다 (2026-08-20).** 전에는 `server.cpp` 가 `#include "lot.cpp"` 하는
   **유니티 빌드**여서, VS 가 폴더의 `.cpp` 를 자동 추가하면 `lot.cpp` 가 두 번 컴파일되고
   `buildLot`·`onTick`·`onCmdResult` 가 **중복 정의**됐다 → `LNK2005`
   ✅ 훅 셋의 **선언을 `parking.h` 로** 올리고 `#include "lot.cpp"` 를 뺐다.
     `lot.cpp` 는 이제 **진짜 번역 단위**다 →
     🔑 **폴더의 `.cpp` 를 전부 컴파일하는 것이 이제 *정답* 이다**
   ⚠ macOS 에서 그 오류를 **재현하고 고쳤다**:
     `c++ -c lot.cpp && c++ -c server.cpp && c++ lot.o server.o`
     → 고치기 전 `duplicate symbol` 3건 · 고친 뒤 **링크 성공**
② 🔴 **`/utf-8` 을 반드시 준다.** 이 소스는 BOM 없는 UTF-8 이고 안내 문구가 한글이다.
   빼면 CP949 로 읽어 **문자열이 깨지는데 컴파일은 통과한다** —
   **빌드 로그로 못 잡고 브라우저 화면에서 처음 보인다**
③ 링크에 **`ws2_32.lib`**
④ `/std:c++14` 이상 (코드는 C++11 이다)
```
🔴 **빌드 오류가 나면 `파일:줄`과 오류 코드를 알려 달라.**
`LNK2005` 면 ①이고, `C####` 면 다른 것이다 — **한 글자로 갈린다.**

---

## 고칠 파일은 **`lot.cpp` 하나다**

```cpp
lot.spot("A1").sensor("P1","A1").actuator("P1","LD");   // 조립
lot.label("P1","LD","안내등");                           // 표시 이름
lot.control("P1","LD").toggle();                         // 화면 조작 UI
```
설명은 같은 폴더의 `GUIDE-sample.md` 에 있다. **이 파일은 따라 치는 것이고 그 문서는 읽는 것이다.**

### 다 만들면 운영으로 옮긴다
`lot.cpp` **하나만** `조별과제샘플/server/` 로 복사하면 된다 — **그것이 유일한 이음매다.**
```
sh net/sync_check.sh   → 두 트리 모두에서 lot.cpp 가 컴파일되는가
```
🔑 공개 API(`parking.h`)가 갈리면 **여기서 빨강**이 난다. 사람이 안 세도 된다.

---

## 포트 — 운영과 **일부러** 다르다

```
dev  : 웹 9900 · 아두이노 9991 · 카메라 5500     ← 기본값. 손잡이 없이 그냥 뜬다
운영 : 웹 9990 · 아두이노 8888 · 카메라 8911
```
🔴 **근거는 실패 방향의 비대칭이다:**
```
dev 값으로 운영에 쓰면  → **안 붙는다.** 즉시 드러난다
운영 값으로 dev 에 쓰면 → 🔴 **운영에 붙는다.** devid 가 겹치면 남의 노드를 쫓아내고,
                          **로컬에서는 멀쩡해 보인다**
```
> **틀렸을 때 조용한 쪽을 기본값으로 두지 마라.**

바꾸려면: `./srv --port-web=9901 --port-ardu=9992 --port-cam=5501`
⚠ 셋이 서로 겹치면 **어느 둘인지 이름으로 말하고 안 뜬다.**

### 아두이노도 같이 맞춰야 한다
`조별과제샘플/ardu/Config.h` 의 **`SERVER_IP` 와 `SERVER_PORT` 를 한 벌로** 본다 —
포트만 고치고 IP 를 안 고쳐서 헤매는 일이 실제로 있었다.

---

## 🔴 운영판에서 **뺀 것** (있었는데 없앤 것)

| 뺀 것 | 왜 |
|---|---|
| 자가검증 5파일 **2,300줄** | 배포 관문이지 디버그 도구가 아니다 |
| 소크 요약 (60초마다 **1,329자** + 종료 총평) | 운영 관측. **콘솔에서 못 읽고** 프레임 로그를 밀어낸다 |
| 기계용 `=== INSTANCE logfmt=…` 경계 줄 | monitor 가 한 파일에 쌓인 인스턴스를 자르려고 읽는 줄. **여기선 자를 것이 없다** |
| 계약값 덤프 2줄 (**289자·188자**) | arduino·monitor 가 계산에 쓴다. 개발자는 안 쓴다 |
| 로그 **파일** (`--log=` · `open_log`) | 출력은 stdout 하나다. 남기려면 `./srv > dev.log` |
| rid 커서 · 노드 대장 영속 | 🔴 `~/parking-logs` 를 **아예 안 건드린다.** 재기동하면 잊는다 — **개발엔 그게 낫다** |
| `exe_path` · `iso8601` · `open_log` · `default_log_path` · `lock_log_exclusive` | 위 것들에 **딸려 죽었다.** 🔑 컴파일러가 `-Wunused-function` 으로 알려 줬다 |

**10,158줄 → 7,681줄 (−24%)**

### ⚠ 그런데 `data_log.json` 은 **아직 쓴다** — 처음에 "영속 전부 뺐다"고 적었다가 고쳤다
```
실행 폴더에 `data_log.json` 이 생긴다 (주차 기록 · 명세 §9)
🔑 그건 **관측이 아니라 제품 기능**이다. `~/parking-logs`(운영 관측)와 다른 것이다
```
> **"전부"라고 쓰기 전에 세라.** 기동 로그가 `data_log.json 이 없어 빈 배열로 만들었다` 라고
> 말하고 있었는데 나는 "영속 전부 뺐다"고 적었다. **한 줄 위에 반증이 떠 있었다.**

## ⚠ **못 뺀 것** (빼려고 봤지만 남긴 것)

```
metrics.h · ledger.h  →  `nodes.h`·`uplink.h` 와 얽혀 있다
```
🔴 빼려면 **전선 코드(상행 파서)를 고쳐야 하고, 그러면 두 트리의 전선이 갈린다.**
**그게 가장 위험한 갈림이다.** → **호출만 끊어 출력을 없앴다.** 코드는 남고 콘솔은 조용하다.

## ⚠ 아직 **안 넣은 것** ("뺐다"가 아니다)

```
index.html — 🔴 web-engineer 가 넣는다. 이 폴더에서 그 파일만 web 소유다
             🔑 **갱신 주체와 소유자를 갈라 놓으면 반드시 낡는다**
             그때까지 `GET /` 는 404 다. 기동 로그가 그렇게 말해 준다
```

---

## 출력은 **stdout · `std::cout`** 이다 (사용자 확정)

파일로 안 쓴다. 개발자가 **눈으로 읽는다.**
그래서 운영판의 200자 넘는 줄들을 걷어냈다 — 지금 최장 줄은 **125자**(구분선)다.

---

## 🔑 세 트리의 차이 — **의도된 것과 낡은 것을 갈라 적는다**

```
server/       운영. **unity 빌드**(`#include "lot.cpp"`) · `selftest_*` **8개를 싣는다**
dev_server/   기여자 배포. **진짜 번역 단위**(VS 가 폴더의 .cpp 를 전부 컴파일한다)
VS_server/    Windows. dev 와 **같은 소스** + VS 프로젝트 파일
```
| 무엇 | 어디 | 왜 — **의도다** |
|---|---|---|
| `selftest_*` 8개 | **server 에만** | 🔑 기여자 배포에 시험을 안 싣는다. 배포 묶음이 작아야 하고, 기여자가 고칠 것도 아니다. ⚠ **낡아서 빠진 것이 아니다** |
| `EXAMPLES.cpp` · `GUIDE-*.md` · `README.md` | **dev·VS 에만** | 기여자가 읽는 것. 운영 트리에 둘 이유가 없다 |
| `index.html` | dev·VS 에만 | **web 소유**. 서버 소스가 아니다 |
| `config.h` 포트 3줄 | server ≠ dev·VS | 동시에 돌려야 한다(운영 8888/9990/8911 · dev 9900/9991/5500) |
| `entry.h` · `listen.h` · `runtime.h` · `serve.h` | server ≠ dev·VS | dev 는 **콘솔 전용**(파일 로그 없음 · 자가검증 없음 · 소크 요약 안 찍음 · 실패를 사람 말로) |
| `parking.h` 훅 선언 · `server.cpp` include | server ≠ dev·VS | `lot.cpp` 가 진짜 번역 단위라서 |
| 🔴 `entry.h` 의 `#include <windows.h>` · `SetConsoleOutputCP(65001)` | **VS 에만** | Windows 콘솔 UTF-8 |
| 🔴 `server.cpp` 의 `#define _CRT_SECURE_NO_WARNINGS` | **VS 에만** | MSVC 경고 |
| `server.slnx` · `.vcxproj` · `.filters` | VS 에만 | VS 빌드 정의 |

> ### 🔴 **위 목록에 없는 차이는 "낡음"이다.** 갱신하는 사람은 `git hash-object` 로 파일별 대조하고
> ### 다른 것만 **내용으로** 반영해라 — **파일 복사 금지**(사용자 확정). 위 셋은 **살려 둔다.**

## ⚠ 이 트리(`VS_server/`)는 **자동 검사 밖이다**

```
net/sync_check.sh  →  `server/` 와 `dev_server/` **둘만** 대조한다. 여기는 안 본다
🔴 즉 이 사본은 **조용히 낡는다.** 낡아도 아무 데서도 빨강이 안 난다
```

**검사에 안 넣은 이유**(값으로 정했다):
```
이 트리에는 **의도된 Windows 전용 차이**가 있다 —
  entry.h   : #include <windows.h> · SetConsoleOutputCP(65001)
  server.cpp: #define _CRT_SECURE_NO_WARNINGS
  그리고 server.vcxproj · server.vcxproj.filters (우리 트리에 없다)
검사를 파일 대조로 걸면 **이 넷이 매번 발화한다** →
🔑 **항상 발화하는 검사는 아무 말도 안 하는 검사다.**
```

**대신 지금 걸려 있는 것 · 갱신하는 사람이 해야 할 것:**
```
✅ EXAMPLES.cpp 는 VS 빌드에 들어 있다(ClCompile) — **거기서 컴파일이 검증된다**
✅ 우리 쪽에서도 그 파일을 c++11/14/17 · -DIN= -DOUT= 로 문법 검사했다
🔴 소스를 갱신할 때는 **`dev_server/` 를 정본으로 삼고 내용을 적용해라**(파일 덮어쓰기 금지).
   위 넷은 이 트리 것이므로 **살려 둬라**
```

⚠ **우리는 Windows 가 없다.** MSVC 로 실제 빌드한 적이 없으므로 *"빌드된다"* 는 **미검증**이다 —
전처리기 수준(빈 매크로 `IN`/`OUT`)까지만 재현해서 확인했다.
