org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

# 이 머신의 기본 java 는 JDK 23 인데 Gradle 8.7 은 JDK 22+ 를 지원하지 않는다.
# 데몬을 AGP 8.6 이 요구하는 JDK 17 로 고정한다. (다른 머신에서는 이 줄을 각자 경로로 바꿔야 한다)
org.gradle.java.home=/Users/idong-u/Library/Java/JavaVirtualMachines/temurin-17.0.13/Contents/Home

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official

# Enabled parallel sync for Gradle 9.4+
org.gradle.tooling.parallel=true
