---
id: REQ-0222
title: 🔴 이동 완료 — 망 상수 셋 교체 후 재굽기 (SERVER_IP 192.168.35.21 · SSID SK_WiFiGIGA50DC_2.4G)
from: root
to: arduino-engineer
status: done
created: 2026-08-18T18:54:10+0900
updated: 2026-08-18T18:58:38+0900
files: ["조별과제샘플/client.ino"]
parent: none
---

# REQ-0222 · 🔴 이동 완료 — 망 상수 셋 교체 후 재굽기 (SERVER_IP 192.168.35.21 · SSID SK_WiFiGIGA50DC_2.4G)

**요청자** `root` → **담당** `arduino-engineer`

## 요청 내용

🔴 **위치 이동 완료. 새 망 값이 확정됐다. 즉시 재굽기해라.**

## 사용자가 준 값 — **그대로 옮긴다**

```
SERVER_IP   192.168.35.21
WIFI_SSID   SK_WiFiGIGA50DC_2.4G
WIFI_PASS   2011050796
```

**루트 독립 확인**: `ipconfig getifaddr en0` → `192.168.35.21`. **사용자 진술과 일치한다.**

## 고칠 줄 — 확인해 뒀다

```
client.ino:91  WIFI_SSID    "3F_302"              → "SK_WiFiGIGA50DC_2.4G"
client.ino:92  WIFI_PASS    "0424719222!!"        → "2011050796"
client.ino:97  SERVER_IP    "192.168.0.29"        → "192.168.35.21"
```

⚠ **주석도 같이 고쳐라.** 91번 줄 주석이 *"서브넷도 192.168.0.x 로 바뀌었다"* 인데
이제 **192.168.35.x** 다. 주석이 값과 어긋나면 다음 사람이 어느 쪽을 믿을지 모른다.

🔑 **새 비밀번호에는 `!` 가 없다** — 예전 값의 셸 히스토리 확장 경고는 이번엔 해당 없다.
다만 **SSID 에 `.` 가 있다**(`2.4G`). 문자열 안이라 문제 없지만 오타 확인해라.

## 🔴 이건 **축이 하나인 굽기다 — 다른 것을 같이 넣지 마라**

**망 상수 셋만 바꾼다.** 상행 배치 상한도, 다른 개선도 넣지 마라.
**이유**: 창 H 에서 수정이 먹은 것이 확인됐다(`>=64B 1,025 거래 → T2 0건`).
**그 상태를 새 망에서 그대로 재현하는 것**이 다음 관측의 기준선이고,
**여기에 다른 변경이 섞이면 "망이 바뀌어서인지 코드가 바뀌어서인지" 를 못 가른다.**

## 절차

```
1. 세 줄 + 주석 수정
2. 빌드 -DBUILD_ID · --selftest 통과 · 회귀 134
3. monitor 에 굽기 통보 (탭 내리게) → 반납 확인 후 upload
4. hex 대조 (IDENTICAL 확인)
5. 🔴 완료 통보를 monitor 와 루트 **양쪽**에
6. INTERVENTIONS.md 에 사전·사후 기록
```

## 붙었는지 확인하는 법

```
서버 로그에 이 줄이 떠야 한다:
  +ARD 주차 노드 접속 — 세션#N · device=P1 · 상대 192.168.35.xxx:yyyyy
                                              ^^^^^^^^^^^^^^ 새 서브넷
```
🔑 **`상대` 칸이 오늘 socket 이 넣은 것이다** — 새 망에서 "우리 보드인가"를 첫 줄에서 가른다.
⚠ **서브넷이 `192.168.0.x` 로 나오면 옛 값이 살아 있는 것**이다.

## ⚠ 서버는 어떻게 되나

**socket 이 확인 중이지만**, `0.0.0.0` 바인딩이면 **PC IP 가 바뀌어도 서버는 그대로 돈다.**
다만 **PC 가 망을 옮겼으므로 서버를 한 번 재기동하는 편이 안전하다** — socket 이 판단한다.
**네가 굽기 전에 서버가 새 망에서 살아 있는지 확인해라.** 서버가 죽어 있으면 붙을 곳이 없다.

## 시간

**사용자가 18:00 종료라고 했다. 지금 약 17:50 이다.**
🔴 **굽기까지만 하고, 관측은 다음 세션이다.** 새 창을 열려고 하지 마라 —
**반쯤 관측하다 끊긴 창이 가장 나쁘다.** 붙는 것까지 확인하면 오늘 몫은 끝이다.

## 왜 필요한가

WIFI_SSID·WIFI_PASS·SERVER_IP 가 컴파일 상수라 망이 바뀌면 재굽기 없이는 장치가 서버에 못 붙는다. 루트가 ipconfig 로 PC IP 192.168.35.21 을 독립 확인했다

## 대상 파일

- `조별과제샘플/client.ino`
## 완료 기준

① client.ino 91·92·97 세 줄과 주석 수정 ② 빌드·selftest·회귀 통과 ③ monitor 통보 후 굽기 ④ hex 대조 IDENTICAL ⑤ 서버 로그의 +ARD 상대 IP 가 192.168.35.x 로 뜨는 것 확인 ⑥ 완료 통보 monitor·루트 양쪽 ⑦ 축은 망 상수 셋뿐 — 다른 변경 금지 ⑧ 관측은 다음 세션

---

## 처리 결과

<!-- 담당자가 여기에 적는다. 처리 후 반드시:
     team/bin/req.sh done REQ-0222 --by arduino-engineer --note "<한 줄 요약>" -->

### ✅ 완료 — 망 상수 셋 교체 후 재굽기

```
업로드 완료   2026-08-18 **18:55:53**   (포트 /dev/cu.usbmodem21201)
칩 대조       **IDENTICAL · 차이 0**    chip-net35.hex = net35/client.ino.hex
칩 판본       커밋 **4f1ee17** · 23916B(74퍼센트) · RAM 1170B(57퍼센트)
회귀          134 PASS / 0 FAIL
굽기 기록     커밋 b13a89a · arduino/INTERVENTIONS.md (사전 18:55:38 · 사후)
```

**바뀐 것 — 요구대로 망 상수 셋뿐이다.** `git diff` 로 증명했다(바뀐 줄 3개 전부 `#define`):
```
91  WIFI_SSID  "3F_302"       → "SK_WiFiGIGA50DC_2.4G"   + 주석의 서브넷 표기 갱신
92  WIFI_PASS  "0424719222!!" → "2011050796"             + `!` 경고 주석 갱신
97  SERVER_IP  "192.168.0.29" → "192.168.35.21"
```
**배치 상한 등 다른 개선은 안 넣었다** — 창 H 판정 상태를 새 망에 그대로 옮기는 것이 목적이다.

⚠ **RAM 이 1156→1170B(+14) 늘었다. SSID 가 6자→20자가 된 것과 정확히 맞는다**
(`client.ino:2606` 의 `strstr(s, WIFI_SSID)` 가 RAM 문자열을 쓴다). **예상 밖 증가가 아니다.**

**굽기 전 확인**: `nc -z 192.168.35.21 9991` 성공 — 서버가 새 망에서 살아 있다.

### 🔴 포트 이름이 바뀌어 있었다 — 다음 사람이 밟을 자리

```
창 G·H 까지 : /dev/cu.usbmodem1101   ← 이동 후 **존재하지 않는다**
지금        : /dev/cu.usbmodem21201
```
monitor 가 먼저 잡았고 루트도 독립 확인했다. **굽기 전에 `arduino-cli board list` 로
`arduino:avr:uno` 인지 확인하고 쏘았다.**
> ⚠ **`docs/arduino/RECOVERY-after-move.md` 가 이 함정을 경고하고 있었는데
> 경고한 그 자리에서 실제로 밟혔다.** 문서에 박혀 있던 옛 값을
> `BURN-CHECKLIST.md`·`LEDGER.md` 에서 갱신했다(LEDGER 는 이력이라 지우지 않고 이어 적었다).

### 🔴 미완 — 완료 기준 ⑤(서버 로그 상대 IP)는 **내가 확인 못 한다**

**서버 로그가 프로젝트 트리 밖이라 내 도구로 안 보인다**(최근 25분 변경 파일 0건).
**socket 에 확인 요청을 보냈다** — `18:55:53` 이후 `+ARD` 의 상대 IP 서브넷이 `192.168.35.x` 인지.

### ⚠ 순서 문제 — 루트의 서버 재기동 지시가 굽기 **뒤에** 도착했다

루트가 *"socket 재기동을 먼저 기다려라"* 를 보냈으나 **이미 구운 뒤였다.**
**장치가 옛 서버 인스턴스에 먼저 붙었을 수 있다** → 첫 접속 로그가 두 번 생길 수 있다.
> **무효가 아니라 표시가 필요한 것이다: 재기동 후 첫 `+ARD` 를 기준선으로 삼고
> 그 앞 것은 "재기동 전"으로 표시해 남긴다.** 재굽기는 불필요하다 — 펌웨어는 이미 새 망 값이다.

⚠ **교훈**: 사전 통보는 했지만 **통보와 굽기 사이가 너무 짧아 루트가 막을 시간이 없었다.**
**통보가 승인 대기가 아니어도 막을 틈은 있어야 한다.**

### 처리 완료 · arduino-engineer · 2026-08-18T18:58:38+0900

망 상수 셋 교체 후 재굽기 완료(18:55:53) · 칩=4f1ee17 IDENTICAL · 포트가 21201 로 바뀌어 있었다 · ⚠ 완료기준⑤(서버 로그 상대 IP)는 socket 확인 대기



---

### ✅ 완료 기준 ⑤ 충족 — 장치가 새 망으로 붙었다 (2026-08-18 19:09)

```
+ARD 주차 노드 접속 · 상대 **192.168.35.79**      ← 🔑 새 서브넷. PC 자신(.21)이 아니다
세션#7 : 19:09:01 기준 **2분 45초 유지 · 프레임 282**
```
> ### **망 상수 교체가 실제로 먹었다. 이동 작업 완료.**

⚠ **이동 이후의 세션·계수·끊김 자료는 사용자 지시로 전부 버린다** —
*"경계 짓지마라 다 버려라. 링크 상태만 확인하고 정상 유지되면 정상으로 판단하라.
물리적 이동환경이 있었다."*
**물리적 이동이라 변수가 너무 많고, 가르는 비용이 그 자료의 값보다 크다.**
**판정 기준은 하나다: 링크가 정상 유지되면 정상.**

**남는 것**: 굽기 성공(hex IDENTICAL · ELF 문자열 확인) · 망 상수 교체 확정 · 접속 확인.
