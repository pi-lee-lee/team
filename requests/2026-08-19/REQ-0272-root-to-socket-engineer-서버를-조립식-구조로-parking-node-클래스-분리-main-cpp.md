---
id: REQ-0272
title: 🔴 서버를 조립식 구조로 — Parking·Node 클래스 분리 + main.cpp 분리 · 배치 API (사용자 지시)
from: root
to: socket-engineer
status: done
created: 2026-08-19T13:27:38+0900
updated: 2026-08-20T13:55:03+0900
files: ["조별과제샘플/server/server.cpp"]
parent: none
---

# REQ-0272 · 🔴 서버를 조립식 구조로 — Parking·Node 클래스 분리 + main.cpp 분리 · 배치 API (사용자 지시)

**요청자** `root` → **담당** `socket-engineer`


## 🔴🔴 **최우선 요구 — 사용 코드가 간단해야 한다** (사용자 강조)

> 사용자 원문: *"그리고 **사용 코드 구조가 간단하게 나와야 함을 꼭! 명시**해달라."*

> ### **이것이 이 작업의 *합격 기준* 이다. 파일이 몇 개인지·줄이 몇 줄인지는 부차적이다.**
> ### **바깥(쓰는 쪽)이 단순해지지 않으면 이 리팩터링은 실패다 — 안이 아무리 깔끔해도.**

**🔑 검증 방법 — 말로 하지 말고 *쓰는 코드를 실제로 써 보여라*:**
```
① 명세에 **사용 예시**를 넣어라. 그 예시가 곧 합격 판정이다
② 지금 코드로 같은 일을 하려면 몇 줄인지 **나란히** 적어라 (전/후)
③ 🔴 **호출자가 몰라도 되는 것을 세라** — 순서·의존·초기화·널 검사.
   그것이 여전히 호출자 몫이면 **자동화가 안 된 것**이다
```

**설계 제약:**
```
✅ 기본값으로 동작한다        — 아무것도 안 정해도 지금 것이 나온다
✅ 어려운 것은 함수가 대신 한다 — 호출자가 순서·의존을 몰라도 되게
✅ 이름만 보고 무엇인지 안다   — 주석을 읽어야 쓸 수 있으면 안 단순한 것이다
🔴 잘못 쓰면 **컴파일이나 기동에서 막힌다** — 못 쓰게 만드는 것이 못 쓰는 것보다 낫다
```
⚠ **"안이 복잡해도 바깥이 단순하면 성공"이다.** 복잡도를 없애는 것이 아니라 **옮기는 것**이고,
**옮길 곳은 함수 안**이다. 사용자가 *"난이도 높은 설정은 최대한 자동화(함수 내에서 수행되도록)"* 라고
말한 것이 정확히 그것이다.

🔑 **그리고 이건 §"계약에 빈 자리가 있으면 받는 쪽이 메운다" 의 API 판본이다** —
**기본값이 없으면 호출자가 지어낸다.** 기본값을 주거나, 안 주면 강제해라.

### 🔴🔴 그리고 **은닉화가 그 수단이다** (사용자 재강조)

> *"**복잡한 구조는 기본적으로 은닉화**하여 개발자(사용자)에게는 **간단한 구조로 이용 가능**해야 한다.
> **매우 중요하다.**"*

> ### **드러난 표면이 곧 계약이다. 표면을 줄이는 것이 이 작업의 본체다.**

```
🔴 밖으로 내보내지 마라 : 내부 상태 · 호출 순서 · 초기화 의존 · 자료구조 · 널 검사
✅ 밖에 두는 것         : **하고 싶은 일을 그대로 적은 이름** 하나

판정 : 헤더(또는 공개 API)만 읽고 쓸 수 있는가?
       🔴 `.cpp`/구현을 열어야 쓸 수 있으면 **은닉이 안 된 것**이다
```

**구체적으로:**
```
· 공개 표면을 **세라.** 지금 몇 개이고 뒤에 몇 개인가 — 수가 줄어야 한다
· 내부 전용은 익명 namespace / static / private 로 내려라
· 🔴 "이 함수를 부르기 전에 저것을 먼저 불러야 한다" 가 **하나도 없어야** 한다
     남아 있으면 그 순서를 **함수 안으로 넣어라** — 그게 사용자가 말한 자동화다
· 실패는 **밖으로 새지 않게** 하되, 조용히 삼키지도 마라 — 우리가 계속 잡아 온 그 자리다
```
⚠ **은닉화가 "감춘다"가 아니다.** 값이 왜 그런지는 여전히 로그·계수로 볼 수 있어야 한다.
🔑 **감출 것은 *복잡함* 이고, 드러낼 것은 *상태* 다. 둘을 바꾸지 마라.**

### 🔴🔴 **왜 그래야 하는가 — 사용자가 근거를 줬다. 이게 판정을 날카롭게 만든다**

> *"이 방식을 선호하는 이유는 **간단한 구조로 코딩 시 코드 작성 시점에 해당 코드의 흐름이 보인다.**
> 복잡한 구조의 경우 **프로세스 흐름이 잘 보이지 않는다.**"*

> ### **목적은 "짧게"가 아니라 🔴 "읽으면 *흐름* 이 보이게" 다.**

**그래서 판정이 이렇게 바뀐다:**
```
❌ 나쁜 판정 : 줄 수가 줄었나
✅ 좋은 판정 : **사용 코드를 위에서 아래로 읽으면 무슨 일이 어떤 순서로 일어나는지 보이는가**
```

🔴 **그리고 이것이 어떤 "단순함"을 *탈락* 시킨다 — 짧아 보여도 흐름을 숨기면 실패다:**
```
❌ 자동 등록·매직 배선 — 한 줄인데 **언제 무엇이 붙는지 안 보인다**
❌ 콜백·이벤트로만 이어진 것 — 호출부에 순서가 없다
❌ 설정 파일이 흐름을 정하는 것 — 코드를 읽어도 흐름을 모른다
❌ 이름이 무슨 일을 하는지 안 말하는 것 — `setup()` · `init()` · `process()`
✅ **하는 일이 순서대로 적혀 있고, 각 줄의 이름이 그 일을 말한다**
```

⚠ **은닉화와 충돌하지 않는다. 갈라 읽어라:**
```
숨긴다  : 그 일을 **어떻게** 하는가 (순서·의존·초기화·자료구조·널 검사)
드러낸다 : 🔴 **무엇을 어떤 순서로** 하는가  ·  지금 상태가 어떤가(로그·계수)
```
🔑 **`함수 안으로 넣어라` 가 `흐름을 감춰라` 가 아니다.**
**한 단계의 *내부* 를 감추고, 단계들의 *나열* 은 호출부에 남긴다.**

**🔑 검증**: 이 코드를 처음 보는 사람에게 사용 예시만 보여 주고
*"이걸 실행하면 무슨 일이 순서대로 일어나나"* 를 물었을 때 **답할 수 있어야 한다.**

## 요청 내용

# 🔴 서버를 **조립식 구조**로 — 클래스 분리 + `main.cpp` 분리 (사용자 지시)

> 사용자 원문: *"서버코드 **parking 클래스와 node 클래스를 별도의 클래스 파일**로 생성하라. server 너무 길다.
> 그리고 **main.cpp 도 따로 빼자 엔트리 포인트를 명확히** 한다. 서버코드 리팩토링을 **내가 지시한 것 외에도
> 진행할 수 있는 것들을** 해달라.
> **최종적으로 원하는 것은 server 에서 클래스와 함수를 이용하여 조립식 구조를 완성하는 것**이다.
> **파킹 자리에 node 의 모듈을 배치하여 해당 파킹자리에서 동작하게 만드는 구조**이다.
> 그리고 **해당 클래스와 함수의 이용법은 쉽게 설정되어야** 한다.
> **난이도 높은 설정은 최대한 자동화(함수 내에서 수행되도록)** 한다."*

## 0. 지금 규모 (루트가 값으로 쟀다)

```
조별과제샘플/server/server.cpp   **6,321 줄 · 414 KB**
  :514  class TeeBuf        :762 struct Node      :822 struct UnknownSock
  :837  struct Zone         :844 struct Slot      :852 struct Pending   :877 struct Conn
  🔴 :890  struct Server  ~  :4808   →  **3,918 줄** ← 덩어리는 여기다
  :6232 int main
기존 헤더 : server_device.h(9K) · server_seam.h(6.6K) · server_wire.h(11.6K · 🔴 고아)
```
**`Node`·`Zone`·`Slot` 구조체 자체는 작다. 문제는 `Server` 안에 전부 들어 있는 것이다.**

## 1. 🔑 목표를 먼저 못 박아라 — **파일 나누기가 목적이 아니다**

> ### **"파킹 자리에 node 의 모듈을 배치하여 그 자리에서 동작하게" 만드는 것이 목적이고,**
> ### **파일 분리는 그 구조가 드러나게 하는 수단이다.**

**그러니 분리 경계를 "줄 수"가 아니라 **"무엇이 무엇을 아는가"** 로 그어라.**
```
Zone(파킹 자리)  이 자리에 어떤 모듈이 배치됐나 · 그 자리의 상태·조작
Node(디바이스)   이 장치가 어떤 모듈을 갖고 있나 · 링크·등록·전선
🔴 둘의 결속      **배치(mapping)** — 지금은 `bind_modules` 하나에 이름 일치로 박혀 있다
```
⚠ **오늘 확정된 것과 맞물린다**: 사용자가 *"주차자리와 디바이스노드는 개별구조다. 연결되지 않는다"* 라고 했다.
**이 리팩터링이 그 문장을 코드 구조로 만드는 일이다.**

## 2. 🔴 방법 — **오늘 우리가 세운 것을 그대로 써라. 이게 이 작업의 핵심이다**

```
① 파일로 떼되 **원래 자리에 `#include`** 한다  →  전처리 결과가 같다
   →  **산출물 `.o` 가 0 차이**  →  **"분리했지만 아무것도 안 바꿨다"를 값으로 증명**
② 그 다음에 실제로 옮기거나 고친다            →  여기서부터 축이 생긴다
```
**arduino 가 펌웨어에서 이 방법으로 846줄을 빼고 `hex` 차이 0 을 얻었다.** 네가 그것을 서버로 일반화했고, **이제 그걸 쓸 자리다.**

🔴 **판별자는 `.o` 다. 실행파일이 아니다.**
```
실행파일 : LC_UUID + ad-hoc 서명으로 **매 링크마다 다르다**(네 실측 49B). 판별자가 아니다
✅ `.o`   : UUID 도 서명도 없다. **다른 경로에서 만들어도 cmp 0** 인 것을 네가 이미 증명했다
```
⚠ **`include` 위치를 원래 자리에서 옮기는 순간 ①이 아니라 ②다.** 그 경계를 지켜라.

## 3. 사용자가 요구한 **API 성질** — 설계 제약이다

> *"이용법은 쉽게 설정되어야 한다. **난이도 높은 설정은 최대한 자동화(함수 내에서 수행되도록)**"*

```
✅ 기본값으로 동작한다 — 아무것도 안 정해도 지금 지형이 나온다
✅ 어려운 것은 **함수가 대신 한다** — 호출자가 순서·의존을 몰라도 되게
✅ 한 줄로 배치된다 : 예) zone("A1").attach(node("P1"), "A1").attach(node("P1"), "B1")
🔴 못 쓰게 만드는 것이 못 쓰는 것보다 낫다 — **잘못된 배치는 컴파일이나 기동에서 막혀야** 한다
```
🔑 **우리 규율 하나를 API 로 옮겨라**: §"계약에 빈 자리가 있으면 받는 쪽이 메운다."
**기본값이 없으면 호출자가 지어낸다.** 그러니 **기본값을 주거나, 안 주면 강제하라.**

## 4. 🔴 이 작업이 건드리는 것 — 먼저 세라

```
· REQ-0263 (노드 통신 경로 추출·잠금)  🔴 **같은 코드를 만진다.** 이 작업에 흡수할지 판단해라
· REQ-0271 (모의/실물 구분 제거)       — 전선 계약 축. 이것과 순서를 정해라
· `server_wire.h` 가 **고아**다(아무도 include 안 함) — 이번에 정리 대상인지 판단
· 자가검증 · 배포 절차서 · 빌드 명령의 경로
```
**어느 것을 이번에 넣고 어느 것을 뺄지 네가 정해서 적어라. 근거와 함께.**

## 5. 순서 — 조각마다 트리가 온전해야 한다

```
① main.cpp 분리        ← 가장 작고 독립적. **여기서 방법(①→②)을 먼저 증명해라**
② Node 분리
③ Zone/Parking 분리
④ 배치(mapping) 구조를 드러낸다  ← **여기가 진짜 작업이다**
⑤ 그 위에 조립 API
```
🔴 **각 조각마다 `.o` 대조를 붙이고, 거동이 바뀌는 조각은 *바뀐다고 미리 적어라*.**
⚠ **반쯤 고친 상태로 커밋하지 마라** — 오늘 네가 ②-a 에서 지킨 그 규율이다.

## 6. 🔴 배포·시점

```
지금 창 S 가 돈다 (기판 교체 뒤 첫 관측 · 13:21~14:21)
✅ 코드·명세 작업은 지금 해도 된다 — 도는 서버를 안 건드린다
🔴 배포는 창 S 판정 뒤. 루트가 시각을 준다
```

## 7. 완료 기준 (1차)

```
① main.cpp 가 분리되고 엔트리 포인트가 명확하다
② Node · Parking(Zone) 이 별도 파일이다
③ 🔴 각 분리마다 **`.o` 대조 결과** — 0 이거나, 다르면 **왜**
④ 조립 API 의 **모양을 명세에 적는다** — 구현 전에 이름을 박아라(web·monitor 가 참조한다)
⑤ selftest 전부 통과 · 무경고
⑥ 4·5번(배치 구조·조립 API)은 1차에서 **설계만** 내도 된다 — 범위를 네가 잘라 적어라
```

## 왜 필요한가

server.cpp 가 6321줄이고 struct Server 혼자 3918줄이라 파킹 자리에 노드 모듈을 배치하는 구조가 코드에 안 드러난다

## 대상 파일

- `조별과제샘플/server/server.cpp`
## 완료 기준

main.cpp·Node·Parking 분리 · 각 분리마다 .o 대조(다르면 왜) · 조립 API 이름을 명세에 먼저 · selftest 통과 · 4·5는 설계만이어도 되되 범위를 잘라 적는다

---

## 처리 결과

<!-- 담당자가 여기에 적는다. 처리 후 반드시:
     team/bin/req.sh done REQ-0272 --by socket-engineer --note "<한 줄 요약>" -->

### 완료 — 2026-08-20 13:53 배포 (`ca7c64e` · pid 91027)

| 완료 기준 | 결과 |
|---|---|
| ① 엔트리 포인트 분리 | `entry.h` 에 `main()`. `server.cpp` 6,321줄 → **19.7KB** |
| ② Node · Parking 별도 파일 | `node.h` · `zone.h` · `nodes.h` · `parking.h` · `lot.h`/`lot.cpp` |
| ③ 분리마다 `.o` 대조 | 앞 단계에서 수행(원장 §98 — 원래 자리에서 `#include` 하면 전처리 결과가 같다) |
| ④ 조립 API 이름을 명세에 먼저 | `docs/net/SPEC-assembly-v2.md` · web 에 REQ-0284 로 발행(닫힘) |
| ⑤ selftest 통과 · 무경고 | **148 pass / 0 fail**. ⚠ `-Wall -Wextra -Wshadow` 경고 30건이 **전부 `selftest_*.h`(시험 코드)** 다 — 운영 헤더는 0건 |
| ⑥ 4·5 는 설계만이어도 됨 | 설계가 아니라 **구현·배포까지 갔다** |

**합격 기준(사용 코드가 간단한가)** — 기여자가 배우는 것이 **다섯**이다:
`spot` · `at` · `parking` · `label` · `module`. `sensor()`/`actuator()`/`gate()`/`Gate` 는 없앴다
(장치가 등록에서 `kind` 첫 글자로 말하므로 조립 표에서 또 가를 이유가 없다).

#### 마무리에서 찾아 고친 것 (`ca7c64e`)
```
🔴 서버가 말하는 문구가 **없는 파일·없는 API** 를 가리켰다
   `센서 이름`→`모듈 이름` · `main.cpp`→`lot.cpp`(그런 파일은 없다) · `sensor(...)`→`module(...)`
🔴 가이드 둘이 **v1 을 가르치고 있었고 배포 폴더 밖에 있었다**
   docs/net/GUIDE-{sample,server-says}.md → 조별과제샘플/dev_server/ 로 이동(복사 아님)
🔴 net/sync_check.sh 의 **음성 대조가 죽어 있었다** — sed 대상 `Spot& actuator` 가 v2 에서 사라져
   아무것도 안 바꾸고 ✅ 를 냈다. 대상을 `Spot& module(` 로 바꾸고 `cmp` 로 "안 바뀌면 경보" 를 넣었다
```

#### 검증 (실행한 값)
```
자가검증 148/0 · 두 트리 컴파일 · sync_check ✅(음성 대조 🔴 rc=1 로 복구 확인)
dev 봉투(mock) : kind "parking"/"area" · grid 1x3 · cells · label · control · zone.completion 없음
운영 : 13:53:19 정지(72151) → 13:53:32 기동(91027 · ca7c64e) → 13:53:35 장치 재등록 n=4
백업 : ~/parking-bin/srv_parking.prev-a8e04c5
```

#### ⚠ 이 REQ 밖으로 남긴 것
```
· 값 에코 `E,<idx>,<val>` — 숫자 확인이 지금 `partial` 까지만. **arduino 굽기 경계 대기**(루트 지시)
· REQ-0248 ③ (cwd 절대경로) — 루트가 "지금 넣지 마라"
· `조별과제샘플/dev_server/index.html` 이 v2 이전 판(07:32) — **web 소유**라 값만 전달했다
```


### 처리 완료 · socket-engineer · 2026-08-20T13:55:03+0900

조립 API v2 구현·배포 완료 (ca7c64e · 13:53:32 · pid 91027) — 기여자가 배우는 것 다섯 · 자가검증 148/0 · 가이드 둘을 v2 로 고쳐 배포 폴더로 이동 · sync_check 음성대조 복구

