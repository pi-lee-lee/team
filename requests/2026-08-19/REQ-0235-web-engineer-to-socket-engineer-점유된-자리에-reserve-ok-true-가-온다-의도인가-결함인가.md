---
id: REQ-0235
title: 점유된 자리에 reserve.ok=true 가 온다 — 의도인가 결함인가
from: web-engineer
to: socket-engineer
status: done
created: 2026-08-19T06:15:10+0900
updated: 2026-08-19T06:20:12+0900
files: ["조별과제샘플/server.cpp (actions 계산)"]
parent: none
---

# REQ-0235 · 점유된 자리에 reserve.ok=true 가 온다 — 의도인가 결함인가

**요청자** `web-engineer` → **담당** `socket-engineer`

## 요청 내용

## 실측

`9900`(`srv_id 7c73c39-…`), 자리 **A3** — 실물 센서가 점유로 보고하는 자리다.

```
state.zones[A3].occupied            = 1        (점유 중)
state.zones[A3].actions.reserve     = {ok:true, reason:null}     ← 🔴 예약을 허용한다
옛 snapshot.slots[A3].occupied      = 1        (같은 값)
```

화면에서 그 자리는 **요약이 "사용 중"인데 예약 버튼이 열려 있다.** 눌러 확인하면
낙관적 UI 의 확인-후 재검증이 막는다(옛 경로는 점유 자리를 예약 대상으로 보지 않는다):

```
"A3 · 그 사이 자리 상태가 바뀌어 예약하지 않았습니다"
```

⚠ **자리 상태는 바뀌지 않았다.** 문구가 원인을 엉뚱한 곳으로 돌린다.

## 물음 — `occupied:1` 인 자리에 `reserve.ok:true` 가 **의도인가**

두 갈래 중 하나일 텐데 내가 정할 수 없다:

**ⓐ 서버 결함** — `actions.reserve` 계산에 점유가 안 들어갔다.
   근거: 옛 계약의 `result=1` 이 **"이미 주차된 자리입니다"** 다. 즉 서버의 예약 처리기는
   점유 자리를 거절한다. 그러면 `actions` 가 `ok:true` 로 열어 주는 것은 **자기 처리기와 모순**이다.
   → 이 경우 `reserve.ok:false` + 사유(예: `occupied`)가 맞고, **내 화면은 고칠 것이 없다.**

**ⓑ 의도된 동작** — "지금 차가 있지만 나중을 위해 예약"이 허용된다.
   → 그러면 **내 화면이 고칠 것이 있다.** 지금은 확인 뒤에 옛 경로가 막아서
   **명령이 아예 안 나간다.** 그 재검증을 새 격자 경로에서 걷어내야 한다.

## 왜 내가 정하지 않고 묻나

**낙관적 UI 의 재검증을 걷어내면 판정자를 서버 하나로 모으는 것**이라 방향은 맞다.
그런데 **ⓐ라면 그 수정이 "곧 거절될 명령을 굳이 보내는" 것**이 되고, ⓑ라면 필요한 수정이다.
🔑 **어느 쪽인지는 `actions.reserve` 를 계산하는 코드가 답한다 — 그건 네 쪽이다.**

⚠ 그리고 **짐작해서 고치면 두 곳에 다른 정의가 생긴다** — 오늘 우리가 반복해서 밟은 자리다.

## 참고 — 이 결함이 왜 이제야 나왔나

앞선 회차들은 **빈 자리(A1)** 를 골라 눌렀다. `A2·A3·B4` 가 점유로 바뀐 뒤(네 hex 해독 수정으로
점유가 옳게 오기 시작한 뒤) 하니스가 **처음 열린 예약 버튼**을 고르면서 A3 에 닿았다.

🔑 **점유가 틀렸을 때는 이 모순이 존재할 수 없었다** — 모든 자리가 비어 보였으니까.
**한 결함을 고치면 그 뒤에 가려져 있던 것이 드러난다.**

## 완료 기준

- `occupied` 인 자리의 `reserve.ok` 가 **의도대로 무엇인지** 명시된다(ⓐ/ⓑ).
- ⓐ면 서버가 `ok:false` + 사유를 주고, 그 사유 코드 이름을 알려 준다(내 `REASON_TEXT`·계열 표에 넣는다).
- ⓑ면 내가 새 격자 경로의 확인-후 재검증을 걷어낸다(그 결정을 이 파일에 남긴다).

## 왜 필요한가

화면 같은 칸에서 요약은 사용 중인데 예약 버튼이 열려 있고, 눌러 확인하면 옛 경로가 막아 잘못된 문구가 뜬다 — 어느 쪽을 고칠지는 actions 계산 코드가 답한다

## 대상 파일

- `조별과제샘플/server.cpp (actions 계산)`
## 완료 기준

occupied 자리의 reserve.ok 가 의도대로 무엇인지 명시된다 · ok:false 라면 사유 코드 이름을 준다 · ok:true 가 의도라면 내가 재검증을 걷어낸다

---

## 처리 결과

<!-- 담당자가 여기에 적는다. 처리 후 반드시:
     team/bin/req.sh done REQ-0235 --by socket-engineer --note "<한 줄 요약>" -->

_(미처리)_

## 처리 결과 (socket-engineer · 2026-08-19)

### 🔴 **ⓐ 다. 내 결함이고 고쳤다. 네 화면은 고칠 것이 없다.**

**근거는 장치 코드다 — 짐작하지 않았다:**
```c
client.ino :  if (occMask & bit) result = 1;      // 이미 점유
```
**그리고 설계 의도가 그 위 주석에 있다:**
> **`occupied=1 ∧ reserved=1` 은 "예약하고 나서 주차한" 성공 상태다.**

🔑 **순서가 정해져 있다. 예약 → 주차이고, 주차 → 예약은 없다.**
**ⓑ("차가 있지만 나중을 위해 예약")는 이 시스템의 의도가 아니다.**

### 고친 것
```json
"reserve": {"ok": false, "reason": "occupied"}      ← 🔴 새 사유 코드
```
**`actions[].reason` 닫힌 집합이 넷에서 다섯이 된다:**
`node_offline` · `node_unregistered` · `module_absent` · `pending` · **`occupied`**

⚠ **키를 빼지 않았다.** 차가 나가면 예약이 가능해지므로 *"뜻이 없다"*(키 부재)가 아니라
*"뜻은 있는데 막혔다"* 다(§6.5 의 존재/부재 규칙). **키를 빼면 화면이 버튼을 영영 안 그리고,
그건 일시 상태를 영구로 답하는 것이다.**

⚠ **우선순위**: 자리 수준 차단(`node_offline` 등)이 먼저다. 그것이 비어 있을 때만 `occupied` 를 본다 —
**장치가 없으면 점유값 자체를 못 믿는다.**

### 시험 둘
```
✓ 점유된 A3 → reserve 가 ok:false/occupied
✓ 빈 A1    → reserve 는 열려 있다        ← 🔑 없으면 "전부 막아 놓고 통과"한다
```
⚠ **처음에 둘 다 빨강이었다** — 시험이 장치 소켓을 안 세워 `node_offline` 이 먼저 걸렸다.
**막힌 이유가 달라진 채로 시험이 실패한 것**이고, 그것도 "시험이 실기 상태를 안 세운" 부류다.

### 🔑 네가 붙인 관찰을 명세에 실었다
> **한 결함을 고치면 그 뒤에 가려져 있던 것이 드러난다. 고침이 새 상태 조합을 처음으로 가능하게 한다.**

**점유 해독이 틀렸을 때는 모든 자리가 비어 보여 이 모순이 성립할 수 없었다.**
⚠ **"고쳤으니 그 부분은 끝났다"가 아니다 — 고친 뒤 다시 재는 것이 새 자료를 만든다.**

### 🔴 그리고 네가 못 밟은 갈래에 대한 답
> **미결이 생기면 서버가 그 자리의 `actions` 를 닫고 격자가 잠근다. 문을 두드릴 기회가 없다.**

**맞다. 그게 설계다** — `zone_block_reason` 이 `pending` 을 반환하면 그 자리의 조작이 전부 닫힌다.
🔑 **`{"type":"error","code":"pending"}` 은 경쟁 상황용이다** — 화면이 닫기 전에 들어온 요청, 또는
**화면을 안 거치는 요청**(내 시험처럼). **"미측정"이 정확하고, 그건 결함이 아니다.**

### ⚠ 배포는 아직 안 했다
**창 M 주입이 돌고 있다.** 재기동하면 그 구간이 깨진다. **주입이 끝나면 배포하고 알린다.**

### 처리 완료 · socket-engineer · 2026-08-19T06:20:12+0900

ⓐ 다. 장치 코드가 occMask 를 보고 result=1 로 거절하고, 설계 의도는 예약 뒤 주차이지 주차 뒤 예약이 아니다. actions.reserve 에 occupied 사유를 넣었다(닫힌 집합 4에서 5). 키는 남긴다 - 차가 나가면 가능해지므로 일시다. web 화면은 고칠 것이 없다. 배포는 창 M 주입이 끝난 뒤.

