---
id: REQ-0132
title: WS 스냅샷에 last_frame_ts 를 넣어 달라 · GET /?쿼리 가 404 (둘 다 크롬 실측)
from: web-engineer
to: socket-engineer
status: done
created: 2026-08-16T19:12:09+0900
updated: 2026-08-16T23:38:28+0900
files: ["조별과제샘플/server.cpp"]
parent: none
---

# REQ-0132 · WS 스냅샷에 last_frame_ts 를 넣어 달라 · GET /?쿼리 가 404 (둘 다 크롬 실측)

**요청자** `web-engineer` → **담당** `socket-engineer`

## 요청 내용

# 서버 쪽 둘 — WS 스냅샷의 `last_frame_ts` 누락, 루트 경로+쿼리 404

둘 다 **오늘 밤 진짜 크롬으로 시험 인스턴스(:10000)에 붙어 실측한 것**이다.
추정이 아니고, 재현 절차와 원문을 같이 남겼다. 둘 다 `server.cpp` 라 내가 못 고친다.

측정 환경: 시험 인스턴스 pid 16279 · `fake_arduino --start-empty` · 운영 미접촉.
하니스 `web/tools/e2e.mjs`, 산출물 `web/artifacts/`.

---

## (1) 🔴 WS 스냅샷에 `last_frame_ts` 가 없다 — 신선도 표시가 **주 경로에서만** 안 돈다

### 실측

브라우저가 받은 스냅샷 프레임 원문(`web/artifacts/ws-snapshot.json`)의 `device` 객체:

```json
"device": { "online": true, "device_id": "P1", "uptime": 326, "seq": 326 }
```

같은 순간 파일 폴백(`data_log.json`)의 `device` 객체:

```json
"device": { "online": true, "last_frame_ts": 1786875051474 }
```

그 결과 화면의 "마지막 프레임" 칸에 이렇게 뜬다 — CDP 로 읽은 실제 DOM 문자열이다:

```
"알 수 없음 (이 경로에는 이 정보가 없습니다)"
```

### 왜 문제인가

**폴백에는 있고 주 경로에는 없다.** 즉 신선도 표시는 WebSocket 이 끊겨 파일로 내려갔을 때만
동작하고, **정상 운영 중에는 영영 "알 수 없음"** 이다. 기능이 있는데 평소에 안 도는 것이라
있는 줄 알고 지나치기 쉽다. REQ-0067 이 화면에 신선도를 넣은 이유가
"죽은 데이터를 살아있는 것처럼 보여주지 않는다"였는데, **주 경로에서 그 목적이 미달**이다.

REQ-0122 때 나는 이걸 **코드를 읽어서** 추정만 해 뒀다(`server.cpp:1174-1178`).
오늘 실물로 확인해서 요청으로 올린다.

### 요구

WS 스냅샷의 `device` 에 `last_frame_ts` 를 넣어 달라.

- **키 이름과 단위를 파일 경로와 똑같이** 해 달라 — `last_frame_ts`, epoch **밀리초**.
  이름이 같은데 단위가 다르면 화면은 조용히 틀린 나이를 그린다. 그게 더 나쁘다.
- 값의 정의도 파일 쪽과 같아야 한다("마지막 S 프레임을 받은 서버 시각"으로 이해하고 있다 —
  다르면 알려 달라. **내가 화면에 "N초 전"으로 그리는 값이라 정의가 곧 표시다.**)
- 장치가 한 번도 프레임을 안 준 상태면 `null` 을 달라. 키 자체를 빼면 지금과 같아진다.

화면은 **이미 이 키를 읽고 있다.** 오면 그날로 동작한다. 없으면 지금처럼 "알 수 없음"으로
안전하게 접히므로, 네 쪽 배포 순서가 내 쪽보다 앞서도 깨지지 않는다.

---

## (2) `GET /?쿼리` 가 404 — `GET /index.html?쿼리` 는 200

### 실측 (같은 서버, 같은 순간)

```
GET /                    → 200
GET /index.html?demo=1   → 200
GET /?demo=1             → 404      ← 이것
```

브라우저에서는 이렇게 보인다: `chrome-error://chromewebdata/` · "127.0.0.1 페이지를 찾을 수 없음".

### 원인 (내 판독 — `serve_file()`)

```cpp
if (path == "/") path = "/index.html";      // ① 쿼리가 붙어 있으면 여기 안 걸린다
...
std::string fn = path.substr(1);
size_t q = fn.find('?');
if (q != std::string::npos) fn = fn.substr(0, q);   // ② 쿼리 제거는 여기
```

`/?demo=1` 은 ① 에서 `"/"` 와 다르므로 index.html 로 안 바뀌고, ② 에서 `?` 앞을 자르면
`fn` 이 **빈 문자열**이 된다 → `ifstream("")` 실패 → 404. **①과 ②의 순서 문제**다.

### 왜 지금 중요한가

시연에서 주소창에 `호스트:9900/?demo=1` 을 치면 **빈 페이지가 뜬다.** 데모 모드는
서버 없이 화면을 보여주는 경로라 시연 대비로 쓸 가능성이 높은데, 그때 `/index.html` 을
빼먹으면 그대로 404 다. 오늘 나도 정확히 여기서 걸려서 시험이 한 번 죽었다.

### 요구

쿼리 제거를 `"/"` 판정 **앞으로** 옮겨 달라(또는 `path` 에서 `?` 이후를 먼저 잘라내고 시작).
그러면 `/?x` · `/index.html?x` · `/data_log.json?t=…` 가 한 규칙으로 처리된다.

### 완료 확인 방법

내 하니스가 이미 이 세 줄을 **매번 검사**한다(`web/tools/e2e.mjs` [9]단계).
지금은 마지막 줄이 빨간색이고, 고쳐지면 **저절로 초록이 된다** — 따로 알려 주지 않아도 된다.

---

## 참고 — 오늘 확인된 것 (네 쪽 회귀 감시에 써도 된다)

브라우저 구간은 **실제로 동작한다.** 진짜 크롬으로 확인했다:

| 확인한 것 | 결과 |
|---|---|
| 예약 왕복 | 타일 클릭 → `<dialog>` 확인 → `{"type":"reserve","slot":"A1",…}` 송신 → `→ARD R,7,A1,…` → `←ARD A,7,A1,0,36` → ack 수신 → **화면에 "내 예약" 205ms** |
| 취소 왕복 | 같은 칸 재클릭 → "예약을 취소하시겠습니까?" → `→ARD C` → 화면 "빈 자리" 복귀 |
| 폴백 폴링 | WS 를 죽은 포트로 돌리면 `data_log.json` 폴링이 10칸을 채운다 |
| 콘솔 | 예외·오류 0건 |

시험 인스턴스는 계속 쓰고 있다. 끝나면 알리겠다 — **내가 내리지 않는다.**

## 왜 필요한가

신선도 표시가 폴백에서만 돌고 정상 운영 중에는 영영 '알 수 없음' 이다. 그리고 시연에서 호스트:9900/?demo=1 을 치면 404 빈 페이지가 뜬다 — 오늘 내 시험도 여기서 한 번 죽었다.

## 대상 파일

- `조별과제샘플/server.cpp`
## 완료 기준

WS 스냅샷 device 에 last_frame_ts(epoch ms, 파일 경로와 같은 키·단위·정의, 없으면 null) 가 온다 · GET /?x 가 200 · 확인은 web/tools/e2e.mjs 의 [5]·[9] 단계가 자동으로 한다

---

## 처리 결과

**둘 다 고쳤고, 2026-08-16 23:3x 에 시험 인스턴스(오프셋 +100 · 10091/10000/5600)에서
실왕복으로 확인했다. 코드를 읽고 판정한 것이 아니다.** 운영 포트는 접촉하지 않았다.

### (1) WS 스냅샷 `last_frame_ts` — 들어갔다

`snapshot_json()` (`server.cpp:1203-1204`). 네가 요구한 셋을 그대로 지켰다:

| 요구 | 결과 |
|---|---|
| 키 이름·단위를 파일과 동일 | `last_frame_ts` · **epoch 밀리초** |
| 값의 정의 | **마지막 유효 S 프레임을 받은 서버 시각**(`ard_last_epoch_ms`). 네 이해가 맞다 |
| 한 번도 못 받았으면 `null` | **`null`** — `0` 을 안 낸다 |

실측 (`ws_probe --port 10000`):

```
device={"online": true, "device_id": "P1", "uptime": 30, "seq": 30, "last_frame_ts": 1786890922577}
```

같은 순간 파일 폴백(`GET /data_log.json`)은 `"last_frame_ts": 1786890892390` —
**같은 키·같은 13자리 epoch ms** 다. 1Hz 하트비트에 맞춰 프레임마다 갱신되는 것도 확인했다.

🔑 **`null` 경로를 따로 시험했다.** 장치를 한 번도 붙이지 않은 별도 인스턴스(오프셋 +200):

```
device={"online": false, "device_id": "?", "uptime": 0, "seq": 0, "last_frame_ts": null}
```

네가 경고한 "1970년으로부터의 나이"는 나지 않는다. (행복 경로만 시험하지 않는다 — LEDGER §6)

⚠ **스냅샷의 최상위 `ts` 와 헷갈리지 마라.** `ts` 는 **스냅샷을 만든 시각**이고
`last_frame_ts` 는 **프레임을 받은 시각**이다. 둘이 갈리는 것이 신선도 표시의 요점이라
일부러 다른 값이다.

### (2) `GET /?쿼리` 404 — 고쳤다

`serve_file()` (`server.cpp:1881-1883`). 네 판독대로 **①과 ②의 순서 문제**였고,
쿼리 제거를 **경로에 관한 모든 판정보다 앞으로** 옮겼다. 실측:

```
GET /                    -> 200
GET /index.html?demo=1   -> 200
GET /?demo=1             -> 200      ← 전에는 404
GET /data_log.json?t=1   -> 200
```

네 번째까지 확인한 것은 "한 규칙으로 처리된다"를 실제로 보이기 위해서다.

### 🔴 다만 — 네 하니스를 내가 못 돌렸다. 그리고 남은 문제가 하나 있다

- **`e2e.mjs` 는 내가 안 돌렸다.** 위는 `curl` + `ws_probe` 실측이다.
  네 [5]·[9] 단계는 **네가 붙을 때 저절로 초록이 되는지 확인해 달라.** 그때 아니면 다시 열어라.
- 🔴 **`GET /` 200 은 시험 인스턴스 기준이다.** 운영에서는 **아직 404 일 수 있다.**
  `serve_file()` 이 **현재 작업 디렉터리 기준 상대경로**로 파일을 열고, 운영 인스턴스의 cwd 는
  `~/parking-bin` 인데 **거기에 `index.html` 이 없다.** 즉 이 REQ 의 (2) 를 고쳤어도
  **운영에서 화면이 뜨는 것은 별개 문제**이고, `--webroot` 옵션이 아직 없다.
  → 이번 배포에 **일부러 안 넣었다**(배포 직전에 미검증 변경을 얹으면 "검증 완료"가 거짓이 된다).
  루트에 결정 항목으로 올렸다. `docs/net/DEPLOY-CHECKLIST.md` §4.3 · §9-3.

**배포**: 위 둘은 커밋돼 있고(`build=22f788d`) **아직 운영에 안 올라갔다.**
루트 신호 대기 중이다. 올라간 시각은 배포 통보에 적는다.

### 처리 완료 · socket-engineer · 2026-08-16T23:38:28+0900

둘 다 고치고 시험 인스턴스(+100)에서 실왕복 확인 — WS last_frame_ts 는 파일과 같은 키/epoch ms 이고 무프레임시 null, GET /?demo=1 은 200. 단 운영 화면 서빙은 cwd 문제로 별개이며 webroot 는 이번 배포에 안 넣었다. 배포는 루트 신호 대기.

