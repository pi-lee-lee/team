---
id: REQ-0110
title: 재가동 1단계 — 보드 접촉 해제 · 플래시 덤프와 기동 창 측정 · 재플래싱은 하지 마라
from: root
to: arduino-engineer
status: done
created: 2026-08-16T16:47:48+0900
updated: 2026-08-16T16:53:24+0900
files: ["arduino/INTERVENTIONS.md"]
parent: none
---

# REQ-0110 · 재가동 1단계 — 보드 접촉 해제 · 플래시 덤프와 기동 창 측정 · 재플래싱은 하지 마라

**요청자** `root` → **담당** `arduino-engineer`

## 요청 내용

사용자가 실물 작업을 끝냈다. **보드 접촉 보류를 해제한다. 네가 1단계 선두다.**

포트도 비워 뒀다 — 잡고 있던 것은 우리 tap 이 아니라 **Arduino IDE 의 시리얼 모니터**(pid 5025)였고
루트가 종료시켰다. 지금 `/dev/cu.usbmodem21201` 점유 0건이다.

## 왜 네가 먼저인가

포트를 쓰는 것은 너와 monitor 의 `serial_tap` 둘뿐인데 **동시에 못 쓴다.**
그리고 덤프는 어차피 보드를 리셋하므로, **관측을 켜기 전에 끝내야** 첫 관측 창이 깨지지 않는다.
monitor 에게는 네가 포트를 반납할 때까지 대기하라고 했다.

## 할 일 (네가 REQ-0109 에서 세운 계획 그대로)

1. **먼저 `arduino/INTERVENTIONS.md` 에 시각을 적어라.** 덤프 전에. 오늘 사고 둘 다 기록되지 않은
   개입이 원인이었다. 이번 리셋도 관측 경계를 만든다.
2. **플래시 덤프 → `/tmp/oldverify/out/` 기대 바이너리와 대조.**
   - 일치 → 사용자가 구운 것이 `fa52a51` 커밋본으로 확정된다.
   - 불일치 → **커밋 안 된 다른 판본이다. 그 사실을 먼저 보고해라.** 덮어쓰기 전에 알아야 한다.
3. **같은 부팅에서 기동 창을 측정해라.** 36초는 nextgen 특성이라 지금 펌웨어에선 무효다 —
   socket 이 서버 타임아웃을 정하려면 이 값이 필요하다.
4. **끝나면 포트를 반납하고 루트에게 알려라.** 그게 monitor 의 관측 재개 신호다.

## 🔴 재플래싱은 하지 마라

지금 칩에 있는 것을 **그대로 두고** 관측을 시작한다. 이유:

- 판정 개정 2 에서 **"EspLink 가 재부팅을 줄였다"는 성립하지 않는 것으로 정리됐다.**
  기준선도 판정창도 재부팅 0 이라 애초에 비교가 안 됐다. 바꿀 근거가 없다.
- 지금 칩의 펌웨어는 공백 결함이 없음을 네가 바이너리로 확인했고, 프레임 형식도 동일하다.
- 무엇보다 **사용자가 방금 실물을 만졌다.** 그 뒤 처음 얻는 관측 창은 "지금 이 구성이 어떤가"를
  재는 데 써야 한다. 여기서 펌웨어를 바꾸면 변수가 둘이 되고, 오늘 캐패시터에서 겪은 함정을 반복한다.

## 사용자가 무엇을 바꿨는지

아직 못 받았다. 루트가 묻고 있다. 받는 대로 전하겠다 —
그전까지 INTERVENTIONS.md 에는 **"내용 미상, 15:26~17:0x 사용자 실물 작업"** 으로 남겨라.
배선이나 전원 구성이 바뀌었을 수 있으므로 **이후 데이터를 이전과 섞어 판정하지 마라.**

## 왜 필요한가

지금 칩에 무엇이 있는지 모른 채 관측을 시작하면 그 데이터를 어느 펌웨어에 귀속할지 알 수 없다. 덤프는 보드를 리셋하므로 관측을 켜기 전에 끝내야 첫 창이 깨지지 않는다.

## 대상 파일

- `arduino/INTERVENTIONS.md`
## 완료 기준

덤프 전 INTERVENTIONS.md 에 시각 기록 · 기대 바이너리와 대조 결과 보고(불일치면 즉시) · 기동 창 실측값 확보 · 포트 반납 후 루트에 보고 · 재플래싱 없음

---

## 처리 결과

<!-- 담당자가 여기에 적는다. 처리 후 반드시:
     team/bin/req.sh done REQ-0110 --by arduino-engineer --note "<한 줄 요약>" -->

**완료 · arduino-engineer · 2026-08-16 16:52 · 포트 반납 완료**

## 1. ✅ 덤프 대조 — **IDENTICAL. 커밋본 그대로다**

```
avrdude -c arduino -p atmega328p -P /dev/cu.usbmodem21201 -U flash:r:...  (32768 바이트)
칩 실물  vs  조별과제샘플/client.ino(fa52a51) 빌드 → 다른 바이트 0개
```

Intel HEX 텍스트를 눈으로 비교하지 않고 **양쪽을 32768바이트 raw 로 파싱해 바이트 단위로 대조**했다.

> **사용자가 구운 것은 커밋본 `fa52a51` 이 맞다. 커밋 안 된 변종이 아니다.**
> → 이제부터의 관측 데이터를 **저장소의 특정 소스에 귀속할 수 있다.** 오늘 내내 못 하던 것이다.
> socket 의 "판본 특정 불가" 단서는 **지금 이후 구간에는 더 이상 붙일 필요가 없다**(과거 구간에는 유효).

## 2. ⏱ 기동 창 = **약 11.3초** (36초는 폐기하라)

t0 = 리셋 해제. 같은 부팅에서 측정했다(추가 리셋 없음).

| 구간 | 시각 |
|---|---|
| 부트로더 → 스케치 시작 | 1.0s |
| 배너 출력 | 1.51s |
| `AT+CWJAP` 시작 | 5.42s |
| **WiFi 결합 완료** | **10.42s** (결합 자체 5.0s) |
| **IP 확보 `192.168.35.79`** | **10.77s** |
| **첫 `CIPSTART` 시도** | **11.29s** |

**서버가 떠 있으면 첫 S 프레임까지 ≈ 12초로 본다.** 옛 36초는 nextgen 특성이라 **이 펌웨어에선 3배 빠르다.**

⚠ **한 구간은 미측정이다**: 측정 시점에 **9991 을 듣는 서버가 없어서**(`lsof` 확인) `CIPSTART` 가
즉시 `ERROR` 로 끝났다. TCP 핸드셰이크 시간이 빠져 있다 — LAN 이라 수십 ms 수준이겠으나
**추정이므로 socket 이 서버를 띄운 뒤 교차 확인해야 확정된다.**

## 3. 🔁 덤으로 관측된 것 — **서버가 없으면 백오프 없이 초당 1.1회 재시도한다**

서버가 내려가 있던 덕에 우연히 잡혔다. `CIFSR → CIPMUX → CIPSTART → ERROR/Unlink` 를
**약 0.90초 주기로 쉬지 않고 반복**한다. 재시도 간격이 늘지 않는다.

→ 서버 다운 구간의 "재연결 폭주"는 **결함이 아니라 설계된 거동**이다. 집계에 감안해야 한다.
→ WiFi 송신이 잦아지므로 **전원 여유가 적은 구성에선 이 구간이 가장 불리하다.**

## 4. 🧾 장치가 문서의 주장 셋을 실물로 확인해 줬다

```
[BOOT] 리셋 원인: 불명 (부트로더가 MCUSR 을 지우고 넘어왔다)
[BOOT] 사다리 4단(ESP 하드리셋선) 미배선 · 6단(워치독) 끔
```

**MCUSR 을 못 쓴다는 것**과 **워치독이 꺼져 있다는 것**을 그동안 소스 근거로만 말해 왔는데,
이제 **장치 자신의 부팅 로그가 같은 말을 한다.** IP·SSID 도 문서 값과 일치한다.

## 5. 지시 준수

- **재플래싱하지 않았다.** 읽기(덤프)와 시리얼 관찰뿐이다. 칩 내용은 그대로다.
- **덤프 전에 INTERVENTIONS.md 에 시각을 선언**하고 `req.sh progress` 로도 알린 뒤 착수했다.
- 보드 리셋은 **2회**(덤프 1 · 캡처 포트 개방 1). 개입 표에 기록했다.
- **16:52 포트 반납 완료**(`lsof` 로 점유 0건 확인). **monitor 는 지금 관측을 시작해도 된다.**

## 6. 남은 것

- 기동 창의 마지막 구간(서버 accept → 첫 S 프레임)은 **서버가 떠야 잰다.**
- 유일하게 남은 미제는 여전히 **10:51:02**(재부팅 없이 링크만 끊김)다.

### 처리 완료 · arduino-engineer · 2026-08-16T16:53:24+0900

덤프 대조 IDENTICAL(0 바이트 차이) - 칩은 fa52a51 커밋본 확정. 기동창 11.3초(36초 폐기). 서버없을때 백오프없이 0.9초 주기 재시도 관측. 재플래싱 없음. 16:52 포트 반납 - monitor 재개 가능

