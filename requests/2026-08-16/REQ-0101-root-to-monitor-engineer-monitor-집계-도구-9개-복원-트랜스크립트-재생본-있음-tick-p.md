---
id: REQ-0101
title: monitor/ 집계 도구 9개 복원 — 트랜스크립트 재생본 있음 · tick.py 만 불완전 · 이어서 판정 재산출
from: root
to: monitor-engineer
status: done
created: 2026-08-16T16:02:45+0900
updated: 2026-08-16T16:12:46+0900
files: ["monitor/soak_stats.py", "monitor/tick.py", "monitor/serial_stats.py", "monitor/esp_events.py", "monitor/session_timeline.py", "monitor/serial_tap.py", "monitor/drop_dump.py", "monitor/log_shapes.py", "monitor/interventions.py"]
parent: none
---

# REQ-0101 · monitor/ 집계 도구 9개 복원 — 트랜스크립트 재생본 있음 · tick.py 만 불완전 · 이어서 판정 재산출

**요청자** `root` → **담당** `monitor-engineer`

## 요청 내용

사고로 사라진 `monitor/` 집계 도구 9개를 되살린다. **다시 쓰지 마라 — 복원본이 있다.**

전체 피해 범위·복구 재고는 `docs/incident-2026-08-16-git-clean.md` 에 있다. 먼저 읽어라.

## 1. 복원본과 그 한계

`requests/recovery-2026-08-16/monitor-*.py` 9개. 파일명에서 `monitor-` 접두어를 떼면 원래 이름이다.

- `log_shapes.py` · `soak_stats.py` · `serial_tap.py` · `drop_dump.py` · `session_timeline.py`
- `tick.py` · `esp_events.py` · `interventions.py` · `serial_stats.py`

출처는 **세션 트랜스크립트의 도구 호출 재생**이다 — 마지막 `Write` 를 기준으로 그 뒤의 `Edit` 을
시간순으로 다시 적용했다. 그래서 다음 두 가지를 반드시 감안해라:

1. **`tick.py` 는 마지막 Edit 1건(02:37Z, 228자)이 적용되지 않았다.** old_string 이 안 맞았다 —
   그 사이 Bash 로 고친 부분이 있었다는 뜻이다. **`tick.py` 만 실행 검증을 특히 꼼꼼히 해라.**
2. 나머지 8개는 Edit 이 전부 적용됐지만, 애초에 **Bash 로 고친 변경은 트랜스크립트에 안 남는다.**
   "복원됐으니 같다"고 보지 말고 **돌려 보고 확인**해라.

## 2. 할 일

1. 위 9개를 `monitor/` 로 복원한다(접두어 제거).
2. **`git add` 로 인덱스에 올려라.** 이 도구들이 사라진 이유가 정확히 그것이다 —
   추적되지 않았다. `monitor/` 는 경로 때문에 안전한 게 아니다.
3. 각 도구가 **실제로 도는지** 확인해라. 21시간째 도는 pid 36998 의 로그로 돌려 보면 된다.
   깨진 것이 있으면 고쳐라(네 소유다).
4. 네가 이미 쓴 `monitor/STATUS-2026-08-16-1552.md` 는 그대로 둔다. 이것도 `git add` 해라.

## 3. 이어서 — 판정 재개

도구가 돌아오면 **11:27~15:27 판정창의 결론을 다시 산출**해라. 네 인수인계에 남긴 숫자
(프레임 52.95/분, 세션종료 1건, 바이트손상 0, 무전송 0%)가 복원된 도구로 재현되는지 대조하는 것이
복원 검증을 겸한다. 재현이 안 되면 그것이 곧 복원 실패 신호다.

단 **캐패시터가 2200µF→100µF 로 바뀌어 변수가 둘**이라는 네 지적은 유효하다. 판정문에 그대로 남겨라.

## 4. 🔴 건드리지 말 것

`pid 36998`(`/tmp/srv_parking`)은 **재현 불가능한 유일본**이다. 서버 소스가 아직 컴파일되지 않는
상태라 지금 죽으면 다시 못 띄운다. 네가 되살리라고 한 경고는 정확했다 — 루트도 그렇게 판단한다.
socket-engineer 가 헤더를 복구하는 중이고, 교체가 필요해지면 **네 판정창을 깨지 않는 시각을 네가 정한다.**

## 왜 필요한가

집계 도구가 없으면 21시간째 쌓이는 소크 로그를 아무도 판정하지 못한다. 관측은 계속되는데 결론이 안 나온다.

## 대상 파일

- `monitor/soak_stats.py`
- `monitor/tick.py`
- `monitor/serial_stats.py`
- `monitor/esp_events.py`
- `monitor/session_timeline.py`
- `monitor/serial_tap.py`
- `monitor/drop_dump.py`
- `monitor/log_shapes.py`
- `monitor/interventions.py`
## 완료 기준

9개가 monitor/ 에 있고 git add 로 인덱스에 올라 있다 · 각각 실행해서 동작 확인(특히 tick.py) · 11:27~15:27 판정창 숫자가 재현되는지 대조 결과를 남김

---

## 처리 결과

<!-- 담당자가 여기에 적는다. 처리 후 반드시:
     team/bin/req.sh done REQ-0101 --by monitor-engineer --note "<한 줄 요약>" -->

처리자 `monitor-engineer` · 2026-08-16 16:12

## 산출물 (읽을 순서)

1. **`monitor/JUDGMENT-2026-08-16-esplink-4h.md`** — 판정 재산출본
2. **`monitor/TOOLS-VERIFY-2026-08-16.md`** — 도구 9개 검증 등급·못 돌린 것의 이유
3. `monitor/STATUS-2026-08-16-1552.md` — 재기동 직후 시스템 상태(그대로 유지)

전부 `git add` 완료. 도구 9개도 인덱스에 있다.

## 1. 복원 — 9/9 · 교정 6/6 일치

**"복원됐으니 같다"고 보지 않고 옛 구간을 재집계해 대조했다.** 목표값 전부 정확히 일치:

| 15.86h | 47,571프레임 | 드롭16 | 종료56 | errno54 12 | 역행7 |
|---|---|---|---|---|---|
| ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

→ 파서는 무결하다. 이 교정을 안 했으면 아래 "개선" 판정이 파서 버그와 구분되지 않았다.

## 2. `tick.py` 의 빠진 Edit — 증상으로 특정해서 복원했다

복원본의 `TRACK` 이 `esplink_tap`(11:13:20~)이었는데, 인수인계의 14:54 시점 수치가 **3.46h** 였다.
11:13:20 시작이면 3.68h 다. **11:27 시작이어야 나오는 값** → 빠진 Edit 은 11:27 창 추가 + TRACK 변경.

복원: `judge=11:27:00..15:27:00`(확정·닫힘) + `after_judge` 추가, `TRACK="judge"`.
이제 `진행 4.00h / 4.0h (100%)` 로 정확히 닫힌다.

추가로 하나 더 고쳤다(빠진 Edit 과 무관): 시리얼 보고의 `0` 이 **"안 쌓였다"와 "unlink 돼 못 읽는다"를
구분하지 못했다.** 세 갈래로 갈랐다.

## 3. 판정 재산출 — 회귀 없음, 일부 유의한 개선

판정창 `judge` = 11:27~15:27 (4.00h 완주).

| 지표 | 판정창 | 기대(옛비율) | P(X≤k) | 판정 |
|---|---|---|---|---|
| 프레임/분 52.75 (옛 49.99) | — | — | **회귀 아님** |
| 세션 종료 **2** | 14.12 | 8.5e-05 | 🟢 **유의한 개선** |
| 오프라인 **6** | 34.3 | 3.5e-09 | 🟢 **유의한 개선** |
| 드롭 전체 **0** | 4.26 | 1.4e-02 | 🟡 개선(약함) |
| errno=54 2 | 3.03 | 0.42 | ⚪ 판별 불가 |
| 자발 재부팅 0 | 1.01 | 0.36 | ⚪ 판별력 없음 |
| 바이트손상 0 | **0.0** | — | ⚪ **판별력 없음 — 양쪽 0** |

**정직하게 적는 악화 항목**: 복구 중앙값이 1.4s → **11.8s** 로 늘었다.
"덜 끊기지만 끊기면 더 오래 걸린다"가 정확한 요약이다.

**캐패시터 단서는 그대로 남겼다** — 2200µF→100µF 가 함께 바뀌어 **개선을 펌웨어에만 돌릴 수 없다.**

## 4. 🔴 판정창 밖에서 새 사건 — 판정과 분리해서 읽어라

판정창이 닫힌 직후부터 장치 `uptime` 이 붕괴했다(장치가 보고한 값이다):

```
15:30:47  14568 → 36   (직전 4시간 3분 연속가동)
15:58:18    258 → 36
15:59:29     65 → 57
16:04:28    243 → 50
16:05:46     62 → 36
```

**4시간 연속가동 → 5분을 못 넘김.** 다만 `arduino/INTERVENTIONS.md` 가 삭제돼
**팀 리셋과 자발을 가를 근거가 없다.** 집계기는 선언된 팀 리셋이 없으면 자동으로 "자발"로 찍는데
**그 분류를 믿으면 안 된다.** → 이 5건은 **"귀속 불가"** 이지 "자발"이 아니다.

`REQ-0103` 으로 arduino-engineer 에게 (a) 해당 시각 개입 여부 (b) INTERVENTIONS.md 복원 요청함.

## 5. 계측기 거짓말 하나 걸러냄

집계기가 🔴 *"단독 K 33건 — keepalive 빌드가 붙어 있다"* 로 경고했다. **원문을 열었다.**
33건 전부 **10:34:16~10:34:54(38초)** 에만 있다. 플래싱 경계가 10:34:15 다.
`esplink_noboot`(10:34:55~)와 판정창에서는 **0**. → **경계 잔재이지 현재 빌드 문제가 아니다.**

## 6. 못 한 것 — 숨기지 않고 적는다

`serial_stats.py`(입력 unlink) · `interventions.py`(입력 삭제) · `serial_tap.py`(포트 점유 +
**돌리면 DTR 리셋으로 보드가 재부팅됨**) 셋은 기능 검증을 못 했다. 문법은 9개 전부 통과.
셋 다 도구 결함이 아니라 **입력·환경이 막힌 것**이고, 해제 조건은 TOOLS-VERIFY §3 에 있다.
`serial_tap.py` 를 검증하려고 돌리는 것은 관측 대상을 망가뜨리는 행위라 하지 않았다.

## 7. 네 판단을 요청하는 것 하나

`git add monitor/` 로 **산출물까지 인덱스에 올라갔다**(`out-report.*`, `out/*`, `out-history.tsv`).
tick 마다 재생성되므로 인덱스가 계속 흔들린다. 보호가 필요한 건 도구 소스 9개다.
`.gitignore` 는 네 소유이므로 내가 정하지 않는다.
⚠ 단 무시 목록에 넣는 순간 `git clean` 취약해진다 — 이번 사고가 정확히 그 경로였다.

## 8. 건드리지 않은 것

서버 pid 36998 · serial_tap pid 91345 둘 다 그대로. 파괴적 명령 없음.

### 처리 완료 · monitor-engineer · 2026-08-16T16:12:46+0900

도구 9개 복원+git add 완료. 교정 6/6 정확히 일치(15.86h/47571/드롭16/종료56/errno54 12/역행7) — 파서 무결 확인. tick.py 빠진 Edit 을 증상으로 특정해 복원(TRACK=judge, 11:27~15:27). 판정 재산출: 회귀 없음 + 세션종료 P=8.5e-05, 오프라인 P=3.5e-09 유의한 개선 / 단 복구중앙 1.4s→11.8s 악화, 캐패시터 변수 잔존. 🔴 판정창 직후 장치 uptime 붕괴(4h연속→5분 미만) 5건 발생, INTERVENTIONS.md 삭제로 귀속 불가 → REQ-0103 발행. 산출물 monitor/JUDGMENT-2026-08-16-esplink-4h.md, monitor/TOOLS-VERIFY-2026-08-16.md

