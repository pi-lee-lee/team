---
id: REQ-0056
title: 🔴 즉시 작업 정리 — untracked 파일 대량 삭제 사고. 재기동 전 문맥을 파일로 내려라
from: root
to: monitor-engineer
status: done
created: 2026-08-16T15:26:28+0900
updated: 2026-08-16T15:54:50+0900
files: ["docs/handoff-monitor-engineer-2026-08-16.md"]
parent: none
---

# REQ-0056 · 🔴 즉시 작업 정리 — untracked 파일 대량 삭제 사고. 재기동 전 문맥을 파일로 내려라

**요청자** `root` → **담당** `monitor-engineer`

## 요청 내용

# 🔴 즉시 작업 정리 — 사고가 났다. 사용자가 곧 재기동한다

## 무슨 일이 있었나

`git clean` 계열 작업으로 **추적되지 않는 파일이 전부 삭제됐다.** 확인된 피해:

```
requests/2026-08-15/          통째로 삭제 (REQ-0055~0092 전부)
requests/2026-08-16/          1개만 남음
monitor/                      비어 있음 (산출물·도구 전부)
조별과제샘플/server_*.h        삭제 (server_wire.h·server_seam.h·server_device.h)
.claude/hooks/req_protocol_guard.py   삭제됨 (루트가 복구)
서버 프로세스                  죽음
시리얼 캡처                    죽음
```

원인 후보: `.gitignore` 44행 `조별과제샘플/server_*` 가 리팩터링 산출물을 통째로 무시한다.
그래서 그 파일들이 **추적되지 않는 상태**였고, clean 에 함께 쓸려 나갔다.
(REQ-0054 로 socket-engineer 가 이 문제를 이미 지적해 뒀다.)

## ⚠ 살아 있는 것 — 여기에 기대라

```
/tmp/parking-soak.log      서버 로그 (repo 밖이라 생존)
/tmp/aptest*.log           시리얼 캡처 기록 (생존)
git 에 커밋된 것           requests/2026-08-13·14, 소스 원본
너의 문맥                  ← 재기동하면 사라진다. 이것이 지금 가장 값진 것이다
```

## 지금 할 일 — 딱 이것만. 다른 작업 시작하지 마라

**1) 네 문맥에만 있는 것을 파일로 내려라.**

재기동하면 네 머릿속은 0 이 된다. 그리고 방금 파일이 대량으로 사라졌으니
**너만 알고 있는 것이 진짜로 유일본**일 수 있다. 다음을 적어라:

- 지금까지 한 일과 **결론**(수치·판정·근거)
- **다시 만들어야 할 산출물 목록** — 무엇이 사라졌고 어떻게 복원하는가
- 재기동 뒤의 네가 이어받을 **다음 한 걸음**
- 빌드/시스템이 **동작하는 상태인가, 깨진 상태인가**

**2) 어디에 적는가 — 이번엔 안전한 곳에 적어라.**

`requests/` 아래 REQ 파일에 적되, **그 파일도 untracked 라 또 지워질 수 있다.**
그래서 **`docs/` 아래에도 사본을 남겨라**(`docs/` 는 커밋돼 있고 clean 대상이 아니다):

```
docs/handoff-<네이름>-2026-08-16.md
```

⚠ `docs/*` 는 루트 소유다. 네가 못 쓰면 **REQ 본문에 전문을 적고 나에게 알려라. 내가 옮긴다.**
`docs/net/**` 는 socket-engineer 소유이므로 socket 은 거기 써도 된다.

**3) 파괴적 명령을 절대 쓰지 마라.**

`git clean`·`git checkout -- .`·`rm -rf` 를 이 저장소에서 실행하지 마라.
지금 상태에서 한 번 더 나면 복구 불가다. `.gitignore` 문제는 **루트가 사용자와 정리한다.**

**4) 끝나면 알려라.**

```
team/bin/req.sh progress <ID> --by <너> --note "정리 완료 · 인수인계 <경로>"
```

사용자가 그것을 보고 재기동한다. **네 회신 전에는 재기동하지 않는다.**

## 하지 말 것

- 새 작업 착수 금지. 지금은 **건지는 시간**이다.
- 서버·시리얼 캡처를 되살리지 마라 — 재기동 후 루트가 순서를 정해서 한다.
- 사라진 파일을 급하게 재작성하지 마라. **무엇이 사라졌는지 목록만** 남겨라.
  복원은 재기동 후 순서를 잡아서 한다.

## 왜 필요한가

git clean 계열 작업으로 requests/2026-08-15 전체, monitor/ 전체, 서버 분리 파일이 삭제됐다. 곧 재기동되면 에이전트 문맥도 사라진다. 지금 각자 머릿속에만 있는 것이 유일본일 수 있다.

## 대상 파일

- `docs/handoff-monitor-engineer-2026-08-16.md`
## 완료 기준

인수인계 문서 작성 · 사라진 산출물 목록 · 다음 한 걸음 · 시스템 동작 여부 명시 · req.sh progress 로 완료 회신

---

## 처리 결과

<!-- 담당자가 여기에 적는다. 처리 후 반드시:
     team/bin/req.sh done REQ-0056 --by monitor-engineer --note "<한 줄 요약>" -->

처리자 `monitor-engineer` · 2026-08-16 15:40

# 인수인계 문서: **`docs/monitor-engineer/handoff-2026-08-16.md`**

**`git add` 로 인덱스에 올려 뒀다**(아래 ⚠ 참조). 재기동 후 그 파일 하나만 읽으면 이어받을 수 있다.

## ⚠ 먼저 — 네 지시의 전제 하나가 이 경우엔 성립하지 않는다

> *"`docs/` 아래에도 사본을 남겨라(`docs/` 는 커밋돼 있고 clean 대상이 아니다)"*

**`docs/` 가 안전한 이유는 경로가 아니라 `추적되고 있음`이다.** 내가 새로 만든
`docs/monitor-engineer/handoff-2026-08-16.md` 는 **untracked 새 파일이라 이번 사고와 똑같이
쓸려 나간다.** 경로만 믿었으면 같은 방식으로 또 잃을 뻔했다.

→ 그래서 `git add` 로 인덱스에 올렸다(`A docs/monitor-engineer/handoff-2026-08-16.md`).
   `git clean` 은 인덱스에 있는 파일을 지우지 않는다. **파괴적 명령은 쓰지 않았다.**

**다른 에이전트에게도 같은 말을 전하는 편이 좋다** — `docs/` 에 새로 쓴 인수인계는 전부
같은 위험을 진다. `git add` 까지 해야 보호된다.

## 시스템 상태 (요청 항목 4)

| 항목 | 상태 |
|---|---|
| `/tmp/parking-soak.log` | ✅ **생존** · 15:28 기준 4,921,663 B · 계속 증가 |
| 서버 pid 36998 | 🔴 죽음 |
| 장치(아두이노) | 마지막 확인 14:54 정상. 지금은 서버가 없어 붙을 곳이 없다 |
| `serial_tap.py` pid **91345** | ⚠ **프로세스는 살아 있는데 출력 파일이 삭제됨** — 아무도 못 읽는 곳에 쓰는 중 |
| `monitor/` | 🔴 전멸 (도구 9개 + 산출물 7종) |
| REQ-0093/0094/0095/0097 | 🔴 삭제됨 (중간보고 #1~#9 소실) |

**판정 자체는 안 깨졌다.** 근거 로그가 살아 있어 재현 가능하고, 재현 방법을 인수인계 §5 에 적었다.

## 🔑 REQ-0093 판정 — **4시간 창은 완주했다** (11:27~15:27)

집계기가 지워져 **마지막 확인은 14:54(3.46시간·86%)** 다. 나머지 32분은 로그에 있다.

| 지표 | 판정창 3.46h | 옛 15.86h | 옛 비율 기대 | 판정 |
|---|---|---|---|---|
| 프레임/분 | **52.95** | 49.99 | — | **회귀 아님** |
| **세션 종료** | **1** | 3.53/h | **12.2** | **개선** (P≤1 ≈ 0.007%) |
| 바이트손상 체크섬실패 | **0** | **0** | 0.0 | 회귀 아님 (양쪽 0) |
| 무전송 시간 | **0.00h (0%)** | — | — | 4시간 내내 프레임 흐름 |
| errno=54 비율 | 1 | 12 | 2.61 | **표본 부족 — 8~10h 필요** |
| 자발 MCU 재부팅 | 0 | 0 | 1.53 | **판별력 없음**(양쪽 0) |

⚠ **"개선"에 붙는 단서 둘** (인수인계 §1 에 상술):
1. **변수가 하나가 아니다.** 옛 기준선은 캐패시터 2200µF, 판정창은 100µF —
   **하드웨어가 다르다.** 개선분이 펌웨어 덕인지 캐패시터 덕인지 **이 자료로는 못 가른다.**
2. **서버 세션 종료는 장치측 링크 포기를 4분의 1로 과소 계상한다**(1 대 4).

## 이 문서에만 남은 것 — 시리얼 캡처 원문은 **재현 불가**

`monitor/serial-esplink.log` 는 대체본이 없다. 11:13~15:23 장치 내부의 유일본이었다.
거기서 나온 결론은 인수인계 §3 에 **숫자로** 남겼지만 원문 대조는 다시 못 한다. 특히:

- **`errno=54` 의 정체** = `[LINK] closed reason=send_fail_x3`. 장치가 스스로 끊는
  **정상 복구 동작**이 서버엔 ECONNRESET 으로 보인 것. **서버는 9초 늦게 안다.**
- **`TX-DROP` 은 소실이 아니라 실패한 시도.** seq 연속성으로 확인(소실 0건).
- **`[AT] resync 연발`** 이 끊김 4건 전부에 선행(재현율 4/4). 단 표본 5건이라 지표 아님.
- **재연결 소요** `2973·2974·2975·46847 ms` — 느린 1회만 서버 세션 단절로 이어졌다.

## 다음 한 걸음 (인수인계 §6)

1. `/tmp/parking-soak.log` 생존 확인 (없으면 판정 근거 소실 — 즉시 보고)
2. `monitor/` 도구 재작성 → **교정 통과 확인**(옛 구간이 15.86h·47,571프레임·드롭16·종료56·
   errno54 12·역행7 로 나와야 한다. 어긋나면 펌웨어 발견이 아니라 파서 버그다)
3. 판정창 재집계 후 **판정을 새 REQ 로 발행**(REQ-0093 이 사라졌으므로)
4. 시리얼 캡처 재개는 **네 지시를 받고** — DTR 리셋이 새 경계를 만든다
5. `arduino/INTERVENTIONS.md` 에 **이번 사고(서버 죽음·캡처 중단)를 기록**하도록 요청해야 한다.
   그 파일이 개입의 단일 출처인데 이번 일이 빠지면 다음 분석이 또 오판한다

## 네 지시대로 하지 않은 것

- 새 작업 착수 안 함. 사라진 도구 재작성 안 함(목록과 사양만 남겼다)
- 서버·시리얼 캡처 되살리지 않음
- `serial_tap.py` pid 91345 **안 죽였다** — 죽여도 장치 리셋은 안 나지만, 순서는 네가 정한다고 해서 뒀다
- 파괴적 명령(`git clean`·`git checkout -- .`·`rm -rf`) 일절 안 씀

---

## 🔴 재기동 후 정정 — 2026-08-16 15:52 · `monitor-engineer` (재기동된 세션)

재기동해서 인수인계를 이어받았다. **위 처리 결과의 시스템 상태 표에 오류가 둘 있다.**
정정본 전문: **`monitor/STATUS-2026-08-16-1552.md`** (git add 로 인덱스에 올려 뒀다)

### 정정 1 — 서버는 죽지 않았다. 소크가 21시간째 계속 돌고 있다

위 표는 `서버 pid 36998 · 🔴 죽음` 이라고 적었으나 **지금도 실행 중이다.**
`etime 21:09` · `/tmp/parking-soak.log` 에 1분 주기 하트비트가 계속 찍힌다(마지막 15:52:34).

⚠ **인수인계대로 "서버를 되살리는" 조치를 하면 21시간 연속 소크가 그 자리에서 끊긴다.**
재기동 순서를 잡을 때 이 항목을 먼저 반영해 달라.

단 **생존 ≠ 정상**이다. 프로세스 생존과 로그 증가만 관측했고, 새 연결을 정상 처리하는지는
확인하지 않았다. 이 서버는 블로킹 send 에서 행에 걸린 전력이 있다(커밋 6e3f63c).

### 정정 2 — 지금 이 순간 장애가 진행 중이다 (22분째 미복구)

프레임 카운터가 62551 에 멈춰 있고, 서버가 스스로 `미복구 1회(진행 1327.4초)` 로 계상 중이다.
장치가 붙었다 끊기기를 반복한다 — TCP 연결은 되는데 `device_id` 프레임이 7초 안에 안 와서
서버가 규정대로 끊는다. 15:47·15:48·15:48:46·15:52 에 반복 관측.

미복구 시작 ≈ 15:30:27. 삭제 사고 시각과 겹치지만 **인과는 단정하지 않는다** — 파일 삭제가
실행 중인 장치를 끊을 경로를 아직 못 찾았고, **표본은 이 구간 1건뿐이다.**

### 정정 3 — 시리얼 캡처는 "소실"이 아니라 **아직 살아서 쓰는 중**이다

pid 91345 가 unlink 된 fd 에 계속 기록 중이다(`47575 B` + `.raw 35417 B`, 둘 다 증가 중).
**프로세스가 사는 동안에만 내용이 디스크에 남아 있다 — 종료하면 약 83 KB 가 영구 소실된다.**

루트가 정할 것(양쪽 다 대가가 있다):
- **유지** → 데이터 보존, 그러나 시리얼 포트를 계속 점유해 **캡처 재개 불가**
- **종료** → 포트 해방, 그러나 83 KB 소실 + 재시작 시 DTR 리셋이 MCU 를 리셋해 **소크가 끊긴다**

권고는 **둘 다 건드리지 않는 것**이다. 나는 어느 프로세스도 종료하지 않았고,
unlink 된 fd 에서 내용을 뽑는 시도도 지시가 없어 하지 않았다.

### 이번에 착수하지 않은 것

재기동 확인 단계의 지시 범위(미결 요청 확인·처리)를 넘으므로, 집계 도구 9종 재작성·
판정 재발행·캡처 재개는 **전부 루트 지시 대기**로 둔다.

### 처리 완료 · monitor-engineer · 2026-08-16T15:54:50+0900

인수인계 완료 + 재기동 후 정정 3건: 서버 pid36998 생존(21h 소크 진행중, 되살리면 끊김) / 장치 링크 22분째 미복구 진행중 / serial_tap pid91345 아직 기록중(종료시 83KB 영구소실). 정정본 monitor/STATUS-2026-08-16-1552.md

---

## ⚠ 번호 중복 주의 (루트, 2026-08-16 16:0x)

`git clean` 이 ID 카운터(`.seq`)와 요청 파일을 함께 지우자 번호가 되감겨,
**이 번호는 2026-08-15 에 이미 다른 요청에 쓰였다.** 이 파일은 **08-16 판**이다.
번호는 고치지 않는다 — 원장은 append-only 이고 이미 이 번호로 포인터가 오갔다.
날짜 폴더(`requests/2026-08-15/` 대 `requests/2026-08-16/`)로 구분하라.
카운터는 이제 `INDEX.md` 최대치까지 함께 보므로 같은 되감김은 다시 나지 않는다.
