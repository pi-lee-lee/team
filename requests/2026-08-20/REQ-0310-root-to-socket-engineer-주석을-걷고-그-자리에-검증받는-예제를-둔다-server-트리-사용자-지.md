---
id: REQ-0310
title: 주석을 걷고 그 자리에 검증받는 예제를 둔다 — server 트리 (사용자 지시)
from: root
to: socket-engineer
status: done
created: 2026-08-20T18:03:36+0900
updated: 2026-08-20T18:17:35+0900
files: ["조별과제샘플/server/parking.h"]
parent: none
---

# REQ-0310 · 주석을 걷고 그 자리에 검증받는 예제를 둔다 — server 트리 (사용자 지시)

**요청자** `root` → **담당** `socket-engineer`

## 요청 내용

사용자 지시:
> *"지금 싹다 감춰졌는데 **주석정리는 좀 해야겠다. 코드보다 주석이 많다.**"*

앞서 확정한 규칙의 집행이다:
> *"**주석 문제는 모든 코드에 심각하다. 주석이 아니라 작업일지다.** 그런 건 주석이 아니다. **일괄로 정리가 필요하다.**"*

## 루트가 잰 값 — 사용자 말이 그대로 맞다

```
조별과제샘플/server/ 주석 3,499 / 코드 6,358 = **0.55**
최악 : config.h **주석 327 / 코드 49** = 6.7 · lot.cpp 70/30 = 2.33 · spot.h 2.05 · parking.h 167/110 = 1.52 · state.h 167/120 = 1.39
```

## ★ 판별자 — CLAUDE.md 에 있는 그대로

> **현재형으로 다시 쓸 수 있으면 주석, 과거형이어야 하면 작업일지다.**

```
✅ 남긴다 : 불변식 · 값의 뜻 · 함정 · 금지와 그 이유
           (예: "이 include 순서를 바꾸면 hex 가 달라진다" · "arg 1=열기 2=닫기"
                "이름이 곧 자리 결속 키다" · "OUTPUT HIGH 로 두지 마라 — 5V 가 3.3V 핀에 실린다")
❌ 걷는다 : 날짜 · REQ 번호 · "오늘" · "~가 잡았다" · "전에는 ~였다" · 실측 이력 · 사고 경위
           · 단계 번호("★ 2단계") · 커밋 해시 · 판정 이력
```
🔑 **지우지 말고 옮겨라** — 값 있는 것은 원장·판정문·커밋 메시지로. **옮길 곳이 없는 것만 지운다.**
   (`"2026-08-20 이동"` 같은 것은 `git log` 가 이미 답한다 → 지운다)

## 🔴 왜 이게 급한가 — 오늘 그 비용을 실제로 치렀다

`Modules.h` 주석이 *"이름이 자리 id 와 같아야 한다"* 를 **경고하고 있었다. 아무도 안 읽었다.**
주변이 전부 작업일지라 그 한 줄이 묻혔고, 그래서 **그 함정을 서버가 말하게** 만드느라 반나절을 썼다.

> ### **주석이 길면 안 읽힌다. 안 읽히면 *정작 필요한 경고* 가 같이 묻힌다.**
> ### **작업일지를 걷는 것은 정보를 버리는 것이 아니라 — 경고를 보이게 하는 것이다.**

## 순서 — **비율 높은 파일부터.** 파일 머리의 긴 서사도 같은 대상이다

목표 비율을 숫자로 박지 않는다(임의 문턱은 낡는다). **대신 이렇게 판단해라:**
```
비율 3 이상인 파일 → 🔴 그건 파일이 아니라 문서다. 먼저 해라
파일 머리 서사     → 그 파일을 **처음 여는 사람에게 필요한 것만** 남긴다
                     *왜 이렇게 됐나* 는 원장으로
```

## 판별자 — **`.o` 바이트 대조 0 (링크 전 — 실행파일은 LC_UUID 로 매 빌드 달라진다)**

```
✅ `.o` 바이트 대조 0 (링크 전 — 실행파일은 LC_UUID 로 매 빌드 달라진다) = "주석만 고쳤다" 의 증거다. 값으로 적어라
✅ 코드 줄 수 불변도 같이 적어라 — 증거가 둘이 된다
✅ 시험 통과 수 불변
🔴 무엇과 대조했는지 같이 써라 — `exit=0` 은 문장이 아니다
```


## 🔴🔴 그리고 이것이 이 작업의 절반이다 — **샘플 코드가 없다** (사용자 지시)

> 사용자 원문: *"**어떻게 사용해야하는지 샘플코드가 전혀 없다.**"*

주석이 많은데 **정작 "이렇게 쓰면 된다" 가 없다.** 예시가 전부 주석 처리되어 있다:
```
// sensors.on("A1", readA1);        ← 주석
// router.on("G1", myGate);         ← 주석
// {"A2","IP",3}, {"B2","IP",10}    ← 주석
초음파 예시                          ← 주석 (하드웨어가 없어 일부러 안 켰다 — 그 판단은 맞다)
`choice`(옵션 위젯) 예시             ← 🔴 **어디에도 없다**
켜져 있는 것 : `LD`(내장 LED) · `L2`(숫자) **둘뿐**
```

> ### **주석 안의 예시는 아무 검사도 안 받는다. 컴파일도 안 되고 시험도 없다.**
> ### **코드가 바뀌면 조용히 낡고, 기여자는 그것을 정답으로 베낀다.**
🔑 오늘 socket 이 `parking.h` 헤더 예시에서 정확히 그것을 찾았다 — *"이 파일만 읽으면 된다"* 고
선언한 파일의 예시가 **1인자 `sensor("A1")` · `main()` 안의 `openPorts`** 로 낡아 있었다.

### 그래서 걷어낸 자리에 **동작하는 예제**를 둬라

```
✅ 컴파일되는 완전한 예제로 옮긴다 — 주석이 아니라 코드
✅ `client.ino` 는 **최소 동작**만 유지 (지금처럼 LED 하나)
✅ 예제마다 하나의 개념 : 디지털 센서 · 아날로그/초음파 센서 · 토글 · 숫자 · **옵션(choice)**
🔴 형태는 네가 정해라 — 아두이노 관용은 `examples/` 지만 지금은 단일 스케치다.
   **판별자: 그 예제가 컴파일 검증을 받는가.** 받지 못하는 형태면 고르지 마라
⚠ 없는 하드웨어를 흉내 내지 마라 — 초음파를 안 켠 네 판단이 맞다.
   **예제는 "이렇게 쓴다"를 보여 주면 되고, 그것이 지금 보드에서 돌아야 하는 것은 아니다.**
   다만 **컴파일은 되어야 한다** — 그것이 낡음을 막는 유일한 장치다
```

### ⚠ 그러므로 이 REQ 의 성공 조건이 둘이다

```
① 작업일지를 걷었다        → 산출물 불변으로 증명
🔴 ② 걷어낸 자리에 **검증받는 예제**가 생겼다 → 컴파일 통과로 증명
```
**①만 하면 사용법이 통째로 사라진다.** 주석이 나빴던 것은 *양* 이 아니라 **검증받지 않는다**는 것이다.

## ⚠ 지키라

```
· include 순서·위치 안 건드린다
· 잠금 파일이 대상에 든다 → 검사가 발화하면 **정상이다**(차단이 아니라 설명 요구).
  답은 산출물 불변이고 커밋 메시지에 이 REQ 를 참조한다
· 🔴 **곧 다시 쓰일 구역은 건너뛰어라** — 두 번 일하게 된다. 새로 쓰는 구역은 처음부터 규칙대로
· 관측 창이 돌고 있다. 산출물이 안 바뀌므로 **창을 깨지 않는다** — 굽기·배포 불필요
```

## ⚠ 순서

**지금 해라.** `.o` 대조로 끝나므로 배포도 굽기도 필요 없고 관측 창을 안 깬다.
⚠ 다만 **REQ-0306(차단봉 제거)로 방금 배포한 판(`9a18c55`)이 돌고 있다.** 주석만 고치면
`.o` 가 같아야 하고, **다르면 그것이 신호다** — 코드를 건드린 것이다.

## 왜 필요한가

server 주석/코드 비가 0.55 이고 config.h 는 6.7(주석 327/코드 49)이다. 그리고 parking.h 헤더 예시는 컴파일도 시험도 안 받아 조용히 낡는다 — 오늘 1인자 sensor() 예시가 낡아 있던 것을 네가 찾았다

## 대상 파일

- `조별과제샘플/server/parking.h`
## 완료 기준

작업일지가 걷혔고 .o 대조 0 으로 증명됐다. 헤더 예시가 검증받는 형태로 옮겨졌거나 낡지 않음이 보장된다. 코드 줄 수 불변

---

## 처리 결과

<!-- 담당자가 여기에 적는다. 처리 후 반드시:
     team/bin/req.sh done REQ-0310 --by socket-engineer --note "<한 줄 요약>" -->

### 완료 — 2026-08-20 (배포 없음 · 산출물 불변)

## ① 작업일지를 걷었다 — 🔴 판별자는 **`.o` 바이트 대조**

```
기준   : 지금 배포 중인 `9a18c55` 트리로 만든 server.o   sha c7e12723…
결과   : config.h · parking.h · spot.h 를 고칠 때마다 재서 **cmp exit=0** (세 번 다)
🔑 링크 전 `.o` 다 — 실행파일은 LC_UUID 로 매 빌드 달라져 판별자가 못 된다
증거 둘 : config.h 는 **코드 49줄이 `diff` 0** 이다(주석만 바뀌었다)
```

| 파일 | 전체 | 주석 | |
|---|---|---|---|
| `config.h` | 397 → **242** | 327 → **174** | 비율 6.7 로 최악이었다 |
| `parking.h` | 302 → **298** | 167 → **163** | ②가 본체다 |
| `spot.h` | 63 → **53** | 39 → **29** | 머리의 단계 서사 제거 |

**걷은 것**: 날짜 · REQ 번호 · *"여기 있었다/없앴다/전에는"* · 실측 이력 · 사고 경위 · 단계 번호
**남긴 것**: 종속 · 불변식 · 함정 · 금지와 그 이유
🔑 **원장이 이미 갖고 있는 것만 걷었다.** 옮길 곳 없는 것은 안 지웠다.

⚠ **`config.h` 는 아직 주석이 많다(174/49).** 문턱 3 을 못 맞췄고, 맞추려 하지 않았다 —
**그 파일의 목적이 "근거가 붙은 숫자"** 이고 더 깎으면 종속이 떨어진다.
`TAKEOVER_GRACE_MS`(IP 다를 때만) · `ACK_TIMEOUT_MS`(유도값) 가 정확히 그 종속으로 지켜지는 값이다.
**비율을 맞추려고 종속을 지우는 것은 이 REQ 가 막으려던 일과 같은 부류다.**

## ② 🔴 걷어낸 자리에 **검증받는 예제** — `조별과제샘플/dev_server/EXAMPLES.cpp`

```
개념 하나에 함수 하나 :
  ① 최소 주차장  ② 모듈 없는 자리  ③ 일반영역  ④ 내 판정(behavior)
  ⑤ 조작 선언(toggle · number · **choice**)  ⑥ 명령 내기  ⑦ 결과 콜백
```
🔑 **`choice` 예시가 다시 생겼다** — REQ-0306 에서 `DR` 이 사라지며 없어진 그 자리다.
   (`SPEC-web-control` 의 *"샘플에 choice 예시가 없다"* 표시는 이것으로 답이 생겼다)

### 왜 파일인가 — 헤더 예시가 실제로 낡아 있었다
`parking.h` 의 *"쓰는 법"* 블록에 **없어진 `DR`** 이 있었고, **같은 줄이 두 번** 적혀 있었고,
`srv.closeDown()` 조각이 `main()` 이 없다는 문단 아래 떠 있었고, `gate` 를 언급하고 있었다.
> **주석 안의 예시는 컴파일도 시험도 안 받는다. 코드가 바뀌면 조용히 낡는다.**

### 검사를 걸었다 — 그리고 **빨강이 나는 것을 확인했다**
`net/sync_check.sh` 가 `lot.cpp` 와 **같은 문**으로 `EXAMPLES.cpp` 를 **두 트리의 `parking.h`** 에
대해 문법 검사한다. 파일이 없으면 초록이 아니라 **경보**다(대상이 사라지면 검사가 무장 해제된다).

🔴 **`lot.cpp` 가 못 잡는 것을 잡는다** — 그것이 이 파일의 값이다:
```
parking.h 의 `Control& choice(` → `choiceX(` 로 바꿔서 재니
  lot.cpp      ✅ 통과      (choice 를 안 쓴다)
  EXAMPLES.cpp 🔴 error: no member named 'choice'
```

## 검증
```
자가검증 149 pass / 0 fail · sync_check ✅(음성 대조 포함) · dev 트리 3-TU 링크 ✅
c++ -fsyntax-only -DIN= -DOUT= parking.h → 통과 (주석에 적은 재현 명령을 실제로 돌렸다)
배포 없음 · 굽기 없음 · 관측 창 안 깼다
```

## ⚠ 남긴 것 · 곁들여 본 것
```
· `state.h`(주석 167) · `lot.cpp`(2.33) 은 안 건드렸다 —
  lot.cpp 는 **곧 다시 쓰일 구역**이고(루트 지시), 그 교재 몫은 EXAMPLES.cpp 로 옮겨 가는 중이다
🔴 · 자가검증에 **드문 흔들림**을 봤다 — 약 30회에 1회 `✗` 하나(항목은 못 잡았다.
     그 뒤 20회 연속 0). **내 변경이 원인일 수 없다**(.o 바이트 동일).
     보류 목록에 올렸다 — 재현되면 그때 항목을 잡는다
```


### 처리 완료 · socket-engineer · 2026-08-20T18:17:35+0900

주석 정리(config.h 397→242 등) + EXAMPLES.cpp 신설 · .o 바이트 대조 0 으로 '주석만 고쳤다' 증명 · sync_check 이 예제를 두 트리에서 문법 검사(빨강 확인) · 149/0 · 배포 없음

