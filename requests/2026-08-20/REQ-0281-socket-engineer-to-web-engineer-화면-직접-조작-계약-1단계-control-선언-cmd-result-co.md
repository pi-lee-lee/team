---
id: REQ-0281
title: 화면 직접 조작 계약 1단계 — control 선언 · cmd_result · confirmed
from: socket-engineer
to: web-engineer
status: done
created: 2026-08-20T00:24:23+0900
updated: 2026-08-20T13:44:43+0900
files: ["docs/net/SPEC-web-control.md", "web/"]
parent: none
---

# REQ-0281 · 화면 직접 조작 계약 1단계 — control 선언 · cmd_result · confirmed

**요청자** `socket-engineer` → **담당** `web-engineer`

## 요청 내용

**명세가 나왔다 — `docs/net/SPEC-web-control.md` 를 읽어라.** 지어낼 자리를 안 남겼다.

## 🔴 네가 올린 빈 자리 둘에 답이 다 들어 있다

### ① `actions` 에 조작 모듈이 없다 → **`control` 선언으로 메운다**
사용자가 설계를 바꿨다: **기여자가 `lot.cpp` 에서 조작 UI 를 선언하고 화면은 그대로 그린다.**
`map` 의 모듈에 `control:{widget,label,...}` 이 붙는다. 위젯은 **셋뿐**이다(`toggle`/`number`/`choice`).
🔑 **화면은 `kind` 를 해석하지 않는다.** kind 가 늘어도 화면을 안 고친다.
🔴 **`control` 키가 없는 모듈은 조작 UI 를 그리지 마라** — 존재/부재 규칙이다.

### ② 🔴 `value` 가 bool 이라 7자리를 못 잰다 → **네 지적이 맞다. 그리고 지금은 못 넓힌다**
전선을 확인했다: 상행 `S` 의 모듈 상태는 **비트마스크**(`decode_mod_bits`)다.
**모듈당 1비트고, 숫자를 실을 자리가 전선에 없다.** 서버 선택이 아니라 전선 사실이다.

→ 네가 준 선택지 셋 중 **셋째**를 골랐다: *"숫자 모듈은 다른 완료 판정을 쓰고 그 사실을 명세에 박는다."*
**`value` 를 숫자로 넓히지 않는다** — 넓히면 `known:false`(모른다)를 `known:true`(안다·틀림)로
바꾸는 것이고, **값 경로가 없는데 값을 만들면 그게 지어내는 것**이다.

**대신 세 값을 갈랐다:**
```
value      장치가 **에코한 비트**
requested  서버가 **마지막으로 보낸 값**(숫자). 보낸 적 없으면 **키가 없다**
confirmed  🔴 화면은 **이 키 하나로 분기한다**
```
🔴 **숫자 모듈은 `confirmed:"settled"` 를 절대 내지 않는다. `"partial"` 이다.**
그리기 권고: ❌ *"1234567 로 설정됨"* → ✅ *"**1234567 보냄** · 에코 **켜짐** · ⚠ **값 확인 불가**"*

⚠ 값 에코 줄(2단계)은 **arduino 굽기가 필요해서** 1단계에 안 넣었다. 닫는 조건을 명세에 적었다.

## ✅ 그리고 네 전제 하나를 정정한다
> *"8자리를 넣으면 `거절` 이 보여야 사용자가 연습이 된다"*

🔴 **8자리 자체는 안 거절된다** — 전선은 32비트를 나른다.
**거절을 밟는 길 셋**: 선언 범위 밖(`out_of_range`) · 선언 안 된 모듈(`not_declared`) · 묶음 5건(`batch_too_big`).
🔑 **"거절이 보여야 연습이 된다"는 네 지적은 맞다. 밟는 길만 바꿨다.**

## 네가 옳게 한 것
**지어내지 않고 올렸다.** 임시 필드를 만들었으면 그건 가짜 자료로 만든 물건이 됐다.
🔑 그리고 **`value` 가 bool 이라는 것을 네가 안 짚었으면 나는 못 봤다** — 나는 전선만 보고 있었다.

## 시점
```
🔴 서버 구현이 아직 안 끝났다. **지금은 명세만 확정이다**
   `control` 선언 · 새 봉투 · confirmed 를 지금부터 만들 수 있지만
   **실물이 나가는 것은 내 배포 뒤다.** 배포하면 알린다
⚠ 명세와 실물이 갈리면 **실물이 아니라 나를 먼저 불러라** — 명세가 정본이고 내가 고친다
```

## 왜 필요한가

화면이 조작할 경로가 0 이고, value 가 bool 이라 숫자 모듈의 완료를 못 판정한다

## 대상 파일

- `docs/net/SPEC-web-control.md`
- `web/`
## 완료 기준

화면이 control 선언대로 위젯을 그리고, cmd_result 셋으로 갈라 표시하며, 숫자 모듈에서 settled 를 한 번도 안 받는다

---

## 처리 결과

<!-- 담당자가 여기에 적는다. 처리 후 반드시:
     team/bin/req.sh done REQ-0281 --by web-engineer --note "<한 줄 요약>" -->

### 진행 중 — **완료 기준 셋을 코드로 대조했다** (2026-08-20 13:41 · web)

⚠ **닫지 않는다.** 셋 중 둘은 값으로 섰고 **하나는 실기 관측 항목이라 배포 전에는 못 잰다.**

```
① control 선언대로 위젯을 그리나        ✅ 실측 — 주입 36 pass / 0 fail
② cmd_result 셋을 갈라 표시하나          ✅ 실측 — 같은 하니스
③ 숫자 모듈에서 settled 를 안 받나       🔴 **미측정** — 실기 관측 항목. 화면이 미배포다
```
근거: `node web/tools/mod-control.mjs` → **36 pass / 0 fail / 3 미측정**
(존재/부재 · 위젯 셋 · 모르는 위젯 표시 · 라벨 폴백 셋 · 범위 차단 · 봉투 필드 · partial 문구 ·
outcome 셋(ok/rejected/no_answer) · 전선 전/후 거절 문구 분리 · 주기 갱신이 입력 칸을 안 덮음)

### 🔴 ③ 은 "안 됐다"가 아니라 **잴 수단이 없었다** — 그래서 만들었다

`mod-control.mjs` 의 skip 문구가 *"`--live` 로 재라"* 였는데 **그 `--live` 가 구현돼 있지 않았다.**
그대로 두면 ③ 은 영원히 미측정으로 남는다(§"감시할 수 없는 것을 조건으로 적지 마라").

`web/tools/mod-control.mjs` 에 실기 관측 경로를 넣었다 — **주입 0 · 클릭 0 · 하행 0**:
```
· 🔴 브라우저를 띄우기 전에 판본을 본다 → 낡으면 WS 한 개도 안 열고 **판정 없이** 끝낸다(exit 2)
· control 선언을 **서버가 보낸 map 에서** 뽑는다 (기대값을 피검체에서 만들지 않는다)
· state 를 관측 창(기본 30초) 동안 훑어 number·choice 의 `confirmed` 표본을 모은다
· 🔴 표본이 **닫힌 값 0건**이면 **미측정** — 하행이 없으면 전부 `unknown` 이라 공허 초록이 된다
· 하행이 필요한 §6 두 항목(outcome 셋 실물 · 보낸 건수 == queued+rejected)은
  **이 도구가 스스로 하행을 걸지 않는다**(실물 조작 · 루트 순서). 사람이 누른 것은 창에 잡힌다
```
**실행한 것 / 안 한 것을 가른다:**
```
✅ 판본 게이트를 실제로 밟았다 — `--live 9990` → 서빙 21b9987f963e ≠ 원본 fd3150fc7a12 →
   `0 pass / 0 fail / 1 미측정` · exit 2 · 브라우저 미기동
🔴 관측 본문(표본 수집·판정)은 **아직 한 번도 안 돌았다** — 배포된 화면이 없으면 도달할 수 없다
✅ 주입 검사 회귀 없음 — 패치 전후 **36 pass / 0 fail / 3 미측정** 동일
```

### ✅ v2(REQ-0284)가 이 계약을 덮었나 — **아니다**
`docs/net/SPEC-assembly-v2.md` 마지막 줄이 명시한다:
`control` · `label` · `send_cmd` · `cmd_result` · `confirmed` **그대로**.
v2 가 바꾼 것은 자리 쪽(`kind` 가 `parking`/`area` 둘 · `module()` 합침 · 등록 전 `value_total:0`)이고
**그건 REQ-0284 에서 이미 화면에 반영했다**(`b44ed53` · `508f68c`).

### 남은 것 — 배포 뒤 한 명령
```
① root/socket : v2 배포 + 화면 배포(node web/tools/deploy-screen.mjs --deploy --port 9990)
                ⚠ ~/parking-bin 은 프로젝트 밖이라 **web 이 못 친다**
② web         : node web/tools/mod-control.mjs --live 9990 --secs 60
                → 여기서 ③ 이 초록이거나 **미측정**으로 나온다(하행 0건이면 미측정이 정답이다)
🔴 ③ 을 초록으로 닫으려면 하행 표본이 필요하다 — **누가 언제 무엇을 누르나**는 루트가 정한다
```
원장에 남긴 것: `docs/web/LEDGER.md` **§5.90**(판별자는 위젯이다 · 거절 두 갈래 · 부재 주장의 분모).

---

## 닫음 — ①② 초록 · ③ 미측정 (루트 지시 2026-08-20 13:5x)

> 사용자: *"쓸모없는 검토 그만해라. 왜 그걸 다들 하고 있냐고."*
> 루트: **③ 측정 요구를 접는다. 실기 관측 안 한다.** 화면 배포·하행 표본·60초 관측 전부 안 한다.

```
① control 선언대로 위젯을 그린다        ✅ 주입 실측 36 pass / 0 fail
② cmd_result 셋을 갈라 표시한다          ✅ 같은 하니스 (전선 전/후 거절도 문구가 갈린다)
③ 숫자 모듈에서 settled 를 안 받는다     ⏭ **미측정 — 하행 표본 없음.** 판정하지 않는다
```
🔴 **③ 을 초록으로 적지 않는다.** 재지 않은 것을 통과로 적으면 다음 사람이 확인된 것으로 읽는다.
측정 경로(`mod-control.mjs --live`)는 만들어 뒀다 — **필요해지면 한 줄이다.** 지금 안 돌린다.

### 처리 완료 · web-engineer · 2026-08-20T13:44:43+0900

①control 위젯 ②cmd_result 셋 갈라 표시 = 주입 실측 36 pass/0 fail 로 닫음. ③숫자 모듈 settled 0건은 '미측정 — 하행 표본 없음' 으로 명시(루트 지시로 실기 관측 접음). --live 경로는 만들어 뒀고 돌리지 않았다.

