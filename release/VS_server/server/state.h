// state.h — 서버가 들고 있는 상태 전부(필드·계수기·중첩 타입). `struct Server` 의 몸통 조각 — server.cpp:178
// ⚠ 단독 컴파일 불가 · include 자리가 곧 문법이다 · 📖 server.cpp "목차"
    // ═══════════════════════════════════════════════════════════════════
    // ⚠ **여기는 "무엇을 기억하는가"다. "무엇을 하는가"는 다른 파일에 있다.**
    //   계수기를 새로 더할 때 §"조건을 적었으면 그것을 보는 감시를 같은 자리에 만들어라" 를 읽어라 —
    //   **세는 것만 만들고 볼 자리를 안 만들면 `0` 조차 안 나온다.**
    // ═══════════════════════════════════════════════════════════════════
    Slot slots[10];

    // ── 지형 ──────────────────────────────────────────────────────
    // 🔴 **`5` 를 코드에 박지 않는다.** 기본값이고 설정에서 온다(설계 §0).
    // ── 필드 ──────────────────────────────────────────────────────────────
    //   🔴 **순서가 곧 배치다.** 옮기면 `.o` 가 달라진다 — 검산이 그것을 잡는다.
    //   ⚠ 아래는 **필드 → 함수 → 타입** 순으로 완전히 갈려 있지 않다.
    //     가르려면 순서를 바꿔야 하고, 그것은 이 단계가 하지 않기로 한 일이다.
    int grid_rows, grid_cols;
    // 🔴 **지형과 판은 `Lot` 의 것이다**. 여기서는 그것을 들고만 있다.
    //   🔑 **판(`epoch`)은 지형을 바꾸는 함수 안에서 그 줄 옆에서 올린다**(설계 §6.8).
    //     떨어뜨리면 **"바꿨는데 안 올린 경로"** 가 생기고, 그건 **탐지 장치가 있는데 안 울리는 것**이라
    //     아예 없는 것보다 나쁘다. → `bump_epoch()` 하나만 그 일을 한다.
    Lot lot;
    std::string last_screen_build_;   // 🔑 같은 판본을 반복해 찍지 않는다(요청마다 오면 로그가 덮인다)
    // 🔴🔴 **`epoch` 는 *이 서버 인스턴스 안에서만* 단조다**(설계 §6.8).
    //   재기동하면 0 부터 다시 시작하므로, 화면이 옛 판을 들고 있으면 **새 서버의 판 1 을
    //   "늦게 온 옛 프레임"으로 무시한다** — 그러면 화면이 영영 새 맵을 안 받고 **오류도 안 뜬다.**
    // 🔑 **"화면이 재접속 때 판을 버려라"를 규칙으로 두면 그건 두 곳에 있는 규칙이다.**
    //   **값으로 실으면 화면이 안 잊는다.** → 봉투마다 `srv_id` 를 같이 보낸다.
    std::string srv_id;
    // `get_map` 남용 방어 — **정합성용이 아니라 서버 보호용**이다(설계 §6.8)
    long long getmap_win_ms; int getmap_in_win; long long getmap_rejects;
    sock_t lsn_ard, lsn_http, lsn_phone;
    std::map<sock_t, std::string> phones;
    std::map<sock_t, std::string> phone_close_why_;   // 끊긴 사유(상대 FIN / errno)
    std::map<sock_t, long long>   phone_since_;       // accept 시각 — 유지 시간을 낸다
    // 🔴 **접속 세대.** accept 마다 +1. `0` = 아직 한 번도 안 붙었다.
    //   🔑 호출자가 *"방금 붙었나"* 를 **자기 상태로** 판정하게 하려는 것이다 —
    //     소비형 술어(`justConnected()`)로 만들면 **읽는 것이 상태를 바꾼다.**
    //     세대 수를 주면 호출자가 자기 값과 비교한다. **숨은 부작용이 없다.**
    long long phone_epoch_;
    // 🔴 **요약에 폰 접속 칸이 없었다**. 그래서 사람이
    //   이름이 비슷한 `WS접속`(=화면 9990)을 골라 읽었다. **이름이 가장 잘 속인다.**
    size_t    phone_max_;            // 동시 최다 — `nc` 와 앱이 공존한 것이 여기 남는다
    long long phone_hold_max_ms_;    // 유지 시간 최대 · 🔑 **절단 축이 요약만으로 보인다**
    long long phone_hold_last_ms_;   // 마지막 유지 시간
    // ⚠ **중앙값은 안 낸다** — 표본을 들고 있어야 하고, 그 비용을 낼 요구가 아직 없다.
    //   🔑 그러니 이 둘로 답할 수 없는 것을 적어 둔다: **분포의 모양은 모른다.**   // 폰 연결 → 수신 버퍼 (연결마다 따로!)
    // ⚠ `ard` 는 이제 "아두이노 연결"이 아니라 **주차 노드의 연결**이다.
    // 이름을 안 바꾼 이유: 이 필드에 얽힌 상태·지표가 79곳인데, 그 전부를 건드리면
    // **하위호환을 코드로 증명할 수 없다.** 지금 도는 단일 노드(P1)가 그대로 동작해야 하고
    // 그게 조원 배포 전까지 유일한 실물이다. 그래서 주차 노드 경로는 **한 줄도 안 바꾸고**,
    // 보조 노드를 옆에 붙이는 쪽을 택했다. 단일 노드 동작이 구조적으로 보존된다.
    // 🔴 **진실은 여기 있다**. 아래 별칭들은 전부 이 안을 가리킨다.
    Node park;
    // ⚠ **별칭은 옛 이름을 유지하기 위한 것이다.** 새 코드는 `park.<필드>` 를 직접 쓴다 —
    //   별칭을 늘리지 마라. 2단계에서 `map<devid,Node>` 로 올릴 때 **별칭이 많을수록 못 올린다.**
    sock_t&      ard      = park.fd;   // **주차 노드**의 연결 (여전히 하나)
    std::string& park_dev = park.devid;  // 주차 노드의 device_id. "" = 미정(first-S-wins)
    std::string& ard_peer = park.peer;   // 현재 주차 노드 소켓의 "IP:포트" (REQ-0215)
    // 🔴 **같은 devid 로 동시에 붙은 것을 센다.** 0 이 아니면 관측 자료가 오염된 것이다 —
    // **어느 보드가 응답했는지 모르는 채로 지표를 읽게 된다.**
    long long reg_ok;          // 등록 완료 수
    long long reg_bad;         // 🔴 형식·개수·이름 위반. **조용히 넘어가지 않는다**
    long long reg_qsent;       // 보낸 `Q` 수(누적)
    long long reg_giveups;     // `Q` 3회에도 안 와서 굳힌 수
    long long reg_widthbad;
    // 🔴 등록 전이라 자리 비트열을 못 푼 프레임 수. **0 이 아니면 그 구간 자리 상태가 없다**
    long long occ_undecoded; bool occ_undecoded_warned;    // 🔴 삼중 검산 ③ 불일치 — `S` 의 hex 폭이 선언 `n` 과 안 맞는다
    // 🔴 **분모를 찍는다** — 자가 치유(§7.6)가 **한 번도 안 돌았는데
    //   아무 흔적이 없었다.** 폭 검사가 *닫히는* 쪽으로 실패해서 오독조차 안 남긴 것이다.
    //   ⚠ **`0` 이 안 남는 결함은 계수로 못 잡는다** — 그래서 "몇 번 고쳤나"(분자)가 아니라
    //   **"몇 번 검사했나"(분모)** 를 센다. `heal_checks == 0` 이면 그 자체가 빨강이다.
    // 🔴 **`occ` 의 비트는 자리 점유만이 아니다** — 비트 `>= SLOT_N` 은 **액추에이터의 현재 상태**다
    //   (arduino 답변 · 명세 §5 "위험 다섯째"). **조작 완료를 판정하는 값이 이것이다.**
    //   ⚠ 새 칸을 만들면 같은 값이 두 곳에 생기고 **갈리는 순간 어느 쪽이 맞는지 알 방법이 없다.**
    //   🔑 그래서 칸을 안 늘리고 **이미 오는 값을 제대로 읽는 쪽**을 골랐다(전선 예산 증가 0).
    // ~~int mod_bits[REG_MODS_MAX];~~ → **`Node` 로 내렸다**(②-c). 노드마다 한 벌이다.
    // 🔴 **마지막으로 요청한 값**(모듈 인덱스 → 0/1). **완료 판정은 대조이지 존재 확인이 아니다.**
    //   ⚠ 처음엔 "그 모듈의 에코가 있으면 settled" 로 짰는데 **그건 "무엇이 됐는가"를 안 본다** —
    //     열라고 했는데 닫혀 있어도 `settled` 가 됐다. **거짓 완료를 막으려던 값이 거짓 완료를 만든다.**
    //   🔑 그리고 `pend` 는 ACK 에 지워지므로 **거기서는 대조할 수 없다.** 별도로 들고 있어야 한다.
    // 🔴 `std::map<int,int> gate_want;` 가 여기 있었다. **2026-08-20 에 없앴다.**
    //   게이트 내장 조작을 지운 뒤로 **쓰는 곳이 하나도 없었고**, 그것을 읽던 자리의
    //   `completion` 도 같이 없앴다. **읽는 곳도 쓰는 곳도 없는 상태를 남기지 않는다** —
    //   🔑 죽은 필드는 **살아 있는 것처럼 읽힌다.**

    // ═══ 8081 자리 선택 (SPEC-web8080-8081-2026-08-26) ═══
    //   🔴 **포트가 아니라 살아 있는 WS 연결로 센다.** HTTP GET 은 순간이라
    //     *"페이지를 받아 갔다"* 와 *"지금 사람이 보고 있다"* 를 못 가른다.
    std::map<sock_t, bool> choosers_;

    // ═══ 이용자용 두 포트 ═══
    //   🔴 **HTML 과 WS 를 같은 포트에서** 처리한다(9900 과 같은 형태).
    sock_t lsn_find_  = BAD_SOCK;    // 8080
    sock_t lsn_choose_ = BAD_SOCK;   // 8081
    // 🔑 **어느 포트로 들어온 연결인가** → `/` 요청에 줄 문서가 갈린다.
    //   ⚠ 없으면 기본(`index.html`)이다 — 9900 은 여기에 안 들어간다.
    std::map<sock_t, std::string> conn_doc_;

    // 🔑 **끊긴 연결을 여기서 걷는다.** 연결 해제 자리가 세 군데(`nodes.h`·`serve.h`·`wire.h`)라
    //   거기마다 훅을 달면 **하나를 빠뜨리는 순간 유령이 남는다.**
    //   ★ 그래서 목록을 따로 관리하지 않고 **`conns` 를 정본으로 삼아 매번 맞춘다.**
    // ── 함수 ──────────────────────────────────────────────────────────────
    int chooser_count() {
        for (std::map<sock_t, bool>::iterator it = choosers_.begin(); it != choosers_.end(); ) {
            if (!conns.count(it->first)) choosers_.erase(it++);
            else ++it;
        }
        return (int)choosers_.size();
    }

    std::map<std::string, long> mod_req;
    // 🔑 재등록 직전의 모듈 목록(`prev_mods_snapshot`)은 **`Node` 로 옮겼다** —
    //   전역 한 벌이면 두 보드가 거의 동시에 재등록할 때 서로의 직전 구성을 덮어
    //   **없는 "구성 변경"** 을 만든다. 죽은 필드는 살아 있는 것처럼 읽히므로 여기서 지웠다.
    long long mod_order_changed;   // 🔴 약속이 깨진 횟수. **0 이 정상이고 1 이상은 조사 대상이다**
    // ~~int mod_bits_n;~~ → **`Node` 로 내렸다**(②-c).
    // 🔴 **분모를 같이 만든다.** `장치거절 0` 만 찍으면 **`0` 이 혼자 서서 건강처럼 보인다** —
    //   `치유 0/386` 은 분모가 보여서 `0` 이 건강임을 말할 수 있는데, 이건 말할 수 없다.
    //   ⚠ **`0/0` 은 최소한 `0/0` 이라고 말해 준다. 분모 칸이 없는 것이 더 나쁘다**(monitor 지적).
    // 🔴 **자리가 아니라 이름으로 모은다**(monitor·루트 합의).
    //   `1/2/3` 처럼 순서로 기억하는 숫자는 반드시 한 번은 어긋나 읽힌다
    //   (`등록 145/133/146` 이 셋 다 맞았는데 어느 자리를 세는지가 안 붙어 있었다).
    //   🔑 **이름을 붙이면 순서를 기억할 필요가 없다.**
    //
    //   띄움 M ─(전선에 나갔나)→ 도달 D ─(답이 왔나)→ 응답 K ─(무엇이라 했나)→ 거절 N
    //   ⚠ **각 화살표가 다른 고장이다**: 큐에서 죽음 · 나갔는데 무응답 · 갔고 거절당함.
    //     두 수만 두면 **무응답이 성공에 섞여 좋아 보이는 쪽으로 틀린다** — 가장 나쁜 방향이다.
    long long gate_q;            // 띄움 — 큐에 들어간 수
    long long gate_sent;         // 도달 — 실제로 전선에 나간 수
    long long gate_ans;          // 응답 — ACK 이 온 수 (result 무관)
    long long dev_reject;        // 🔴 `result=3`(장치가 거절) — `ack_fail_count`(안 갔다)와 **다른 칸**이다
    long long heal_checks;       // §7.6 예약 불일치 **검사**가 실제로 돈 횟수 (분모)
    long long heal_fires;        // 그중 불일치를 찾아 `C` 를 재하달한 횟수 (분자)
    long long res_undecoded;     // `S` 의 예약 마스크를 못 읽은 프레임 수
    bool      res_undecoded_warned;
    long long dup_devid_reject;   // ①에서 거절한 횟수 = **다른 IP 가 산 자리를 노렸다**
    long long takeover_grace;     // 유예를 넘겨 교체한 횟수 = 진짜 죽은 것으로 판단
    std::map<std::string, AuxNode> aux;   // device_id → 추가 노드 (**상·하행 모두**)
    std::vector<UnknownSock> unknown;     // id 미상 소켓 — 첫 유효 프레임에서 승격
    // 🔑 `aux_conflicts` 가 여기 있었다 — *"주차 노드가 아닌데 `S` 를 보낸 장치 수"*.
    //   **멀티에서는 그것이 이상 상태가 아니라 정상이다.** 모든 노드가 `S` 를 보낸다.
    int  admit_rejects;                // 상한 초과로 거절한 연결 수
    std::string& ard_buf   = park.buf;
    bool&        ard_seen  = park.seen;
    long long&   ard_last_ms = park.last_ms;

    // ── 하행 슬롯 큐 (docs/net/DESIGN-server-slot-queue.md · 반송파 슬롯 구조)
    //
    // **이벤트가 전송을 만들지 않는다.** 명령이 생기면 큐에 담고, **장치 프레임이 도착한
    // 순간(= 내 창의 시작)** 에 큐 전부를 **한 거래로 묶어** 보낸다.
    //
    // 🔑 **창 시작은 시계가 아니라 사건이다** — 장치가 느려지면 내 창도 같이 밀린다(자기교정).
    // 그래서 위상을 추정할 필요가 없다. 추정은 상대가 정상일 때만 맞는데, 우리가 고치려는
    // 것은 상대가 비정상인 순간이다(원장 §8.19).
    // ── 타입 ──────────────────────────────────────────────────────────────
    struct DownQ {
        // 🔴 **이 줄을 받을 장치.** 큐가 하나라도 **소켓은 노드마다 따로**라, 이 칸이 없으면
        //   배출이 *"큐의 맨 앞"* 을 아무 노드에나 보낸다 — P2 로 갈 줄이 P1 에 떨어진다.
        //   ⚠ 그리고 그건 **오류를 안 낸다.** 받은 보드에 그 모듈이 없어 조용히 버려질 뿐이다.
        std::string devid;
        std::string line;        // 전선에 나갈 완성된 줄(LF 포함)
        sock_t      ws_fd;
        std::string brid;        // 브라우저 rid
        std::string slot;
        uint16_t    wire_rid;
        long long   deadline_ms; // enqueue 때 **한 번 박는다**
        // 🔴 **창 원자성 표지.** 0 = 낱개. 같은 값이면 **한 창에 전부 나가거나 전부 미뤄진다.**
        //   ⚠ 없으면 "묶어진다"(관찰)이고 있으면 "묶는다"(보장)다 —
        //     내 셋을 넣기 전에 남의 것이 큐에 있으면 **호출자 모르게 쪼개진다.**
        long long   batch_id;
        // 🔑 **분류는 이미 코드에 있었다**: `ws_fd == BAD_SOCK` = 서버 내부 발생.
        // 재동기·치유·은퇴·폰 배정이 전부 그것이고, 브라우저 조작만 fd 를 갖는다.
        // **기준**: "이 명령이 없으면 서버와 장치가 갈린 채로 남는가?" → 그렇다면 중요다.
        // 🔴 **치유 명령이 연타에 밀려나면 치유 자체가 죽는다.**
        bool        important;   // true = 중요(거절 금지 · 배출 우선)
    };
    std::vector<DownQ> downq;
    long long downq_bytes;
    long long batch_count;       // 배치 거래 수
    long long batch_lines;       // 배치로 나간 줄 수
    // 🔴 **정반대인 둘을 한 칸에 세지 않는다**(원장 §8.16 이 `error` 에서 겪은 그것이다).
    //   `q_rejected` = 큐가 넘쳤다 → **설계 문제**(cap·깊이를 다시 봐야 한다)
    //   `q_nodev`    = 장치가 없다 → **링크 문제**(cap 과 아무 상관 없다)
    // 합쳐 두면 `큐거절 3` 을 보고 cap 을 올리려 하는데 실제로는 장치가 없었던 것일 수 있다.
    long long q_rejected;        // queue_full 로 거절한 수(두 축의 합)
    // 🔴 **바이트 축과 건수 축을 갈라 센다.** 같은 `queue_full` 이라도 처방이 다르다:
    //   바이트 초과 → `cap`(전선이 못 나른다) · 건수 초과 → 장치 ACK 배출률(장치가 못 삼킨다)
    //   합쳐 놓으면 `cap` 을 올려 놓고 왜 안 낫는지 몇 시간 찾게 된다(§8.23-(38) 과 같은 함정).
    long long q_full_n;          // 그중 **건수 축**으로 거절한 수
    int       s_max_b;           // 관측된 `S` 프레임 최대 길이 — 배출 6 의 전제 감시
    bool      s_worst_warned;    // 전제 붕괴 경고를 한 번만 낸다
    long long q_nodev;           // 장치 연결이 없어 거절한 수
    long long q_dup;             // already_pending 로 거절한 수
    // ⚠ **창 수**다(창당 1). 줄 수로 세면 같은 줄이 창마다 다시 세어져 누계가 부풀려진다 —
    // 원장 §4.2 의 "28건은 실질 3개 사건" 과 같은 형태의 함정이다.
    long long q_deferred;        // 바이트 상한에 걸려 뭔가를 남긴 **창의 수**
    long long q_dropped_link;    // 세션이 끝나 큐에서 버린 줄 수 — **조용히 사라지지 않게 센다**
    // ⚠ **RTT 를 추측하지 않는다.** `pend` 에 `sent_ms` 가 있고 ACK 도착 시각을 아니까
    // **왕복이 실측으로 쌓인다.** 그 값은 `RTT + 장치 처리시간` 이라 **RTT 의 상한**이고,
    // 창을 좁히는 방향이라 **보수적 = 안전**하다.
    // 🔑 세션마다 초기화한다 — 옛 세션의 병리적 한 건이 새 세션의 창을 영구히 조이면 안 된다.
    long long rtt_max_ms;        // 이 세션의 최대 왕복(창 계산에 쓰는 값)
    long long rtt_last_ms;       // 마지막 왕복 — 보고용
    long long rtt_n;             // 표본 수. **0 이면 "안 쟀다"이지 "0ms"가 아니다**
    long long win_skips;         // 창을 이미 지나 다음 `S` 를 기다린 횟수
    long long dmax_flushes;      // `S` 가 안 와서 창을 포기하고 쏜 횟수
    // 🔴 **창 포기도 박자를 지킨다.** `tick()` 은 200ms 마다 도는데 조건이 "조용하다" 하나면
    // 장치가 침묵하는 내내 **200ms 버스트**가 된다(재전송이 1500ms 마다 큐를 채우므로 상시화된다).
    // 그러면 **장치가 가장 힘들어하는 구간에서 슬롯 규율이 통째로 꺼진다** —
    // ⚠ **서버에 조용한 것은 장치가 송신 안 한다는 뜻이 아니다**(`busy s...`·`[TX-RESYNC]` 가
    // 정확히 그 상태다). 설계 §3 의 "포기하고 쏜다"는 단수다. 슬롯당 1회로 묶는다.
    // 🔑 **이 둘은 `Node` 로 옮겼다**(`n.last_dmax_ms`·`n.dmax_armed`) — 침묵은 보드마다 따로다.
    //   전역으로 두면 조용한 보드 하나가 **말짱한 보드의 창까지 포기시킨다.**
    // 🔴 **창 포기는 침묵 한 번에 한 번이다**
    // 종전 규칙은 **침묵이 길수록 더 많이 쏘았다**: 8초 침묵이면 2.4초에 첫 포기, 그 뒤
    // 슬롯마다 한 번 = **약 5회**. 하필 **장치가 가장 못 받는 구간에서 가장 많이 쏜 것**이다.
    // 🔑 **재시도는 조건이 바뀌었을 때 뜻이 있다. `S` 가 안 온 동안 조건은 안 바뀐다** —
    //   같은 근거로 다시 쏘는 것은 새 시도가 아니라 같은 시도의 반복이다.
    // → 장치가 **한 줄이라도 말하면** 다시 무장한다(그것이 "조건이 바뀌었다"의 관측 가능한 형태).
    // §9.1 `device.last_frame_ts` 전용 — **epoch 시각**이다. ard_last_ms 를 쓰면 안 된다:
    // 그건 now_ms() 기반(윈도우에서는 부팅 후 경과)이라 바깥으로 나가면 수십 년짜리 값이 된다(28행 경고).
    long long&   ard_last_epoch_ms = park.last_epoch_ms;
    long long ard_uptime;
    long  ard_seq;
    std::string ard_dev;

    // ── 세션을 가로지르는 uptime 기억 ──────────────────────────
    // **왜 별도 필드인가**: `ard_uptime` 은 재연결 때 -1 로 버려진다. 그건 결함이 아니라
    // §7.4 가 의도한 것이다 — 기준선 없이 판정하면 연 480회 오탐이 돌아온다.
    // 그래서 그 필드는 **손대지 않는다.** 대신 세션을 넘겨 살아남는 기억을 따로 둔다.
    //
    // **무엇을 고치는가**: 지금 서버는 재연결을 전부 "재부팅"이라고 부른다. 실제로는
    // 08-16 기준선에서 새 연결 36건 중 **35건이 재부팅이 아니라 링크 재접속**이었다.
    // 17:21:36 에 서버가 "재부팅 감지"를 찍었지만 uptime 은 1286 → 1295 로 **늘었다.**
    // 장치는 죽지 않았는데 장부에는 재부팅으로 남는다.
    //
    // ⚠ **이 값은 보고 전용이다. 어떤 판정에도 먹이지 않는다.**
    //   `uptime_says_reboot()` 이 보는 것은 여전히 `ard_uptime` 뿐이다.
    long long xs_uptime;          // 직전 세션에서 마지막으로 본 uptime (-1 = 기억 없음)
    long long xs_last_ms;         // 그 프레임이 **도착한** 시각(now_ms). 공백 G 를 재는 데 쓴다
    std::string xs_dev;           // 그 uptime 의 주인 — 다른 장치면 비교하지 않는다
    long long xs_reconnect_reboot;   // 재연결인데 uptime 이 되감김 = 진짜 재부팅
    long long xs_reconnect_link;     // 재연결인데 uptime 이 이어짐 = 링크만 다시 선 것
    long long xs_reconnect_unknown;  // 기억이 없거나 장치가 달라 못 가른 것

    // ═══ 🔴 **노드별 재연결 장부** — 위 `xs_*` 는 주 노드 하나만 기억한다 ═══
    //   그래서 보조 노드 넷은 *"리셋인가 링크인가"* 가 **아예 안 나왔다**(`0/0`).
    //   ⚠ 아무리 정확히 세도 **없는 줄은 못 센다** — 관측자가 편향돼 있으면 세는 법으로는 못 고친다.
    //   🔑 키는 **devid** 다. 소켓이 바뀌어도 같은 장치면 이어서 본다.
    struct ReconMem {
        long long uptime;   // 직전 세션에서 마지막으로 본 uptime (-1 = 기억 없음)
        long long last_ms;  // 그 프레임이 도착한 시각 — 공백 G 를 잰다
        ReconMem() : uptime(-1), last_ms(0) {}
    };
    std::map<std::string, ReconMem> recon_;
    // 🔵 `xs_*`(주 노드 전용 장부)와 `recon_[park]` 이 **어긋난 횟수.**
    //   둘은 같은 프레임에서 같은 값을 받으므로 **0 이어야 한다** — 0 이 아니면 이관이 샜다.
    long long recon_split_ = 0;

    // ── 이음매 큐 ────────────────────────────────────────────
    // 디바이스 계층이 도메인 함수를 **직접 부르지 않게** 하는 것이 이 단계의 목표다.
    // "장치에서 이런 일이 있었다"만 담고, 그것이 무엇을 뜻하는지는 도메인이 정한다
    // (`server_seam.h` 의 계약). 소비는 `run()` 루프 끝에서 한 번에 한다.
    //
    // ⚠ 단계 C 는 **한 종류씩** 옮긴다. 지금 옮긴 것은 `DEV_DISCONNECT` 뿐이고
    // 나머지 호출은 아직 직접 호출로 남아 있다 — 중간 상태지만 **매 조각이 동작한다.**
    std::vector<DeviceEvent> pending_events;

    std::map<sock_t, Conn> conns;      // HTTP/WS 클라이언트
    std::map<uint16_t, Pending> pend;
    // 🔴 발행 커서. **단조 증가**하고 전선에 나가는 값은 `rid_cursor % RID_SPACE` 다.
    //   단조로 두는 이유: 디스크에 적을 때 "몇 바퀴째인지"가 필요 없어지고, 재시작 이어받기가
    //   **더하기 하나**가 된다. 순환값만 들면 "앞"과 "뒤"가 모호해진다.
    long long rid_cursor;
    long long rid_reserved_to;         // 디스크에 **미리 적어 둔** 상한. 넘으면 새 블록을 예약한다
    bool      rid_persist_on;          // 🔴 기본 false — `rid_cursor_load()` 를 부른 실기 경로에서만 켠다
    std::string rid_cursor_file;       // 절대경로. 빈 값이면 영속 불가
    // ── 카메라 pull (shots.h · SPEC-camera-pull.md) ───────────────────────────
    // 🔑 대장 하나가 **유일한 출처**다 — 별도 seq 커서 파일을 안 만든다.
    //   기동할 때 이 대장에서 **오늘 날짜의 최대 seq** 를 찾아 이어 발급한다.
    //   ⚠ 진실을 둘로 두면 갈릴 자리가 생긴다(`rid` 는 커서와 격리표가 다른 것이라 갈랐다).
    // 🔴 **`rid` 무리 *뒤* 에 둔다.** 사이에 끼우면 선언 순서와 초기화 순서가 어긋나
    //   `-Wreorder-ctor` 가 난다(실제로 냈다). **무리를 쪼개지 마라.**
    std::deque<Shot> shots_;           // 오래된 것이 앞. SHOT_RING_CAP 초과 시 앞에서 뺀다
    long long   shot_day_;             // 지금 발급 중인 날짜(yyyymmdd). 바뀌면 seq 를 1 로
    int         shot_seq_;             // 마지막 발급 seq (0 = 아직 없음)
    std::string shots_file_;           // 절대경로. 빈 값이면 영속 불가
    bool        shots_persist_on_;     // 🔴 기본 false — `shots_load()` 를 부른 실기 경로에서만
    long long   shot_issued_, shot_answered_, shot_failed_;
    long long   shot_exhausted_;       // 🔴 seq 9999 소진으로 거절한 수. wrap 하지 않는다
    long long   shot_evicted_;         // 링에서 밀려난 수(정상이다 — 분모로 쓴다)
    long long   shot_orphan_;          // 🔑 대장에 없는 shot 응답. **재기동 뒤 늦은 응답**이 이 모양이다
    long long   shot_sentinel_;        // 🔴 번호판이 문자 그대로 "-1" 이라 거절한 수
    long long   shot_multiphone_;      // 🔴 폰이 둘 이상일 때 낸 요청 수(누적 사건)
    long long   shot_nophone_;         // 폰이 없어 발급 못 한 시도(누적). 출력은 아끼고 계수는 다 센다
    // 🔑 직전 요약 시점의 스냅샷 — **최근 60초 도착률**을 만든다.
    //   `mutable` : `soak_line()` 이 `const` 이고 그 호출이 곧 구간 경계다.
    mutable long long prev_eframes_, prev_evalues_;
    // 🔴 `E` 가 나른 값 / 빈 칸(= 못 쟀다). 🔑 **도착률이 아니라 전이의 내역이다** —
    //   `E` 는 전이 때만 오므로 *"몇 %가 왔나"* 를 물으면 안 된다(§15.2 : 사건이지 상태가 아니다).
    long long evalues_, emiss_;
    // 격리표 — rid → (재사용 가능 시각, **해제 순번**). `pend` 를 떠날 때 여기 들어간다.
    //
    // 🔴 **순번을 시각과 따로 든다.** 처음엔 시각 하나만 들고 "가장 이른 시각"을 가장 오래 묵은
    //   것으로 삼았는데 **자가검증이 그것을 깨뜨렸다**: 발행이 빠르면 `now_ms()` 가 같은 값을
    //   돌려주어 **동률**이 되고, 동률에서는 늘 같은 칸이 뽑혀 **재사용 간격이 1** 이 됐다.
    //   ⚠ 그 값이면 장치 멱등창(16) 안에서 재사용이 일어나 **명령이 조용히 삼켜진다.**
    //   🔑 **시각으로 순서를 매기면 시계 해상도가 곧 순서다.** 순서가 필요하면 순번을 따로 들어라.
    // ── 타입 ──────────────────────────────────────────────────────────────
    //   🔑 **쓰는 자리 옆에서 선언한다.** 위로 모으고 싶겠지만 그 사이의 필드가 같이 밀리고,
    //     그러면 **배치가 바뀐다**(선언 순서가 곧 배치다). 그래서 여기 둔다.
    struct RidQ {
        long long until_ms;            // 이 시각 전에는 재사용하지 않는다
        long long seq;                 // 해제 순번 — **강제 해제 때 FIFO 를 보장한다**
        RidQ() : until_ms(0), seq(0) {}
    };
    std::map<uint16_t, RidQ> rid_quar;
    long long rid_rel_seq;             // 해제 순번 발급기(단조)
    long long rid_alloc_n;             // 발행 누계 (분모)
    long long rid_skips;               // 발행하려다 pend/격리라 건너뛴 횟수
    long long rid_forced;              // 🔴 공간이 차서 격리를 조기 해제한 횟수. 0 이 정상
    long long rid_exhausted;           // 🔴 pend 가 공간을 다 먹어 발행 자체를 못 한 횟수
    long long ack_unknown_rid;         // pend 에 없는 rid 의 ACK — 늦은 ACK/재전송 중복
    long long ack_slot_mismatch;       // 🔴 ACK 에코 자리 ≠ 서버가 보낸 자리 (멱등 캐시 서명)
    long long mod_name_conflict;       // 🔴 다른 노드가 이미 잡힌 모듈 이름을 다시 주장 (REQ-0260 전까지의 한계)
    // 🔴 **한 장치 *안* 의 이름 중복.** 위 칸은 노드 *사이* 만 본다 — 다른 사건이다.
    //   web REQ-0179 §① 이 `(devid,name)` 을 전역 신원으로 쓰므로 **1 이상이면 그 키가 애매하다.**
    long long mod_dup_name;
    // 🔴 **어느 자리에도 안 붙은 모듈 수.** 조용히 건너뛰면 —
    //   *"코드는 도는데 결과가 없다"* 가 다인원 협업에서 가장 큰 벽이라 값으로 만든다.
    long long mod_unbound;
    // 🔴 선언된 보드가 아닌 것이 그 이름으로 붙은 횟수(원장 §9.33).
    //   🔑 **0 이어야 정상이다.** 0 이 아니면 스케치가 바뀌어 구워졌는지 본다.
    long long mod_claimed_other_;
    // 🔑 recv 타임아웃 되읽기 값을 **첫 소켓에서 한 번만** 찍는다
    bool rcvto_logged_;
    // 🔴 같은 devid 로 세션이 대체된 횟수와 **그때 본 상대 IP 종류**(원장 §9.36).
    //   🔑 **M ≥ 2 = 겹침 경보** · **M = 1 = 그 보드의 재접속**. 계수만으로는 못 가른다
    long long adopt_replace_n_;
    std::set<std::string> adopt_peers_;
    // 🔑 **분모** — 결속을 시도한 모듈 선언의 누계. `미결속모듈 N/M` 의 `M`.
    //   ⚠ 분모 없이 `N` 만 두면 **정상 구성에서도 늘 빨강**이라 그 칸이 배경이 된다.
    //     (자리를 하나만 켜면 나머지는 안 붙는 것이 당연하다 — 사용자가 그렇게 정했다)
    //   🔴 그래도 **"정상"이라고 라벨하지 않는다**(monitor 지적) —
    //     라벨을 붙이면 그 값이 **검산 대상에서 빠진다.** 비율만 보여 주고 판단은 사람이 한다.
    long long mod_seen_;
    // 🔴 조립 표(사용 코드) 선언 문제 수. **0 이 정상.** 기동 때 한 번 센다
    long long asm_warn_;
    long long batch_seq_;          // 배치 표지 발급기. 0 은 "낱개" 라 안 쓴다
    // 🔴 **배치가 통째로 다음 창으로 밀린 횟수.** 0 이 정상이다.
    //   ⚠ 오르면 **창이 붐빈다**는 뜻이다 — 묶음이 커졌거나 남의 하행이 많다.
    //     그때 호출자는 여전히 "전부 같이 나갔다"를 보장받지만 **늦게** 나간다.
    long long batch_deferred;
    // 🔴 예시 `devid` 로 붙은 횟수. **0 이 정상**이고 1 이상은 기여자가 자기 것으로 안 바꿨다는 뜻
    long long devid_example_;
    // 🔴 **누적이 아니라 계기(gauge)다** — "지금 갈린 자리가 몇 개인가".
    //   처음에 `state` 를 낼 때마다 올리는 누적으로 만들었더니 **방송 횟수를 세고 있었다**
    //   (실측 `센서갈림 32` — 사건은 둘인데 방송이 32번이었다).
    //   🔑 **파생 상태는 사건이 아니다. 사건처럼 세면 방송 빈도에 비례해 부풀고 창끼리 비교가 안 된다.**
    int  sensor_split_now;             // 지금 두 센서가 갈린 주차 자리 수
    int  sensor_split_max_;            // 🔑 그 계기의 **창 최대치**(위 `mod_missing_max_` 주석 참조)
    long long not_reservable_n;        // 자리가 아닌 id 로 온 예약/취소 요청 (B* 등)
    std::string last_bits;             // data_log 중복 쓰기 방지

    // §7.5 예약 은퇴 — occupied 1→0 전이를 보려면 직전 값이 기준선으로 필요하다.
    // base_valid=false 면 기준선이 없다는 뜻이고, 그 프레임에서는 판정하지 않는다.
    // 🔑 **직전 값은 `slots[].occupied` 자신이다** — 별도 사본(`base_occ`)을 두지 않는다.
    //   사본을 두면 자리 갱신이 결속표를 거치는 지금 **두 배열이 다른 것을 가리키게 된다.**
    bool base_valid;

    // §12A 테스트 모드. **이 두 값의 출처는 S 프레임의 tmask 이지 서버의 기억이 아니다.**
    // T 의 ACK 를 받았다고 무장됐다고 단정하면, 해제 ACK 를 놓쳤을 때 서버는 "해제됨"이라
    // 믿고 화면은 가짜 값을 경고 없이 진짜로 표시한다 — §12A.6 이 막으려는 사고 자체다.
    // tmask 를 진실로 삼으면 다음 프레임에서 저절로 맞춰진다(§12A.4).
    bool test_armed;
    int  test_ovr[10];        // 1 = 그 칸의 occupied 는 주입된 값. **되돌림 판정의 직전 값이기도 하다**(§7.5-3)

    // 🔴 **시험용 이음매 두 개. 판정에는 절대 관여하지 않는다.**
    //   ⚠ 이것을 켜는 `--selftest` 는 **지금 이 트리에 없다**(`cli.h` grep 0 · `selftest.h` 없음).
    //     즉 지금은 **아무도 켜지 않는다** — 지우려면 산출물이 바뀌므로 별도 판단이 필요하다.
    // resync_count : 재동기화가 실제로 몇 번 일어났는지 세어 §7.4 오탐을 증명한다.
    // no_disk      : 자가검증이 작업 디렉터리의 data_log.json 을 덮어쓰지 않게 막는다.
    // ── 노드 대장 (온보딩 2단계 · `docs/net/DESIGN-node-ledger.md`) ──────────
    // 🔴 **판정에 관여하지 않는다.** 결속은 여전히 이름 일치다. 이건 *기억* 이다.
    // ── 자리 동작 방식 (기여자 안 1단계 · `spot.h`) ─────────────────────────
    // 🔑 지금은 **모든 자리가 이 하나를 쓴다.** 자리마다 다른 구현을 꽂는 등록은 2단계다.
    //   ⚠ 그래서 이 단계의 거동 변화가 0 이고, 판별자가 **봉투 대조**다.
    SpotBehavior default_spot_;

    // ── 명령 결과 콜백 (`cmdresult.h`) ─────────────────────────────────────
    // 🔑 등록 안 하면 `0` 이고 아무 일도 안 일어난다 — 로그는 그대로 남는다.
    CmdResultFn cmd_cb_;
    long long   cb_ok_, cb_rejected_, cb_noanswer_;   // 갈래별 누계. 요약에 싣는다

    // ── 자리 점유 변화 콜백 (`spot.h` 의 `OccupancyFn`) ─────────────────────
    // 🔑 등록 안 해도 **계수는 센다** — 볼 자리를 선택적인 것에 묶지 않는다.
    OccupancyFn   occ_cb_;
    // ── 수동 입력 폴백(상행 `plate_manual`) ────────────────────────
    // 🔑 등록 안 하면 `not_ready` 로 거절한다 — **조용히 버리지 않는다.**
    //   그 서버가 그 기능을 안 가졌다는 것을 화면이 알아야 사람에게 말해 줌다.
    ParkingServer::ManualPlateFn manual_cb_;
    // 🔑 8081 자리 선택. **반환형이 `ManualPlateResult` 와 같다** — 거절 어휘를 한 벌로 둔다.
    ParkingServer::SlotPickFn pick_cb_;
    // ── 입차 진행 봉투(`state.entry`) ────────────────────────────
    // 🔴 **항상 싱는다.** 비었으면 `phase:"idle"` 로 나간다 — 필드를 빼면
    //   화면이 *"지금 입차가 없다"* 와 *"이 서버엔 그 기능이 없다"* 를 못 가른다.
    ParkingServer::EntryStatus entry_;
    // 🔴 **떠 있는 요청을 선점하지 않으려고 버린** 요청번호 없는 번호판 수(http.h::on_plate ②).
    //   🔑 조용히 버리면 다음 사람이 *"번호가 사라진다"* 를 못 찾는다. 세서 말한다.
    long plate_orphan_;
    // 🔵 **이미 닫힌 촬영에 온 답**(persist.h::shot_answer). 새 설계에서 stale·폰끊김으로 생긴다.
    long shot_late_;
    // 🔵 흐름이 **스스로 닫은** 촬영 요청 수(`shot_cancel`). 사유별 분해는 로그에 있다.
    long shot_cancelled_;
    // ═══ 🔵 상행 로그 요약 (명세 2026-08-27) ═══════════════════════════════
    //   🔑 **보드마다 따로 센다** — 한 계열로 합치면 주기·간격·비율이 전부 뒤섞인다
    //     (`uplink.h:33` 이 낱줄에 devid 를 박은 것과 같은 이유다)
    struct ArdSum {
        long long win_start;   // 이 구간이 시작된 시각 (0 = 아직 없음)
        long long seq_first, seq_last;
        long      rx;          // 이 구간에 받은 S프레임 수
        long long last_ms;     // 직전 프레임 도착 시각 — `gapmax` 계산용
        long long gapmax_ms;
        long long up_first, up_last;
        std::string occ, rsv;  // 구간 **끝** 값 (16진 문자열 그대로)
        // 🔴 **사건 뒤 이 시각까지는 낱줄로 찍는다.** 0 = 요약 모드
        long long burst_until;
        ArdSum() : win_start(0), seq_first(0), seq_last(0), rx(0), last_ms(0),
                   gapmax_ms(0), up_first(0), up_last(0), burst_until(0) {}
    };
    std::map<std::string, ArdSum> ard_sum_;
    // 🔴 **보드가 0대여도 요약 줄은 나와야 한다**(monitor 요구 · 실측 근거 있다):
    //   로그가 1분 전까지 자라는데 그 줄이 전부 브라우저 트래픽이고 장치는 6시간 부재였다.
    //   ★ 요약이 *"사건이 있을 때만"* 나오면 **부재와 요약기 고장이 같은 모양**이 된다.
    long long ard_sum_none_at_;
    // 🔴 필드 수가 모자란 옛 형식 `S,0,0,P2,31` — 서버가 안 받는다(`f.size()>=6`).
    //   ⚠ **못 잡는 것은 정상이지만 세고는 있어야 한다** — 늘면 *"장치가 옛 형식으로 보낸다"* 다
    long ard_short_frames_;
    // 🔵 **지금 처리 중인 그 번호가 또 온 것.** 정상이라 조용히 센다 —
    //   `send.mode=every_stable_frame` 이면 초당 1건이 정상이다. `plate_orphan_` 과 **다른 축**이다.
    long plate_repeat_;
    // 반복 로그 억제 — 같은 번호를 초당 찍으면 하루 86,400줄이다
    std::string dup_last_plate_;
    long long   dup_last_ms_;
    long        dup_since_log_;
    // ── 폰 유휴 하트비트(serve.h::phone_ping_if_due)
    long long phone_ping_at_;      // 마지막 발신 시각(단조). 0 = 아직 안 보냈다
    long long phone_ping_seq_;     // 발신 수 — 요약에 실린다
    ParkingServer* owner_;                 // 콜백에 넘길 공개 객체. 없으면 콜백을 안 부른다
    std::map<std::string, bool> occ_prev_; // 자리 id → 직전 점유
    // ── 🔴🔴 **`kind` 캐시** — `"devid\tname"` → 등록에서 본 `kind`
    //
    //   🔑 **왜 필요한가**: 조립표(`lot.cpp`)는 **센서인지 조작인지 안 가른다** — 그건 장치가
    //     등록(`D`)에서 말한다. 그런데 **노드가 끊기면 `mods` 가 비어 물어볼 곳이 사라진다.**
    //     그러면 *"센서는 AND · 조작은 모듈별"* 규칙을 **적용할 수가 없다** — 죽은 모듈이
    //     센서였는지 조작이었는지 모르니까.
    //
    // 🔴 **세션을 넘어 산다.** `reg_reset()`/`session_reset()` 이 비우지 **않는다** —
    //   끊긴 동안 답해야 하는 것이 이 캐시의 존재 이유다.
    // 🔑 재등록에서 `kind` 가 바뀌면 **그때 덮어쓴다**(장치가 새로 말한 것이 이긴다).
    std::map<std::string, std::string> kind_cache_;
    // 🔴 **키가 없으면 "아직 안 봤다" 이고, 그때는 콜백을 안 부른다.**
    //   안 그러면 기동 직후 모든 자리가 한 번씩 "변했다"로 불린다 — 그건 변화가 아니라 첫 관측이다.
    long long occ_change_n_;               // 변화 횟수(상승+하강). 요약에 싣는다
    // ── `E`(전이) 계수 — SPEC §15.3
    //   🔑 `eframes_` 는 **사건 수**다. `V` 의 `vframes` 와 달리 슬롯 수에 안 비례한다
    //   🔴 `etrans_split_/etrans_cmp_` : `E` 의 `r` 과 `occ` 비트가 갈린 수 / 비교 성립 수.
    //     ★ **§15.10(판정 슬롯당 1회)이 실렸는지를 판정하는 유일한 관측자다.**
    //     ⚠ 분모를 같이 든다 — 분모 없는 `0` 은 *건강* 인지 *못 셈* 인지 안 갈린다.
    long long eframes_, etrans_cmp_, etrans_split_, ebad_;
    // 🔴 판정 수치 하행 `K` (SPEC §15.4) — 등록 때 채우고 창마다 한 묶음씩 뺀다
    //   🔑 `cfg_gap_slots_` 는 *"한 창에 다 못 보낸 횟수"* 다 — 예산 산식이 맞나를 잰다
    // 🔴 **노드마다 따로 쌓는다.** 한 벌만 두면 둘째 보드가 등록하는 순간 `clear()` 가
    //   **첫 보드의 대기분을 지운다** — 그 보드는 판정 수치를 영영 못 받고, 그런데도
    //   로그에는 *"큐에 넣었다"* 가 남아 있어 **보낸 것처럼 보인다.**
    std::map<std::string, std::vector<std::string> > cfg_q_;
    long long cfg_sent_, cfg_skipped_, cfg_gap_slots_;
    // 🔑 **계기(gauge)다.** *"지금 안 온 것이 몇 개"* 이지 *"몇 번 일어났나"* 가 아니다 —
    //   등록마다 다시 센다. 사건처럼 세면 방송 빈도에 비례해 부풀어 창끼리 비교가 안 된다.
    long long mod_missing_;            // 조립 표엔 있는데 등록에 안 온 모듈 수
    // 🔴 **계기에는 최대치를 짝지운다.** 계기의 `0` 은 *"지금 0"* 이지 *"창 내내 0"* 이 아니다 —
    //   요약 주기(60초) 틈에서 떴다 지면 **아무 데도 안 남는다**.
    //   🔑 **누적 합이 아니라 최대치다** — 같은 상태가 계속되면 합은 부풀지만 최대는 안 부푼다.
    long long mod_missing_max_;
    // 🔴 **분모다.** 이 노드의 devid 로 **조립 표에 선언된** 모듈 수(계기 — 등록마다 다시 센다).
    //   ⚠ 이것이 없으면 `등록안온모듈 0` 이 두 뜻을 겸한다:
    //     `0/5` = **다섯이 다 왔다**(건강)  ·  `0/0` = **이 devid 로 선언된 것이 아예 없다**
    //   🔑 뒤쪽은 **건강이 아니라 배선·조립표 오설정**이다 — 굽기·자리 교체 직후에 가장 잘 난다.
    //     분모 없이는 그 둘이 로그에서 **같은 글자**다.
    long long mod_declared_;
    long long act_nocontrol_;          // 🔑 명령 모듈인데 조작 선언이 없다(누적 · 등록마다)
    // 🔴 하강(찼다→비었다)이 **한 프레임만이라 흡수된** 횟수(누적).
    //   🔑 `점유변화` 와 짝이다 — 이 값이 크면 **해제가 오지만 안 이어진다**
    long long occ_fall_absorbed_;
    // 🔑 짝 계수 — **값이 있어서(=멀다) 지연 없이 통과한** 하강 수.
    //   ★ 이 값이 0 이 아니면 *"멀다/못 쟀다 를 가른 처방이 실제로 일한다"* 는 증거다
    long long occ_fall_fast_;
    // 🔴 액추에이터의 `occ` 비트와 `V` 값이 **갈린 횟수**. 0 이 정상이고 1 이상은 조사 대상이다
    //   (장치에서 `echoIs()` 와 `state()` 가 **둘 다** 비트를 정하면 오른다)
    long long actstate_split;
    // 🔴 **분모.** 비교가 *성립한* 횟수(액추에이터 칸에 값이 실려 온 횟수).
    //   🔑 `0/0` 은 **못 쟀다**이고 `0/N` 은 **정상**이다 — 분모가 없으면 둘이 같아 보인다.
    //   ⚠ 실기에서 정확히 그 오독이 난다: `.state()` 붙은 액추에이터가
    //     0 인데 `상태갈림 0` 이 **훅이 돈다**로 읽혔다(arduino 가 짚었다).
    long long actstate_cmp_;
    // 🔴 **하강만 한 프레임 확인한다** — 초음파는 **간헐 반사 실패**로 한 프레임 `0` 을 낸다
    //   (monitor 실측: `8 → 0 → 8`). 그것을 변화로 읽으면 **아무도 안 건드렸는데 LED 가 토글된다.**
    //   🔑 실패 모드가 **거짓 0** 한쪽이므로 지연도 한쪽에만 건다 — 상승은 즉시다.
    //     그래서 사용자가 손을 대는 순간의 반응은 **안 느려진다.**
    std::map<std::string, int> occ_fall_n_;   // 자리 id → 하강 후보를 연속 몇 번 봤나

    NodeLedger ledger_;
    long long  ledger_new_, ledger_review_;   // 이 인스턴스에서 본 사건 수(요약에 싣는다)

    int  resync_count;
    bool no_disk;

    // ═══ 관측 계수기 — `metrics.h` 에서 옮겨 왔다 ═══════════════════════════
    //   🔑 **필드는 여기 한 곳에 모은다.** "무엇을 기억하는가" 를 한 파일에서 읽는다.
    //   🔴 **순서를 바꾸지 마라** — 선언 순서가 곧 배치다. `.o` 대조가 그것을 잡는다.
    // ---------- 소크 관측
    // "2시간 안 끊겼다"는 **로그가 증명해야** 한다. 접속·종료 순간만 찍히면 그 사이의 침묵을
    // 아무도 증언하지 않는다 — 조용한 로그와 죽은 링크가 똑같이 보인다.
    // 핵심 지표는 **프레임 수신 간 최대 공백**이다. 평균은 링크가 반쯤 죽어도 예쁘게 나온다.
    long long soak_start_ms;      // 프로세스 기동(now_ms)
    int  ard_sessions;            // ARD 접속 횟수
    long long sess_start_ms;      // 현 세션 시작. 0 = 세션 없음
    long long sess_frames;        // 현 세션 수신 줄
    long long sess_last_line_ms;  // 현 세션 마지막 줄 수신 시각
    long long sess_max_gap_ms;    // 현 세션 최대 공백
    long long all_frames;         // 누적 수신 줄
    long long all_max_gap_ms;     // 누적 최대 공백 — **세션 경계는 넘지 않는다**(끊긴 시간은 공백이 아니다)
    long long all_max_gap_at;     // 그 공백이 끝난 시각(epoch_ms)
    long long link_down_ms;       // ARD 연결이 없던 누적 시간
    long long link_down_since;    // 0 = 연결돼 있음
    // 재부팅 감지를 **원인별로** 센다. §7.4 는 "새 연결이 1차 신호, uptime 추론은 2차 방어선"이라고
    // 정했는데, 그 주장이 실기에서 맞는지는 이 두 숫자의 비율로만 확인된다.
    // (불변식: reboot_by_conn + reboot_by_uptime == resync_count)
    int  reboot_by_conn;
    int  reboot_by_uptime;
    int  offline_episodes;        // 3.5초 무프레임 판정 횟수(§3.4)
    // ⚠ 아래 셋은 **모순 대조용**이다(죽은 탐지기 규칙 5). 상행만 세면
    // "조용한 링크"와 "시끄럽지만 전부 버려지는 링크"(AT 잡음 §6.2)가 요약에서 똑같아 보이고,
    // 상행만 보면 **하행이 통째로 죽어도 요약이 멀쩡하다**(실기에서 +IPD 0건이던 바로 그 경우).
    // 버린 줄은 **원인별로** 센다. 합만 세면 "전선에서 깨졌다"와 "장치가 프로토콜 아닌 것을
    // 흘린다"가 같은 숫자가 된다 — 진단이 정반대인데도. 특히 `모름` 이 오르면 AT 응답 같은
    // 비프로토콜 텍스트가 소켓에 새고 있다는 뜻이라 체크섬 손상과 완전히 다른 사건이다.
    long long drop_cksum;         // 체크섬 불일치 — 전선/장치에서 바이트가 깨졌다
    long long drop_overlong;      // 64B 초과 줄(§2.1)
    long long drop_unknown;       // 모르는 타입 문자 — 비프로토콜 텍스트 유입 의심
    long long drop_noise;         // LF 없이 64B 초과 → 버퍼 비움(잡음)
    // 승격 전(id 미상) 소켓에서 버린 줄 .
    // **왜 필요한가**: 이 경로의 줄은 지금까지 **어느 카운터도 안 올리고 사라졌다.**
    // 그래서 "안 왔다"와 "왔는데 못 셌다"가 구별되지 않았다 — 08-16 에 `TX-RESYNC 4 : 버린줄 0`
    // 을 두고 팀이 판정을 못 내린 원인이 이것이다. 그리고 **마감된 소켓 56건이 전부 이 경로**라
    // 지금 그 소켓들이 무엇을 받았는지 아무도 모른다. 0 이 "해당 없음"인지 "못 셈"인지 가른다.
    long long drop_prepromo;      // 승격 전 소켓에서 버린 줄(체크섬 실패·비S프레임 포함)
    long long drop_prepromo_buf;  // 승격 전 소켓 버퍼가 상한을 넘겨 통째로 비운 횟수
    long long retx_count;         // 하행 재전송 횟수(§7.3)
    long long ack_fail_count;     // 하행 ACK 최종 실패 횟수
    bool ard_online;              // 온·오프라인 **엣지** 판정용 직전 상태(§3.4). 아래 루프 주석 참조
    long long last_report_ms;     // 주기 보고 시각

    // ---------- 복구 계측
    // 왜 "최대공백"으로 부족한가 — 공백은 **얼마나 나빴나**만 말하고 **스스로 돌아왔나**를
    // 말하지 않는다. 사용자 요구가 "오류에서 정상으로 복구하는 구조"로 바뀌었으므로
    // 판정 지표도 바뀌어야 한다: **끊긴 뒤 몇 초 만에 저절로 프레임이 다시 왔는가.**
    // 이 숫자가 없으면 장치 쪽 복구 사다리가 듣는지를 사람이 로그를 눈으로 세어
    // 판정하게 된다.
    long long offline_since_ms;   // 오프라인이 **시작된** 시각(now_ms). 0 = 온라인
    int  offline_at_session;      // 그 순간의 ard_sessions — 같은 연결/재연결 복구를 가른다
    std::vector<long long> recov_ms;  // 복구시간 표본(중앙값용). RECOV_SAMPLE_MAX 에서 멈춘다
    long long recov_dropped;      // 상한을 넘겨 못 담은 표본 수 — 조용히 버리지 않는다
    int  recov_same_conn;         // 같은 TCP 연결에서 프레임이 되돌아왔다(링크가 살아 있었다)
    int  recov_reconn;            // 재연결한 뒤에야 되돌아왔다(사다리가 소켓을 다시 세웠다)
    long long recov_worst_ms;     // 최악 복구시간
    int  zombie_reaps;            // 유휴 마감으로 회수한 소켓 수 — **앱 경로**
    int  keepalive_reaps;         // ETIMEDOUT 으로 죽은 소켓 수 — **OS 경로**
    // ── nodes.h 에서
    const ParkingLot* lot_ = 0;   // 🔑 선언 자리에서 초기화한다 — 초기화 목록에 넣으면
                              //   선언 순서와 어긋나 `-Wreorder` 가 난다(실제로 났다)
    // ── wire.h 에서
    std::vector<sock_t> dead_conns;      // 전송 실패로 끊어야 할 연결. 루프 끝에서 거둔다
    // ── wsjson.h 에서
    bool map_empty_warned = false;
    int  ws_peak = 0;      // 이 인스턴스에서 동시에 붙었던 **WS 소켓 수**의 최대
                           //   ⚠ 사람이 보는 화면 수가 아니다 — 탐침·하니스·주입기도 센다
    // ── persist.h 에서
    std::vector<std::string> log_entries;
    RidPool ridpool_;
    // ── http.h 에서
    std::map<std::string, std::string> plate_slot;   // 번호판 → 배정된 자리
