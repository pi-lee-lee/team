# 상태 — 2026-08-20 17:2x (세션 연속)

⚠ **앞 판(12:50)은 통째로 갈았다.** 그 사이 배포 4회·굽기 3회가 있었다.

## 지금 도는 것
```
서버 : pid **94569** · build **bb1527e** · 14:23:52 배포 · 8888/9990/8911
       cwd=/Users/idong-u/parking-bin · 로그 ~/parking-logs/parking-server.log
화면 : ~/parking-bin/index.html · **git=e0270c3** · 15:56:19 (표지 확인 · 미배포 0건)
장치 : 칩 **0e046f9** · 16:40:27 굽기 · 모듈 **4**(A1 B1 LD L2) · 등록 **54B**
       🔴 **소스가 칩보다 앞서 있다** — 컴포넌트 판(REQ-0304 2단계)이 커밋돼 있고 **안 구웠다**
망   : SSID 3F_302 · 서버 IP 192.168.0.29
```

## 🔴 오늘 가장 큰 것 — **장치 미접속의 원인은 TX 선 빠짐이었다**
```
사용자가 실물에서 찾았다. 코드가 아니었다
우리 후보는 전부 소프트웨어였다(서버 IP 변경 · dev 포트 9991 · 옛 서브넷) — **전부 틀렸다**
🔑 `[NET] 대상` 은 Uno 가 **자기 Serial** 에 찍는 것이고 **ESP 로 나갔다는 증거가 아니다**
   찍힌 줄과 나간 명령을 같은 것으로 읽었다
그 뒤 : 창 AM **101분 무이탈**(옛 천장 86분 초과) · 창 AN 36분
⚠ "고쳐졌다"로는 못 간다 — 표본 1 · 축 셋(선 재배선 · 서버 3판 · 배선 정착)
```

## 진행 중 — REQ-0304 컴포넌트 패턴 (arduino)
사용자가 형태를 **코드로 지정하고 승인**했다:
```c
void setup() {
  Serial.begin(115200); node.begin();
  node.sensor  ("A1").pin(2);
  node.actuator("LD").pin(LED_BUILTIN).on(cmdLed);
  node.actuator("L2").on(cmdL2);           // 핀 없는 모듈
}
void loop() { node.tick(); }
```
```
✅ 배열·등록 호출·IP/OG·전역 객체 선언 **전부 없다** · 서버 lot.cpp 와 같은 체이닝
✅ 문턱 통과 : RAM +62B ÷ ramLow **602B** = 10.3% (사전 등록 ≤15%)
   🔑 ramLow 602B — 컴파일러 정적 여유 834B 는 **28% 과대평가**였다
✅ begin()/pin() 순서 의존 0 · 센서 앞쪽 연속 불변식 **소멸**(isSensor 루프)
🔴 미완 : 시험이 무너졌다 — 등록이 런타임이라 `setup()` 전엔 MODULE_N == 0
   ㉠(하네스 머리에서 setup 호출) 적용 완료 · **남은 실패는 `[31]` 하나로 좁혔다**
   ⚠ 원인 미상. 추측 귀속 안 했다. **굽기 금지 — 판별자가 없다**
```

## 미결
```
🔴 [31] 시험 실패 (arduino · 다음 조각)
🔴 차단봉(DR) — **사용자 답 대기.** 실물 없음 · E1/X1 은 가상(꺼짐) · 서버가 찾는 DR 은 어느 표에도 없다
   ① 그냥 둔다 ② 서버 이름 정정 ③ 가상을 켠다(하드웨어 불필요 · choice 예시가 돈다) ④ 없앤다
· 주석 정리 + **예제 코드** — 본문 준비됨. ardu 주석/코드 **1.28**(최악 EspLink_gate.h 13.3) · server 0.55
  🔑 컴포넌트가 같은 구역을 다시 쓰므로 그 뒤에 낸다. *"샘플코드가 전혀 없다"*(사용자)를 같이 푼다
· 화면 폴백 라벨 — kind 가 IP/OG 로 좁혀지면 액추에이터가 전부 "안내등"으로 뜬다(web 분석 완료)
  🔑 근본은 lot.label 을 다 주는 것. A(문구)만 하면 B(라벨)를 아무도 안 한다(web 경고)
· DEFERRED : socket docs/net/DEFERRED.md · arduino docs/arduino/DEFERRED.md
```

## 오늘 사용자 확정
```
· 검토·측정 그만. **구현만.** 판정 문서·장기 관측 안 만든다
· 형태를 말이 아니라 **코드로** 준다 — 그러면 안 흔들린다
· 기여자 수준 = **아두이노 기본 설정 + 센서 연동.** 그 수준을 넘는 설정은 난이도를 올린다
· 주석이 아니라 작업일지다. 일괄 정리 필요
· CLAUDE.md 에 오늘 교훈을 **남기지 않는다**(사용자 지시)
```

## 규약 개정 (오늘)
```
🔴 화면 배포도 monitor 에게 통보한다 — 오늘 넷을 안 알렸다
   근거: 무해 판정은 **사후에만** 가능하고 통보는 **사전**이어야 값이 있다
· 창을 닫을 때 개입을 통보 목록이 아니라 **배포본 mtime** 으로 센다(통보는 대조용)
  ⚠ 파일을 남기는 개입만 잡힌다 — 전원·배선·포트는 안 보인다
```


---

# 🔴 자리이동 20:09:43 — 정지 상태

```
관측 차단(monitor 자원 0) · 포트·굽기 중단(arduino) · 배포 중단(socket·web)
푸시 완료 : origin/main = 8509076 (미푸시 0)
동결값   : 서버 IP 192.168.0.29(**유선** · WiFi 미접속) · 칩 SSID 3F_302 · SERVER_IP 192.168.0.29
```

## 자리 잡으면 이 순서

```
① 🔑 **아무도 손 안 댄 상태에서 `occ` 를 먼저 읽는다**
   `0` → 정상 · `4`·`8`·`C` → 🔴 벽/물체가 60cm(US_NEAR_CM) 안이다 → 센서를 옮긴다(굽기 0회)
   ⚠ 자극을 주기 전에 이 기준선을 떠라 — 안 뜨면 "손을 댔는데 이미 1이었다" 를 못 가른다
② 새 값 **셋을 한 벌로** : SSID · WiFi PASS · 서버 IP
   🔑 `SERVER_IP` 와 `SERVER_PORT` 는 한 벌이다(소스 주석). 하나만 고치고 헤매는 것이 이 파일의 함정
③ arduino : Config.h 갱신 → 굽기 **한 번**
④ socket : 서버 재기동 · monitor : 새 창(t0 = 굽기·배포 완료 시각. "이동+5분" 아니다)
```

## 🔴 다음 작업의 **메인** (사용자 확정)

**센서 변화 콜백** — `lot.cpp` 에 훅 하나. socket 에게 REQ.

```cpp
void onOccupancyChanged(ParkingServer& srv, const std::string& spot, bool occupied) {
  if (spot == "A1" && occupied) srv.send("P2", "LD", 1);   // 🔓 다른 아두이노에게 지시
}
```
```
✅ 이미 되는 것 : `send(devid, module, value)` 가 **devid 를 받는다** → 장치 간 지시가 구조상 가능
🔴 빠진 것 : **센서 변화를 잡는 훅.** 지금 srv 에 점유·값 읽기 API 가 0 이다
             (deviceReady · nowMs · maxPerBatch · send · openPorts · serveOneTick · closeDown 뿐)
🔑 ㉡(변화 콜백)로 확정 — ㉠(읽기 API + onTick 폴링)은 기여자가 직전 값을 static 으로 기억해야 한다
   그리고 서버는 **이미 그 변화를 판정한다**(화면 방송용). 없던 판정을 만드는 게 아니다
목표 시나리오 : 차가 들어오면 표시등·차단봉·안내 표시기를 엮는다 — 전부 "센서 변화 → 다른 장치 명령" 이다
```
