# 상태 — 2026-08-20 12:50 (세션 연속 진행 중)

⚠ **이 파일의 앞 판(00:20 작성)은 낡아서 통째로 갈았다.** 예약 재개가 발화했지만
**세션이 안 끊겨서 그동안 계속 일했다.** 아래가 지금이다.

## 지금 도는 것
```
서버 : pid **72151** · build=**a8e04c5** · 10:45:46 배포 · ports **8888 / 9990 / 8911**
       cwd=/Users/idong-u/parking-bin · 로그 ~/parking-logs/parking-server.log
장치 : 칩 **d41ad13** · 모듈 **4** (A1 B1 LD **L2**) · 등록 54B
       🔴 **10:57:03 부터 끊겨 있다** (세션 1회 · 프레임 545 뒤 무접속)
화면 : /Users/idong-u/parking-bin/index.html · git c39a733 계열
망   : SSID **3F_302** · 서버 IP **192.168.0.29** (자리이동 09:04 굽기 반영)
```
⚠ **dev_server 시험 인스턴스 6개가 좀비로 떠 있던 것을 12:50 에 정리했다.**
🔴 **시험 인스턴스는 반드시 끈다** — 운영 재기동 때 포트를 먼저 잡으면 사고다.

## 오늘 한 것 (00:20 이후)
```
✅ 화면 조작 — 기여자가 `lot.control(...)` 로 선언 → 화면이 렌더링
   toggle / number / choice · 결과가 콜백으로 화면에 · 거절 두 갈래(전선 전/후)
✅ 게이트 특례 제거 — 조작 경로가 `control` **하나**
✅ `label` — 표시 이름의 정본이 기여자에게 (센서에도 붙는다)
✅ `kind` 는 **첫 글자만** 계약 (`I` 관측 · `O` 명령) — 정해진 목록 제거
✅ 포트 분리 — 운영 9990/8888/8911 · dev 9900/9991/5500 · `--port-offset` 제거
✅ 자리이동 굽기 — 45초 만에 붙었다(어제는 ~20분)
✅ `client.ino` 370줄 · `#if` 0개 · LED 하나 · 시험 커버리지는 하네스로 유지
✅ **dev_server 단독 구동** — `./srv` · stdout · index.html 포함 · 밖 참조 0
✅ VS 빌드 오류 해소 — `lot.cpp` 를 진짜 번역 단위로(편집 2곳) · `parking.h` 가 `IN`/`OUT` `#undef`
✅ `-Wshadow` 3건(`dead` 가 셋을 겸함) — 양쪽 트리
```

## 🔴 진행 중 — v2 조립 API (socket)
사용자 확정: **조립 API 를 다섯으로 줄인다.**
```cpp
lot.spot("A1").at(0,0).parking().label("1번 자리")
      .module("P1","A1").module("P1","B1").module("P1","LD");
lot.spot("E1").at(0,2).label("입구").module("P1","DR");
```
```
❌ 없앤다 : `gate()` · **`Gate` enum** · entrance/exit 구분 · `sensor()`/`actuator()` 구분
✅ `parking()` 만 엔진 동작을 바꾼다 (점유·예약·옛격자) · **기본은 일반영역**
✅ `at(행,열)` 은 **사용자 인식용**. 엔진은 안 쓴다. 안 적으면 선언 순서
🔴 딸린 결과 : 합치면 **등록 전 센서 수를 모른다** → `0`(모름)으로 낸다. 화면이 "아직 모름"으로 그린다
⚠ 규모 : 자가검증 호출부가 여러 파일에 흩어져 있다. "다섯 줄"이 아니다
```

## 미결
```
· v2 API 구현 → dev 검증 → 운영 → 배포
· 🔴 **장치 재접속** — 10:57 부터 끊김. 가장 빠른 것은 전원 재투입
  ⚠ 시리얼 탭이 안 돌아서 **장치가 무엇을 하는지 아무도 못 본다**
· `struct Server` 2·3단계 분리 — 🔑 **빌드 오류는 이미 해소됐다.** 목적이 "구조"면 한다
· 값 에코 `E,<idx>,<val>` — 숫자 확인이 지금 `partial` 까지만
· 압축 배포 3폴더(ardu · dev_server · web) — 준비됨
· 미측정 : sensors.on 효과 · 초음파(배선) · no_answer 콜백(링크 절단 시 저절로)
```

## 사용자 확정 사항 (오늘 새로 나온 것)
```
· **굽기는 제약이 아니다** — 비용을 고려하지 마라. 다만 **경계는 지킨다**
· **장애 분석 그만** — monitor 는 관측만. 보고는 물을 때만
· **두 벌이여도 된다** (server / dev_server) — 개발자는 dev 만 쓴다
· 압축 배포 : **ardu · dev_server · web** 3폴더
· 아두이노 IDE 프로젝트 이름은 **`client`** 여야 한다(안 그러면 첫 컴파일에서 막힌다)
```
