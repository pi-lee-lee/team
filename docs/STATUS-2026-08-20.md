# 상태 — 2026-08-20 (리미트 95% 정지) · 재개 12:50

## 지금 도는 것
```
서버 : pid 48254 · build=600d94c · 22:45:34 배포 · 무자극 (🔴 죽이지 마라)
장치 : 칩 57fd573 (펌웨어 6eb99f8) · 00:10:31 굽기 · flash 27,824(86%) · RAM 1,261
       모듈 6 (A1 B1 LD LC DR L2) · 등록 76B · hex 폭 2
       SAMPLE_SIM_SENSORS=1 · VIRTUAL_MODULES=0
```

## 🔴 다음 한 걸음 — 화면 조작 (REQ-0281)
사용자 지시: *"web을 통해서 직접 컨트롤"* + *"모든 센서에 활용가능하도록"* +
*"기여자 코드에서 작성된 코드를 기준으로 호출"*
```
기여자가 lot.cpp 에서 선언 → 서버가 봉투로 내림 → 화면이 렌더링만
  lot.control("P1","LD").toggle("표시등")
  lot.control("P1","LC").number("표시기", 0, 9999999)
  lot.control("P1","DR").choice("차단봉", {{1,"열기"},{2,"닫기"},{3,"잠금"},{4,"해제"}})
🔑 choice 가 곧 명령표다 — 지금은 서버·장치 주석 두 곳에 적고 있다. 선언이면 한 곳
```
**핵심 공사**: `send_to_module` 의 `ws_fd = BAD_SOCK` — 지금 구조로는 결과가 화면에 못 돌아간다.
`downlink.h` 는 잠겨 있으나 socket 의 concern(REQ-0272)이라 REQ 를 달고 정면으로 간다.
**전선 형식은 한 글자도 안 바뀐다.**

**단계 둘** (1단계는 굽기 불필요):
```
1  value(에코) / requested(보낸 값) / confirmed 를 갈라 보낸다
   confirmed = pending·settled·partial·mismatch·unknown  (앞 넷은 zone.completion 어휘)
   🔴 숫자 모듈은 settled 를 절대 안 낸다 — 오면 서버 결함
2  값 에코 줄 `E,<idx>,<val>,<ck>` — 다음 굽기. 평상 0B · 실릴 때 21B · RAM +22B
```

## 미결 — 무엇을 기다리는가
```
· 화면 조작 구현 (socket 진행 중 · web 은 명세 대기로 착수 안 함) ← 대기이지 방치 아님
· 전수검사 — 각본을 접고 화면 조작으로 전환. **아직 안 함**
  (socket 30항목 PLAN-full-check · arduino 6구역 목록은 나와 있다)
· 사용자 코드 검토 요청 — 화면 조작이 되면
· B11 무응답 콜백 — 미측정. 닫는 조건: 명령 발행 중 링크 절단 (개입 불필요 · 저절로 밟힌다)
· A11 초음파 실물 — 미측정. 닫는 조건: HC-SR04 배선
· 카메라 요청 기반 조회 — 설계 접수만. 🔴 번호판 **7자리 숫자 확정**(한글 배제 · "일단")
  Q3(정지 상태 충돌) 미답
· 접점 납땜 — 별도 경계. 🔑 납땜 전 기준선 확보됨: UART 손상 0.29%(3,461) · 0.31%(4,219)
```

## 오늘 검증된 것
```
㉠ 묶음 4건 → **+IPD 헤더 1건** (연발 아님) · ACK 도 S 와 한 거래 — 왕복 각 1회
㉡ 5건 거절 · ㉢ 실행 원자성 없음 · ㉣ 단건 회귀
에코 : DR 1→true · **2→false** (echoIs)
⑤ : n=6 · zones 2(A1+E1) · value_known 2/2 · 미결속 0/6
A10 입력 훅 : occ `00` → **`20`** 으로 닫힘 (sensors.on → readRealSensor → 훅 사슬이 실기에서 돈다)
lot.cpp 76줄 · main() 제거 · 단독 문법 검사 exit 0
```

## ⚠ 다음 세션이 먼저 읽을 것
```
docs/net/RESUME-2026-08-20.md      (socket)
docs/arduino/RESUME-2026-08-20.md  (arduino)
monitor/RESUME-2026-08-20.md       (monitor)
docs/web/RESUME-2026-08-20.md      (web)
docs/DESIGN-camera-request-2026-08-19.md
```
