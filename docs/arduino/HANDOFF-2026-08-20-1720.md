# arduino — 재개용 (2026-08-20 17:2x)

## 0. 🔴 지금 칩과 소스가 **다르다**

```
칩   = `0e046f9`  16:40:27 굽기 · flash 26,660 · RAM 1,212
       구성 : setup 두 줄 판(`node.begin()` + 표 기반 등록) · 모듈 4 · 등록 54B
       부팅 표지(실측) : [SENS] A1=핀2 B1=핀9 · proto v1 / 2 sensors / 4 modules
                        [BOOT] 사다리 4단 배제(설계) — hang 을 없애 불필요
                        [REG] 등록 전송 54B · D,*,7,4,6D
🔴 소스 = **컴포넌트 체이닝**(REQ-0304 2단계). flash 27,362 · RAM 1,276. **칩에 없다**
```
⚠ **`INTERVENTIONS.md` 의 16:40 `IDENTICAL` 을 현재로 인용하지 마라.** 그 시각의 사진이다.
⚠ **monitor 가 도는 창의 장치는 `0e046f9` 다** — 컴포넌트 판이 아니다.

## 1. 🔴 다음 한 걸음 — **`[31]` 하나다**

```
`bash arduino/test/run_stage1.sh`  →  `[31] S 프레임이 실제로 hex 로 나간다` **안에서 죽는다**
                                      `[32]` 는 시작도 못 한다
귀속 실험(했다) : `main()` 의 `setup()` 을 빼면 **Abort trap** · 넣으면 **Segfault**
                → **`setup()` 이 유일한 원인이 아니다.** 지형이 있는 쪽이 더 멀리 간다
배제(했다)      : `moduleNameOf` NUL 종단(정상) · `MODULE_CAP` 부족(13 으로도 같은 자리)
🔴 **추측을 붙이지 않았다.** 그럴듯한 설명을 근거로 파지 마라
```
**시험이 서면**: 전선 바이트 diff 0 확인 → 굽기 예고(monitor·루트) → 굽기 → `GUIDE.md` 한 번에.

## 2. 확정된 값 (다시 재지 마라)

```
`ramLow` = **602 B** (정적 여유 834B 는 예산이 아니다 — 스택이 232B 내려간다)
모듈 상한 = **8**   (증분 62B ÷ 602 = 10.3% · 문턱 ≤15%)
🔴 배치 버퍼가 정한 절대 상한 = **13**  (`11n + 11 <= 160`)
`constexpr` + `PROGMEM` + 함수 포인터 : **된다**(`-std=gnu++11` · 재귀만)
`const char (&)[3]` : 이름 길이를 **타입이 강제한다**(3글자면 컴파일이 그 줄을 짚는다)
로컬 쓰기 API : ❌ **못 연다** — 에코가 `occ` 에 실려 계약이 깨진다(원장 §84)
```

## 3. 이 판에서 사라진 것 (되살리지 마라)

```
`SENSOR_N` · `SLOT_PIN[]` · `SPOT_N` · `slotCol/Row` · `simOcc` 계열 · `srcReal`
`applySensorPinMode` · 라우터 등록표 둘(`fn_[MODULE_N]`) · `MODULE_TABLE` 의 `constexpr PROGMEM`
🔑 "센서를 표 앞쪽에 연속으로" **불변식도 사라졌다** — `isSensor(i)` 루프가 그것을 없앴다
```

## 4. 안 건드린 것

```
차단봉 `DR` — 사용자 답 대기(루트가 갈래를 올렸다). 실물 없고 서버 조립 표에만 있다
`GUIDE.md`  — 형태가 네 번 바뀌는 동안 안 고쳤다. **시험이 서고 나서 한 번에**
값 에코 `C` — DEFERRED
```

## 5. 원장에서 읽을 것

`docs/arduino/LEDGER.md` **§79~87** (오늘 것) · `DEFERRED.md`(미룬 것 + 되살릴 조건)
