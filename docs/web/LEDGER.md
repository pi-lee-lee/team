# web 도메인 원장

> **이 파일에는 시간이 지나도 참인 것만 적는다.** 그날의 상태·진행은 날짜 붙은 파일로 간다
> (`FINDING-`·`STATUS-`·`handoff-`).
> 규칙: ① 근거 없이 적지 않는다(실측인지 추정인지 문장에 드러낸다) ②
> 틀린 항목은 지우지 말고 취소선과 사유를 남긴다 ③ append 가 기본이다.

시작: 2026-08-16 · web-engineer
(그 전까지 이 파일은 없었다. 이날 브라우저 구간을 처음 실측하면서 만들었다.)

---

## 1. 화면이 붙는 곳

### 1.1 서버는 HTTP 와 WS 가 **같은 포트 하나**다 — 오프셋은 둘을 같이 옮긴다 (실측)

`server.cpp` 의 리스너 하나가 `index.html` 정적 서빙과 `GET /ws` 업그레이드를 겸한다
(`lsn_http = listen_on(PORT_HTTP + g_port_offset)`, 2102행 / `path == "/"` → `/index.html`, 1855행).

- 운영: **9900** · 시험 인스턴스(`--port-offset=100`): **10000**
- 실측 근거: `http://127.0.0.1:10000/` 이 페이지를 주고, 같은 포트로 `ws://127.0.0.1:10000/ws`
  업그레이드가 성사됐다(CDP `Network.webSocketCreated` 로 확인, 2026-08-16 19:07).

**따라서 "페이지를 준 포트 = WS 포트"가 이 시스템의 불변식이다.** 화면이 WS 포트를 따로
정하려 들면 그 순간 두 서버를 섞을 수 있다 — 실제로 그랬다(§1.2).

### 1.2 🔴 함정 — 화면이 `location.port` 를 무시하고 상수 9900 을 쓰면 **운영으로 샌다**

2026-08-16 이전의 `index.html` 이 그랬다. 호스트만 `window.location` 에서 가져오고
포트는 `const WS_PORT = 9900` 이었다.

결과(코드 판독 + 그 뒤 실측으로 확인):

> 시험 서버(:10000)가 준 페이지를 열면 **WebSocket 은 운영(:9900)** 으로 붙는다.
> 폴백 폴링만 상대경로라 :10000 으로 간다 → **한 화면이 두 서버를 섞는다.**
> 그 화면에서 예약을 누르면 **운영 장치로 하행 명령이 내려간다.**

**이 함정의 지독한 점**: 시험 인스턴스 로그에는 아무것도 안 남는다. 흔적은 운영 로그에만
생기므로 **깨진 줄도 모른다.** 포트·`flock`·로그 분리를 아무리 겹쳐도 화면에 박힌 상수
하나가 전부 통과했다.

지금은 `resolveWsPort()` 가 `?ws=` → `location.port` → 9900 순으로 정한다.
회귀 검사: `node web/tools/check-index.mjs` §5.

### 1.3 함정 — 리버스 프록시·80/443 뒤에서는 `location.port` 가 **빈 문자열**이다 (추정, 미실측)

그 경우 §1.2 의 규칙이 문서화된 기본값 9900 으로 떨어진다. 프록시가 WS 를 다른 데로
넘긴다면 `?ws=` 로 명시해야 한다. **이 구성은 이 프로젝트에 아직 없어서 실측하지 않았다.**

### 1.4 폴백은 상대경로 `data_log.json` 이다 → 기본 경로에서는 WS 와 항상 같은 오리진 (실측)

둘이 다른 서버를 가리킬 수 있는 유일한 경우는 `?ws=` 를 명시했을 때이고, 그때 화면이
빨간 경고를 띄운다(실측: `web/artifacts/4-fallback.png`).

---

## 2. 서버가 화면에 주는 것

### 2.1 WS 스냅샷의 `device` 에는 `last_frame_ts` 가 **없다** (실측)

> ⚠ **이 항목은 빌드에 딸린 사실이다. `build=22f788d` 부터는 아니다 — §5.1 을 먼저 읽어라.**
> (아래 내용은 그 빌드 **이전**에 대해 여전히 참이고, 운영에 무엇이 올라가 있는지는 별개다.)

2026-08-16 19:07, 시험 인스턴스에서 받은 실제 프레임(`web/artifacts/ws-snapshot.json`):

```
WS   device = { online, device_id, uptime, seq }
파일 device = { online, last_frame_ts }
```

→ **신선도 표시는 폴백 경로에서만 동작한다.** 정상 운영 중에는 화면이 계속
`"알 수 없음 (이 경로에는 이 정보가 없습니다)"` 를 띄운다(실측한 DOM 문자열).

REQ-0132 로 socket 에 요청했다. 오면 화면은 그날로 동작한다(이미 그 키를 읽고 있다).

### 2.2 🔴 함정 — `GET /?쿼리` 는 **404** 다. `GET /index.html?쿼리` 는 200 (실측)

> ⚠ **이 항목도 빌드에 딸린다. `build=22f788d` 부터는 200 이다 — §5.1.**
> 그리고 **이 응답 코드 자체가 빌드 판별자다**: 붙은 서버가 REQ-0132 이전인지 이후인지
> `GET /?demo=1` 한 줄로 갈린다(404 = 이전 · 200 = 이후).

```
GET /                  → 200
GET /index.html?demo=1 → 200
GET /?demo=1           → 404
```

원인(판독): `serve_file()` 이 `path == "/"` → `/index.html` 매핑을 **쿼리 제거보다 먼저**
한다. `/?demo=1` 은 `"/"` 와 다르므로 매핑을 놓치고, 쿼리를 자르면 파일명이 빈 문자열이 된다.

**시연에서 `호스트:9900/?demo=1` 을 치면 빈 페이지가 뜬다.** 주소에 `/index.html` 을 반드시
넣어라. REQ-0132 로 요청했고, 고쳐지면 `web/tools/e2e.mjs` §9 가 저절로 초록이 된다.

### 2.3 화면은 서버 **로그**를 읽지 않는다 (REQ-0122 에서 확인, 여전히 참)

입력 경로는 WS JSON 과 `data_log.json` 둘뿐이다. 그래서 로그 줄의 지표 이름이 바뀌는
`오프라인` → `무프레임` 개명에 화면은 걸리지 않는다.

### 2.4 화면은 최상위 `occupied`/`reserved` **비트열을 읽지 않는다.** `slots[]` 만 본다 (실측)

비트열이 멀쩡히 있어도 `slots` 가 비면 10칸 전부 "상태 미상"이 된다
(측정: `web/artifacts/probe-slots.json` 의 `bits` 사례). 서버가 지금 둘 다 보내므로 문제는
없지만, **`slots[]` 를 빼는 변경은 화면을 조용히 눈멀게 한다.**

---

## 3. 화면이 상태를 말하는 방식

### 3.1 `slots[]` 결손은 "빈 자리"로 보이지 **않는다** — 통념 정정 (실측)

> ~~"slots 가 없으면 10칸이 조용히 빈자리로 보인다"~~ — **틀렸다.**
> 2026-08-16 실측: 결손 칸은 `상태 미상 / 데이터 없음`(점선 테두리)으로 그려진다.
> 근거는 `normalizeSnapshot()` 이 칸마다 다는 `known: bySlot.has(id)` 이고,
> `tileView()` 가 `!slot.known` 이면 `view:'unknown'` 을 돌려준다.
> 측정 원문: `web/artifacts/probe-slots.json` (5개 사례 · `web/artifacts/slots-*.png`).

**다만 진짜 빈틈은 다른 데 있었다**: 그 말이 **칸 안에만** 있었다. 10칸 중 3칸만 미상이면
나머지 7칸이 정상이라 화면 전체는 멀쩡해 보인다. 그래서 같은 날 격자 위에 **집계 배너**를
추가했다(`#slots-banner`). 배너는 집계만 하고 칸의 문구를 대신하지 않는다.

### 3.2 함정 — 이 화면의 "누를 수 없음"은 `disabled` 가 아니라 `aria-disabled` 다 (실측)

`t.button.setAttribute('aria-disabled', …)` 만 쓰고 `button.disabled` 는 건드리지 않는다.
**의도된 설계다**: 진짜 `disabled` 로 만들면 키보드·스크린리더 사용자가 그 칸에 도달조차
못 해서 "왜 못 누르는지"를 들을 수 없다. 클릭 핸들러가 `aria-disabled` 를 보고 이유를 띄운다.

→ **자동 검사에서 `el.disabled` 를 읽으면 항상 `false` 라 "막힘이 안 먹는다"고 오판한다.**
실제로 이날 그 오판을 한 번 했다. 반드시 `getAttribute('aria-disabled')` 를 읽어라.

### 3.3 소켓이 끊겨도 화면은 비지 않는다 — 폴백이 이어받는다 (실측)

살아 있는 WS 를 끊으면 **2ms 안에** 연결 표시가 "연결 끊김 — N초 후 재연결 시도"로 바뀌고,
칸 10개는 **직전 상태를 유지**한다("상태 미상"으로 떨어지지 않는다). 그 뒤 자동 재연결된다.
즉 이 화면의 실패 모드는 "빈 화면"이 아니라 "오래된 값 + 그렇다는 표시"다.
(측정 절차: `e2e.mjs` §6 — 브라우저에서 `transport.ws.close()` 로 소켓만 끊는다)

### 3.4 예약 왕복은 브라우저 클릭에서 화면 반영까지 **약 205ms** (실측 2회)

시험 인스턴스 + `fake_arduino`(로컬) 기준이다. 실물 ESP·무선이 들어가면 달라진다 —
**이 숫자를 실물 경로의 기대값으로 옮겨 쓰지 마라.**
구간 정의: `#confirm-dialog` 의 확인 버튼 클릭 → 그 칸의 상태 문구가 "내 예약"으로 바뀜.

---

## 4. 시험 방법에 대해 참인 것

### 4.1 브라우저 검증은 의존성 없이 된다 — CDP 직접 (실측)

node 22 의 전역 `WebSocket`·`fetch` 로 크롬 DevTools 프로토콜을 직접 말한다
(`web/tools/cdp.mjs`). puppeteer 같은 것을 들이지 않았다.
`--headless=new` + **매번 새 임시 `--user-data-dir`**(사용자 프로필을 재사용하면 잠금 충돌).

### 4.2 함정 — 상태 문구를 `waitFor` 로 기다리면 **바뀌기 전 값에 즉시 걸린다** (실측)

빈 문자열이 아닌 모든 문자열은 참이다. `waitFor("…textContent")` 는 "빈 자리"에 즉시
성공하고, 그러면 4ms 만에 "화면이 안 바뀌었다"는 **거짓 실패**가 난다.
반드시 **불리언**을 기다려라: `/예약/.test(…)`.
이날 이 한 줄 때문에 종단 시험이 두 번 잘못된 결론을 냈다.

### 4.3 시험 인스턴스의 `data_log.json` 은 오프셋으로 갈리지 않는다 (실측)

`atomic_write_log()` 가 **cwd 상대경로**로 쓴다. `--port-offset` 이 갈라 주는 것은
**로그 파일 경로뿐**이다(`.test+100`).

- 운영 인스턴스의 cwd 는 `/Users/idong-u/parking-bin` 이라 **운영 파일과는 안 부딪친다**
  (`lsof -p <pid> -a -d cwd` 로 확인).
- cwd 를 `조별과제샘플` 로 잡는 시험 인스턴스는 `조별과제샘플/data_log.json` 을 덮어쓴다.
  ~~그 파일은 git 추적 대상이라 커밋에 딸려갈 수 있다~~ — **틀렸다.** `.gitignore:53` 에
  있어 추적되지 않는다(socket-engineer 정정, `git ls-files` 로 확인). **되돌릴 것이 없다.**
  내가 `git ls-files` 를 돌려 보지 않고 "추적 파일"이라 단정한 것이 원인이다.

### 4.4 🔴 화면이 서빙되는지는 **서버를 어디서 띄웠느냐**로 결정된다 (실측 — 단, 근거는 GET 이 아니다)

`serve_file()` 은 `index.html` 을 **cwd 상대경로**로 연다. 즉 **바이너리 위치가 아니라
작업 디렉터리에 `index.html` 이 있어야 화면이 나온다.** 없으면 그 포트는 404 만 준다.

2026-08-16 시점의 실제 상태:

```
/Users/idong-u/parking-bin/     ← 운영(pid 75781)의 cwd
  srv_parking · data_log.json · stderr.log      ← index.html 이 **없다**
```

> ⚠ **`pid 75781` 은 낡았다** (2026-08-16 23:5x 정정). 21:05:10 머신 재부팅으로 종료됐다 —
> 비정상 종료가 아니라 SIGTERM 정상 종료다(root 확인 · 운영 로그 13608행
> `INSTANCE-END reason=normal frames=10176 sessions=11`). 나는 23:47 에 프로세스·LISTEN 이
> 비어 있는 것만 봤고, 경위는 root 가 로그로 채웠다.
>
> **이 항목의 결론은 그대로 참이다** — 낡은 것은 pid 하나뿐이고, "그 디렉터리에 `index.html`
> 이 없어서 운영은 화면을 못 준다"는 결론은 pid 와 무관하다.
>
> 교훈: **원장에 pid 를 적으면 반드시 썩는다.** 재부팅 한 번이면 끝이다. 경로·cwd·포트는
> 안 썩는다. 다음부터는 프로세스를 가리킬 때 pid 대신 **cwd 와 포트**로 적는다.

→ **운영 인스턴스는 관측용이고 화면을 서빙하지 못한다.**

**이 결론의 근거가 무엇인지가 결론만큼 중요하다** — 세 가지이고, **셋 다 운영 포트를 건드리지
않는다**(관측 창 규칙):

1. `server.cpp:1892` `std::ifstream f(fn.c_str(), std::ios::binary)` — 상대경로다(소스)
2. `~/parking-bin/` 의 파일 목록에 `index.html` 이 없다 (`ls`, web·root 가 각각 독립 확인)
3. 같은 방식으로 쓰는 `data_log.json` 이 **그 디렉터리에 실제로 생겨 있다** — cwd 상대라는 방증

즉 근거는 **소스와 파일 목록**이지 *GET 응답*이 아니다. GET 은 일부러 안 보냈다 —
얻을 것이 없고 운영 로그에 accept 한 줄이 남아 monitor 의 계수에 섞인다.
(**"필요 없으면 안 건드린다"** 가 이날 지킨 규율이다. 확인하고 싶은 충동이 관측을 오염시킨다.)

시연에서 화면을 띄우려면 **`index.html` 이 있는 디렉터리에서 서버를 띄워야 한다** —
지금 그런 곳은 `조별과제샘플/` 이다. 루트 판단으로 **사본을 두지 않고**
`--webroot` 로 경로 해석을 고치는 방향이 socket 에 넘어가 있다(사본이 둘이면 갈라진다).

**이 함정이 지독한 이유**: 서버는 정상 기동하고 로그도 정상이며 WS 도 열린다.
화면만 안 나온다. "서버는 떴는데 왜 페이지가 없지"를 시연장에서 만나게 된다.

### 4.5 `?demo=1` 은 서버에 트래픽을 만들지 않는다 (코드 판독 + 실측)

`transport.start()` 가 `connect()`·`startPolling()` **앞에서** 반환한다.
관측 창이 걸려 있을 때 화면 검증에 쓸 수 있는 유일한 경로다.
단, 주소는 `/index.html?demo=1` 이어야 한다(§2.2).

### 4.6 브라우저 검증을 다시 돌리는 절차

```bash
# ① 시험 인스턴스는 **socket 에게 요청한다**(server.cpp·net/ 은 web 소유가 아니다).
#    요구할 것: --port-offset · cwd=조별과제샘플(§4.4) · 조용한 보드(재부팅·자동입차 없이)
#    선례: REQ-0128

# ② 브라우저 없이 도는 회귀 — 시험 인스턴스가 없어도 된다
node web/tools/check-index.mjs

# ③ 종단 시험 — 진짜 크롬. 운영 포트를 주면 스크립트가 거부한다
node web/tools/e2e.mjs --port 10000        # --head 를 붙이면 창이 보인다

# ④ slots 결손처럼 **서버가 만들 수 없는 상태**는 주입으로 본다(트래픽 0)
node web/tools/probe-slots.mjs --port 10000
```

**시험 인스턴스를 web 이 내리지 않는다** — 띄운 쪽이 내린다. 끝나면 알린다.
`e2e.mjs` 는 예약을 걸었다가 **반드시 취소까지 하고 끝난다.** 시험이 상태를 남기면
다음 사람이 그것을 실제 예약으로 읽는다.

---

## 5. REQ-0132 이후 (2026-08-16 23:5x 추가 · web-engineer)

### 5.1 REQ-0132 은 고쳐졌다 — 단 **socket 의 실측**이고 web 하니스로는 아직 확인 못 했다

`build=22f788d` 에서 둘 다 고쳐졌다(socket-engineer 가 오프셋 +100 시험 인스턴스에서
`curl` + `ws_probe` 로 실측 · REQ-0132 `## 처리 결과`):

- WS 스냅샷 `device.last_frame_ts` — 파일과 **같은 키·epoch ms·같은 정의**, 무프레임이면 `null`
- `GET /?demo=1` → **200**

**출처를 분명히 한다: 이것은 socket 의 측정이지 web 의 측정이 아니다.**
`web/tools/e2e.mjs` [5]·[10] 으로 브라우저 왕복을 확인하는 것이 남아 있고, 그러려면
시험 인스턴스가 필요하다(§4.6 ①). 화면이 그 값을 실제로 그리는지는 **아직 아무도 안 봤다.**

그리고 **운영에서 화면이 뜨는 것은 여전히 별개 문제다** — §4.4 의 cwd 문제는 그대로이고
`--webroot` 는 이 빌드에 들어가지 않았다.

### 5.2 🔴 파일의 `last_frame_ts` 는 **얼어 있다** — 같은 키인데 신선도가 다르다

(소스 판독 `server.cpp:1333-1345` + socket 실측 정황. **파일 정지 자체는 아직 미실측** —
확인법은 아래.)

`write_log_if_changed()` 는 상태 키가 바뀔 때만 파일을 쓰고, **`last_frame_ts` 를 그 키에서
일부러 뺐다.** 소스 주석이 이유를 적어 뒀다 — 넣으면 매 프레임이 변화가 되어 **초당 한 번**
디스크를 쓴다. 의도된 설계다.

→ **상태 변화가 없으면 `data_log.json` 은 다시 써지지 않고, 그 안의 `last_frame_ts` 는
그 시각에 멈춘 채로 있다.** WS 는 프레임마다(1Hz) 갱신된다.

socket 의 REQ-0132 실측이 이것과 부합한다 — "같은 순간"의 두 값이 실제로는 벌어져 있었다:

```
WS   last_frame_ts = 1786890922577
파일 last_frame_ts = 1786890892390     차 = 30,187ms
```

**화면에 대한 뜻**(내 코드 `renderFrameAge()` 판독): 폴백 경로에서 `now - last_frame_ts` 는
"장치가 침묵한 시간"이 **아니라** `기록의 나이 + 기록 시점의 침묵` 이다. 지금 화면은 두 항을
나눠 보여 주긴 한다(`· 기록 시점에 이미 X 침묵` 과 `renderAge()` 의 `기록 시각 기준 N초 전`).
**거짓말은 아니지만 앞 문구만 읽으면 멀쩡한 장치를 "N분 전 수신"으로 읽는다.**
문구 조정 여부는 시험 인스턴스에서 실제 화면을 보고 정한다 — 지금 고치면 검증 없는 변경이 된다.

⚠ **비교 규칙**(CLAUDE.md): 두 `last_frame_ts` 는 **이름·단위·값의 정의가 같고 갱신 시점만
다르다.** 그래서 더 위험하다 — 차이를 "시계 어긋남"이나 "서버 버그"로 읽으면 틀린다.
확인법(트래픽 최소): 상태 변화가 없는 동안 `GET /data_log.json` 을 60초 폴링해
`last_frame_ts` 가 **그대로인지** 본다. 그대로면 이 항목이 실측으로 승격된다.

### 5.3 🔴 하니스 함정 — **결함의 존재를 단언하는 검사는 고쳐지는 순간 거짓 경보가 된다**

`e2e.mjs` [5] 가 `!('last_frame_ts' in device)` 를 단언하고 있었다. 결함을 실측으로 고정해
REQ 의 근거로 삼으려던 것이었는데, **고쳐지면 빨간불이 되는 검사**였다.

socket 이 "[5] 가 초록이 되는지 확인해 달라, 아니면 다시 열어라"고 했다. 뒤집지 않고 그대로
돌렸다면 하니스가 빨간불을 냈을 것이고, 나는 **멀쩡히 고쳐진 것을 회귀로 판정해 REQ 를 다시
열 뻔했다.** 고친 쪽이 벌을 받는 검사다.

옳은 형태는 같은 파일의 [10] 이다 — `GET /?x 는 200 이어야 한다` 로 써 두면 결함이 있는 동안
빨간불이고 **고쳐지면 저절로 초록**이며 그 뒤로는 회귀 감시가 된다. 하나의 검사가 셋을 다 한다.
2026-08-16 23:5x 에 [5] 를 이 형태로 뒤집었다.

### 5.4 단계 번호 정정 — 정적 서빙 검사는 **[10]** 이다

`[9]` 는 콘솔 오류 검사다. REQ-0132 본문과 §2.2 에 내가 `[9]` 라고 잘못 적었고 socket 도 그대로
인용해서 두 도메인의 기록에 같은 오기가 남았다. **번호로 가리킬 때는 파일을 열어 확인한다.**

### 5.5 `--port` 필수 + 운영 포트 거부가 **팀 표준**이 됐다

`e2e.mjs:25-30` 의 방식(기본값 없음 · `FORBIDDEN=['9900','9991','5500']` 이면 즉시 중단)이
팀 표준으로 채택됐다(root 통보 · socket 이 `net/ws_probe.py`·`net/fake_arduino.py` 에 적용).
**기본값이 운영 포트인 도구는 "시험한다고 생각하면서 운영에 붙는다"** — 포트 분리·flock·
로그 분리·chmod 를 전부 통과하는 종류의 사고다(§1.2 와 같은 형태).

### 5.6 🔴 web 은 하행이 **실물 장치에 닿는 것을 한 번도 확인하지 않았다**

§3.4 와 REQ-0132 의 "예약 왕복 성공 · 205ms"를 **하행 도달의 증거로 인용하면 안 된다.**
그 측정의 상대는 전부 `fake_arduino`(로컬 프로세스)였다. 확인된 것은 정확히 이것이다:

```
브라우저 클릭 → WS reserve → 서버 → **fake_arduino** → ack → 화면 갱신
```

실물 ESP·무선 구간은 **이 경로에 들어 있지 않았다.** "브라우저에서 예약이 된다"는
서버까지의 왕복을 증명하지 **서버 밖을 증명하지 않는다.** 같은 문장이 두 가지를 뜻할 수
있고 이름이 같아서 더 위험하다(CLAUDE.md 비교 규칙).

**web 이 실물 하행 시험에 보탤 수 있는 것**: 브라우저에서 출발하는 가장 현실적인 경로를
`e2e.mjs` [3]·[4] 가 이미 자동화해 뒀다(예약 → ack → 화면 확인 → **반드시 취소까지**).
상대를 fake 대신 실물 보드로 바꾸면 그대로 쓴다. 다만 **실물에 하행을 쏘는 판단은 web 의
것이 아니다** — 배타적 자원이라 루트가 순서를 정한다.

⚠ **화면에서 출발하는 시험은 §4.4 에 먼저 걸린다** — `index.html` 이 있는 디렉터리
(`조별과제샘플/`)에서 서버를 띄우지 않으면 페이지가 404 라 시험이 시작도 못 한다.

### 5.7 🔴 §5.2 의 격차에는 **상한이 없다** — 폴백 화면이 멀쩡한 장치를 죽은 것처럼 그릴 수 있다

socket-engineer 가 §5.2 를 확인해 주면서 덧붙인 것이고, **방향이 내 정리보다 나쁘다.**

파일은 상태가 바뀔 때만 써지므로, 상태가 8시간 안 바뀌면 파일의 `last_frame_ts` 는 **8시간 전
값**이다. 폴백 화면은 `now - last_frame_ts` 를 그리므로 **"8시간 전 수신"** 이라고 말한다 —
장치는 1Hz 로 멀쩡히 보내고 있는데도. **REQ-0067 이 막으려던 것("죽은 데이터를 살아있는 것처럼
보이지 않기")의 정확한 반대 방향, 즉 거짓 경보다.**

폴백 경로에서 화면이 실제로 아는 것은 이것뿐이다:

| 아는 것 | 값 |
|---|---|
| 기록이 쓰인 시각 | `ts` |
| **그 시각 기준** 침묵 | `ts - last_frame_ts` |
| 기록 자체의 나이 | `now - ts` |
| **지금 장치의 침묵** | **모른다** — `0` 과 `now - last_frame_ts` 사이 어디든 될 수 있다 |

→ 지금 화면은 마지막 줄을 **모른다고 말하지 않고 상한값을 단정**한다. 고칠 방향은 여기다.
**WS 주 경로는 이 문제가 없다**(스냅샷은 매번 새로 만들어져 실시간이다 — socket 확인).
즉 **정확한 경로가 죽었을 때만 부정확한 경로가 뜨는데, 하필 그때 거짓 경보를 낸다.**

서버는 고칠 것이 없다(초당 쓰기를 되살리는 셈이라 더 나쁘다 — §5.2). **화면 쪽 문제로 가져간다.**
~~아직 안 고쳤다~~ → **고쳤다. §5.31 을 봐라**(2026-08-17 · 실측 근거 §5.29 확보 뒤).

### 5.8 화면이 장치 끊김을 **인지하는 임계값은 3.5초**다 (실측)

`server.cpp:102` `OFFLINE_MS = 3500` · `886` `device_online()`. 화면은 서버가 말해 줄 때만
안다. → **3.5초보다 짧은 끊김은 화면에 아무 흔적도 안 남긴다.**
실측(2026-08-17, 시험 인스턴스 10500 · 95표본): 2초 침묵 동안 `online=false` 표본 **0개**,
배너·연결표시·10칸 상태·`aria-disabled` **변화 0건**.

⚠ **"화면에 아무 일도 없었다 = 링크가 멀쩡했다"가 아니다.** 이 함정이 지독한 이유는
**짧은 끊김일수록 화면이 못 막아 명령이 그대로 나간다**는 것이다. 안전한 구간이 아니라
**관측되지 않는 구간**이다.

### 5.9 🔴 끊긴 것이 **어느 링크인지** 화면 위쪽만 봐서는 모른다 (실측)

장치가 끊겨 `online=false` 인 동안에도 `#conn-text` 는 **"WebSocket 연결됨"** 그대로다.
**배너도 안 뜬다.** 브라우저↔서버는 멀쩡하기 때문이고, 그건 사실이다.

→ 그래서 **화면 위쪽은 정상으로 보이는데 10칸이 전부 안 눌린다.** 이유는 칸의 보조 문구
(`센서 끊김 — 예약 불가`)와 접혀 있는 장치 패널에만 있다. 실측 문구:

```
오프라인 중  A1 = 빈 자리 / "센서 끊김 — 예약 불가" / aria-disabled=true
복귀 후      A1 = 빈 자리 / "눌러서 예약"          / aria-disabled=false
```

**"연결됨"과 "예약 불가"가 동시에 참일 수 있다** — 두 링크가 다르기 때문이다(§3.3 과 별개 사건).

### 5.10 🔴 `deviceDown` 때문에 걸린 예약을 화면이 **"자리 상태가 바뀌었다"고 거짓 보고**한다

`requestReserve()`(index.html 1711-1716)가 확인을 누른 **뒤에** `tileView(slot).action !== 'reserve'`
를 재검사한다. `deviceDown` 이면 그 분기에 `action` 이 없어 걸러지는데, 문구는 한 가지뿐이다:

```
"그 사이 자리 상태가 바뀌어 예약하지 않았습니다"      ← 자리 상태는 안 바뀌었다
```

실측 3/3(2026-08-17). **동작은 옳다**(못 가는 명령을 안 보낸다). **문구가 거짓이다.**
읽는 사람은 "누가 먼저 채갔나"로 읽고 엉뚱한 곳을 본다.
→ 고칠 방향과 이유는 `docs/web/FINDING-link-blink-2026-08-17.md` §5. ~~**아직 안 고쳤다.**~~

> ⚠ **정정(2026-08-17 19:5x · §5.22)** — **고쳤고 실측됐다.** 커밋 `a3922e0`(코드) ·
> `FINDING-link-blink-2026-08-17.md` **§7.1** 에서 **실제 WS 경로로 3/3** 재측정됐다
> (새 문구: `"장치 연결이 끊겨 예약을 보내지 않았습니다 — 자리 상태는 그대로입니다 …"`).
> **위 문장이 낡은 채로 남아 있어 내가 그것을 그대로 루트에 보고했다.** 결함의 기술(무엇이
> 거짓말이었나)은 여전히 참이라 지우지 않는다 — 낡은 것은 마지막 줄 하나였다.

### 5.11 `last_frame_ts` 의 **시계열**이 "장치 부재"와 "장치 침묵"을 가른다 (실측)

화면에는 둘을 가르는 표시가 없다. 값 하나로는 못 가르지만 **시계열은 가른다**:

| 보이는 것 | 뜻 |
|---|---|
| 값이 계속 **바뀐다**(나이가 진동) | 프레임이 오고 있다 = **장치 붙어 있음** |
| 값이 **하나로 얼어 있다**(나이가 단조증가) | 프레임이 없다 |

2026-08-17 에 이것으로 자료 하나를 구했다 — 측정 직전 3분간 장치가 아예 안 붙어 있었는데,
내 표본 24초에 서로 다른 `last_frame_ts` 가 **19개**여서 그 구간이 아님을 **내 자료만으로**
증명할 수 있었다(남의 말이 아니라).

⚠ **한계**(socket 지적): 침묵이 길면 그 동안도 "얼어 있다"로 보인다.
**판별자는 침묵보다 긴 시간을 볼 때만 유효하다.** 6초 침묵 구간에서 6초만 보고
"장치 없음"이라고 하면 틀린다.

### 5.12 시각은 **추정해서 적지 마라** — `date` 를 붙여 넣어라

2026-08-17 에 개입 통보에 `00:1x` 라고 적었다. 실제는 `08:2x` 였다. 날짜가 바뀐 것만 보고
자정 근처로 짐작한 것이다. monitor 가 잡았다.

**왜 사소하지 않은가**: 개입 기록은 시각이 전부다. `00:1x` 로 기록됐으면 **이미 판정문이
나간 창3 구간 안에 없는 개입이 하나 생긴다** — 그 판정문의 "개입 없음"이 결론의 일부였다.

→ 통보·기록에 시각을 쓸 때는 **문장으로 조심하지 말고 `date` 출력을 그대로 붙여 넣는다.**
추정할 자리를 없애는 쪽이 주의문보다 강하다(monitor 조언).

### 5.13 `handleServerMessage()` 에는 **캐치올이 없다** — 모르는 타입은 조용히 무시된다 (실측)

`조별과제샘플/index.html` 1885-1968행. `if (msg.type === 'snapshot'/'ack'/'error') { … return; }`
사슬이고 마지막 `error` 블록이 1966행에서 닫힌 뒤 **1967행이 `// 모르는 type 은 조용히
무시한다.`** 이며 곧바로 함수가 끝난다.

**왜 이것이 원장에 남을 값인가**: 이 성질 하나가 **프로토콜을 앞으로 늘릴 때 안전한 방향을
결정한다.**

- **새 타입을 추가하는 변경** → 옛 화면은 무시 → **깨지지 않는다.** 서버를 먼저 배포해도 된다
- **기존 타입에 필드를 추가해 뜻을 바꾸는 변경** → 옛 화면은 **그 타입의 옛 뜻대로 동작한다**
  → 서버 배포가 **그 순간 옛 화면을 깨뜨린다**

REQ-0155(하행 큐의 "접수됨") 때 이 판정이 실제로 방향을 정했다. `ack` 에 상태 필드를 넣는
안이었으면 옛 화면이 `type==='ack'` 만 보고 **전선에 나가지도 않은 요청을 "예약되었습니다"로
선언**했을 것이다.

⚠ **근거 정정 이력**: 처음에는 `msg.type` 세 곳만 grep 하고 이 결론을 적었다. **그건 "세 타입을
검사한다"만 증명하지 "끝에 else 가 없다"를 증명하지 않는다.** 함수 끝까지 읽어 확인한 뒤
근거를 바꿨다 — **결론은 같았지만 근거가 부실했다.** 캐치올이 있었다면 위험의 크기가 달랐다
("조용히 무시" 가 아니라 "시끄럽게 오작동").

### 5.14 🔴 화면의 자체 타임아웃이 **짧으면 서버 큐를 키운다** (설계 함정)

REQ-0155 에서 나온 것. 서버가 하행을 큐에 담게 되면 이 연쇄가 생긴다:

```
큐 대기 → 화면 자체 타임아웃(ACK_TIMEOUT)이 먼저 터짐 → 롤백
→ 사용자가 다시 누름 → 새 rid (서버 pending 은 이미 풀려 has_pending_for 도 안 막는다)
→ 큐에 한 건 더
```

**즉 화면이 "안전하게" 되돌리는 동작이 시스템 부하를 키운다. 타임아웃이 짧을수록 더 쌓인다.**
socket 이 "같은 자리 중복은 서버가 막는다"고 적어 뒀던 항목이 이것 때문에 취소선으로 정정됐다 —
**그 방어는 pending 이 살아 있는 동안만 유효하다.**

→ **규칙**: 화면의 타임아웃은 **서버가 준 상한에 묶는다. 화면이 짐작하지 않는다.**

### 5.18 🔴 **예외로 죽은 실행이 "통과"로 찍혔다** — 집계기는 예외도 세야 한다

2026-08-17 실측. `blink.mjs` 의 `failed` 는 `check()` 만 올리는데, 실행이 예외로 중단되면
**`check()` 가 한 번도 안 돈다 → 실패 0건 → "통과"** 였다. 화면 첫 줄에 `💥` 가 떠 있는데
**마지막 줄이 "통과"** 이고 구간 기록 파일에도 `종료 (통과)` 로 남았다.

**즉 아무것도 못 쟀는데 초록을 내는 검사기다.** §5.3 과 같은 뿌리다 — **검사기가 틀렸는데
대상이 틀렸다고(또는 맞다고) 말하는 상태는 그 자체로는 절대 안 드러난다.**

`process.exitCode = 1` 은 걸려 있었지만 **사람이 읽는 줄과 파일에는 안 반영**됐다.
종료 코드는 셸이 보고 사람은 마지막 줄을 본다 — **둘이 다른 말을 하면 사람 쪽이 이긴다.**

→ **규칙: 실패 집계는 `catch` 에서도 올린다.** 그리고 **"통과/실패"를 적는 곳이 여럿이면
전부 같은 값을 봐야 한다**(콘솔·로그 파일·구간 기록·종료 코드).

**어떻게 찾았나가 중요하다**: monitor 의 규칙 — *"도구를 만들면 실제로 돌아간 증거 한 줄을
보고에 같이 넣어라. 출력이 없으면 안 돈 것이다"* — 를 따라 **아무도 안 듣는 포트로 한 번
돌려 봤더니** 나왔다. **안 돌려 봤으면 못 봤다.**

### 5.17 도구의 기본값이 **딴 데를 가리키면 조용히 틀린 답을 낸다** — 없애지 말고 **유도해라**

2026-08-17 실측. `e2e.mjs` 의 `--log` 기본값이 `test+100` 이었는데 시험 인스턴스가 `+600` 이라
**엉뚱한 로그 파일을 조용히 읽고** *"서버가 R 을 안 내보냈다"* 는 **거짓 실패**를 냈다.
예약은 멀쩡히 성공했고 화면도 "내 예약"이었다 — **제품이 아니라 하니스가 틀린 것**이다.

같은 뿌리의 사고가 하루에 셋이었다(socket 확인): `ws_probe` 의 기본 포트가 운영 · `frame_gaps`
의 경로 하드코딩 · 이것. **공통점은 "도구가 자기가 어디를 보는지 말하지 않는다"** 이다.

→ **해법 둘 중 유도가 낫다:**

| | 방식 | 대가 |
|---|---|---|
| 없애기 | 필수 인자로 만든다(`--port` 처럼) | 매번 쳐야 한다. **다른 값을 넣을 여지는 남는다** |
| **유도하기** | **대상에서 계산한다**(`포트 10500 → test+600.log`) | **틀릴 수가 없다** |

**운영을 건드릴 수 있는 것(포트)은 없애고, 대상에서 정해지는 것(로그 경로)은 유도한다.**

### 5.15 확정된 계약 — `queued` (REQ-0155)

**원본은 socket 의 `docs/net/DESIGN-server-slot-queue.md` §4-B**(커밋 `3cf6ed9`·`8ba70cb`)다.
여기에는 **화면 쪽이 지켜야 할 것만** 적는다. 값이 갈리면 원본이 이긴다.

```json
{"type":"queued", "rid":"…", "slot":"A1", "ahead":2, "expires_ms":<남은 지속시간 ms>}
```

- **`expires_ms` 는 "이 프레임을 받은 시점 기준 남은 상한"**(지속시간). 절대 시각이 아니다.
  서버는 `deadline = enqueue + cap` 을 항목마다 한 번 박고 낼 때마다 `deadline − now` 를 보낸다
  → **단조 비증가가 구조상 보장된다.** 🔑 **두 시계를 안 섞는 방식이다** — 서버는 자기 시계로
  차이만 내고, 화면은 받은 지속시간을 자기 시계로 잰다. **어느 쪽도 상대 시계를 안 읽는다.**
- **화면 규칙**: `queued` 를 받을 때마다 **타이머를 다시 건다**(`expires_ms + ACK_TIMEOUT`).
- **`queued` 뒤에는 반드시 `ack` 또는 `error` 가 온다** — 서버가 지키기로 한 보장이다.
  **이게 없으면 중간 상태가 영구가 된다**(타일이 영원히 맥동한다).
- **거절 코드 둘을 다른 문구로 그린다**: `queue_full`(시스템 사정 — "지금 붐빕니다") ·
  `already_pending`(**사용자 잘못이 아니다** — "이미 처리 중입니다").
- **`ahead` 로 시간을 환산하지 않는다.** 그러려면 창당 처리 건수(`rxmax`)를 화면이 들고 있어야
  하는데 그건 실측 중이고 내 도메인도 아니다 — **화면에 그 상수를 박는 순간 전제가 바뀌면
  조용히 틀린다.** 화면은 **"앞에 N건"까지만** 말한다.

⚠ **`cap` 의 값은 `rxmax` 실측에 종속이다. 의미는 확정, 값은 미정.**
~~**`cap` 은 서버의 큐 깊이(언제 `queue_full` 을 돌려주나)이지 화면 값이 아니다**~~
🔴 **이 정의는 틀렸다 — §5.27 참조.** `cap` 은 팀 전체에서 세 가지 뜻으로 쓰인다(ms·B·건수) — 화면은
`expires_ms` 를 **받아서 쓰므로** 이 대기에 안 걸린다(socket 이 자기 "값 나오기 전에 만들지
마라"를 `cap` 한정으로 정정했다).

#### 5.15.1 화면 쪽 구현 상태 (2026-08-17 · **주입으로 확인, 실제 서버 수신은 미확인**)

> 🔎 **이어지는 항목**(2026-08-17 19:5x): 이 표의 뒤를 §5.19(거절 코드 둘) · §5.20(타이머
> 재설정을 **시계로** 확인) · §5.21(그 측정에서 나온 하니스 함정)이 잇는다.
> 재현 도구는 `web/tools/queue-contract.mjs` 하나다(서버 없이 돈다).

`queued` 수신 처리를 넣었다. 서버는 아직 `queued` 를 안 보내므로 **주입으로 확인**했다
(데모 모드 · 트래픽 0):

| 경우 | 화면 |
|---|---|
| `ahead:2, expires_ms:4800` | `접수됨 · 전송 대기…` / `서버가 받았습니다 — 앞에 2건` · 타이머 재설정됨 |
| `ahead:0` | `… — 다음 차례입니다` |
| **`expires_ms` 없음** | 타이머를 **안 건다** · `(언제까지인지는 서버가 알려 주지 않았습니다)` |
| 그 뒤 `ack` | `예약 확정됨`(status `confirmed`) — **`queued` 를 안 거친 보통 경로와 동일** |

🔴 **`expires_ms` 가 없을 때 숫자를 지어내지 않는다**(루트 지시). 짐작한 상한으로 되돌리면
**지금 고치는 거짓 롤백이 다른 모양으로 돌아온다.** 대신 타이머를 안 걸고 "모른다"를 적는다 —
계약상 `queued` 뒤에는 반드시 `ack`/`error` 가 오므로 그때 끝나고, 그 보장이 깨지면 타일이
대기로 남는데 **그건 조용한 거짓말보다 낫다.**

⚠ **마지막 줄("보통 경로와 동일")은 비교로 확인한 것이다** — `queued` 를 거친 rid 와 안 거친
rid 를 같은 화면에서 각각 `ack` 시켜 둘 다 `confirmed` 인 것을 봤다. **"안 깨졌겠지"로 넘기면
회귀를 못 본다.**

### 5.16 하행 아이들 1초 — **전역**이고 `canAct()` 하나에 걸린다 (사용자 지시 · 실측)

**값의 근거**: 하행은 **전송 슬롯 주기 1.2초** 안에서 서버 차례에만 나간다. **1초 아이들이면
슬롯당 최대 1건**이 되어 전송 구조와 맞는다. **주기보다 짧게 잡은 것은 의도**다 — 길게 잡으면
보낼 수 있는 슬롯을 비우게 되고, 짧게 잡아 큐가 한 칸 차는 편이 낫다(큐는 서버가 비운다).

**전역인 이유**: 사용자가 재현한 모양이 **서로 다른 자리를 연달아 누르는 것**이라 자리별로 걸면
10칸을 돌아가며 눌러 그대로 통과한다. 그리고 막는 대상이 자리가 아니라 **전선 하나**다 —
슬롯 주기는 자리마다 있는 것이 아니라 링크에 하나 있다.

**붙인 자리가 둘뿐인 것이 요점이다**:

| 무엇 | 어디 | 왜 거기 하나로 되나 |
|---|---|---|
| 차단 | `canAct()` | 타일·시뮬 버튼이 **전부 이것을 본다** → 한 줄로 전체가 걸린다 |
| 시작점 | `transport.send()` | 하행 세 경로(reserve·cancel·sendCommand)가 **전부 지나간다** → 새 경로가 생겨도 저절로 걸린다 |

⚠ **실제로 나간 경우에만 찍는다.** 전송 실패까지 아이들로 세면 연결이 끊긴 동안 누른 것 때문에
**복구 직후 1초를 못 누르게 된다.**

⚠ **조용히 무시하면 안 된다** — 안 먹혔다고 생각하면 **더 세게 누르고**, 그러면 막으려던 것을
화면이 만든다. 그래서 누르지 않아도 보이는 자리(칸 보조 문구)에 남은 시간을 넣는다.
그리고 아이들 문구를 **다른 차단 사유보다 먼저** 본다 — 이 상태는 **곧 풀린다**는 점에서
성격이 다른데 "연결이 없다"로 말하면 고장으로 읽혀 사용자가 엉뚱한 곳을 본다(§5.10 과 같은 형태).

**실측**(진짜 크롬 · 데모 모드 · `file://` · **트래픽 0**):

```
누르기 전  : "눌러서 예약"        · aria-disabled=false · 잠긴 칸 5
보낸 직후  : "잠시 뒤 가능 (1.0초)" · aria-disabled=true  · 잠긴 칸 10
+0.4초     : "잠시 뒤 가능 (0.7초)"
+0.9초     : "잠시 뒤 가능 (0.2초)"
+1.2초     : "눌러서 예약"        · aria-disabled=false · 잠긴 칸 5   ← 스스로 풀린다
```

~~⚠ **아직 안 본 것**: **서버가 서빙한 페이지에서 진짜 WS 로** 보냈을 때.~~ 위는 데모 경로
(`demo.send`)이고 실제 경로는 `ws.send` 다 — **둘 다 같은 `stamp()` 를 지나므로 게이트와
표시는 같지만, "같은 코드를 지난다"는 판독이지 실측이 아니다.** ~~시험 인스턴스가 생기면 본다.~~

> ⚠ **정정(2026-08-17 19:5x · §5.22)** — **봤다.** `FINDING-link-blink-2026-08-17.md` **§7.2**
> 에서 시험 인스턴스 10500 · `blink.mjs --case D` 로 **실제 WS 경로 실측**: 보내자마자 10칸
> 전부 잠김 · **다른 칸에도** 남은 시간이 뜬다(전역이라는 직접 증거) · **1065ms** 에 스스로 해제.
> 위 판독은 그대로 참이고, 낡은 것은 "아직 안 봤다"는 상태 문장이다.

#### 5.16.1 🔴 **아이들 1.0초 < 슬롯 1.2초는 알고 둔 것이다 — "고치지 마라"**

이론상 사용자가 슬롯이 실어 나르는 것보다 조금 빨리 만들 수 있다. **그런데 문제가 안 된다**:
넘치는 분은 **큐가 흡수하고 `queued` 로 보인다** — 조용히 사라지지 않는다.

정확히 맞추려면 아이들을 슬롯 주기(1.2초)와 같게 하면 되고 **한 줄 변경**이다.
**사용자 확정: 1.0초로 간다**(`docs/DESIGN-slot-carrier-2026-08-17.md` §화면).

→ **다음 사람이 이 불일치를 "버그"로 보고 고치지 않게 하려고 적는다.** 숫자가 안 맞는 것을
발견하면 고치고 싶어지는데, **여기서는 안 맞는 것이 결론이다.**

### 5.19 거절 코드가 표에 없으면 **"오류가 발생했습니다"로 떨어진다** (실측 · 대조군으로 확인)

`ERROR_TEXT` 에 없는 `code` 는 `msg.message` → 없으면 그 일반 문구로 간다(2073-2075행).
대조군 측정: 표에 없는 `zzz_unknown` 주입 → `A3 · 오류가 발생했습니다`.

**왜 이것이 문구 문제로 안 끝나나**: `queue_full`·`already_pending` 은 **둘 다 사용자 잘못이
아닌데** 그 문구는 고장이나 자기 실수로 읽힌다 → **다시 누른다** → §5.14 의 증폭 고리다.
즉 표의 결손이 부하로 바뀐다.

2026-08-17 에 둘을 넣었다(REQ-0159). **코드 문자열은 원본
`docs/net/DESIGN-server-slot-queue.md` §4-B 를 grep 해 확인하고 붙였다** — 이 원장에 옮겨
적힌 이름(§5.15)을 근거로 쓰지 않았다. 스펠링이 다르면 **영원히 안 걸리는 죽은 코드**가 되고
화면은 그대로 일반 문구를 낸다(§5.17 과 같은 형태의 조용한 오답).

측정: `node web/tools/queue-contract.mjs` → **25 pass / 0 fail**.

> ✅ **해소됨 — 커밋 `335215f`**(2026-08-17, 루트 승인 후). 확인: `git grep -c queue_full HEAD
> -- 조별과제샘플/index.html` → **1건**(변경 전 0건). **아래 문단은 지우지 않는다** — 이 함정이
> 실재했다는 기록이고, 커밋 전 상태를 남기는 것이 §5.22 의 요점이다.

🔴 ~~**2026-08-17 19:5x 시점에 이 변경은 아직 커밋되지 않았다 — 워킹 트리에만 있다.**~~
`조별과제샘플/index.html`(ERROR_TEXT) · `web/tools/queue-contract.mjs`(신규) ·
`web/tools/check-index.mjs`([6]) 셋이다. **§5.22 를 나 자신에게 적용한 결과 적어 둔다**:
다음 사람이 그 규칙대로 `git grep queue_full HEAD` 를 돌리면 **0건**이 나오고
"거절 코드 미구현"으로 판정한다 — 내가 이번에 당한 것과 정확히 같은 함정이다.
**커밋 여부를 확인하기 전에 "안 됐다"고 결론내지 마라.** 커밋되면 이 문단을 지우지 말고
해시를 덧붙여라.

⚠ **주입이다.** 확인된 것은 "화면이 그 코드를 받으면 이렇게 말한다"까지고,
**서버가 그 코드를 그 상황에서 보내는 것은 아직 아무도 안 봤다**(socket 의 큐가 미구현).

### 5.20 🔴 `queued` 의 타이머 재설정이 자체 6초를 **실제로 대체한다** — 시계로 확인 (실측)

`expires_ms=4800` 을 주입하고 **6500ms**(자체 `ACK_TIMEOUT` 6000 초과) 기다린 뒤에도
pending 이 `status='queued'` 로 **살아 있었다**. 타일도 `접수됨 · 전송 대기…` 유지.

→ **큐에서 정상 대기 중인 요청을 화면이 되돌려 사용자가 다시 누르는 고리(§5.14)가 닫혔다.**

⚠ **§5.15.1 과 무엇이 다른가**: 그쪽은 "주입으로 확인"이었지만 **시간을 흘려 보지 않았다.**
타이머 재설정은 **시계 없이는 판독일 뿐이다** — `clearTimeout` 을 부르는 줄을 읽는 것과
6초를 넘겨 살아 있는 것을 보는 것은 다른 증거다. 이번에 후자를 얻었다.

### 5.21 🔴 하니스 함정 — **뒤늦게 도착한 비동기 메시지가 "가장 새 메시지" 자리를 차지한다** (실측)

`queue-contract.mjs` 첫 판본이 `#messages .msg` **첫 줄**(=가장 새 메시지)을 읽었다.
데모 전송 계층이 시작 메시지를 **비동기로** 넣는데 그것이 내 주입 **뒤에** 도착해서,
첫 사례가 `"§5.3 예제 스냅샷을 적용했습니다…"` 를 읽었다. **그런데도 `14 pass / 0 fail`.**
`두 문구가 서로 다르다` 까지 통과했다 — **한쪽이 남의 메시지였기 때문이다.**

**§5.18 과 같은 뿌리(아무것도 못 쟀는데 초록)이고, 다른 것은 예외가 아니라 순서다.**

→ **규칙: DOM 에서 결과를 고를 때 순서(첫 줄·마지막 줄)에 기대지 마라.** 내 것임을 증명하는
표식으로 고른다 — 여기서는 `rollback()` 이 붙이는 `"<자리> · "` 접두다(데모 메시지에는 없다).

🔑 **이것을 잡은 것은 대조군이다.** 표에 없는 코드를 같이 재지 않았다면 "문구가 서로 다르다"는
초록만 보고 넘어갔다. **대조군은 대상을 검증하는 동시에 하니스를 검증한다.**

### 5.22 상태를 인용하기 전에 **대상 파일의 커밋 시각**을 봐라 — 문서는 커밋보다 늦게 낡는다

REQ-0159 는 `queued` **미구현**을 근거로 발행됐다(19:35). 실제 구현은 커밋 **`d2ae90a`
(18:57)** 에 이미 들어 있었다 — 문서는 **발행 시점에 이미 낡아 있었다.** 근거로 인용된
*"`index.html` 에 `queued` 문자열 0건"* 은 그 커밋 이전 상태였다
(`git grep -c queued d2ae90a~1 -- 조별과제샘플/index.html` → 0건 · `d2ae90a` → 6건 ·
현재 6건. **셋 다 돌린 출력이다.**)

> ⚠ **근거 정정 이력** — 처음에 `HEAD~1 → 0건` 이라고 적었는데 **그 명령을 돌리지 않았고,
> 돌려 보니 6건**이었다(`d2ae90a` 는 `HEAD~1` 보다 앞선 조상이다). 결론은 안 바뀌었지만
> **근거가 실행되지 않은 명령이었다** — 하필 이 절을 쓰면서 그랬다.
> 🔑 **재발을 막는 형태로 원인을 적는다: `HEAD~n` 은 내 파일의 과거를 가리키지 못한다.**
> `HEAD` 는 다른 도메인의 커밋으로 계속 움직이므로 상대 지시는 **오늘 맞고 내일 틀린다.**
> → **파일의 과거를 가리킬 때는 `git log -- <파일>` 로 얻은 커밋 해시를 직접 쓴다.**

**같은 일이 이 원장에도 있었다**: §5.10(`아직 안 고쳤다`)·§5.16(`실제 WS 미확인`) 이 낡았다 —
`FINDING-link-blink-2026-08-17.md` **§7.1**(고친 문구 3/3)·**§7.2**(아이들 실제 WS 경로)가
18:5x 에 실측했다. ⚠ **나는 그 낡은 문장을 그대로 루트에 보고했다**(기동 보고) — 정정을
`req.sh notice` 로 냈다.

→ **규칙: "아직 안 됐다"를 인용할 때 문서를 근거로 쓰지 마라. 대상 파일의 커밋 시각을 봐라.**
이 원장은 append 가 기본이라 **낡은 문장이 지워지지 않고 남는다** — 그것이 설계이자 함정이다.
고친 사람이 원장의 옛 문장을 갱신하지 않으면, 다음 사람은 **이미 된 일을 다시 하러 온다.**

### 5.23 🔴 §1.2 의 보호는 **사용자가 운영 URL 을 직접 열면 지나간다** (실측 · socket 통보)

실측(2026-08-17, socket 이 `lsof` 로 잡아 통보):

```
pid 995  com.apple.WebKit.Networking   127.0.0.1:63400 -> 127.0.0.1:9900 (ESTABLISHED)
```

**Safari/WebKit 이 운영 서버에 붙어 있었다.** 내 것이 아니다 — 이 세션의 측정은 전부 크롬
(매번 새 임시 프로필 · §4.1)이고 `file://` 데모라 소켓을 아예 안 만든다.

**무엇이 새로 참인가**: §1.2 가 닫은 것은 *"시험 페이지가 화면에 박힌 상수 포트로 **운영** WS 에
붙는 것"* 이고, 그건 `resolveWsPort()` 가 페이지 포트를 따라가게 해서 닫혔다.
**그러나 사람이 브라우저에 운영 주소를 직접 치면 페이지도 WS 도 운영이다** — 규칙대로 동작한
결과이고 **화면의 결함이 아니다.**

→ 즉 **이 경로는 코드로 막을 수 있는 종류가 아니다.** §1.2 의 교훈("화면에 박힌 상수 하나가
포트·flock·로그 분리를 전부 통과한다")과 형태가 비슷해서 같은 결함으로 읽기 쉬운데, **다르다**:
그쪽은 화면이 규칙을 어긴 것이고 이쪽은 사람이 운영을 연 것이다. 고칠 대상이 없다.

⚠ **함정**: 그래서 **운영 로그의 예약이 자동 시험인지 사람이 만든 것인지 화면 쪽 근거로는
못 가른다.** 운영 로그를 읽을 때 **사람이 열어 둔 탭**을 후보로 두지 않으면, 설명 안 되는
세션 하나를 고장으로 쫓게 된다.

### 5.24 화면 타이머 발화 시각은 **`expires_ms + 6000`** 이다 — 시계로 확정 (실측)

```
B6  expires_ms=300  주입 → 실제 발화 **6362ms**   (기대 300+6000=6300)
B4  expires_ms=4800 주입 → 자체 6초를 넘겨 6500ms 뒤에도 여전히 queued
```

`node web/tools/queue-contract.mjs` · 27 pass / 0 fail · `file://` 데모 · 트래픽 0.

🔑 **여기서 파생되는 성질**: `expires_ms = deadline − now` 이고 `deadline = enqueue + cap` 이므로
**발화 시각은 마지막 `queued` 가 언제 왔든 `deadline + 6000` 으로 고정된다.** 서버가 단조 비증가를
공짜로 얻은 그 구조의 부산물이다 — 화면이 `queued` 를 몇 번 받든 절대 시각이 안 움직인다.

#### 5.24.1 ~~🔴 미해결 —~~ **해결됨. 격차는 900ms 가 아니라 3900ms 였다** (socket 확인 · REQ-0166 done)

> ✅ **socket 이 확인했다: 출발점은 이탈 기준이다. 겹침은 실재했다.**
> 그리고 **숫자가 더 나빠졌다** — socket 이 자기 6.9초를 **두 번 잘못 셌다**고 정정했다
> (틱 해상도 `3×200` 과 창 대기 상한이 `D_max`(2400)인 것을 빼먹었다). **최종 9900ms.**
> ```
> ack_budget = 3 × (ACK_TIMEOUT 1500 + 틱 200) + 2 × (D_max 2400) = 9900ms   ← 이탈 기준
> ```
> → 같은 기준으로 세우면 **화면 6000 대 서버 9900 = 3900ms 부족.** `cap`(큐 대기 예산)이
> 3900ms 보다 작으면 **큐를 탄 항목이 거의 다 겹친다** — 내가 "좁다"고 쓴 것은 **틀렸다.**
> **`cap` 값을 모르는 상태에서 폭을 좁다고 단정한 것이 잘못이다**(아래 §5.24.2).
> **해법은 §5.26 으로 구현했다.**

⚠ **아래는 확인 전에 쓴 내 계산이다. 방향은 맞았고 폭은 틀렸다** — 지우지 않는다.

socket 통보(2026-08-17, 서버가 `queued` 를 보내기 시작했다 · `docs/net/LEDGER.md` §8.23):

| socket 이 알려 준 것 (그쪽 실측·설계) | |
|---|---|
| `expires_ms` 가 재는 것은 **큐에서 나갈 때까지**다 | 최종 결말까지가 아니다(넣으면 내 `+6000` 과 **이중 계산**) |
| 재전송도 큐를 타서 **이탈 후 최악이 4.5초 → ≈6.9초** | `ACK_TIMEOUT_MS` 1500 + 창 대기 ≤1200, 3회 |

**①이 이탈 후 예산을 `expires_ms` 에서 뺐으므로 그 몫을 덮는 것은 내 `ACK_TIMEOUT`(6000) 하나다.
그런데 6000 < 6900.** `T_exit ≤ deadline` 이라 하면:

```
겹침 ⟺ T_exit + 6900 > deadline + 6000 ⟺ T_exit > deadline − 900
```

→ **큐가 자기 마감 직전 900ms 안에 빠져나간 항목에서만** 겹친다. 좁지만 실재하고
**하필 혼잡할 때** 걸린다. 🔑 **`cap` 값과 무관하다** — 900 은 `6900 − 6000` 에서만 나온다.

**겹치면**: 화면이 서버보다 900ms 먼저 롤백 → 재클릭 → **새 rid 로 큐에 한 건 더**
(`has_pending_for` 도 안 막는다, 그 pending 은 이미 풀렸다) = **REQ-0155 §5-(2) 증폭 고리 재발.**

⚠ **비교 규칙을 여기서 한 번 밟을 뻔했다**(CLAUDE.md). socket 은 `10.8초`(= `cap` 4800 **큐 대기** +
6000 **이탈 후**)와 `6.9초`(**이탈 후만**)를 비교해 "여유 3.9초"로 봤다. **출발점이 다르다.**
같은 기준(큐 이탈)으로 정렬하면 **6000 대 6900** 이다. **이름도 단위도 같아서 더 위험했다.**

🔴 **`ACK_TIMEOUT` 을 내가 올려서 막지 않았다** — §5.14 가 *"화면의 타임아웃은 서버가 준 상한에
묶는다. 화면이 짐작하지 않는다"* 다. 올리면 비큐 경로의 정상 타임아웃도 같이 늘어난다.

⚠ **`onQueueExpired` 문구는 겹침이 일어나는 바로 그 경우에 거짓이 된다** —
`"상한 시간 안에 장치로 보내지 못해"` 인데 실제로는 **보냈고** 장치가 ack 를 안 준 것이다
(§5.10 과 같은 계열). **REQ-0166 의 결정 뒤에 고친다** — 서버가 항상 먼저 답하게 되면 그 경우가
사라지므로 지금 고치면 검증 없는 변경이다.

### 5.25 서버가 보내는 `message` 보다 **화면의 `ERROR_TEXT[code]` 가 이긴다** (코드 판독 · 2075행)

```js
const text = ERROR_TEXT[code] || (typeof msg.message === 'string' ? msg.message : '오류가…');
```

→ **코드가 표에 있으면 서버 문구는 안 쓰인다.** 실례: `already_pending` 에 서버는
`"그 자리는 이미 처리 중입니다"` 를 보내는데(socket 통보) 화면에는 내 문구가 나간다.
socket 도 이 구조를 확인하고 **"내 message 는 폴백"** 으로 정리했다(REQ-0166).

**의도된 것이고 이유가 있다**: 문구는 화면 소유다 — 서버 문구는 **다음 행동**을 말하지 않는다.
`already_pending` 에 "다시 눌러라"를 안 넣은 것이 그 예다(또 거절될 뿐이고 그게 연타를 부른다).

⚠ **함정**: 그래서 **서버 문구를 고쳐도 화면은 안 바뀐다.** 표에 있는 코드의 문구를 바꾸려면
`ERROR_TEXT` 를 고쳐야 한다. 반대로 **표에 없는 코드는 서버 문구가 그대로 사용자에게 보인다** —
서버가 새 코드를 추가하면 화면은 조용히 서버 문구를 쓰게 된다(깨지지는 않는다 · §5.13 과 같은 성질).

### 5.26 확정된 계약 — 화면이 기다리는 전부는 **`expires_ms + ack_budget_ms`** (실측)

원본은 socket 의 `docs/net/DESIGN-server-slot-queue.md` §4-B. 값이 갈리면 원본이 이긴다.

```json
{"type":"queued","rid":"…","slot":"A1","ahead":2,"expires_ms":4800,"ack_budget_ms":9900}
```

| 구간 | 값 | 무엇을 덮나 |
|---|---|---|
| `expires_ms` | 서버가 준다 | **큐 이탈까지** (남은 지속시간 · 단조 비증가) |
| **`ack_budget_ms`** | 서버가 준다 | **이탈 후 결말까지** (재전송·창 대기 포함) |

**🔴 `ACK_TIMEOUT`(6000)은 이 산식에서 빠졌다.** 그것이 §5.24.1 의 3900ms 구멍이었다.

**실측**(`node web/tools/queue-contract.mjs` → **33 pass / 0 fail** · `file://` · 트래픽 0):

```
B6  expires_ms=300 + ack_budget_ms=500 → 실제 발화 821ms (기대 800)
    🔑 6000 이 아직 섞여 있으면 6800ms 가 된다 — 빠진 것이 이 한 줄로 증명된다
B1  deadlineAt − expiresAt = 9900 (준 값 그대로)
B7  expires_ms 만 오고 ack_budget_ms 가 없으면 → 타이머 안 걸고 "모른다"를 적는다
```

🔴 **한쪽만 온 경우에 `ACK_TIMEOUT` 으로 메우지 않는다.** 이유가 있다: 그 값을 **계산해서**
보내는 서버라면 항상 싣고, **안 싣는 서버는 큐가 없어 `queued` 를 아예 안 보낸다.**
→ `queued` 가 왔는데 이 값이 없으면 **모르는 판본**이라는 뜻이고, 그 자리에서 짐작한 상수를
쓰는 것이 정확히 §5.24.1 로 막은 실수다.

⚠ **`expiresAt` 과 `deadlineAt` 은 다른 값이다.** "모른다" 판정은 **`deadlineAt`** 으로 한다 —
이탈 시각만 알고 결말 상한을 모르면 **여전히 모르는 것**이다. 둘을 헷갈리면 모르는 것을
안다고 말한다(B7 이 그 케이스다).

#### 5.26.1 `onQueueExpired` 문구를 고쳤다 — 서버에는 **"큐에서 못 나가서 취소"라는 결말이 없다**

~~`"서버가 접수했지만 상한 시간 안에 장치로 보내지 못해 …"`~~ → **틀렸다.**
socket 확인: **서버는 큐 마감을 넘겼다고 항목을 버리지 않는다.** 마감은 `queue_full` 판정과
`expires_ms` 계산에만 쓴다(버리면 "버림"이 되고 어차피 다음 창에 나간다).

→ **그런 결말이 서버에 없으므로 화면이 그 원인을 말하면 무조건 거짓이다.** 이 타이머가 터지면
그건 **진짜로 답이 안 온 것**이고, 어느 구간에서 막혔는지는 화면이 모른다.
현재 문구: `"서버가 접수했지만 알려 준 상한 시간 안에 결과가 오지 않아 요청을 되돌렸습니다"`.

🔑 **미룬 판단이 옳았던 사례다.** §5.24.1 을 쓸 때 이 문구를 같이 고치려다 "결정 뒤에"로
미뤘는데, 그 사이에 **고칠 이유 자체가 바뀌었다**(겹침 때문 → 그런 결말이 없기 때문).
먼저 고쳤으면 맞는 문구를 틀린 이유로 적었을 것이다.

### 5.27 🔴 `cap` 은 **한 문서 안에서 두 가지, 팀 전체로 세 가지**다 (문서 판독)

| 어디 | 뜻 | 단위 |
|---|---|---|
| socket §4-B `deadline = enqueue_ms + cap` | 큐 대기 예산 | **ms** |
| socket §8 "값이 채워졌다 — `cap` · 채택 320B" · §1 "`cap` 은 건수가 아니라 바이트다" | 창당 전송 한도 | **B** |
| ~~내 원장 §5.15 "`cap` 은 서버의 **큐 깊이**"~~ | — | **틀렸다** |

**§5.15 의 정의를 정정한다** — 내가 "큐 깊이(언제 `queue_full` 을 돌려주나)"라고 적었는데
그건 셋 중 어느 것도 아니다. `queue_full` 문턱은 별개 값이고, `deadline` 에 들어가는 `cap` 은
시간이다. 원장 규칙 ②대로 지우지 않고 여기 남긴다.

⚠ **왜 위험한가**: `deadline = enqueue + cap` 에 **바이트 값(320)** 을 대입하면 마감이
**320ms** 가 된다 — 그럴싸한 숫자라서 안 걸린다. §5.24.1 의 겹침 폭도 이 값에 종속인데,
나는 그 값을 모르는 채 "좁다"고 썼고 그것이 틀렸다.

→ **규칙: `cap` 이라는 낱말을 단위 없이 인용하지 마라.** 시간·바이트·건수가 전부 `cap` 으로
불린다. CLAUDE.md 의 비교 규칙이 **한 이름 안에서** 걸린 사례다.

> ✅ **socket 이 고쳤다**(2026-08-17): 코드는 처음부터 이름이 갈려 있었다 —
> `DOWNQ_WAIT_CAP_MS`(ms · 4800) 대 `DOWN_BATCH_CAP_B`(B · 320). **섞인 것은 문서였다.**
> 🔑 그리고 그 애매함이 **내 원장에 틀린 항목을 만들었다**(§5.15 의 "큐 깊이"). 남의 문서의
> 모호함은 내 원장으로 번진다 — 옮겨 적을 때 단위를 확인하는 것이 그래서 값이 있다.

### 5.28 🔴 **서버가 `queued` 를 실제로 보낸다 — 브라우저 종단으로 확인** (실측 · 시연 구성)

지금까지 화면 쪽 계약 검증은 **전부 주입**이었다(§5.26). 그 칸이 채워졌다.
`node web/tools/queued-live.mjs --served 10500` → **17 pass / 0 fail** · 독립 3회 실행.
구성: 서버(`build=bf84e00`)가 **페이지도 WS 도** 준다 = 시연 구성 · `fake_arduino` 1.2초 주기.

```
서버가 보낸 프레임 : {"type":"queued","slot":"A2","ahead":0,"expires_ms":4800,"ack_budget_ms":9900}
화면 pending       : {"status":"queued","hasDeadline":true,"gap":9900}
그 뒤              : {"type":"ack","result":0}   → 타일 "예약됨 (입차 전) / 내 예약"
```

**`deadlineAt − expiresAt` = 서버가 보낸 `ack_budget_ms` 그대로다.** 화면이 짐작하지 않고
받아서 쓴다는 것이 실측으로 확인됐다(§5.26 의 산식이 실제 프레임에서도 성립).

~~⚠ **조건 — 이 값들은 flush 시점 (A) 전제다**~~ → **조건이 해제됐다. (A) 로 확정됐다.**
(socket·arduino 확인 · `docs/net/LEDGER.md` §8.23-(17)) **근거가 값이 아니라 구조다**:
장치 코드에 **"수신 창"이라는 상태가 없다** — 송신 창 하나뿐이고 수신은 `loop()` 마다 무조건이다.
🔑 **서버가 `S` 를 받은 순간 장치는 이미 송신을 끝낸 뒤다 — TCP 로 왔다는 것 자체가 증거다.**
→ 아래 간격에 조건을 붙이지 않아도 된다.

#### 5.28.1 🔴 `queued`→`ack` 간격은 **고정 한 슬롯이 아니다 — 위상에 따라 갈린다** (실측 3표본)

```
594ms · 982ms · 1030ms      n=3
(정의: queued 프레임 도착 → ack 프레임 도착 · 브라우저 시계 · fake_arduino 1.2초 주기)
```

socket 은 자기 실왕복에서 **1.206초**를 보고 *"정확히 한 슬롯"* 으로 적었다 — **`n=1` 이었고
그 사실이 안 적혀 있었다.** 내 `n=3` 이 갈리므로 **"다음 창까지 남은 시간"**(0~1200ms)이다.

> ✅ **socket 이 철회했다**(`docs/net/LEDGER.md` §8.23-(22) 취소선 · 커밋 `913b7e8`).
> **정의는 둘이 같았다 — 갈린 것은 해석이다.**
> 🔑 **구조가 강제하는 것은 값이 아니라 상한이다: `≤ 한 슬롯`은 참, `= 한 슬롯`은 거짓.**
> **상한을 값으로 바꿔 말한 것이 오류의 전부다.** 그리고 그 값이 *"구조가 강제한 값"* 으로
> 보고되어 **루트 문서에 "이 설계가 도는 증거"로 옮겨 적혔다** — 틀린 상수가 남의 문서로
> 전파되는 경로가 정확히 이 모양이다.
>
> ⚠ **그래서 표본 수를 반드시 적는다.** socket 이 `n=1` 을 안 적은 것이 이 사고의 입구였다.
> **분포를 말하려면 `n` 이 필요하고, `n` 없는 값은 상수로 읽힌다.**

**왜 원장에 남기나**: 이것을 상수로 굳히면 **화면이 "몇 초 걸린다"를 예측하려는 유혹**이 생기고,
그 예측은 위상에 따라 틀린다. §5.15 가 이미 `ahead` 로 시간을 환산하지 말라고 했는데 **같은
함정의 다른 입구**다. 🔑 **화면은 남은 시간을 짐작하지 않는다 — `expires_ms` 를 받아서 쓴다.**

### 5.29 §5.2 · §5.7 이 **실측으로 승격됐다** — 파일의 `last_frame_ts` 는 진짜로 얼어 있다

`GET /data_log.json` 5초 간격 12표본. ⚠ **표본을 여기 그대로 옮긴다** — 원문은
`web/artifacts/s57-filefreeze.txt` 인데 **`web/artifacts` 는 `.gitignore` 대상이라 추적되지
않는다.** 경로만 적어 두면 다음 사람에게는 **없는 근거**다(원장 규칙 ①).

```
20:27:00 ts=1786965976313 lft=1786965976313      ← 이하 8표본 내내 **한 값**
20:27:05 ts=1786965976313 lft=1786965976313
20:27:10 ts=1786965976313 lft=1786965976313
20:27:15 ts=1786965976313 lft=1786965976313
20:27:20 ts=1786965976313 lft=1786965976313
20:27:25 ts=1786965976313 lft=1786965976313
20:27:31 ts=1786965976313 lft=1786965976313
20:27:36 ts=1786965976313 lft=1786965976313
20:27:41 ts=1786966059584 lft=1786966059584      ← 내가 예약/취소로 **상태를 바꾼 뒤**에만 갱신
20:27:46 ts=1786966059584 lft=1786966059584
20:27:51 ts=1786966059584 lft=1786966059584
20:27:56 ts=1786966059584 lft=1786966059584
```

두 `ts` 의 차 = **83,271ms.** 즉 20:27:36 표본이 읽은 기록은 그 시점에 이미 **약 80초 묵은
것**이었다(첫 표본 이전부터 얼어 있었다 — 40초는 내가 **본** 구간이고 실제 정지는 더 길다).

그 40초 동안 **장치는 살아서 1Hz 로 보내고 있었다**(WS 쪽 `last_frame_ts` 는 24표본에 서로 다른
값 6개 · 같은 시각 e2e 측정에서 WS 대 파일 간격 **96,558ms**).

→ **§5.2(파일은 상태 변화 때만 써진다)와 §5.7(그래서 폴백 화면이 멀쩡한 장치를 죽은 것처럼
그린다)이 추정에서 실측으로 바뀌었다.** 관측된 최대 나이는 약 **80초**이고 **상한이 없다.**

⚠ 그리고 `ts == last_frame_ts` 가 **모든 표본에서 같았다** — 즉 폴백에서 `now − last_frame_ts`
는 사실상 **기록의 나이**다. §5.7 의 표에서 "지금 장치의 침묵"을 모른다고 한 것이 맞다.

**서버는 고칠 것이 없다**(초당 쓰기를 되살리는 셈이라 더 나쁘다 · socket 동의).
**화면 문구 문제로 남는다 — 아직 안 고쳤다.**

### 5.30 🔴 하니스 결함 둘 — **둘 다 "제품 실패"로 위장했다** (실측)

e2e 를 이 인스턴스에 처음 돌렸을 때 34/36 이었고 **두 실패 다 내 하니스였다.** 고친 뒤 **36/36.**

**(1) 칸을 문구로 골랐다.**
```js
cells.find(c => c.state.includes('빈자리')) || cells[0]      // 실제 문구는 '빈 자리' (공백)
```
`find` 가 **한 번도 안 맞았고** `|| cells[0]` 폴백이 그것을 가렸다. A1 이 우연히 빈 날에는
**36/0 으로 통과**해서 깨진 줄 몰랐다. A1 이 "사용 중"인 날 비로소 *"확인 대화상자가 안 뜬다"*
는 거짓 실패가 났다. → **동작(`data-action`)으로 고르고, 못 찾으면 폴백하지 않고 멈춘다.**
🔑 **문구는 바뀌고 동작은 계약이다.** 내가 이 세션에 문구를 여럿 고쳤으니 특히 그렇다.

**(2) `--log` 기본값이 또 딴 데를 봤다 — 유도로도 못 막았다.**
§5.17 에서 "없애기보다 유도하기"라고 정리했는데, **유도는 이름 규칙이 안 바뀔 때만 맞다.**
포트에서 유도한 `~/parking-logs/…test+600.log` 가 맞았을 때조차, 서버가 `net/slotq-test-*.log`
로 쓰기 시작하자 깨졌다. 그리고 없는 파일이 **조용히 빈 배열**이 되어
`❌ 서버가 장치로 R 을 내보냈다 → 없음` 으로 나왔다 — **하니스 결함이 제품 판정으로 위장했다.**

→ 고친 형태 둘:
1. **경로를 항상 찍는다**(`· 서버 로그 = …`). §5.17 의 뿌리가 *"도구가 자기가 어디를 보는지
   말하지 않는다"* 였는데 **정작 그 한 줄이 없었다.**
2. **없으면 "제품 실패"가 아니라 "하니스 실패"라고 말한다.** 여전히 빨강이다 — "검사 못 했다"를
   초록으로 넘기면 분모가 사라진다. 달라진 것은 **누가 틀렸는지 문장이 말한다**는 것이다.

> 📌 **이 두 수정의 커밋을 찾을 때**: `web/tools/e2e.mjs` 변경은 `3f99345`,
> `web/tools/queued-live.mjs` 신규는 `7eb8e91` 에 들어가 있다 — **둘 다 web 이 아닌 다른 작업의
> 커밋이고 메시지도 무관하다**(루트가 `git add` 로 쓸어 담았다고 확인 · REQ-0172).
> **코드는 온전하지만 "왜"가 그 커밋에 없으므로 근거는 이 절이 유일한 기록이다.**
> 그 뒤 CLAUDE.md 에 `git commit -m "…" -- <경로>` 규칙이 신설됐다 —
> `.git/index` 는 저장소당 하나라서 **경로를 명시한 `add` 로는 못 막는다.**

🔑 **§5.21 과 합쳐 이 세션에 하니스 결함이 셋이다**(비동기 메시지 순서 · 문구 선택 · 로그 경로).
**전부 제품이 아니라 도구가 틀렸고, 셋 다 초록이나 엉뚱한 빨강으로 나왔다.**
→ **규칙: 하니스가 낸 빨강을 제품 판정으로 쓰기 전에 "도구가 본 것"을 먼저 확인한다.**
`❌` 하나가 나왔을 때 물을 첫 질문은 *"제품이 틀렸나"* 가 아니라 **"내가 맞는 데를 봤나"** 다.

### 5.31 §5.7 을 고쳤다 — 폴백은 이제 **침묵을 단정하지 않고 상한으로 말한다** (실측 전후)

`renderFrameAge()` 를 **파일 경로에만** 갈랐다(`snap.source === 'file'`).
검사: `node web/tools/fallback-age.mjs` — **고치기 전 5 pass / 6 fail → 고친 뒤 11 pass / 0 fail.**

**고치기 전**(§5.29 에서 실제로 관측한 모양을 주입: 기록 80초 묵음 · `ts == last_frame_ts`):

```
"1분 전 수신 · 기록 시점에 이미 0초 침묵"          ← 장치는 그때 1Hz 로 보내고 있었다
```

**고친 뒤**:

```
"⏱ 침묵 최대 1분 (상한 — 지금 보내고 있는지는 이 경로로 모릅니다) · 기록은 1분 전
 · 기록 시점에 이미 0초 침묵"
```

**무엇을 근거로 이 문구인가**: 폴백에서 화면이 아는 것은 **`침묵 ≤ now − last_frame_ts`** 뿐이고
**아래 경계는 0** 이다(장치가 방금 보냈어도 상태가 안 바뀌면 파일은 안 써진다 · §5.2).
그래서 상한이라고 적고 나머지는 모른다고 적는다 — §5.7 의 표 마지막 줄("지금 장치의 침묵 =
모른다")을 화면이 드디어 그대로 말한다.

**같이 고친 것 둘**:
- `online === false` 를 `"그 뒤로 장치 프레임 없음"`(지금에 대한 단정) → **`"기록 시점엔 오프라인"`**.
  **그 값도 같은 얼어붙은 기록에서 온다** — 하나만 고치면 다른 하나가 같은 거짓말을 계속한다.
- 경보 기호 `⚠` → **`⏱`**. **이 상태는 고장이 아니라 모르는 것**이고, 경보를 붙이면
  멀쩡한 장치에 경보가 뜬다 — 그게 고치려는 결함 자체다.

🔴 **WS 주 경로는 일부러 안 건드렸다.** 거기서는 값이 프레임마다 새로 만들어져 실시간이라
`"N 전 수신"` 이 **참**이고, 상한 문구로 바꾸면 **정확한 정보를 잃는다.**
`fallback-age.mjs` [3] 이 그 대조군이다 — WS 에 `상한` 이 **안** 붙는 것을 단언한다.
**"고치는 것"과 "번지게 하는 것"을 가르는 검사가 없으면 정확한 경로까지 흐려진다.**

⚠ **반대 방향(REQ-0067)을 되살리지 않았는지 확인했다.** REQ-0067 은 *"죽은 데이터를 살아있는
것처럼 보이지 않기"* 인데, 새 문구는 **살아 있다고 말하지 않는다 — 모른다고 말한다.**
그리고 화면이 오래된 값을 쓰고 있다는 신호는 **다른 축**이 담당한다(`silenceMs()` 는
`receivedAt` 기준 = 화면이 마지막으로 무언가를 받은 뒤 경과 · 칸마다 `⏱ N 전 값` 칩 + 흐린 격자).
🔑 **두 축이 다른 것을 잰다**: 하나는 *장치의 침묵*, 하나는 *화면 자료의 나이*.
이번 수정은 앞의 것만 건드렸다 — **이름이 비슷해서 섞기 쉬운 자리다.**

### 5.32 설계 개정 — **루트가 확정한 계약 항목** (2026-08-17 · 구현 전)

조사서는 `docs/web/ANALYSIS-redesign-impact-2026-08-17.md` 다. 그쪽은 **web 의 요구**이고,
아래는 그중 **루트가 확정으로 올린 것**만이다. **둘을 갈라 적는다 — 요구와 확정은 다르다.**

> ⚠ **상태 표기를 정확히 한다**: 아래는 **루트 확정**이고 **socket 의 구현 합의는 계획 REQ 에서
> 받는다.** 서버 쪽 형태(키 이름·전선 형식)는 **아직 아무것도 정해지지 않았다.**
> 값이 갈리면 그때 나온 원본이 이긴다(§5.15·§5.26 과 같은 규율).
> 🔑 **이 절이 필요한 이유**: 이 확정들은 **대화로만 왔다.** 원장에 옮기지 않으면
> 세션이 죽는 순간 없어진다 — 오늘 그 형태로 문맥을 잃은 일이 이미 있었다.

| # | 확정된 것 | 왜 (내가 낸 근거) |
|---|---|---|
| ① | **서버가 "맵 정의"를 상태와 별개로 준다** — 격자 · 칸 목록 · 칸마다 `kind` · 칸마다 모듈 목록 | **"없는 ID"의 뜻이 뒤집힌다.** 지금은 *데이터 없음*(점선)인데 자리가 유동이면 *존재하지 않음*이다. **화면에서 반대로 말해야 하는 두 가지를 지금 코드는 하나로만 안다** |
| ② | **조작 가능성을 모듈 단위로 판정한다** (그 모듈이 속한 장치가 사는가) | 단일 `device.online` 이면 **거짓 허용**이 생기고, 그 하행은 **닿지 않는 장치로 나가 큐·재전송까지 돈다** |
| ③ | **`known` 을 자리 단위 → 모듈 단위로 내린다** | 장치가 완전 독립이라 **"일부만 아는 상태"가 상시 정상**이다. 지금은 자리 하나 = 상태 하나 |
| ④ | **`kind: parking\|entrance\|exit`** | 사용자의 *"이름만 다르다"* 는 **구조에는 참, 화면 상태 어휘에는 거짓**이다 — 입구에 "예약됨"이 없다 |
| ⑤ | **맵 정의는 기존 `slots[]` 를 고치지 말고 별도 키로 추가한다** | §5.13 — 새 타입/새 키는 옛 화면이 조용히 무시해 안전하지만, **기존 항목의 뜻을 바꾸면 서버 배포가 그 순간 옛 화면을 깨뜨린다** |

**④의 정확한 표현**(루트 정정): 사용자 지시를 뒤집는 것이 아니다 —
**"동일한 것은 구조이고 다른 것은 상태 어휘"** 로 갈린다.

#### 5.32.1 아직 안 정해진 것 — **여기를 확정으로 읽지 마라**

- **자리 ID 와 격자 좌표를 가르는가** — 내 의견은 "가른다"(좌표를 ID 로 쓰면 자리를 옮길 때
  예약이 따라 움직인다). **미확정.**
- **모듈을 화면에 얼마나 보여 주나** — 칸 안에 다 · 눌러 펼치기 · 집계만. **사용자에게 물을 것**이고
  루트가 **다른 질문과 묶어서** 낸다(질문을 겹쳐 보내면 답이 섞인다).
- **미상 표시의 형태** — `⏱`(조용) 대 `⚠`(경보)를 갈라야 하고 **미상 모듈 집계 한 줄**이 후보.
  ⚠ **두 힘이 서로 민다**: 상시 상태라 조용해야 하지만 **너무 조용하면 REQ-0067**(모르는 것을
  아는 것처럼)로 돌아간다. **"조용하되 반드시 보여야 한다"** 가 요구다. **형태 미정.**
- **모듈 상태의 나이** — 장치가 N 대면 `last_frame_ts` 도 N 개다.
  **§5.31 의 상한 문구는 장치 하나를 전제한다** → 모듈 단위로 다시 풀어야 할 수 있다. **미정.**

#### 5.32.2 개정에도 **안 깨지는 것** — 다시 만들지 마라

루트가 계획서에 넣기로 한 목록이다(§4 조사서에 근거).

| 그대로 가는 것 | 왜 |
|---|---|
| WS/폴백 이중 경로 · `resolveWsPort` | 자리 구조와 무관 (§1.1·§1.2) |
| `queued` · `expires_ms` · `ack_budget_ms` 타이머 계약 | **rid 단위**다. 자리 수와 무관 (§5.26·§5.28) |
| **하행 아이들 1초** (`canAct()` · `transport.send()` 두 지점) | **전선 하나**에 걸린 것이라 자리가 늘어도 그대로 (§5.16) |
| 폴백 신선도 상한 문구 (§5.31) | 장치 축이다 — 단 위 "모듈 상태의 나이"가 열려 있다 |

🔑 **이 목록의 값은 "안 바뀐다"가 아니라 "왜 안 바뀌는지"다.** 아이들 1초가 살아남는 이유는
그것이 **자리가 아니라 전선**에 걸려 있기 때문이고, 그 근거가 없으면 다음 사람이 다시 본다.

### 5.33 표시 원칙 — **기호는 원인을, 강조는 영향을 나타낸다** (두 축을 섞지 마라)

REQ-0181 에서 나왔지만 **그 개정에 딸린 것이 아니라 이 화면의 표시 원칙**이라 원장에 둔다.
(개정 문맥과 상세는 `docs/web/DESIGN-module-state-2026-08-18.md` §2. **여기 복사하지 않는다.**)

| 축 | 무엇 | 값 |
|---|---|---|
| **기호축** | **원인** | `⏱` 모른다 · `⚠` 고장·차단 확정 · (없음) 정상 |
| **강조축** | **영향** | **조작을 막는가** — 막으면 눈에 띄게, 안 막으면 조용한 배경 |

**왜 이것이 원칙인가**: 두 요구가 서로 미는 자리가 반복해서 나온다 —
**상시 켜진 경고는 아무도 안 읽는다**(§3.1 이 배너를 정상에서 침묵시킨 이유) 대
**모르는 것을 아는 것처럼 보이면 안 된다**(REQ-0067). **한 축으로 풀려고 하면 둘 중 하나를
버리게 되고, 그때 나오는 답이 "적당히 조용하게" 같은 조정값이다.**

🔑 **두 축으로 갈라 두면 조정값이 필요 없다** — 강조축은 **`actionable`/`blocked_reason` 에서
파생으로 나오므로**(§5.32 ② · 서버가 계산) 화면이 아무것도 짐작하지 않고, **집계가 정상에서
저절로 침묵한다.** 미학 판단이 정의에서 나오는 결과로 내려온다.

**적용 이력**: §5.31(폴백 문구)에서 `⚠`→`⏱` 로 바꾼 것이 이 원칙의 첫 사례다 — 그때는
"고장이 아니라 모르는 것"이라는 **기호축만** 썼고, 강조축은 폴백이 드물어서 필요가 없었다.
**미상이 상시가 되는 구성에서 두 번째 축이 필요해졌다.**

⚠ **한계**: 지금은 **설계 원칙이고 실측이 아니다.** 사람이 이 구분을 실제로 읽어 내는지는
확인하지 않았다 — 시각 형태(점선·흐림·칩)는 표시량 확인 뒤에 정해지고, **그때 브라우저로 봐야
한다.** 원칙이 옳아도 형태가 그것을 전달하지 못할 수 있다.

### 5.34 🔴 **화면의 정상은 서버의 정상이다. 장치의 정상이 아니다** (소스 판독 · 확정)

화면의 입력은 **WS 스냅샷**과 **`data_log.json`** 둘뿐이고(§2.3 · 여전히 참) **둘 다 서버가
만든다.** → **화면은 장치가 겪은 것을 볼 통로가 없다.** 이것은 추정이 아니다:

- `arduino/.slotbuild/client/client.ino` 의 계수 출력이 **전부 `Serial.print`**(USB)다.
  `cksumng` 줄에 **장치 스스로 적어 뒀다**: *"체크섬 불일치(하행 프레임 파괴).
  **서버 계수로는 안 보이는 손실이다.**"*
- 전선의 S 프레임에는 그 계수가 **없다**(`docs/net/parking-protocol.md`):
  `S,<seq>,<occupied>,<reserved>,<uptime>,<devid>[,<override>],<cksum>`

**실증 사례**(루트 전달 · 창 B): 같은 구간을 **서버는 실패 0**, **장치는 `cksumng=5` ·
`ssovf=5` · `oow=304`** 로 봤다. **둘 다 맞다 — 다른 것을 세기 때문이다.**

🔴 **그래서 이 문장을 조심해야 한다: "화면이 정상이었으니 그 구간은 정상이었다."** 틀렸다.
화면이 본 것은 **서버가 아는 범위의 정상**이다. 오독을 막을 유일한 방법이 이 항목이다.

#### 5.34.1 센서에는 자가 치유가 있고 **액추에이터에는 없다** — 구조적 차이

| | 에코 | 결과 |
|---|---|---|
| 센서(`occupied`) · `reserved` | **매 슬롯 올라온다** | 틀리면 다음 프레임에 드러나고 **서버가 `C` 재하달로 치유**(실측 ≈1초 · `FINDING-link-blink-2026-08-17.md` §8) |
| **액추에이터**(안내등·유도등·차단봉) | 🔴 **없다**(프레임에 필드가 없고 REQ-0179 완료 기준에도 에코 항목이 없다) | **아무도 되짚지 않는다.** 하행 프레임은 실제로 파괴되는데(`cksumng=5`) 그것을 드러낼 값이 없다 |

**자가 치유가 액추에이터에 안 걸리는 이유도 구조적이다**: 그 기전은 *"장치의 값과 서버의 값이
다르다"* 를 비교하는 것인데 **비교할 값이 안 올라오면 비교가 없다.**

→ **화면 규율**: 에코가 없는 모듈은 **"켜짐"으로 그리지 않는다. "켜라고 보냈음"까지만** 말한다.
§5.31 의 규율(모르는 것을 단정하지 않는다)과 같고, **이름을 붙이면 "거짓 완료"** 다 —
루트가 채택한 **"거짓 허용"의 사촌**이다(그쪽은 못 갈 명령을 보내는 것, 이쪽은 안 된 것을
됐다고 말하는 것). **시연에서 안내등이 안 켜졌는데 화면은 켜졌다고 하는 모양이다.**

~~⚠ **계약 판정 대기**~~ → ✅ **닫혔다: (가) 전체 에코 채택**(REQ-0184 done · socket 명세 §5).
**출력 모듈도 상태 비트열에 실린다** → **위 표의 액추에이터 행이 센서 행과 같아진다**
(비교할 값이 올라오므로 자가 치유가 액추에이터로 확장된다).

🔑 **arduino 가 독립으로 같은 구멍을 먼저 짚었다** — 나는 *"화면이 거짓을 그릴 수 있다"*,
그쪽은 *"서버가 자기 명령의 결과를 못 본다"* 였고 **같은 사실의 두 얼굴이다.**
**두 도메인이 다른 언어로 같은 결함에 도달한 것 자체가 그 결함이 실재한다는 증거다.**

🔴 **그러나 내 `≈1초` 를 액추에이터에 인용하지 마라** (루트·socket 이 좁혔다 · 2026-08-18).
**기전이 확장되는 것과 시간이 같은 것은 다르다.** 내가 실측한 ≈1초는 **센서 방향**이고,
재하달 대상이 다르다(**등 하나** 대 **예약 마스크**) → **지연도 다를 수 있다.**
**액추에이터 방향의 치유 시간은 아직 아무도 안 쟀다** — 에코가 들어간 뒤 재는 것이 첫 검증 항목이다.

⚠ **이것이 방금 내가 배운 함정의 재발 방지다**: FINDING §8.3 은 **기전을 정확히 적어 놓고
결론에 범위를 안 써서** 일반 사실로 읽혔다. 여기서 **같은 실수를 반대 방향으로 할 수 있었다** —
"기전이 확장된다"를 "숫자도 그대로 간다"로 옮기는 것. **확장되는 것은 기전뿐이다.**

#### 5.34.2 🔴 에코가 생겨도 **"켜졌습니다"는 과한 주장이다** — 층이 하나 더 있다

socket 이 좁혀 준 한계(명세 §5). **에코 채택으로 §5.34.1 이 닫혔다고 읽으면 안 된다:**

```
에코가 말하는 것     : 장치가 그 핀을 그렇게 설정했다
에코가 말하지 않는 것: 전구가 실제로 켜졌다 · 차단봉이 물리적으로 올라갔다
```

**전류 감지가 없으면 끊긴 배선·나간 전구는 에코에 안 나타난다.** 전류 감지는 하드웨어 결정이고
**이 계약 밖**이다 — 그래서 **그 한계가 계약에 남는다.**

→ **화면 규율**: 액추에이터는 **`"켜졌습니다"` 로 단정하지 않는다. `"장치가 켰다고 보고합니다"`** 다.

🔑 **왜 이 한 칸이 중요한가**: 시연에서 **전구가 안 켜졌는데 화면이 "켜졌습니다"** 를 띄우면
보는 사람은 **화면을 의심하지 않고 자기 눈을 의심하거나 엉뚱한 곳을 본다.**
`"장치가 켰다고 보고합니다"` 는 **다음에 볼 곳(배선·전구)을 가리킨다.**

⚠ **이것이 §5.31 과 같은 계열이고 세 번째 사례다.** 층마다 화면이 아는 범위가 다르다:

| 화면이 말할 수 있는 최대치 | 근거 |
|---|---|
| "보냈다" | 내 프레임 |
| "서버가 접수했다" | `queued` |
| "서버가 처리했다고 답했다" | `ack` |
| **"장치가 그렇게 보고한다"** | **에코**(REQ-0184 로 새로 생겼다) |
| ~~"물리적으로 그렇게 됐다"~~ | 🔴 **전류 감지 없이는 아무도 모른다** |

**규칙: 화면 문구는 이 표에서 자기가 가진 근거의 칸을 넘지 않는다.**

### 5.35 🔴 `ack_timeout` 이 **드물게 뜨는 것은 알려진 상태다 — 새 고장으로 쫓지 마라**

socket 창 B 실측(2026-08-18 · REQ-0183 회신): **장치의 주기 디버그 출력(`[CNT]`)이 60초마다
하행 한 건을 깨뜨린다.** 서버가 전송 창을 지켜도 **장치 내부에서 일어나므로 막을 수 없다.**
재전송이 대부분 살려낸다(같은 창에서 **ACK 실패 0** · 300/300 완주 ×2회).

→ **화면에 `error/ack_timeout` 이 아주 드물게(≈60초에 1건 이하) 뜰 수 있다.**

**왜 원장에 넣나**: 이것은 **회귀가 아니다.** 안 적어 두면 다음 사람이 드물게 뜨는 그 한 건을
새 고장으로 쫓는다 — 오늘 이 팀이 그 형태로 여러 번 시간을 썼다(없는 고장 추적).

⚠ **문구는 고칠 것이 없다.** `ERROR_TEXT.ack_timeout` = `"장치가 응답하지 않았습니다"` 이고
**실제로 장치가 ack 를 못 준 것**이므로 거짓이 아니다. §5.31 과 달리 여기서는 화면이 단정해도
맞는다 — **서버가 재전송을 소진한 뒤 준 사유**이기 때문이다(경우 C 실측 · `FINDING` §8.1 C2).

🔑 **구분해 둘 것**: 이 `ack_timeout` 은 **서버가 준 사유**이고, 화면의 **자체 타임아웃**은
다른 사건이다(`onSelfTimeout` — 서버가 아무 말도 안 했을 때). 로그·보고에서 섞으면 원인을 못 가른다.

### 5.36 확정된 계약 — `actions` 는 **조작별**이다 (REQ-0183 · socket 명세 §6.5)

**원본은 socket 의 `docs/net/DESIGN-bridge-node-module-zone.md` §6.5.** 값이 갈리면 원본이 이긴다.
여기에는 **화면 쪽이 지켜야 할 것만** 적는다.

```
zone 마다:  actions: { reserve: {ok, reason}, open_gate: {ok, reason}, … }
확정 집합:  reserve · cancel · open_gate · close_gate      ← **넷이다**
```

> ⚠ **초안은 여섯이었다**(`light_on`·`light_off` 포함). ~~그 둘~~은 **빠졌다**
> (socket 명세 §6.5 · 커밋 `9b61c1b`). **이유를 같이 적는다 — 결론만 고치면 다음 사람이
> "왜 없지" 하고 되살린다:**
> 1. **안내등은 예약 상태에서 서버가 파생시키는 출력**이다. 사용자 조작으로 열어 두면
>    **화면이 서버의 자동 제어와 싸운다** — 눌러 켠 등이 다음 예약 변화에 꺼지고, 그 원인이
>    화면에도 로그에도 *"누가 이겼나"* 로만 남는다.
> 2. 🔑 **계약에 이름을 넣는 것 자체가 구현을 부른다.** `queued ✅` 오기가 *"합의됐으니
>    구현됐겠지"* 로 읽힌 것과 **같은 형태**다(§5.15 이력). **안 쓰는 이름을 닫힌 집합에 두지 않는다.**
>
> **뒤집힐 수 있는 자리다**: 사용자가 *"안내등을 손으로 켜고 싶다"* 고 하면 `light_on` 을 넣되
> **"수동이 자동을 얼마나 오래 이기는가"를 같이 정해야 한다** — 그 값 없이 넣으면 위 싸움이 난다.

**`kind` 별 유효 조작**(socket 명세 §6.5 · 원본이 이긴다):

| `kind` | 사용자 조작 = `actions` 에 나온다 | 서버가 **파생**시키는 것(조작 아니다) |
|---|---|---|
| `parking` | `reserve` · `cancel` | **안내등** — 예약 상태에서 서버가 켠다 |
| `entrance` · `exit` | `open_gate` · `close_gate` | 유도등 · 번호판 인식 자동 개방 |

**왜 단일 `actionable: bool` 이 아닌가**: 한 자리가 조작을 둘 이상 가지면(입출구는 차단봉과
안내등이 **서로 다른 모듈**에 걸린다) **어느 것이 막혔는지 `bool` 하나로 안 갈린다.**
🔴 **그리고 그것은 `cap` 사고와 같은 형태다** — 한 이름이 조작마다 다른 것을 가리키면 나중에
*"이 자리는 `actionable` 인데 왜 안 눌리지"* 가 나온다(§5.27).

⚠ **내가 이 어긋남을 스스로 못 봤다.** 내 설계서 §3.3 에 *"입출구는 예약이 아니다 · 조작이
있다면 다른 확인 문구가 필요하다"* 를 적어 두고 **자료 구조는 단일 `bool` 로 요구했다.**
socket 이 잡았다. → **결론을 적어 두고 그 결론을 자기 것에 적용하지 않는 형태**이고,
오늘 세 도메인이 각자 한 번씩 밟았다(monitor 는 `oow` 과대계상, socket 은 `n=1` 과 재전송 방향).

**화면 쪽 규칙 둘**:

1. **조작 이름은 닫힌 집합이다.** 화면은 **모르는 조작을 그릴 수 없다** — 버튼 문구·확인
   대화상자가 이름에 딸린다. `kind` 별 유효 조작도 명세가 정한다(`parking` 에 `open_gate` 없음).
2. 🔑 **모르는 조작 키는 조용히 무시한다** — `handleServerMessage` 에 캐치올이 없어 **새 타입이
   안 깨지는 것과 같은 성질**(§5.13)을 `actions` 안에서도 갖는다.
   → **새 조작을 추가하는 배포가 옛 화면을 깨뜨리지 않는다.** 대가로 **서버는 기존 키의 뜻을
   바꾸지 않고 추가만 한다**(socket 이 받았다). **뜻을 바꾸는 것이 §5.13 이 경고한 방향이다.**

#### 5.36.1 `blocked_reason` 계열은 **셋**이다 — `busy` 를 뺀 이유

| 계열 | 코드(초안) | 화면 |
|---|---|---|
| `unknown` | `node_offline` · `node_unregistered` | **`⏱`** |
| `fault` | `module_absent` | **`⚠`** |
| **`busy`** | `pending` · `queue_full` | **곧 풀린다 — 남은 시간/재시도 안내** |

**`busy` 를 둘 중 어디에 넣어도 틀린다**: `⏱`(모른다)는 **거짓**이고(우리는 안다),
`⚠`(고장)은 **정상 동작 중의 일시 상태를 고장으로** 그린다.

🔑 **선례가 이미 화면에 있었다** — 하행 아이들 1초(§5.16). 그때 적어 둔 이유가 그대로다:
*"이 상태는 곧 풀린다는 점에서 성격이 다른데 '연결이 없다'로 말하면 고장으로 읽혀 사용자가
엉뚱한 곳을 본다."* **`busy` 는 그 표시 언어를 쓴다** — 같은 문제를 두 번 풀지 않는다.

🔴 **`busy` 는 미상 집계에 넣지 않는다**(§5.33 의 집계 규칙). 세면 **정상 운영이 경고를 만든다.**

⚠ **`node_offline` 이 `fault` 가 아니라 `unknown` 인 것에 주의**: 서버가 아는 것은
**`OFFLINE_MS=3500` 무프레임**이지 *"장치가 죽었다"* 가 아니다(§5.8 이 같은 것을 적어 뒀다 ·
기준선에서 새 연결 36건 중 35건이 링크 재접속이었다). **이름이 결론을 담지 않게 한 것이다.**

#### 5.36.2 🔴 **`actions` 가 빈 것과 모듈이 없는 것은 다른 상태다** — "없음/빈 것"의 세 번째 판본

내 요구 둘이 겉으로 충돌해 보였고 socket 이 구분으로 풀었다(명세 §6.5):

```
"모르는 조작은 그릴 수 없다"   ↔   "조작이 없는 모듈도 보여 줘야 한다"
```

→ **조작이 없는 모듈은 모듈 목록에 나오고 표시만 된다.** 유도등·안내등이 그렇다 —
**보이지만 누를 것이 없다.** 서버가 파생시키는 출력이라 사용자 조작이 없을 뿐, **모듈은 있다.**

🔑 **이것이 이 도메인에서 "없음"을 잘못 읽는 세 번째 자리다.** 같은 함정이 층마다 나온다:

| 어디 | 헷갈리는 둘 |
|---|---|
| §2.4 · 자리 | `slots` 에 없는 ID = **데이터 없음** 대 **그 자리가 존재하지 않음** |
| 설계서 §3.2 · 격자 | **빈 칸**(zone 이 없다) 대 **미상 자리**(zone 은 있고 상태를 모른다) |
| **여기 · 모듈** | **`actions` 가 빈 모듈**(있고 조작만 없다) 대 **모듈 자체가 없음** |

→ **규칙: 이 화면에서 "비어 있다"를 보면 항상 "없는 것인가 모르는 것인가 조작만 없는 것인가"를
먼저 갈라라.** 셋을 같은 그림으로 그리면 **존재하는 것을 없다고 말하거나 없는 것을 고장으로 만든다.**

### 5.37 시각 형태는 **표로 적으면 안 보이고 그려 보면 보인다** (실측 — 결함 둘이 그림에서 나왔다)

REQ-0198 에서 칸 레이아웃을 정할 때, 규칙을 표로 정리한 뒤 **최악 칸 하나를 ASCII 로 그렸다.**
그 순간 **표에는 없던 결함 둘이 드러났다:**

1. **`⏱` 가 한 칸에 세 개**(칸 칩 1 + 모듈 2)가 되고 **강조축이 유일한 구분자**가 된다.
   표에는 *"기호는 겹쳐 쓰고 위치로 가른다"* 라고만 적혀 있어서 **밀도가 안 보였다.**
2. **한 칸이 7줄이면 자리 7개에 49줄**이고, 줄이면 **이유 문구가 먼저 밀린다** —
   그런데 **그 문구가 `aria-disabled` 의 존재 이유**다(§3.2). **밀어낼 수 없는 것을 밀어내려던 것.**

🔑 **왜 원장에 남기나**: 이 도메인에는 *"규칙이 옳은지"* 와 *"형태가 그것을 전달하는지"* 가 **다른
질문**이고, 앞은 논증으로 되지만 **뒤는 그려 봐야 안다.** §5.33 의 한계 서술(*"원칙이 옳아도 형태가
전달하지 못할 수 있다"*)을 **문서 안에서 스스로 시험하는 방법**이 이것이다.

→ **규칙: 시각 형태를 명세로 정할 때 최악 사례 한 칸을 반드시 그린다.** 정상 사례는 늘 예뻐서
아무것도 드러내지 않는다. **그리고 그림에서 나온 의심은 실측 목록으로 옮긴다** — 그림도 사람이
읽는지는 증명하지 않는다(§5.33 한계 그대로).

⚠ **한계**: ASCII 는 **밀도와 순서만** 드러낸다. **대비·색·글자 크기는 안 드러난다** —
`⏱` 셋이 실제로 구분되는지는 여전히 브라우저다. **그림은 실측을 대신하지 않고 실측 대상을 만든다.**

### 5.38 `ACK_TIMEOUT` 전수 조사 — **서버 예산이 바뀌어도 화면은 안 고친다** (REQ-0200 · 소스 전수)

socket 이 서버 `ACK_TIMEOUT_MS` 를 고치면 `ack_budget_ms` 가 그 값에서 계산돼 바뀐다.
**화면 쪽 대응이 필요한지 전수로 확인했다**(`grep ACK_TIMEOUT|ack_budget_ms|deadlineAt`).

**화면 상수 `ACK_TIMEOUT = 6000` 이 살아 있는 곳은 셋뿐이고 전부 서버 예산과 무관하다:**

| 어디 | 무엇을 재나 | 서버 예산에 걸리나 |
|---|---|---|
| `markPending` 최초 타이머 | **서버가 아무 말도 안 할 때** | ❌ `queued` 는 서버-로컬이라 ms 단위다 |
| `onSelfTimeout` 문구 | 위 타이머의 사유 문장 | ❌ |
| `cmdPending`(테스트·시뮬 명령) | 그 명령의 응답 대기 | 🔴 **걸린다 — 아래 5.38.1** |

**예약 경로의 타이머는 `exp + budget` 이고 둘 다 서버가 준 값이다**(§5.26 · 프레임마다 새로 읽는다).
→ ✅ **판정: 화면 코드 변경 불필요.**

#### 5.38.1 🔴 **"같은 배포" 제약은 화면에는 없다** — 옛 예산으로 도는 구간이 안 생긴다

루트가 *"서버만 올라가면 화면이 옛 예산으로 돈다"* 를 제약으로 걸었는데 **화면은 예산을 캐시하지
않는다.** `const budget = Number(msg.ack_budget_ms)` 를 **`queued` 프레임마다** 읽으므로
**배포 직후 첫 `queued` 부터 새 값이다.**

⚠ **남는 창은 하나뿐이다**: **배포 순간에 이미 날아가 있던 요청**은 타이머가 옛 예산으로 걸려 있다.
**요청 한 건의 수명만큼이고 지속되지 않는다.** → **같은 배포로 묶을 이유가 없다**(묶어도 해는 없다).

🔑 **`ack_budget_ms` 를 서버가 계산해 내려보내게 한 설계(REQ-0166)의 배당금이다** —
그때 socket 이 *"리터럴이 아니라 상수에서 계산한다"* 로 닫아 둔 덕에 **서버 상수가 바뀌어도
화면이 따라온다.** 그것이 바로 이 배포에서 화면 변경을 0으로 만든 이유다.

#### 5.38.2 🔴 인접한 구멍 — **테스트·시뮬 명령 경로에는 REQ-0166 구멍이 남아 있다** (소스 판독)

`handleServerMessage` 의 `queued` 분기가 **`state.pending` 만 본다**(`state.pending.get(msg.rid)`,
없으면 `return`). **`state.cmdPending` 은 안 본다.**

그런데 socket 명세는 **`M`·`T` 도 같은 큐를 탄다**고 정했다. 즉:

```
테스트·시뮬 명령 → 서버 큐에 담긴다 → queued 가 온다 → 화면은 그것을 조용히 버린다
                → cmdPending 타이머는 6초 고정 → 큐 대기가 6초를 넘으면 거짓 실패 문구
```

**REQ-0166 에서 예약 경로에 대해 닫은 그 구멍이 이 경로에는 그대로 있다.**
⚠ **아직 실측하지 않았다** — 소스 판독이다. 그리고 **피해가 예약보다 작다**(시연·진단 기능이고
화면 상태를 안 바꾼다 — `"화면 상태는 바꾸지 않았습니다"` 문구가 붙어 있다).

🔴 **이번 배포에 끼워 넣지 않았다.** 이유가 팀 규율이다 — **한 교체에 변경 둘을 넣지 않는다**
(socket 이 `ACK_TIMEOUT_MS` 를 REQ-0166 때 일부러 안 건드린 것과 같은 이유).
**별건으로 처리한다.**

#### 5.38.3 `ack_budget_ms` **9900 → 12600ms** (socket REQ-0199 · 커밋 `8b3fce4`)

```
ACK_TIMEOUT_MS : 1500 → 2400  (= 2 × DOWN_SLOT_MS · 리터럴이 아니라 유도식)
ack_budget_ms  = 3×(2400+200) + 2×2400 = 12600ms      ← 서버가 계산해 보낸다
```

**왜 커졌나**(socket): `sendAck` 이 슬롯 창을 지키게 되면서 **정상 왕복이 135ms → 1230ms** 가 됐다.
**옛 문턱 1500 은 여유가 271ms 뿐이라 늦은 ACK 이 상시 재전송을 만들고 있었다.**
새 값은 **한 슬롯(기다림) + 한 슬롯(한 번 놓쳐도 봐줌)** 이다.

⚠ **대가**: 최악 실패 통보가 그만큼 늦다 → **화면이 `partial`/`pending` 을 더 오래 보여 준다.**
**거짓 롤백보다 늦은 확정이 낫다**는 판단 그대로다(§7.4.3: 오래 남으면 서버가 `blocked_reason` 을
보내 계열이 바뀌므로 **화면이 짐작할 일이 없다**).

✅ **화면 코드 변경 0** — 산식(`expires_ms + ack_budget_ms`)은 그대로고 **값만 커졌다.**
⚠ **위 §5.24.1·§5.26·§5.28 의 `9900` 은 그 시점 값이다.** 지우지 않는다 — 그때의 판정
근거였고 **격차 계산(6000 대 9900)의 역사가 사라지면 왜 `ack_budget_ms` 가 생겼는지 못 읽는다.**

🔴 **하니스 맹점 — 이것이 이 항목에서 가장 중요하다**

`queue-contract.mjs` 의 `SRV_BUDGET` 을 12600 으로 고쳤다. **그런데 그것이 깨져서 고친 것이 아니다:**

```
그 하니스는 값을 **주입하고 같은 값으로 단언**한다
→ 서버가 보내는 값이 바뀌어도 **절대 빨간불이 되지 않는다**
```

**즉 서버 값 변화를 잡을 수 있는 검사가 팀에 없었다.** `queued-live.mjs` 만 실제 프레임을 읽는데
거기서도 **특정 값을 단언하지 않는다**(박으면 서버가 바뀔 때마다 하니스가 깨진다 — §5.3 의
"고친 쪽이 벌을 받는 검사").

→ **그래서 "지금 서버가 보내는 값"의 단일 기록은 이 원장이고, 하니스 상수는 그 사본이다.**
**사본이 낡는 것을 막는 것은 검사가 아니라 이 문장이다.**

⚠ **이 방법의 남은 약점을 정직하게 적는다: "언젠가는 낡는다"가 남는다.**
문장은 사람이 읽어야 작동하고, 검사와 달리 **안 읽으면 아무 일도 안 일어난다.**
**지금은 이 정도로 둔다** — 값을 박는 대안이 **고친 쪽을 벌하는 검사**(§5.3)가 되기 때문이다.

🔴 **재검토 조건을 미리 정한다**(루트 지시 · 조건 없이 "열어 둔다"는 잊힌다):

```
아래 중 하나가 실제로 일어나면 검사 형태를 다시 본다
① 사본(하니스 상수)이 낡은 채로 돌아 **판정이 한 번이라도 틀렸을 때**
② 서버가 이 값을 **한 배포에 두 번 이상** 바꾸기 시작할 때 (사람이 못 따라간다)
③ 이 값에 **화면 문구·집계가 딸리게 될 때** (지금은 타이머에만 쓴다)
```

**후보 형태**(그때 고른다): `queued-live.mjs` 가 받은 값을 **원장에서 읽은 기대치와 대조**하고
**다르면 실패가 아니라 경고**를 낸다 — **socket 을 벌하지 않으면서 낡음을 드러내는 형태다.**
⚠ **지금 만들지 않는다** — 위 셋 중 아무것도 안 일어났고, **안 일어난 문제에 검사를 만드는 것이
§5.3 의 다른 얼굴**이다(결함의 존재를 단언하는 검사).

### 5.39 🔴 `cmdPending` 구멍 — **"고쳐졌다"로 읽지 마라. 임계가 밀렸을 뿐이다** (2026-08-18 · 진행 중)

**커밋 `bb04319` 를 보고 이 항목이 닫혔다고 읽는 것을 막기 위해 먼저 적는다**(§5.22 의 교훈을
선제 적용 — 커밋 메시지는 그날의 주장이고 원장이 상태다).

**무엇이 있었나**: §5.38.2 의 구멍(`queued` 분기가 `state.cmdPending` 을 안 본다)을 **사용자가
실물로 재현했다** — 20개 이상 연타에서 `"응답이 없습니다 (자체 타임아웃)"`. 🔴 **그 문구가
오진을 만들었다**: *"이게 esp01 리셋 아닌가"* 로 읽혀 **없는 장치 리셋을 쫓았다**(시리얼에 리셋
표지 0건). **거짓 실패의 비용은 문구가 아니라 남이 엉뚱한 곳을 파는 시간이다.**

⚠ **내가 §5.38.2 에 적은 *"피해가 예약보다 작다"* 는 틀렸다.** 지우지 않는다 —
🔑 **틀린 이유가 중요하다: 근거가 소스 판독뿐이었다.** 그리고 그때 **"소스 판독이고 실측 아니다"를
명시해 둔 덕에 사용자 재현 한 줄로 즉시 뒤집을 수 있었다.** **범위를 적어 두는 것의 값이 현금화됐다.**

**고친 것**(커밋 `bb04319`): `queued` 가 두 맵을 다 본다 · 명령 경로 타이머를
`expires_ms + ack_budget_ms` 로 다시 건다 · `queued` 를 메시지·시뮬 패널에 **보이게** 한다.
✅ 주입 확인: `queue-contract.mjs` 케이스 **C** · 39 pass / 0 fail(커밋 `32d37c9`).

#### 🔴 지금 말할 수 있는 것은 여기까지다

```
사용자 검증: "20개정도는 버티지만 넘어가면 역시 터진다"
→ 수정 전 20개에서 났고, 수정 후 20개는 버틴다
→ **임계가 밀렸다** = 이 구멍이 원인의 **일부**였다는 증거
→ 🔴 **그 위에 다른 것이 있다. 아직 정체를 모른다**
```

⚠ **분모가 없다**: 몇 개째에서 나는지 · 그때 문구가 무엇인지 **아직 안 물었다**(루트가 물어 뒀다).
**그 답이 오기 전에 이 항목을 실측으로 승격하지 않는다.** 지금 승격하면 또 **범위 없는 결론**이다.
⚠ 그리고 **장치측 계수기가 안 읽히는 문제**가 같이 떠 있다(`[CNT]`·`[RAM]`·`[DIAG]` 로그 0건 ·
`DEBUG=0` 굽기 의심 · monitor 판정 중) — **그것이 확정돼야 "화면 밖"을 말할 수 있다**(§5.34).

### 5.40 🔴 하니스 맹점이 셋이다 — **어디를 안 밟는지가 무엇을 잡는지보다 중요하다**

| # | 형태 | 결과 |
|---|---|---|
| §5.30 | 칸을 **문구**로 골랐다(`'빈자리'` vs `'빈 자리'`) · `--log` 가 딴 데를 봤다 | **하니스 결함이 제품 실패로 위장** |
| §5.38.3 | 값을 **주입하고 같은 값으로 단언**한다 | **서버 값이 바뀌어도 절대 빨간불이 안 된다** |
| **§5.39** | **예약 경로만 밟았다**(`state.pending`) | **`sim_step` 경로의 결함을 못 잡았다 — 사람이 찾았다** |

**셋이 다른 형태인데 결과가 같다: 검사가 있는데 그 검사가 잡을 수 없다.**
🔑 **그리고 있다는 사실이 없다는 것을 가린다** — `39 pass / 0 fail` 이 *"그 경로도 봤다"* 로 읽힌다.

→ **규칙: 하니스를 믿기 전에 "이것이 안 밟는 경로"를 세어라.** 통과 수는 **밟은 것의 수**이고
**분모가 아니다.** ⚠ 이번에도 **케이스 C 를 넣고서야** `queued` 를 받는 맵이 둘이라는 것이
검사에 들어왔다 — **그 전까지 39건 중 그 경로는 0건이었고 출력에는 그 사실이 없었다.**

### 5.41 🔴 `queue_full` 이 뜨면 **"사용자가 빨랐다"가 아니라 하행이 막혔다는 신호다** (두 도메인 산술)

socket 이 준 속도 계산(REQ-0208 후속)과 내 아이들 게이트를 맞춰 보면:

```
쌓이는 속도 : 아이들 1초(§5.16)가 하행 세 경로 전부에 걸려 있다      → 최대 ≈1건/초
나가는 속도 : 창당 4건 / 슬롯 1.2초                                  → ≈3.3건/초
→ **나가는 쪽이 3배 빠르다.** 사용자큐상한 16건에 정상적으로 닿을 수 없다
```

→ **`queue_full` 은 정상 조작으로는 안 뜬다. 뜬다면 하행이 막혔다는 뜻이다.**
🔑 **그래서 그 문구가 보이면 "붐빈다"가 아니라 조사 대상이다.**

⚠ **내 §5.36.1 의 계열 배정과 어긋날 수 있다.** 거기서 `queue_full` 을 **`busy`(곧 풀린다)** 에
넣었는데, **하행이 막혀서 찬 것이면 곧 풀리지 않는다.** 🔴 **화면은 그 둘을 구분할 수 없다** —
받는 것은 `queue_full` 하나이고 *"왜 찼는지"* 는 안 온다((가)/(나)를 못 가르는 것과 같은 구조 · §5.34).

**문구는 그대로 둔다**(`"…잠시 뒤 다시 눌러 주세요"`). 근거: socket 확인 — **큐는 1.2초에 4건이
빠지므로 "잠시"가 실제로 1~2초이고 사실과 맞다.** 즉 **하행이 정상인 한 문구가 옳다.**
⚠ **바꿀 근거는 "그 문구가 반복해서 보인다"는 관측이고 아직 없다.** 관측 없이 고치지 않는다.

**확정 자료는 내 것이 아니다**(§5.34): 그 순간 동시 대기는 서버 요약의 `대기 N줄` 이고
16 에 실제로 닿았는지는 `큐넘침(건수축)` 이다. **socket 이 시각별로 뽑아 준다.**
→ **문구(내 축) + 계수(socket 축)를 맞추면 확정된다.** 어느 한쪽만으로는 안 된다.

⚠ **이 절은 산술이고 실측이 아니다.** 두 도메인의 설계값(아이들 1초 · 창당 4건 · 슬롯 1.2초)에서
나온 것이고, **셋 중 하나가 바뀌면 결론이 바뀐다.**

### 5.42 개정 확정값 (2026-08-18 · 사용자·루트) — **대화로만 있던 것을 옮긴다**

**§5.32 의 확정 다섯에 이어진다.** socket 의 전선 명세(REQ-0225)보다 먼저 받은 것들이고,
**값이 갈리면 그 명세가 이긴다.**

| | 확정 | 화면에 걸리는 것 |
|---|---|---|
| ✅ | **자리 : 격자칸 = 1 : 1**(사용자) | 설계서 §3.4 의 "확인 대기"가 **닫혔다** |
| 🔮 | 나중에 큰 자리가 여러 칸을 차지할 수 있다 | **구조가 막지 마라. 지금 만들지도 마라** |
| 🔑 | 좌표를 **`(row, col)` 한 쌍**으로 · **자리 식별자와 칸 좌표를 분리** | REQ-0179 의 *"좌표와 신원은 독립"* 과 같은 방향 |
| ✅ | 격자 **5×5** · 입구/출구 **가장자리** | **기본값이고 설정 파일로 바뀐다 — 구조가 여기 묶이면 안 된다** |
| ✅ | **모듈 ID 는 고유값이 아니라 명칭이다** | 🔴 **`name` 단독 조회 금지** — 전역 신원은 `(devid, name)`(REQ-0179 §①과 일치) |
| ✅ | 🔴 **모듈이 여러 아두이노에 흩어진다 — 1보드 = 1구역이 아니다** | **화면이 그 가정을 하면 안 된다**(§5.34.1 · §5.36 이 전부 여기서 나왔다) |

#### 5.42.1 🔴 규칙 — **명세에 계산 주체가 없으면 받아 적지 말고 물어라**

루트가 REQ-0225 에 *"파생값마다 계산 주체를 명시"* 를 걸었다. 화면 쪽 규율로 받는다:

```
파생값을 명세에서 볼 때 → "누가 계산하나"가 적혀 있나 확인한다
안 적혀 있으면         → 🔴 화면이 계산하는 쪽으로 기본값이 잡힌다 → socket 에 묻는다
```

🔑 **근거는 내가 겪은 두 사례다.** `ack_budget_ms`(REQ-0166): 계산 주체가 비어 있어 화면의
`ACK_TIMEOUT` 이 그 자리를 메우고 **3900ms 구멍**이 됐다. `actionable`(REQ-0183): 규칙만 있고
주체가 없어 **화면이 조합하면 거짓 허용**이 될 수 있었다.
**둘 다 "비어 있으면 화면이 메운다"였다** → **빈 자리를 발견하는 것이 화면의 일이다.**

### 5.43 🔴 `GET /index.html → 200` 은 **"내 코드가 서빙된다"가 아니다** (실측 2026-08-19)

실기 대조를 시작하려고 시험 인스턴스(`10000`)에 붙었더니 **화면이 안 떴다.** §4.4(cwd 에
`index.html` 이 없어 404)를 의심했는데 **200 이었다.** 그래서 서빙된 것을 재 봤다:

```
서버가 준 것 : 127,344 B · `zone-grid` **0건**
내 파일      : 177,062 B · 개정 격자 있음
```

→ **서버가 자기 cwd 의 옛 사본을 서빙했다.** 내 편집은 그 인스턴스에 없었다.

🔴 **이 함정이 404 보다 나쁘다.** 404 는 즉시 멈추지만 **200 + 옛 사본은 되는 것처럼 보인다** —
그대로 진행하면 **남의 코드를 재고 내 결과라고 보고한다**(§5.30 계열: 계측 대상이 딴 것이었던 형태).
⚠ **하필 대조가 "내 가정이 서버와 맞는가"를 재는 자리라, 옛 사본으로 재면 "안 맞는다"가 나오고
그 원인을 내 코드에서 찾게 된다.**

→ **`live-map.mjs` 가 붙자마자 판본 지표를 먼저 확인한다**(`#zone-grid` · `requestMap` ·
`state.srvId`). 🔑 **하나라도 없으면 대조를 진행하지 않고 멈춘다** — *"제품 판정으로 읽지 마라"* 를
같이 찍는다. **틀린 결론보다 멈추는 것이 낫다.**

⚠ **일반화**: 브라우저로 재는 모든 하니스는 **"지금 보고 있는 페이지가 내가 만든 그 페이지인가"를
먼저 물어야 한다.** 지금까지는 `file://` 로 내 파일을 직접 열었기 때문에 그 물음이 필요 없었고,
**서버가 서빙하기 시작한 순간 생긴 새 축**이다.

### 5.44 실기 대조 — **옛 `slots[]` 와 새 `state` 는 갈리지 않는다** (실측 2026-08-19 · `db138fe`)

`node web/tools/live-map.mjs --port <시험포트>` → **18 pass / 0 fail / 1 미측정.**

```
① 봉투 키    map(type·srv_id·epoch·grid·zones) · state(+ts_ms) · 자리(id·kind·cells·modules)
             cells = [[row,col]] · kind 셋 · 자리 12 · grid 5×5      → 전부 내 가정과 일치
② srv_id·epoch  두 봉투에서 같다 · get_map 왕복이 실기에서 돈다 · 재기동 시 srv_id 변화도 밟혔다
③ 🔴 옛 slots[] 대 새 state   **열 자리 대조 · 차이 0건**
```

🔑 **③이 이 대조의 목적이었다.** 옛 경로를 살려 둔 것이 안전장치인데 **둘이 다른 말을 하면 그
안전장치가 "무엇이 참인가"를 흐린다**(루트 지적). 지금은 안 갈린다 — socket 이 **같은 서버 상태에서
파생시키기** 때문이다(`push_snapshot()` 이 두 봉투를 같은 순간에 낸다).

⚠ **이것은 "지금"이다.** 서버가 두 경로를 **따로 손대면 갈릴 수 있다.**
→ **`live-map.mjs` 가 그 회귀 검사다.** 이중 경로를 유지하는 동안 **정기적으로 돌려라**(루트 지시).

⚠ **미측정 1건**: 모듈 상태 키(`devid·name·value·known`)는 **장치가 등록돼야** 볼 수 있다.
🔑 **통과로 넘기지 않고 따로 센다** — 통과로 넘기면 **안 본 것이 본 것이 되고**(§5.40), 실패로 두면
**늘 빨강인 검사는 아무도 안 읽는다**(§3.1 형태). **`검사 0건은 그 자체로 빨강` 의 짝이다.**

#### 5.44.1 🔴 실기가 드러낸 것 — **`module_absent` 가 두 상태를 한 칸에 담는다** (소스 확정)

장치가 **한 번도 안 붙은** 시험 인스턴스에서 **12자리 전부 `module_absent`** 가 왔다.
그런데 socket 계약표는 그 상태를 **`node_unregistered`**(*"등록을 못 받아 모듈 사상이 없다"*)로
정의한다. 소스가 원인을 확정한다:

```cpp
// server.cpp:2126
if (z.modules.empty()) return "module_absent";   // ← 등록 전에도 여기서 끝난다
```

🔴 **socket 이 자기 명세에 이걸 하지 말라고 적어 뒀다**: *"`node_unregistered` 와 `module_absent` 를
합치면 '등록이 안 왔다'와 '그런 장치가 없다'가 같은 칸에 들어간다."* **구현이 그 경고대로 됐다.**

**화면에 미치는 영향이 두 겹이다:**

| | |
|---|---|
| **계열이 틀린다** | `module_absent` = **`fault`**(`⚠` 고장) · 그런데 실제는 **구성 상태**(아직 등록 안 됨 = `unknown`·`⏱`) |
| **집계가 침묵한다** | 내 배너는 `unknown` 계열만 센다 → **12자리가 다 막혔는데 배너가 조용하다** |

🔑 **즉 배너 침묵은 내 설계 탓만이 아니었다 — 틀린 계열의 코드를 먹은 결과다.**
⚠ **화면 쪽에서 고칠 수 없다.** 서버가 아는 것(등록 여부)을 화면은 모른다 — **화면이 추측하면
§5.14 를 어긴다.** → **서버가 코드를 갈라 주면 내 규칙이 그대로 옳게 동작한다.**

**루트가 요구한 구분과 같은 것이다**: *"한 번도 등록된 적 없음"(구성) 대 *"등록됐다가 끊김"(고장).
🔑 **계약에는 이미 그 구분이 있다**(`node_unregistered` 대 `node_offline`) — **구현이 앞단에서
합쳐 버렸을 뿐이다.** 새 코드를 만들 필요가 없다.