# 재개 지시 — 2026-08-19 09:55 · **위치 이동**

> 사용자 지시로 예약됐다. **`docs/STATUS-2026-08-19.md` §0-A 와 같이 읽어라.**
> 이 파일은 *이동* 만 다룬다. 나머지 재개 상태는 §0-A 가 정본이다.

## 0. 🔴 가장 먼저 — **이동했으므로 앞 세션의 환경값이 전부 낡았다**

```
❌ 서버 IP 192.168.35.21     — 새 망에서 바뀐다
❌ WiFi SK_WiFiGIGA50DC_2.4G — 새 AP 로 바뀐다
❌ 포트 /dev/cu.usbmodem21201 — 재연결마다 바뀐다 (원래 그렇다)
⚠ 창 O 판정·미배포 다섯은 그대로 유효하다 (코드·자료는 망과 무관)
```

## 0-B. 🔴🔴 **칩이 덮였다 — `9edbcca` 가 아니다** (사용자 통보)

> 사용자 원문: *"내가 테스트를 목적으로 `arduino/.basefw/client.ino` 파일 이용하여 플래싱하였다.
> 테스트에 맞게 다시 플래싱하여서 이용하도록 해라."*

```
🔴 지금 칩 = arduino/.basefw/client.ino  (2026-08-16 18:52 · 139,300B)
❌ STATUS §0-A 의 "칩 9edbcca" 는 **낡았다**
   저장소 정본 조별과제샘플/client.ino 는 251,589B — **112KB 차이**
```

### 그 칩에 **없는 것**(08-17~08-19 작업 전부)
```
hex 인코딩 · D 등록 · 모듈 표 · ESP 이름 · 가상 O* 모듈(E1·X1)
SEND OK 바이트 매칭 · 링버퍼 비우기 · penddrop/pendFills 계수 · espInit
```
🔴 **그리고 옛 WiFi·IP 가 박혀 있다**(`SK_WiFiGIGA50DC_2.4G` / `2011050796` / `192.168.35.21`).
**새 위치에서는 접속조차 안 된다.** 이건 고장이 아니라 **예상된 상태**다.

### 재개 시 할 일
```
① arduino 가 **지금 칩이 정말 .basefw 인지 값으로 확인**한다 (hex 대조)
   ⚠ 사용자 전언을 근거로 쓰지 마라 — 우리 규칙이다. 확인 비용은 대조 한 번이다
② 굽는 대상은 🔴 **조별과제샘플/client.ino** 다. `.basefw` 도 `arduino/client/` 도 아니다
   (저장소에 client.ino 가 일곱 개다 — arduino §27.5)
③ SSID·PASS·IP 셋을 반영해서 **한 번에** 굽는다
```
⚠ **`.basefw` 를 지우거나 고치지 마라.** 잔재가 아니라 **의도된 기준선**이다(arduino §27.5).
**의도된 것과 잔재가 같은 이름이라 더 나쁘다**는 경고가 이미 원장에 있다.

### ⚠ 창 O 자료는 유효하다 — 범위를 좁게 읽어라
```
✅ 창 O 는 07:30 에 이미 닫혔다. 그때 칩은 9edbcca 였다 → **자료·판정 유효**
🔴 창 O **이후** 이 보드에서 나온 것은 다른 칩의 것이다
```
**monitor 의 탭이 계속 돌고 있었다면 그 구간은 창 O 가 아니다.** 판정에 섞지 마라.

## 1. 새 환경값 (사용자가 준 값)

```
SSID  3F_302      🔴 아래 경고를 읽어라
PASS  0424719222!!
```

### 🔴 SSID 대소문자가 두 번 다르게 왔다 — **확정하지 말고 확인해라**
```
1차 지시 : 3f_302     (소문자 f)
2차 지시 : 3F_302     (대문자 F)   ← 나중 값
```
**WiFi SSID 는 대소문자를 구분한다.** 어느 쪽이 실제 AP 인지는 **전언이 아니라 스캔이 답한다.**
```bash
/System/Library/PrivateFrameworks/Apple80211.framework/Versions/Current/Resources/airport -s | grep -i 3f_302
# 또는 시스템 WiFi 목록에서 눈으로
```
🔑 **굽기 전에 확인해라.** 틀리면 **접속이 안 되는데 원인이 펌웨어처럼 보인다** —
그러면 없는 결함을 쫓게 된다. 우리가 이미 여러 번 치른 비용이다.
⚠ **나중 값(`3F_302`)을 기본으로 쓰되, 스캔 결과가 다르면 스캔이 이긴다.**

### 🔴🔴 비밀번호에 `!!` 가 있다 — **셸에서 그대로 치지 마라**

앞 판 주석이 *"이번 값엔 `!` 가 없다(히스토리 확장 걱정 없음)"* 라고 적어 뒀다.
**이번 값에는 있다. 그 전제가 뒤집혔다.**

```bash
❌ echo "0424719222!!"          # 대화형 bash 에서 히스토리 확장이 일어난다
❌ req.sh --body "…0424719222!!…"   # CLAUDE.md 가 이미 금지한 형태 (2026-08-16 사고)
✅ 작은따옴표 · 인용된 heredoc(<<'EOF') · 파일에 적어 --body-file 로 넘긴다
```
🔑 **펌웨어에 들어간 뒤에는 반드시 값으로 확인해라** — 확장이 일어나면 **조용히 다른 문자열이 박힌다.**
`arduino` 는 굽기 전에 소스에서, 굽고 나서 접속 성공으로 두 번 확인해라.

## 2. 순서 — 🔴 **IP 를 먼저 확정하고 나서 굽는다**

```
① 새 망에 이 PC 를 붙인다
② 새 IP 를 **값으로** 확인한다 :  ipconfig getifaddr en0
③ 서버를 새 IP 로 기동/확인한다 (bind 는 0.0.0.0 이라 대개 그대로 뜬다)
④ arduino 가 client.ino 셋을 같이 고친다 — WIFI_SSID · WIFI_PASS · SERVER_IP
⑤ 굽는다  → ⑥ 등록 D 프레임이 서버 로그에 뜨는지로 확인
```
⚠ **④를 ②보다 먼저 하면 IP 를 두 번 굽게 된다.** 굽기는 관측 경계를 만든다 — 한 번으로 묶어라.

### 고칠 자리 (arduino 소유 · 루트는 못 고친다)
```
조별과제샘플/client.ino:91  #define WIFI_SSID   "SK_WiFiGIGA50DC_2.4G"
조별과제샘플/client.ino:92  #define WIFI_PASS   "2011050796"
조별과제샘플/client.ino:97  #define SERVER_IP   "192.168.35.21"
```
⚠ `client.ino:93~95` 에 **앞뒤 공백 금지** 경고가 있다 — 실제로 `" 192.168.35.21"` 로 구워진 적이 있다.

⚠ `arduino/client/client.ino` 와 `requests/recovery-2026-08-16/` 에도 같은 상수가 있다.
**그건 잔재다**(arduino §27.5 — `client.ino` 가 저장소에 일곱 개). **굽는 것은 `조별과제샘플/client.ino` 다.**

## 3. 🔴 이 굽기는 **A[1] 배포와 같은 경계에 묶어라**

미배포 다섯이 대기 중이다(§0-A). **굽기도 서버 재기동도 둘 다 관측 경계를 만든다.**
```
✅ 한 경계 : 서버 IP 변경 + A[1] 다섯 배포 + 펌웨어 굽기(SSID·PASS·IP)
             → 그 뒤 새 창 하나
❌ 따로    → 창이 세 번 조각난다
```
⚠ **그러면 A[1] 판정에 "망 이동"이라는 축이 하나 붙는다. 판정문에 그것을 적어라.**
🔑 판별자는 §"축이 하나 느는가"다 — **망 이동은 축이다.** 다만 **묶지 않으면 창이 더 조각나므로**
이번에는 묶고 **축을 명시**하는 쪽이 낫다. 이 판단의 근거를 판정문에 남겨라.

## 4. 이동 뒤 값으로 확인할 것 (기억·전언 금지)

```
· 새 IP        ipconfig getifaddr en0
· 서버 판본    로그 `=== INSTANCE … build=` 줄
· 칩 판본      등록 D 프레임 + arduino 의 hex 대조
· 포트         arduino-cli board list   (재연결마다 바뀐다)
· 화면         node web/tools/deploy-screen.mjs --check
               🔴 서버 cwd 가 바뀌면 서빙본이 통째로 갈린다 — 이동 뒤 반드시 다시 재라
```

## 5. 팀 상태

`team/team.sh` 세션 `learnteam` 이 살아 있으면 에이전트가 그대로 있다.
죽었으면 `team/team.sh --fresh`. **재개 시 첫 지시는 "각자 자기 원장의 마지막 상태를 확인하라"** 다 —
이동 전 마지막 보고가 낡았을 수 있다.

---

## 예약 정보 (2026-08-19 07:41 에 걸었다)

```
발화 시각 : 09:55
방식      : team/bin/poke-root.sh → tmux send-keys → pane %0 (루트 창)
pid       : 2094
문장      : "작업재개하라. docs/RESUME-09-55-move.md 와 docs/STATUS-2026-08-19.md §0-A 를
             먼저 읽어라. 위치가 이동했으므로 새 IP 를 값으로 확인하고 나서 굽는다."
```
**취소** `kill 2094` · **시각 변경** `kill 2094` 후 다시 건다.

⚠ **tmux 세션 `learnteam` 이 죽으면 예약도 죽는다.** `Ctrl-b d`(분리)는 괜찮다.
⚠ **발화 시각에 작업 중이면 입력이 중간에 끼어든다** — 정지 상태로 두는 것이 좋다.
✅ **pane %0 이 Claude UI 인 것을 쏘기 전에 확인했다**(`Opus 5 (1M context)` · `auto mode on`).
   셸이었으면 문장이 명령으로 실행됐을 자리다.
