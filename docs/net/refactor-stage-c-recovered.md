# REQ-0096 리팩터링 — 단계 A·B 결과와 단계 C 계획 (트랜스크립트에서 복구)

---

> # ⛔ 이 문서의 **계획 부분은 폐기됐다** (2026-08-16 23:4x)
>
> **이 문서는 2026-08-16 18:0x 이전 판이다.** 아래 "단계 C" 절을 **지도로 쓰지 마라.**
> 사료로서 남겨 둔다(복구 경위와 원 기록 인용에 값이 있다). **지우는 것이 아니라 못을 박는다.**
>
> ### 지금은 어디를 봐야 하나
>
> | 무엇 | 어디 |
> |---|---|
> | **구조 전체 — 처음 보는 사람은 여기부터** | **`docs/net/ARCHITECTURE.md`** |
> | 이 도메인에서 참인 것(사실 목록) | `docs/net/LEDGER.md` §7 |
> | 범위를 그렇게 정한 이유(논쟁 기록) | `docs/net/FINDING-stage-c-scope-2026-08-16.md` |
> | 설계 원본(정본) | `조별과제샘플/server_seam.h` 머리말 |
>
> ### 🔴 아래 표에서 **틀린 것 셋** — 그대로 따르면 잘못 만든다
>
> 1. **"단계 C" 라는 이름** — 글자 체계를 버렸다(LEDGER §7.3). `plan-windows-and-mapping.md`
>    §5.7 의 A~D 와 **글자가 겹쳐 실제로 섞여 읽혔다.** 지금 이름은
>    **이음매 1(아두이노 상행) · 이음매 2(아두이노 하행) · 이음매 3(폰)** 이다.
> 2. **"5곳"** — 문서마다 세는 대상이 달랐고, 표에 없던 **`send_ard` 가 하나 더 있었다**
>    (FINDING §1). 개수를 이 표에서 인용하지 마라.
> 3. **`DEV_CONNECT`(표 1번)가 대상이라는 것** — **이음매 2(하행)로 넘어갔다.**
>    `resync_reservations()` 는 이름이 로그 계열로 읽히지만 **송신기다** —
>    `dispatch('R')` 로 **전선에 하행을 쏜다.** 이벤트로 미루면 송신 순서가 뒤집힌다
>    (`→ARD R` 이 첫 `←ARD S` 보다 먼저인 것이 실측 기준선이다 · LEDGER §7.2).
>    같은 이유로 `DEV_SENSORS` 도 제외됐다. → **상행의 상한은 4/6 이고, 그건 막힘이 아니라 구조다.**
>
> ### 그 밖에 낡은 것
>
> - **"C 는 시작도 안 했다"** → **틀렸다.** 상행 4/6 이관이 끝났고 `DEV_ACK` 는
>   **격리 검증까지** 통과했다(LEDGER §7.6).
> - **행 번호 전부** — 원 기록은 2244행 판, 이 표는 2391행 판 기준이다. **현재는 2676행이다.**
>   LEDGER §5.5: **복구된 기록은 단서로 쓰고 매번 다시 세라.**
>
> _(폐기 표시 사유: REQ-0135. LEDGER §5.5 가 "복구된 기록을 지도로 쓰지 마라"고 경고해 두고
> 정작 그 위험물을 안 치운 상태였다.)_

---

> **복구 경위**: 2026-08-16 `git clean` 사고로 REQ-0096 요청 파일이 사라졌다.
> 세션 `363b40c4` 트랜스크립트(행 3383 `req.sh progress`, 행 3393 최종 인수인계 Write)에서
> 복원했다. socket-engineer · 2026-08-16 재구성.
>
> ⚠ **행 번호는 원 기록(2244행 판) 기준이라 지금과 다르다.** 아래 표는 **현재 파일에서
> 다시 찾아 확인한 값**이다 — 원 기록을 그대로 옮기지 않았다.

---

## 단계 A·B — **완료됐다** (사고를 넘어 살아남음)

| 파일 | 상태 |
|---|---|
| `조별과제샘플/server_device.h` | **175행.** SHA1·base64·`ws_accept`·`cksum`·`build_line`·`verify_line`·`WS_GUID` |
| `조별과제샘플/server_seam.h` | `DeviceEvent`(68B) · `DeviceCommand`(34B) · 헬퍼 3개 |
| `조별과제샘플/server.cpp` | 원 2391행 → **2244행**. 위 둘을 include. 그 외 구조 변경 없음 |

원 기록의 상태 판정: *"중간 상태가 아니다. A·B 는 완결됐고 C 는 시작도 안 했다."*
→ 그 판정은 지금도 유효하다. 빌드 경고 0 · 자가검증 통과 상태다.

> 참고: 사고로 `server_device.h`·`server_seam.h` 가 삭제됐다가 REQ-0099 로 복구됐고
> 루트가 커밋(`fa52a51`·`5bb5553`)했다. **지금은 추적되므로 같은 사고로는 안 사라진다.**

---

## 단계 C — 이음매를 넘는 호출 **5곳**을 이벤트로 바꾼다

**목표**: `Server` 에 `std::vector<DeviceEvent> pending_events;` 를 두고, 아래 5곳을
직접 호출 대신 `push_back` 으로 바꾼 뒤 `run()` 루프 끝에서 한 번에 소비한다.
→ 디바이스 계층이 도메인 함수를 직접 부르지 않게 된다.

### 🔴 함정 — `grep` 하면 12곳이 나오는데 그중 5곳만 대상이다

원 기록의 경고를 그대로 옮긴다:

> `grep -n "resync_reservations\|push_snapshot\|write_log_if_changed"` 로 다시 찾아라.
> **12곳이 나오는데 그중 디바이스 계층 안에 있는 5곳만이 대상이다.**
> 나머지 7곳은 도메인→도메인이라 무관하다.

**행 번호를 믿지 말고 함수 이름과 문맥으로 찾아라.** 편집하면 밀린다 — 실제로 밀렸다
(로그 계약 v1.0 을 넣으면서 약 100행 늘었다).

### 대상 5곳 (현재 파일에서 재확인한 위치)

| # | 현재 행 | 함수 / 문맥 | 현재 호출 | 바꿀 이벤트 |
|---|---|---|---|---|
| 1 | **877** | `adopt_as_parking()` (861행) | `resync_reservations("새 연결")` | `DEV_CONNECT` |
| 2 | **2120** | `run()` — recv 실패로 세션 종료 | `push_snapshot()` | `DEV_DISCONNECT` |
| 3 | **2267** | `run()` — 유휴 마감 회수 | `push_snapshot()` | `DEV_DISCONNECT` |
| 4 | **2321** | `run()` — 오프라인 엣지 | `write_log_if_changed()` | `DEV_OFFLINE`/`DEV_ONLINE` |
| 5 | **2322** | `run()` — 오프라인 엣지 | `push_snapshot()` | 〃 |

⚠ 원 기록은 1번을 `resync_reservations()` 로 적었으나 **실제 시그니처는 `why` 인자를 받는다**
(`resync_reservations(const char* why)`, 1400행). 이벤트로 옮길 때 그 사유 문자열을
`DeviceEvent` 에 실을지 결정해야 한다 — 지금 로그가 그 문자열을 찍고 있으므로 **버리면 관측이 준다.**

### 진행 조건 (루트 지시)

- **한 번에 하나씩.** 여러 변경을 한 교체에 묶지 않는다 — 깨지면 어느 것 때문인지 못 가른다.
- 각 단계마다 **빌드 경고 0 + `--selftest` + 실왕복**.
- 서버 교체 전 루트에 **사전 통보**(승인 대기 아님), 구 인스턴스는 SIGTERM 정상 종료.

---

## 원 기록에 함께 있던 것 — 옮겨 둔다

**늦은 보고에 대한 자기 지적** (원문):

> 12:05 와 14:08 두 번, 작업을 끝내고도 `progress` 를 안 쳐서 **멈춘 것처럼 보였다.**
> 루트가 침묵을 볼 방법이 없었던 것이 원인이고, `req.sh progress` 가 그래서 생겼다.
> **앞으로 긴 작업 중에는 단계마다 친다** — 완료 보고만으로는 부족하다.

**서버 교체 시 기록 위치** (원문 지시):

> 교체하면 REQ 에 `교체 시각 + 빌드 식별자` 한 줄을 적어라.
> ⚠ `arduino/INTERVENTIONS.md` 에는 **넣지 마라** — 보드 리셋 전용이라 **없는 재부팅을 만든다.**

> 위 교체 명령에 나오는 `net/swap_server.sh` 는 사고로 사라졌고 아직 복구하지 않았다.
> 체크포인트 `refs/claude/checkpoint-7fb8eb20` 에 원본이 있다.
> 지금은 수동 교체로 하고 있다(빌드 → SIGTERM → 새 인스턴스 → 실왕복 확인).
