# socket-engineer 인수인계 — 2026-08-16 삭제 사고 이후

> ⚠ **이 문서는 기억이 아니라 생존 증거에서 재구성한 것이다.**
> REQ-0055 가 요청한 "재기동 전 세션의 문맥 덤프"는 **불가능하다** — 이 문서를 쓰는 세션은
> 이미 재기동된 새 세션이고, 사고 이전 세션의 문맥은 **회수 불가로 소실됐다.**
> 아래 내용 중 기억에서 나온 것은 하나도 없다. 전부 지금 실행한 관측 명령의 출력이다.
> 각 항목에 **검증** / **추정** 을 표시했다. 표시가 없으면 검증된 사실이다.
>
> 작성 시각 2026-08-16 15:51 KST · 작성자 socket-engineer(재기동 후 첫 세션)

---

## 0. 한 줄 결론

**런타임은 살아 있고, 소스 트리는 빌드가 깨져 있다.** 서버 프로세스(pid 36998)는 사고를
겪지 않고 21시간째 계속 돌고 있지만, 그 바이너리를 **다시 만들 수 없다** — `server.cpp` 가
`#include` 하는 헤더가 삭제됐고 그 헤더는 git 어디에도 없다.

---

## 1. 검증한 현재 상태 (2026-08-16 15:51 기준)

| 항목 | 상태 | 근거 |
|---|---|---|
| 서버 프로세스 | **살아 있음** `pid 36998` `/tmp/srv_parking` | `ps aux` — 기동 시각 **8/15 18:42**, 사고(8/16 15:2x)를 넘겨 생존 |
| 리슨 포트 | **9991 / 9900 / 5500 전부 LISTEN** (와일드카드 `*`) | `lsof -nP -iTCP -sTCP:LISTEN -p 36998` |
| 맥 IP | **192.168.35.21** (en0) | `ipconfig getifaddr en0` |
| 웹 화면 | **응답함** `HTTP 200`, 111,798 bytes, 2.2 ms | `curl http://192.168.35.21:9900/` |
| 소크 로그 | **살아서 갱신 중** — 마지막 줄 15:50:34, mtime 15:51 | `/tmp/parking-soak.log` (repo 밖이라 생존) |
| 아두이노 링크 | **끊김 — 미복구 진행 중** | 아래 §4 |
| `server.cpp` 빌드 | **깨짐 — 컴파일 불가** | 아래 §3 |

### ⚠ REQ-0055 본문의 "서버 프로세스 죽음" 은 사실과 다르다
서버는 죽지 않았다. 8/15 18:42 에 뜬 프로세스가 지금까지 한 번도 끊기지 않고 살아 있다.
루트의 피해 목록에서 이 항목은 **정정 대상**이다.

### ⚠ REQ-0077 은 최소한 1번 항목이 이미 처리됐다
REQ-0077(8/15 16:02, "서버 재기동 + 새 망 기준선")의 상태는 `claimed` 이고 처리 결과가
`_(미처리)_` 로 남아 있지만, **그 파일 자체가 8/15 16:06 체크포인트 스냅샷이라 이후 기록이
유실된 것**이다(파일 머리말이 그렇게 경고한다). 실제로는 그 뒤 18:42 에 새 빌드가 기동돼
세 포트를 리슨 중이고 IP 도 새 대역(192.168.35.21)이다. **`_(미처리)_` 를 "안 했다"로 읽지 마라.**

---

## 2. 사라진 산출물 목록과 복원 경로

삭제된 파일은 복원 난이도가 **셋으로 갈린다.** 이 구분이 이 문서의 핵심이다.

### (A) 체크포인트 ref 에서 그대로 복원 가능 — net/ 도구 6종

`refs/claude/checkpoint-7fb8eb20` (8/15 16:06) 안에 원본이 남아 있다.
현재 `net/` 에 **없는** 파일:

| 파일 | 상태 |
|---|---|
| `net/swap_server.sh` | 삭제됨 · 체크포인트에 있음 · **git 에 커밋된 적 없음**(`git log` 결과 공백) |
| `net/soak_probe.py` | 삭제됨 · 체크포인트에 있음 |
| `net/soak_report.py` | 삭제됨 · 체크포인트에 있음 |
| `net/reconnect_stats.py` | 삭제됨 · 체크포인트에 있음 |
| `net/uptime_anchor.py` | 삭제됨 · 체크포인트에 있음 |
| `net/zombie_probe.py` | 삭제됨 · 체크포인트에 있음 |

복원 명령(**아직 실행하지 않았다** — REQ-0055 가 복원을 금지하고 루트가 순서를 정하기로 했다):

```
git show refs/claude/checkpoint-7fb8eb20:net/swap_server.sh > net/swap_server.sh
```

> 주의: 체크포인트는 **8/15 16:06** 시점이다. 그 이후의 수정분은 이 경로로도 못 살린다.

현재 `net/` 에 남아 있는 것: `deadbeat_client.py` · `fake_arduino.py` · `fake_phone.py` ·
`test_server.py` · `ws_probe.py` · `README.md`.

### (B) 커밋된 옛 `server.cpp` 에서 내용을 다시 추출해야 함 — `server_device.h`

**이것이 가장 심각하다.**

- `조별과제샘플/server_device.h` · `server_wire.h` · `server_seam.h` 삭제됨.
- **체크포인트 ref 에도 없다.** `git ls-tree refs/claude/checkpoint-7fb8eb20` 로 확인 —
  `조별과제샘플/` 아래엔 `README.md`·`client.ino`·`index.html`·`server.cpp` 뿐이다.
- 즉 **git 어디에서도 원본 파일을 되찾을 수 없다.** (이것은 검증된 사실이다)
- **왜 안 담겼는가는 추정이다.** 후보가 둘이고 내 증거로는 못 가른다:
  (a) 당시 `.gitignore` 의 `조별과제샘플/server_*` 규칙이 소스까지 무시했다,
  (b) **체크포인트 ref 가 전부 8/15 것인데(가장 최신 16:06) 헤더를 만든 리팩터링은
  8/16 12:01 이다** — 애초에 헤더 생성 이후의 스냅샷이 존재하지 않는다.
  어느 쪽이든 **복구 불가라는 결론은 같다.** 원인 규명에 시간 쓰지 마라.

다만 **내용은 복원 가능하다.** 이 헤더들은 원래 모놀리식 `server.cpp` 에서 뜯어낸 것이고,
뜯기 전 버전이 커밋 `6e3f63c`(HEAD)에 그대로 남아 있다:

```
git show HEAD:조별과제샘플/server.cpp
```

확인된 위치(HEAD 판 기준): `struct SHA1` 122행 · `base64()` 181행 · `ws_accept()` 196행 ·
자가검증 벡터 1402~1406행. `server_device.h` 의 include 주석이 명시한 내용
("SHA-1·base64·ws_accept·체크섬")과 일치한다. → **재작성이 아니라 재추출이면 된다.**

**이 계획을 검증했다.** 워킹트리 `server.cpp` 를 grep 한 결과 `struct SHA1` 과 `base64()` 의
**정의가 남아 있지 않고**, `ws_accept` 는 **호출부만 2곳**(1532행 WebSocket 핸드셰이크,
2106행 selftest) 있다. 즉 리팩터링이 정의를 헤더로 **옮겼고** 그 헤더가 사라진 것이 맞다.
→ HEAD 에서 뽑아 헤더로 만들면 **중복 정의가 나지 않는다.** 계획대로 진행 가능.

`server_wire.h` · `server_seam.h` 는 **현재 `server.cpp` 가 include 하지 않는다**(191행의
`server_device.h` 가 유일한 로컬 include). 지금 빌드에 필요한 것은 `server_device.h` 하나뿐이다.
나머지 둘은 리팩터링이 거기까지 안 갔거나 계획 단계였을 수 있다 — **추정이다.**

### (C) 복원 대상 아님 / 다른 담당 소유

- `requests/2026-08-15/` 전체, `monitor/` 전체 — 내 소유가 아니다. 루트·monitor-engineer 몫.
  (참고: 위 체크포인트 ref 에 `requests/2026-08-15/` 일부가 남아 있을 가능성이 높다. **추정** —
  내가 확인하지 않았다. 루트가 같은 방법으로 조회해 볼 값어치는 있다.)

---

## 3. 빌드는 깨져 있다 — 이것이 다음 세션이 가장 먼저 알아야 할 사실

```
조별과제샘플/server.cpp:191:  #include "server_device.h"
```

이 헤더는 **삭제됐고 §2(B) 대로 git 에서 되찾을 수 없다.**
따라서 **지금 `server.cpp` 는 컴파일되지 않는다.**

> 정확히 말하면 이것은 **없는 include 로부터의 추론이지 내가 돌린 컴파일이 아니다.**
> 빌드를 시도하지 않았다 — 사고 직후라 부작용 있는 명령을 피했다. 다만 `server.cpp` 안에
> `SHA1`·`base64` 정의가 없고 `ws_accept` 호출부만 남아 있는 것까지 확인했으므로(§2B),
> 헤더 없이 링크될 여지는 없다.

동시에:

- 워킹트리 `server.cpp` 는 HEAD 대비 **수정 상태**(`git status` → ` M`), mtime **8/16 12:01**.
- 돌고 있는 바이너리 `/tmp/srv_parking` 의 mtime 은 **8/15 18:42**.
- → **바이너리는 현재 소스에서 나온 것이 아니다.** 지금 소스로 재빌드하면 다른 물건이 되고,
  애초에 헤더가 없어 빌드 자체가 실패한다.

**결론: 돌고 있는 서버는 "재현 불가능한 마지막 정상 산출물"이다.**
`/tmp/srv_parking` 을 죽이면 되살릴 수단이 없다.

> 🔴 **다음 세션에게: 헤더를 복구하기 전에는 pid 36998 을 절대 죽이지 마라.**
> 백업을 먼저 떠 두는 것이 안전하다: `cp /tmp/srv_parking /tmp/srv_parking.bak-0816`
> (아직 실행하지 않았다 — 루트 판단 대기)

---

## 4. 살아 있는 관측 — 아두이노 링크가 44초 주기로 헛돈다

`/tmp/parking-soak.log` 최근 구간이 **일정한 패턴을 반복**한다:

```
15:47:19  +? 연결 수락 — device_id 대기 중(7초 안에 유효 프레임 없으면 끊는다)
15:47:26  ✂ id 미상 소켓 마감 — 7000ms 안에 유효 프레임이 없었다(§3.4 판정의 2배)
15:48:03  +? 연결 수락 — …
15:48:10  ✂ id 미상 소켓 마감 — …
```

즉 **TCP 연결은 매번 성립하는데, 7초 안에 유효 프레임이 한 줄도 오지 않아 서버가 끊는다.**
약 44초 간격으로 무한 반복 중이다.

15:50:34 소크 통계 스냅샷:

| 지표 | 값 | 읽는 법 |
|---|---|---|
| 소크 경과 | 21:07:55 | 8/15 18:42 기동과 일치 |
| 세션 | 66회 (**현재 끊김**) | |
| 프레임 | **62551 에서 정지** | 최근 구간 내내 1도 안 늘었다 |
| 미복구 | 🔴 **1회 진행 중 1207초** | 15:30 경 시작 — 사고 시각(15:2x) 직후 |
| 링크없음 누적 | 01:12:46 | |
| 최악 복구 | 787.6초 · 중앙 1.5초 | |
| 버린 줄 | 53 (체크섬 49 / 과길이 2 / 잡음 2) | |

**판정: 서버 수신 경로는 정상 동작 중이다**(accept 하고, 규약대로 7초 타임아웃을 걸고, 끊는다).
**말단이 유효 프레임을 안 보내고 있다.** 원인 후보 — **전부 추정, 검증 안 함**:
사고로 시리얼 캡처가 죽으면서 말단 쪽 상태가 함께 흐트러졌을 가능성 / ESP 펌웨어 상태 /
망 전환 잔재. **말단은 arduino-engineer 소유이므로 내가 손대지 않았다.**

또한 이 카운터들은 **8/15 18:42 기동분 누적이라 두 망(192.168.50.x → 192.168.35.x)이 섞여 있다.**
REQ-0077 2번("소크 기준선을 새로 잡아라")은 여전히 미해결로 봐야 한다 — 단, 기준선을 새로
잡으려면 서버를 재기동해야 하고, §3 때문에 **지금은 재기동이 곧 영구 손실**이다.
**순서가 뒤집혔다: 헤더 복구 → 재빌드 성공 확인 → 그 다음에 재기동/기준선.**

---

## 5. 다음 한 걸음 (루트가 순서를 정할 것 — 내가 임의로 시작하지 않았다)

우선순위 순. 1번이 다른 모든 것의 전제다.

1. **`server_device.h` 재추출** — `git show HEAD:조별과제샘플/server.cpp` 에서 SHA1·base64·
   `ws_accept`·체크섬을 뽑아 헤더로 만든다. 완료 판정은 **컴파일 성공**, 그리고
   `./server --selftest`(RFC 6455 §1.3 벡터 + 라인 체크섬 자가검증)가 통과하는 것.
   소유: `조별과제샘플/server*` → **socket-engineer(나)**.
2. **바이너리 백업** — 1번 전에 `cp /tmp/srv_parking /tmp/srv_parking.bak-0816`. 30초짜리 보험.
3. **`net/` 도구 6종 복원** — §2(A) 의 체크포인트 ref 에서. 소유: **나**.
4. **아두이노 링크 조사 요청** — §4 의 44초 주기 증상. `req.sh` 로 arduino-engineer 에게 발행.
   **내 소유가 아니므로 요청으로 넘긴다.**
5. **재기동 + 새 기준선** — REQ-0077 의 2·4번. **1번이 끝나기 전엔 하지 않는다.**
6. `swap_server.sh` 에 방화벽 재등록 추가(REQ-0077 3번) — 3번 복원 후.

---

## 6. 재발 위험 — ✅ 이미 해소됐다 (루트가 먼저 고쳐 놨다)

**초안에서 나는 "`.gitignore` 를 먼저 고쳐라"라고 썼다. 확인해 보니 이미 고쳐져 있었다.**
직접 읽고 검증한 현재 `.gitignore` 43~50행:

```
# 검증 중에 만드는 임시 빌드(server_test 등). **소스는 추적한다.**
# ⚠ 2026-08-16: 이 규칙이 `조별과제샘플/server_*` 였다가 server_wire.h·server_seam.h·
#   server_device.h 같은 **소스까지 무시**했다. git status 에 안 보이니 아무도 몰랐고,
#   git clean 이 돌자 통째로 사라졌다(REQ-0054). 확장자를 가려서 바이너리만 막는다.
조별과제샘플/server_test
조별과제샘플/server_*.exe
조별과제샘플/server_*.o
조별과제샘플/server_*.out
```

즉 사고 원인 규칙은 **확장자 한정으로 좁혀졌고, 사고 경위까지 주석으로 남아 있다.**
→ **지금 `server_device.h` 를 다시 만들면 그 파일은 추적된다.** 같은 사고는 반복되지 않는다.
→ 헤더 복구 전에 `.gitignore` 를 손볼 필요가 **없다.** §5 의 순서를 막는 선행 조건은 사라졌다.

**교훈 하나**: 이 항목의 초안은 REQ-0055 본문을 그대로 믿고 쓴 것이었다. 그런데 같은 본문의
"서버 프로세스 죽음"은 §1 에서 이미 거짓으로 판명됐다. **사고 직후에 쓰인 피해 목록은
그 자체가 검증 대상이다** — 인용하지 말고 매번 원본을 직접 확인하라.

남은 노출: **이 인수인계 문서와 복원할 `net/` 도구 6종은 아직 untracked 다.** 커밋을 권한다.
`.gitignore` 는 루트 소유이며 내가 손대지 않았다.

---

## 7. 이 세션이 실행한 명령 전부 (전부 읽기 전용)

`ps aux` · `lsof -nP -iTCP -sTCP:LISTEN -p 36998` · `ipconfig getifaddr en0` ·
`curl -s -o /dev/null http://192.168.35.21:9900/` · `tail -40 /tmp/parking-soak.log` ·
`date` · `ls -l` · `grep -n '#include "' 조별과제샘플/server.cpp` ·
`grep -n -E 'struct SHA1|std::string base64|ws_accept' 조별과제샘플/server.cpp` ·
`.gitignore` 통독 · `git log/status/show/ls-tree/for-each-ref`

**파괴적 명령은 하나도 실행하지 않았다.** 파일 생성은 이 문서 하나뿐이다.
서버·시리얼 캡처를 되살리지 않았고, 사라진 파일을 복원하지 않았다 — REQ-0055 지시대로 목록만 남겼다.
