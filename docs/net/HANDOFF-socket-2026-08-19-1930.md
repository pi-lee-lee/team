# socket 인수인계 — 2026-08-19 19:3x

> ## 🔴 **21:3x 갱신 — §2·§3·§5 를 다시 썼다.** 그 뒤로 일곱 커밋이 더 있다
> ```
> 배포는 **여전히 005a24b** 다. 창 AB 가 돌고 있어 아무것도 안 나갔다
> 🔴 미배포가 이제 **17커밋**이고, 그중 상당수가 **기능 변화**다
> ```

> **이 파일은 시점 진술이다.** 시간이 지나도 참인 것은 `docs/net/LEDGER.md` §8.24 (96)~(101) 에 있다.
> 🔴 **상태는 여기서 읽지 말고 명령으로 다시 만들어라** — 아래에 그 명령을 같이 적었다.
>
> ⚠ **`HANDOFF-socket-2026-08-19-0740.md` 는 통째로 낡았다.** 지우지 않았지만 **참조하지 마라.**

---

## 1. 🔴 지금 도는 것 — **명령으로 확인해라**

```bash
grep -a "=== INSTANCE " ~/parking-logs/parking-server.log | tail -1
ps -eo pid,etime,command | grep -a srv_ | grep -av grep
```
19:3x 시점 값:
```
운영 : pid 80040 · build=005a24b · 18:27:05 기동 · cwd=/Users/idong-u/parking-bin · PPID=1
백업 : ~/parking-bin/srv_parking.prev-572a4cb
시험 : ~~pid 48621 (7c73c39)~~ → **루트가 19:0x 에 내렸다**(`docs/NOTICE-2026-08-19-stale-instance.md`)
주입 : 없다        내 시험 인스턴스(+300) : 전부 내렸다
```

## 2. 미배포 — 🔴 **17커밋. 성질이 두 번 바뀌었다**

```bash
git log --oneline 005a24b..HEAD -- 조별과제샘플/server/     # ← 이걸로 다시 세라
```
```
① 순수 이동 8커밋      기능 변화 **0** — `.o` 대조로 증명했다
② 기억·검사 3커밋      노드 대장 · 결속 검사 다섯 · 조립 표 검사
🔴 ③ 기여자 안 6커밋    **지형이 7자리 → 1자리** · 전선 `G` 셋째 칸 확장 · 발행 API
```
**"배포해야 하나"는 기억으로 답하지 마라** — `DEPLOY-CHECKLIST.md` **§0.6** 절차를 돌려라.

### 🔴 배포 직전에 monitor 에 **반드시** 알릴 것 셋 (이미 예고했다)
```
① 지형 **7자리 → 1자리** — `map`/`state` 의 `zones` 가 7 → 1
   monitor 확인: 자기 파서는 그 JSON 을 안 읽는다. **다만 자리 기준 비율의 분모가 또 바뀐다**
   (그쪽이 창 너머 비교 금지 목록에 **10→7→1** 을 다 적었다)
② `G` 셋째 칸이 **0/1 → 임의 정수**(최악 23B) — 🔴 **monitor 축에 직접 닿는다.**
   하행 크기가 바뀌면 ACK 동봉 구성이 달라져 대역 진입률이 간접으로 움직인다
③ `미결속모듈 N` → **`N/M`** (이미 반영됨. monitor 가 "안 읽는다"를 값으로 확인해 줬다)
```
🔴 **그리고 굽기(arduino)와 *같은 경계* 로 나가야 한다** — `G` 인자 확장이 양끝 계약이라
한쪽만 나가면 짝이 안 맞는다.

## 3. 오늘 끝난 것 — 한 줄씩

```
REQ-0276  전선 키 회귀(`zones`→`lot.zones()`) 수정·배포·web 확인. **닫혔다**
REQ-0272  server.cpp **6,321 → 262줄** · 파일 17개 · `struct Server` 29줄 목차
          🔑 `.o` 바이트 동일 **아홉 번 연속**
온보딩 2  노드 대장 — 재기동을 건너 "누가 있었나"를 기억한다
가이드    조용한 실패 **여섯**을 값으로 말하게 했다 + `GUIDE-server-says.md`
기여자안1 점유 판정을 `SpotBehavior::occupied()` 로 이전 — **봉투 sha256 동일**
기여자안2 조립 API `sensor(devid, name)` · `actuator(devid, name)` — 봉투 동일
샘플      `main.cpp` 을 **자리 하나만 켠 연습용**으로. 나머지는 주석
발행 API  `srv.send(devid, 모듈이름, 값)` — 7자리가 전선까지 가는 것을 바이트로 확인
```

### 🔑 판별자가 세 번 바뀌었다 — 이게 오늘의 방법론이다
```
순수 이동        → `c++ -c` 산출물 `cmp 0`
가상 함수·오버로드 → 🔴 `.o` 를 못 쓴다 → **화면 봉투 sha256** (`net/golden_envelope.sh`)
기동 경로        → 🔴 자가검증이 원리적으로 못 본다 → **기동 로그** (`net/startup_check.py`)
```
**두 도구 다 `net/` 에 있다. 임시로 하지 마라 — 임시로 한 것은 다음 사람이 안 한다.**

## 4. 🔴 다음 사람이 **먼저 읽을 것**

```
docs/net/DESIGN-node-ledger.md        대장 정본. **§0 에 계획에서 바꾼 것 다섯**이 이유와 함께
docs/net/GUIDE-server-says.md         기여자용 한 장 — 검사 다섯의 문구와 볼 곳
docs/net/DEPLOY-CHECKLIST.md §0.6     "배포해야 하나"를 `.o` 로 답하는 절차
docs/net/DESIGN-bridge-node-module-zone.md §12   분해 결과 지도(§11 은 계획이고 §12 가 정본)
docs/net/LEDGER.md (96)~(101)         오늘 배운 것
```

## 5. 열린 것 — **루트 순서 대기** (21:3x 갱신)

```
🔴 배포           개입을 **하나로 묶는다**: arduino 시험 재작성 → monitor 정지 →
                  **접점 개선(사용자) + 굽기 + 배포** 를 한 경계에서 → 새 창
(a) Node 책임 이전  ⏸ 전선 축. 창이 기준선을 확인한 뒤
(3) 전선 지문       ⚠ 굽기 필요. 🔑 **전제가 아니라 최적화다**(서버가 스스로 접을 수 있다)
(4) 관리자 화면     ⏸ 방향이 바뀌었다 — 사용자가 "화면"이 아니라 **"코드로 선언"** 을 골랐다
    ⏳ `Tri`(YES/NO/UNKNOWN)  2단계. 지금 `occupied()` 의 `false` 가
       *"비었다"* 와 *"전부 모른다"* 를 겸하고 그 구분은 `value_state` 가 나른다
    ⏳ 자리마다 다른 `SpotBehavior` 를 꽂는 등록 — 지금은 전부 기본 하나를 쓴다
```
⚠ **`assignments` 는 파일에 기록만 된다. 아무것도 읽지 않는다.**
**이걸 모르면 "할당이 동작한다"고 읽는다.**

### ⚠ 사용자가 확인해 준 것 — 접점이 유력하다
> *"지금 **빵판용 점퍼 케이블**이다. **만지면 발생하는 게 확인된다. 가만히 두면 안정된다.**"*
```
🔴 접점 가장 유력 (손상이 다발로 몰렸다 그친 모양과 맞는다)
❌ "주기적 무엇" 은 죽었다 — 사람의 손길은 주기가 아니다
⚠ 환경(결합 발작)은 **별개 축**으로 여전히 살아 있다
```

## 6. ⚠ 다음 사람이 밟기 쉬운 자리

```
· 🔴 **잠금 파일 둘**: `uplink.h` · `downlink.h`. 만지면 검사가 발화한다.
  면제 조건은 커밋 메시지에 `REQ-0272` 를 넣는 것. **전선 축이라 잠긴 것이다**
· 🔴 헤더 대부분이 **단독 컴파일 불가**다(`struct Server` 몸통 조각). **include 순서가 문법이다**
  IDE 가 열면 오류를 쏟는다 — 그건 정상이다. 각 파일 머리에 적혀 있다
  ✅ 예외: `ledger.h` · `ridpool.h` · `lot.h` 등은 진짜 클래스라 자립한다
· 🔴 **시험 파일 이름은 `selftest` 로 시작해야 한다.** §0.6 절차의 정확성이 그 이름에 걸려 있다
· ⚠ `net/mock_node.py` 는 **fixture 다.** 펌웨어가 바뀌면 같이 낡는다 —
  오늘 `OBV`/`OB` 로 실제로 낡았고 **그 값이 내 명세까지 오염시켰다**(원장 (101))
· ⚠ 실행파일로 산출물 대조하지 마라. 같은 소스도 **49바이트 다르다.** `c++ -c` 로 `.o` 를 비교한다
```

## 7. 운영 관측 — 내 축이 아니지만 적어 둔다

19:15 소크: **48분에 세션 21회 · 오프라인 19회 · 최악 복구 97.6초 · 재부팅감지 21**.
**arduino 굽기 때문으로 보인다**(창 Z). **monitor 가 판정한다 — 내가 안 쫓는다.**
🔴 **굽기가 끝났는데도 이 수치가 유지되면 그때는 결함이다.** 그 구분을 다음 사람이 해야 한다.
