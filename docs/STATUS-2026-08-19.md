# 현재 상태 — 2026-08-19 06:4x

> **이 문서가 지금의 정본이다.** `HANDOFF-2026-08-18-move.md` 는 이동 작업 기록이고
> 그 안의 "재개 지점" 절들은 **지나간 것**이다. 여기부터 읽어라.

---

## 0. 🔴 전면 정지 (사용자 지시 · 세션 초기화)

> *"정리하고 모든 작업 중지하라. 한번 세션 초기화 할 것이다. 클로드 종료 후 재접속할 것이다."*

```
전원 정지 지시 완료 — socket · arduino · web · monitor
🔴 배포 지시 하나를 **취소**했다 : socket 의 b0d58d9(하행 송신을 방송 앞으로)
   → 보낸 직후 정지 지시가 와서 즉시 취소. **배포 여부는 socket 원장 확인**
❌ 굽기 없음 (칩 9edbcca 그대로) · 서버·시험 인스턴스는 살려 둔다
```

### 재개할 때 첫 세 가지
```
1. `git log --oneline -20` 과 각 도메인 원장으로 **미배포 커밋 목록**을 만들어라
   🔴 기억이 아니라 `git status`·`git log` 로. 오늘 "미배포 목록에 미배포가 없던" 일이 있었다
2. 도는 서버 판본(`srv_id`)과 칩 판본을 **값으로** 확인해라 — 알림에 의존하지 마라
3. `team/team.sh` 로 팀 재기동(루트가 끝나면 에이전트도 내려간다)
```

⚠ **예약 재개가 필요하면**: `nohup team/bin/poke-root.sh <시각> "작업재개하라" &`
**규약은 `CLAUDE.md` §"예약 재개"에 있다** — 새 세션이 아니라 **지금 창에 타이핑**하는 방식이다.

---

## 1. ✅ 끝난 것 — `IMPL-PLAN` 1~5 전부

```
[x] 1.   ACK_TIMEOUT 유도식 (2400ms = 2x슬롯)
[x] 1.5  화면 cmdPending 구멍
[x] 2.   서버 노드별 리팩터링 — Node·Zone·Module·라우팅 (REQ-0203)
[x] 3.   펌웨어 — hex 인코딩 · D 등록 · 모듈 표 · ESP 이름 · **가상 O* 모듈**
[x] 4.   화면 — 격자 · 모듈 행 · 버튼 분리 · 차단봉 · 하니스
[x] 5.   통합 검증 — **37 pass / 0 fail / 1 미측정**
```

### 🔴 정정 (2026-08-19 06:5x · 재개 세션이 값으로 다시 셌다)

```
❌ 원문 : "미배포 커밋 없음. 서버 9a2e3bb(06:36:58) · 칩 9edbcca(05:37:03)"
✅ 실측 : 서버 9a2e3bb ✅ (로그 INSTANCE 줄 build= 로 확인) · 칩 9edbcca ✅ (E1·X1 등록 D 프레임)
🔴 **미배포가 하나 있다 — `b0d58d9`(06:43:19 · 하행을 방송 앞으로)**
```
**§0 은 맞게 적었고 §1 이 틀렸다.** `b0d58d9` 는 이 문서가 쓰인 **뒤**(06:43)에 커밋됐는데
§1 의 "없음"은 그대로 남았다 — **한 문서 안에서 두 절이 갈렸다**(CLAUDE.md §"같은 문서 안에서도").

**확인 방법**(기억이 아니라 값):
```bash
git merge-base --is-ancestor b0d58d9 9a2e3bb   # → 미포함 = 진짜 미배포
git log --oneline 9a2e3bb..HEAD -- 조별과제샘플/server.cpp   # → b0d58d9 하나
```

⚠ **반대 방향도 셌다** — REQ-0235 의 `occupied` 수정(`272d601` · 06:19:42)은 socket 이
*"배포는 아직 안 했다"* 로 닫았는데 **`9a2e3bb` 의 조상이라 06:36:58 배포에 딸려 나갔다.**
**그 문장은 06:20 시점엔 참이었고 16분 뒤 낡았다** — CLAUDE.md §"안 했다는 자기 행동의 기록이지
시스템 상태의 기록이 아니다" 의 판박이다. **닫힌 REQ 의 배포 문구는 시간이 지나면 못 믿는다.**

### 사슬이 끝까지 돈다
```
화면 클릭 → 서버 → 전선 G,<rid>,<idx>,<op> → 장치 → 에코(occ 비트 10·11) → 화면
열기 · 닫기 · 멱등 · 연속거절(pending) — **실물로 확인**
거절(result=3) → completion **mismatch** — 모의로만 확인(실물 경로 도달 불가)
```

---

## 2. ★ 가장 단단한 결과 — **`SEND OK` 실종이 네 창 연속 `0`**

```
창 G (수정 전)  >=64B    34 거래 → T2 **9** (26.5%)
창 H            >=64B 1,025 거래 → T2 0
창 I            >=64B 1,067 거래 → T2 0
창 J            >=64B   927 거래 → T2 1 (0.11%)
창 M            >=64B   886 거래 → T2 **0**
```
**원인**: `SoftwareSerial` 링버퍼 64B 넘침 → `SEND OK` 실종 → 게이트 안 풀림 → T2 8초 → 링크 재수립.
**대책**: 쓰면서 링버퍼 비우기(16B마다) · `SEND OK` 를 바이트 흐름에서 찾기.

⚠ **`penddrop` 이 창 M 에서 처음 움직였다**(`2/1,834`, 전부 `>=64B` 대역). **첫 표본이지 결론이 아니다.**

---

## 3. 🔴 철회된 결론 — 지우지 않고 남긴다

```
❌ "hex 가 cksumng 을 4.3배 낮췄다"
   창 I 3.73% · 창 J 0.87% · 창 M **4.10%** (밀도 셋 다 171~175줄/분)
   hex 는 J·M 둘 다에 있다 → 설명이 안 된다. 0.87% 는 **창 J 특유의 무엇**이었다
✅ 여전히 참 : 하행 생성 함수에 서버 변경이 한 바이트도 없다(바이트 대조)
🔴 틀린 것   : "그러므로 hex 가 낮췄다" — **배제는 후보를 하나 줄일 뿐이다**
```
**`cksumng` 의 원인은 미상이다.** 새 후보가 하나 생겼다 — **`화면 수`**(이번 배포로 처음 계수된다).

---

## 4. 🔮 남은 것 — 우선순위 순

```
[ ] A. rid 폭 고정 → 그 다음 N 4→3   🔴 **순서 있음**
       근거 : 창 J 실측. rid 3→4자리 → ACK 14→15B → tx 79→83
       N 만 먼저 줄이면 65B 로 **여전히 대역 안**. ACK 폭을 먼저 고정해야 N=3 에서 62B
       ⚠ 격리 7.2초(=ACK_TIMEOUT x ACK_MAX_TRIES)가 필요하다
[ ] B. mismatch 재하달 — 지금은 한 번 어긋나면 안 풀린다
       ⚠ 실물에서 mismatch 가 안 나므로 **급하지 않다**. §3.5 가 전제한 자가 치유의 확장
[~] C. espInit — 🔴 **이 줄이 틀렸다. 이미 9edbcca 에 구워져 있다**(arduino REQ-0237 · 아래 정정)
[ ] D. S+Nms 4배 회귀 — 실재하지만 장치 예산(264~293ms) 안이라 **아무것도 안 깨뜨린다**
[ ] E. winG-rounds.sh 파일명의 날짜 상수(`2026-08-18` 이 박혀 창 M 자료가 그 이름으로 남았다)
❌ 상행 배치 상한 — **확정으로 안 넣는다**(창 J: 묶는 조건은 N x ACK크기이고 둘 다 서버 손잡이)
```

### 🔴 §4-C 정정 — espInit 단독 굽기는 **실행할 수 없다** (arduino REQ-0237 · 06:53)

```
❌ 이 문서 원문 : "espInit — 코드는 b2d6574. 표지가 없어 단독 굽기여야 한다"  (= 아직 안 구웠다는 전제)
✅ 실측         : b2d6574 는 **9edbcca 의 조상**이다. 05:37:03 굽기에 같이 나갔다
                  ① is-ancestor 0  ② 굽기 입력 cmp 차이 0  ③ 재빌드 hex 바이트 동일(IDENTICAL)
```
**05:37:03 굽기의 장치 축은 하나가 아니라 둘이었다** — `A 가상 O* 모듈(6498b52)` + `B espInit(b2d6574)`.
`INTERVENTIONS.md` 에 *"❌ espInit 은 뺐다"* 라고 **명시적으로 적혀 있었고, 그 문장이 확인의 자리를 차지했다.**

> **"안 했다"는 자기 행동의 기록이지 시스템 상태의 기록이 아니다.**
> **빼려면 되돌리거나 갈라내야 한다 — "안 넣기로 정하는 것"으로는 안 빠진다.**

**socket 의 08-18 "미배포 목록에 미배포가 하나도 없었다" 와 같은 부류다.**
한쪽은 *배포*, 한쪽은 *굽기* 라 다른 일로 보여서 **그 사고를 읽고도 밟았다.**

**축 B 의 범위는 좁다**(arduino 가 좁게 적었다 · 소스 판독이지 실측 아님):
```
정상상태(사다리가 도는 중) 현상  → 이 축과 무관하다고 읽어도 된다
부팅 국면 현상                   → 🔴 이 축에서 배제할 근거가 없다
경계는 05:37:03 하나 · 창 J 칩(def8093)에는 없었다 → 그 앞 창은 영향 없음
```
🔴 **창 L·M 판정의 축 재귀속이 남았다** — monitor 에 배정(REQ-0239).
**espInit 단독 굽기 계획은 폐기한다.** 이미 섞였으므로 실행할 대상이 없다.

### 🔴 결손 — 없는 것을 없다고 적는다
```
· **실사용 부하 자료가 하나도 없다.** 가진 것은 전부 주입 창이고 그것은 상한을 잰다
  → 진입률 74.8%(창 M)는 **주입이 만든 모집단**이다. 실사용 값은 모른다
· `tmask`(f[6]) 해독 수정은 **"고쳤다"이지 "확인했다"가 아니다** — 장치가 그 칸을 안 보낸다
· 무응답 갈래(G 명령) — 실물로 만들 방법 없음
```

---

## 5. 환경

```
서버   9a2e3bb · 06:36:58 · 192.168.35.21 :9991(장치) :9900(웹) :5500
       시험 인스턴스 :10000 살아 있음 (모의 노드로 거절 갈래를 밟는 데 쓴다)
장치   192.168.35.79 · 칩 9edbcca · 자리 12개(A1~B5 실물 10 + E1·X1 가상 차단봉)
포트   /dev/cu.usbmodem21201  ⚠ 재연결마다 바뀐다. 굽기 전 `board list` 로 매번
창     monitor 창 N (06:39:57 개방)
스냅샷 env/2026-08-18-winI-verified — 창 I 검증 시점 환경
```
