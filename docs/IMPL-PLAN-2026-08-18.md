# 구현 계획 — 설계 개정 (2026-08-18)

**사용자 지시**: *"구현착수하라. 곧 리미트 제한이 걸린다. 구현계획을 명확하게 세워
제한해제후에도 작업을 이어갈수 있도록 준비하고 시작하라."*

> ### 🔴 **세션이 끊겨도 이 문서 하나로 이어받는다.**
> 다음 세션은 **이 문서를 먼저 읽고 §"지금 어디까지"** 를 본다. 원장(`requests/INDEX.md`)이
> 실제 진행이고 이 문서는 **순서와 이유**다. 둘이 어긋나면 **원장이 이긴다.**

---

## 지금 어디까지 — **여기를 매 단계마다 갱신한다**

```
[x] 1. ACK_TIMEOUT 유도식 — ✅ 배포 완료 (build=c719d87 · pid 80947 · 12:28:40)
        ACK_TIMEOUT 2400ms(=2x슬롯) · ack_budget_ms 12600 · 화면 코드 변경 0(값을 서버가 준다)
[ ] 1.5 🔴 cmdPending 구멍 — **사용자 실물 재현. 즉시 수정** (아래 §1.5)
[ ] 2. 서버 노드별 리팩터링 (socket)
[ ] 3. 펌웨어 — ESP 클래스 · 자리 유동화 · 등록 프레임 (arduino)
[ ] 4. 화면 — 격자 · 모듈 행 · 버튼 분리 · 하니스 (web)
[ ] 5. 통합 검증
```

**마지막 갱신**: 2026-08-18 12:4x · 1단계 배포 완료 · **1.5 가 최우선으로 끼어들었다**

---

## 딛고 선 것 — 이것들은 이미 끝났다. 다시 하지 마라

```
✅ 측정 종료   노드 하나의 통신 구조 확정 (monitor/JUDGMENT-2026-08-18-winE.md §6)
              하행 파괴 0(분모 300) · 큐가 링크 붕괴 방지 · 왕복은 슬롯 대기가 정한다
✅ 설계 명세   docs/net/DESIGN-bridge-node-module-zone.md      (531줄 · 원본)
              docs/arduino/DESIGN-esp-class-and-modules-2026-08-18.md (190줄)
              docs/web/DESIGN-module-state-2026-08-18.md      (248줄)
              docs/web/DESIGN-tile-visual-2026-08-18.md       (347줄)
✅ 사용자 결정 docs/DECISIONS-pending-user-2026-08-18.md
              표시량 A(칸 안에 다) · 하드리셋선 배제 · 나머지 넷은 진행을 안 막는다
```

🔴 **명세가 원본이다. 구현하다 명세와 다르게 하고 싶으면 명세를 먼저 고치고 그 이유를 남겨라.**

---

## 단계별 — 무엇을 · 왜 그 순서인가 · 무엇이 끝의 조건인가

### 1. `ACK_TIMEOUT` 유도식 — **socket + web 한 배포** `[먼저인 이유: 측정 도구 복구]`

**지금 문제**: `ACK_TIMEOUT=1500` 이 정상 왕복 **1230ms** 에 **271ms 여유**뿐이다.
그래서 **재전송에 늦은 ACK 이 섞이고**, 그것을 가르는 판별자가 **파괴가 드물수록 오탐이 커진다.**

```
수정 후 : 늦은 ACK 이 빠지고 재전송이 파괴만 센다  →  🔑 판별자가 필요 없어진다
```

**입력은 실측으로 있다**: 왕복 마지막 `1229 · 1225 · 1230`(세 창·두 망·두 펌웨어에서 고정).
⚠ **`rtt_max` 를 쓰지 마라** — 창 D 최대가 `1599` 이고 **최대에 맞추면 실패 감지가 느려진다.**
**중앙값 + 한 슬롯 여유**가 맞고 **둘 다 실측으로 갖고 있다.**

🔴 **web 과 같은 배포여야 한다** — `ack_budget_ms` 가 그 값에서 계산된다.
**서버만 올리면 화면이 옛 예산으로 돈다.**

**끝의 조건**: 서버·화면이 같은 해시로 배포되고 `queued` 왕복이 새 예산으로 도는 것을 확인.
⚠ **`재전송` 의 뜻이 바뀌므로 창 간 대조 불가 칸으로 표시한다**(socket 이 미리 적어 뒀다).


### 1.5 🔴 `cmdPending` 구멍 — **사용자가 실물로 재현했다. 최우선**

**사용자 보고**: *"웹이 살아있어서 한걸음 진행을 20개 이상 보내면 응답이 없습니다 (자체 타임아웃)."*

⚠ **처음에 "esp01 리셋"으로 보고됐고 우리도 그렇게 조사했다** — 그런데 **시리얼 로그에
리셋 표지가 0건**이었다. 사용자가 화면 문구를 알려 주자 **1초에 갈렸다.**

```js
index.html:1948
if (p.kind === 'sim_step') state.simOutcome = { level:'err', text:'응답이 없습니다 (자체 타임아웃)' };
```

**`sim_step` 이 "한걸음 진행"이고, 이건 화면이 자기 타이머로 포기한 것이다. 장치는 정상이다.**

#### 기전 — web 이 오늘 찾아 놓고 미룬 그 구멍이다

```
20개 이상 누른다 → 큐에 쌓인다 → 서버가 queued 를 보낸다
→ 🔴 queued 분기가 state.pending 만 보고 state.cmdPending 은 안 본다
→ sim_step 경로에서 그 프레임을 조용히 버린다
→ 6초 고정 타이머가 그냥 만료 → "응답이 없습니다"
```

**서버도 장치도 정상인데 화면만 실패라고 말한다.** 그래서 로그에 흔적이 없다.

⚠ **web 이 "피해가 예약보다 작다"고 판단해 별건으로 미뤘고, 사용자가 실물로 뒤집었다.**
**20개 규모에서는 거의 확실히 재현된다.**

#### 고칠 것

```
· queued 분기가 state.cmdPending 도 본다
· 🔴 6초 고정 대신 ack_budget_ms(12600)를 쓴다 — 지금은 큐 대기(최대 4800ms)를 못 견딘다
· 예약 경로(REQ-0166)에서 이미 한 것과 같은 처리다 — 그 경로엔 닫혀 있고 이 경로엔 안 닫혔다
```

**끝의 조건**: 20개 이상 연타에서 `자체 타임아웃` 이 안 나고 `queued` 가 화면에 보이는 것.
🔴 **사용자 조작으로 검증한다** — 그것이 이 결함을 드러낸 유일한 자극이다.

#### ★ 이 사건에서 배운 것 (규칙 후보)

> **증상 보고를 받으면 그 문구를 코드에서 먼저 찾아라.**

**루트가 `grep 'System Ready'` 0건을 보고 "로그와 안 맞는다"고만 했다.** 문구를 소스에서 찾는 것이
**첫 수순이어야 했다** — 사용자가 문구를 알려 주자 1초에 갈렸다.
⚠ **그리고 "리셋"이라는 사용자 낱말을 그대로 조사 대상으로 삼은 것**도 같은 실수다.
**사용자는 증상을 보고하지 원인을 보고하지 않는다.**

### 2. 서버 노드별 리팩터링 `[2번인 이유: 나머지 셋이 이 계약 위에 선다]`

**전역 상태를 `Node` 로 내린다** — `ard_last_ms`·`downq`·`downq_bytes`·`rtt_max_ms`·
`win_skips`·`dmax_flushes` 전부. **이미 "그 소켓의" 값이라 개념 변경이 아니라 그릇 변경이다.**

🔴 **약화 금지**(사용자 강조: *"작동은 해야 한다!! 없애는 것이 아니다"*):
`DOWN_BATCH_CAP_B` · `q_deferred` · `D_max` 창 포기 박자 · `queued` · 배포 순서.
**규칙은 한 글자도 안 바뀐다. 한 벌을 N 벌로 만들 뿐이다.**

**같이 들어가는 것**: `Zone`/`Module` 자료 구조 · 하행 라우팅(`zone → (devid,name) → 그 노드 큐`) ·
`actionable`/`blocked_reason` 계산 · 맵 정의를 상태와 **별도 키**로 · 완료 판정(전부 일치해야 `done`).

⚠ **`first-S-wins`·`aux_conflicts` 는 없어진다** — 모듈 등록이 장치를 명시하므로 추측이 불필요하다.

**끝의 조건**: 노드 1대(P1)로 **지금 동작이 그대로 유지**되는 것. **회귀가 먼저다.**
🔴 **여기서 새 기능을 검증하려 하지 마라** — 2대가 없다.

### 3. 펌웨어 `[3번인 이유: 서버 계약이 확정된 뒤라야 프레임을 맞출 수 있다]`

```
· ESP 클래스 init/read/write/reset  (reset 의 정의를 좁히지 마라 — 하강 로직이 갈 곳이다)
· 자리·모듈 유동화 (SLOT_N 상수 제거 · 비트 순서가 신원인 결합을 끊는다)
· 등록 프레임 D/Q (socket 명세 §5 · REG_TIMEOUT = 3 × 슬롯주기 · Q 3회 상한)
· 액추에이터 에코 (출력 모듈도 n 폭 비트열에)
· 다음 굽기 목록의 계측 넷 — 🔑 [CNT] 가 손실원에서 빠져 이제 얹어도 안전하다
· 소스 주석 REQ-0187 오기 정정 (client.ino:134 · 1462)
```

⚠ **굽기 규약 그대로**: 사전 기록 → monitor 통보 → 정지 확인 → 굽기 → hex 대조 →
**완료 통보(monitor·루트 양쪽)**. 🔴 **축을 하나씩** — 계측과 거동을 한 굽기에 섞지 마라.

**끝의 조건**: hex 대조 IDENTICAL · 회귀 통과 · 서버 재접속 확인.

### 4. 화면 `[4번인 이유: 서버가 새 계약을 주기 시작한 뒤라야 그릴 것이 있다]`

```
· 격자 렌더링 (맵 정의 · 빈 칸은 aria-hidden 장식)
· 모듈 행 (A 안 · 기호축 × 강조축 · 미상 집계 한 줄)
· 🔴 칸이 버튼인 구조를 버린다 → 칸은 컨테이너 · 조작은 명시적 버튼
· 🔴 하니스 셋을 같이 고친다 — e2e · queue-contract · queued-live 가
     .tile[data-slot] + data-action 으로 고른다. 안 고치면 시험이 조용히 아무것도 안 잡는다
```

**끝의 조건**: 하니스 셋이 새 선택자로 통과 · **브라우저로 눈으로 본다**
(web 한계: *"원칙이 옳아도 형태가 그것을 전달하지 못할 수 있다"*).

### 5. 통합 검증

```
· 1대(P1)로 종단 왕복 — 예약 · 조작 · 상태 표시
· web §11 실측 넷 (⏱ 셋 구분 · 밀도 · reduced-motion · 범례 부하)
· 회귀: 지금 되는 것이 안 깨졌나
⚠ 다중 노드는 보드가 한 대뿐이라 못 한다 — 그 사실을 판정에 적는다
```

---

## 세션이 끊겼을 때 — 다음 세션이 할 일

1. **이 문서 §"지금 어디까지"** 를 읽는다
2. **`team/bin/team-status.sh`** 로 미결 요청과 에이전트 상태를 본다
3. **`requests/INDEX.md` 꼬리 20줄**로 마지막 이벤트를 본다 — **원장이 실제 진행이다**
4. **각 도메인 원장**(`docs/net/LEDGER.md` · `docs/arduino/LEDGER.md` · `docs/web/LEDGER.md`)에
   그 도메인의 최신 상태가 있다
5. **에이전트에게 직접 물어라** — 세션이 살아 있으면 자기가 어디까지 했는지 안다

⚠ **모르면 새로 시작하지 말고 물어라.** 오늘 "기록 없는 굽기"로 반나절을 태운 기록이
`arduino/INTERVENTIONS.md` 첫 절에 있다.

## 규율 — 구현 중에도 지킨다

```
· 굽기·배포 전에 통보하고 관측을 멈춘다 (CLAUDE.md §프로세스는 자원이 아니다)
· 축을 하나씩 바꾼다 — 동시에 둘을 바꾸면 원인이 사라진다
· 커밋은 git commit -m "..." -- <경로>  (add 로는 남의 staged 가 딸려 온다)
· 확정 / 추론(근거 포함) / 미측정 을 갈라 적는다
· 명세와 다르게 구현하려면 명세를 먼저 고치고 이유를 남긴다
```
